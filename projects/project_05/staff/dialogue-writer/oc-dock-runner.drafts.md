# Drafts — oc-dock-runner (s01e02, westeros-smallfolk)

## No speaking beats authored.

Scan of `active-project/theater/proto-lines/s01e02.md` for the SVO pattern `oc-dock-runner speaks` returns zero results. The runner appears in the episode only as a relay-target ("37 the wasps relay oc-dock-runner") — Taylor's POV registers the runner's presence and movements, but the runner does not speak in this episode.

The absence is the record. Per the brief's instruction, an empty dialogue file with only the schema header has been written to `active-project/theater/dialogue/oc-dock-runner.md`.

No drafts. No entries. No anti-pattern flags. No card-signature claims to defend.

If R2 graph reveals a beat-and-line license the bones did not surface (the runner reacting silently to an event Taylor co-observes), the R2 add-cap (≤3 per character per run) is the path. R1 with no speaking-beat anchors produces nothing.
