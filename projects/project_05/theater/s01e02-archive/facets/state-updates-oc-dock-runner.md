# source: oc-dock-runner
# facet: state-updates
# episode: s01e02

# Slice scope: actor-state for oc-dock-runner only.
# Per-character POV-restricted authorship per rubric § Authority.
#
# Pre-cull entries: 0
# Post-cull entries: 0
# Cull count: 0
#
# Authoring note (floor defense per rubric § Author / reviewer notes):
# The dock-runner appears in s01e02 proto-lines exactly once — @37 (the wasps
# relay oc-dock-runner). This is Taylor's swarm-sense perception of the runner's
# continued presence within sensorium; it is registration on Taylor's side, not
# a structural delta on the runner's side. The runner does not move, change
# inventory, change posture-as-state, or undergo any tracked-field mutation
# across the episode. Trust-test residue from e01 (the assessment that Taylor
# is dangerous, directional, unknown; trust-ledger small positive) is a stable
# pre-episode state baked into stm/ltm at e01 close; nothing in e02 flips it.
# No range-extension errands are written into e02 proto-lines for the runner.
# No on-stage transactional encounter occurs in e02 — the tanner-family arc
# carries the transactional surface; the runner is off-stage.
#
# Per rubric § Reality strip-test: removing any candidate entry, the runner's
# fields would still be at the same value at the next beat. Sparsity is
# load-bearing; over-firing corrupts canonical memory.
#
# Flagged seams (for cross-facet review, not state-update fires):
# - @37 wasp-relay: confirms runner-still-in-Flea-Bottom-sensorium-range. If
#   range or position-bin tracking were a tracked field on the runner's state
#   schema (it is not currently), @37 could license a knowledge-of-being-sensed
#   fire — but only if the runner *perceives* the swarm, which the runner does
#   not (per gap-as-load-bearing: runner does not know what the girl is, which
#   includes not knowing the mechanism). Refusal-CORRECT.
# - No e02 beat carries a trust-ledger flip. The ledger holds at small-positive
#   from e01 close. If a later episode carries a burn-or-honor event on either
#   side, that is where the ledger field flips.
