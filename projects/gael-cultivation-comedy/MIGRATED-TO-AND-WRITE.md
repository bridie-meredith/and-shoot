# ⛬ RETIRED — migrated to and-write

**Date:** 2026-06-14
**What this was:** the `and-experiment` live project (Gael Targaryen cultivation-reincarnation
comedy) — promoted to `active-project/` on 2026-06-14, developed spine-first (the bible + a
thread-checked outline + 14-cast), no chapters drafted yet.

**Why retired:** the project + its working process were **migrated in their entirety to `and-write`**
as the live home, under the new **`serial`** project type (spine-first, chapter-by-chapter — the
formalization of this project's process: idea-injection into a living spine + per-chapter
multiple-choice narrowing). and-write is multi-type / multi-concurrent and is where this project
continues. This copy is the **frozen archive** (provenance); do not develop it further.

**Where it lives now:** `and-write` → `projects/gael-cultivation-comedy/`. The formalized process is
`and-write` → `project-types/serial/{type.md, runbook.md}` + the `/spine`, `/narrow`, `/chapter`
commands + `scripts/check-threads.py`.

**Provenance is preserved:** this directory (the full pre-migration state) stays archived here per
the preservation invariant (archive, never delete); git history holds every prior state. The
`active-project/` slot is now free for a future and-shoot project.
