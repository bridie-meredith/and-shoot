```yaml
audit:
  scope: chapter
  target: b01c20
  timestamp: 2026-06-05
  gate: /and-write Phase 5 — continuity audit
  findings:

    - id: fault-001
      type: pass
      what: HANDOFF-IN open_threads coverage check across all 30 bones / 5 scenes
      why: n/a — pass
      criteria: null
      # Detail:
      # contempt-without-refusal LOCKED: honored. b01c20s01n05 axes_held political_register-prot rank 9
      # LOCKED rationale explicit; s02n06 holds same; s03n06 holds same; s04n06 holds; s05n06 holds.
      # No bone contradicts it.
      #
      # recognition beginning not-yet-terminal: honored. s01n04 marks moral_legibility aperture-opens
      # ("cleanliness of the entry IS the first pressure of recognition"); s04n05/n06 deliver the
      # terminal recognition event. Trajectory from not-yet-terminal to terminal is present and
      # correctly staged — s01 opens aperture, s04 delivers full recognition.
      #
      # ward-contact severed: honored. Chapter opens post-Daven-severance; the decommission in s03
      # is the next severance, which is consistent with c19 tether-loss momentum. No opening bone
      # contradicts the severed-contact state.
      #
      # Wren anchor 7.5 in coverage-gap lanes: honored. s04n01 "the insect-feed runs in the east-of-
      # water-gate lanes" — feed active and held in exactly those lanes. Coverage gap held open for
      # Wren consistent with 7.5 anchor state. No bone implies Wren already dead or the gap already
      # breached before s04.
      #
      # position-prot-collapse rank 4: honored. s01 does not open with a position-collapse event
      # (s01 is succession-in-feed, world-axes move). Collapse resumes at s02n04/s03n03/s05n01/n06.
      # Entering rank is not stated explicitly in a bone (bones do not declare ranks) but no bone
      # contradicts the rank-4 entry state; the collapse drawn across s02-s05 sums to ~3.0 as
      # contracted, consistent with entering at 4 and exiting at 1 LOCKED.
      #
      # social_tether-prot-collapse rank 4.5: honored. s02/s03 draw -1.0 and -1.5 respectively;
      # s05 draws -1.0 more. Total collapse drawn across chapter sums to ~3.5 (contracted 3.5),
      # consistent with entering at 4.5 and exiting at 1 LOCKED. No opening bone contradicts.
      #
      # Viserys death window active: honored. s01n01 "the servant-passages empty" — the death
      # arrives in feed before any bell. The opening bone delivers this thread on its first beat.
      #
      # false attribution in Otto's picture: honored. s03n03 "the decommission message arrives"
      # and scene 3 chunk (memory.md lines 12724-12729) explicitly preserves the ambiguity:
      # "the false attribution (Elder Norren's standing in Otto's picture) has either been
      # discovered and the picture corrected, or the picture simply no longer requires the
      # attributed node — the message does not distinguish." s03n03 rationale names
      # "function-addressed; no name, no rank, no acknowledgment." The bones do not resolve the
      # ambiguity, consistent with the chunk's deliberate non-resolution.

    - id: fault-002
      type: pass
      what: HANDOFF-IN world_state check — "KL 122 AC entering Dance-ignition threshold; position-world rank 8; political_register-world rank 8"
      why: n/a — pass
      criteria: null
      # Detail:
      # position-world entering rank 8 / political_register-world entering rank 8: honored.
      # s01 draws position-world +0.5 (cl07b, magnitude 1 at bone level) and political_register-world +0.5
      # (cl07c, magnitude 1). These are first draws within the chapter toward rank 9 LOCKED. No bone
      # opens with position-world or political_register-world already at 9, which would contradict the
      # entering rank-8 state. The chapter correctly draws both axes from 8 → 9.
      #
      # Dance-ignition threshold: honored. s01n01 (servant-passages empty = Viserys death),
      # s01n03 (Holdfast routes activate = succession machine executing). The world state of
      # "Dance-ignition threshold" is immediately delivered and correct.

    - id: fault-003
      type: pass
      what: HANDOFF-IN character_state check — Taylor's entering ranks
      why: n/a — pass
      criteria: null
      # Detail (cross-referencing memory.md chapters[b01c20].handoff_in.character_state):
      # political_register-prot rank 9 LOCKED: honored. Every bone that holds or moves political_register-prot
      # uses "LOCKED rank 9" rationale. No bone implies a rank below 9 at entry. Consistent.
      #
      # moral_legibility rank 7: honored. s01n04 opens the aperture with +0.2 draw, consistent with
      # entering at 7 and drawing toward 8. s02n05 adds +0.2. s04n05 adds +2.0 (bulk terminal draw).
      # s04n06 adds +1.0. s05n05 adds +1.0 (terminal close). Cumulative = ~4.4 across 5 draws at bone
      # magnitude notation; per the scene aggregate contracts: s01 +0.2, s02 +0.2, s04 +0.5, s05 +0.1
      # = +1.0 chapter total consistent with contracted +1.0. Entering at 7, exiting at 8 LOCKED. No
      # contradiction.
      #
      # social_tether-prot-collapse rank 4.5: see fault-001 detail; no contradiction.
      # position-prot-collapse rank 4: see fault-001 detail; no contradiction.
      # capability rank 8.5: honored. Every capability axes_held rationale references "8.5 load" or
      # "full-deployment held." No bone moves capability. s05n01 capability rationale: "feed closure
      # is the final capability act; the architecture returns to substrate; capability axis ends at
      # terminal state." No bone contradicts 8.5 entry.
      # relational_anchor rank 7.5: honored. s04n01 is set in the "east-of-water-gate lanes" with the
      # feed running normally there — the gap held open for Wren, consistent with anchor at 7.5
      # (not yet at 9). s04n02 through s04n07 deliver the +1.5 LOCK draw. No opening bone implies
      # anchor already at 9.
      # moral_framework rank -4: honored. Every moral_framework axes_held rationale uses
      # "consumed-as-compass" or "no new breach." No bone contradicts the rank -4 entry state.

    - id: fault-004
      type: pass
      what: REACHABILITY — chapter goal delivery check
      why: n/a — pass
      criteria: null
      # Detail:
      # Goal: "Show the audience the feed-signal loss from Wren's lanes as the recognition event —
      # not a named loss, not a ledger entry, the absence of signal in the place Taylor held open —
      # and close on the contempt complete, the ledger accurate, nothing remaining to refuse."
      #
      # Feed-signal loss as recognition event: delivered by s04 physics chain.
      # s04n01 establishes feed active in east-of-water-gate lanes.
      # s04n02 smoke fills the lanes (physics arrival).
      # s04n03 heat disperses insects (physics acts).
      # s04n04 insects scatter (physics consequence).
      # s04n05 signal drops from the lanes (recognition arrival as physics, not perception).
      # s04n06 lanes go blank (concrete physical state change).
      # s04n07 Taylor lifts stylus and does NOT open a ledger line (held discipline — no entry).
      # The chain is: physics → signal loss → held-without-entry. This precisely delivers the
      # goal's "absence of signal in the place held open, not a ledger entry."
      #
      # Contempt complete: s05n05 "taylor-hebert-kl-122ac runs the ledger" — moral_legibility +1.0
      # terminal draw, s05n05 rationale "the accuracy is the record of what she did and what it cost
      # and what she refused to price and what the refusal cost." s05n06 event_map entry
      # "contempt-complete" covered by s05n05+s05n06. political_register-prot LOCKED rank 9 held
      # throughout s05. Consistent with goal.
      #
      # Ledger accurate, nothing remaining to refuse: s05n05 rationale explicit: "not to find an error;
      # the accuracy is the record." s05n06 exits south gate with "no coin above subsistence."
      # handoff_out open_threads: [] — empty, consistent with nothing remaining.
      #
      # handoff_out consistency with what bones leave behind:
      # position-prot-collapse rank 1 LOCKED: s05n01 (-magnitude 2) + s05n02 (-magnitude 1, position draw)
      # + s05n06 (-magnitude 2) total = -5 bone-unit-draws; scene-level deltas s02 -0.7, s03 -1.0, s05 -1.3
      # = -3.0 contracted; exiting rank 1. Consistent.
      # social_tether-prot-collapse rank 1 LOCKED: s02 -1.0, s03 -1.5, s05 -1.0 = -3.5 contracted.
      # Consistent with handoff_out rank 1 LOCKED.
      # relational_anchor rank 9 LOCKED: s04 +1.5 draw confirmed by s04n02/n03/n04/n05 bone draws.
      # Consistent.
      # moral_legibility rank 8 LOCKED: s01 +0.2, s02 +0.2, s04 +0.5, s05 +0.1 = +1.0 contracted.
      # Entering rank 7 + 1.0 = rank 8. Consistent.
      # position-world rank 9 LOCKED: entering rank 8; s01 +0.5, s03 +0.3, s05 +0.2 = +1.0 contracted.
      # Consistent.
      # political_register-world rank 9 LOCKED: entering rank 8; s01 +0.5, s03 +0.2, s05 +0.3 = +1.0
      # contracted. Consistent.

    - id: fault-005
      type: pass
      what: POV check — perception-verb leaks on taylor-hebert-kl-122ac across all 30 SVOs
      why: n/a — pass
      criteria: null
      # Detail: full SVO scan conducted. Taylor-subject SVOs:
      # b01c20s01n04: "taylor-hebert-kl-122ac lifts the stylus" — physical act, not perception.
      # b01c20s01n05: "taylor-hebert-kl-122ac marks the ledger" — physical act.
      # b01c20s02n05: "taylor-hebert-kl-122ac opens the feed" — physical act.
      # b01c20s03n05: "taylor-hebert-kl-122ac opens the ledger" — physical act.
      # b01c20s03n06: "taylor-hebert-kl-122ac marks the social_tether entry" — physical act.
      # b01c20s04n07: "taylor-hebert-kl-122ac lifts the stylus" — physical act.
      # b01c20s05n01: "taylor-hebert-kl-122ac closes the feed" — physical act.
      # b01c20s05n04: "taylor-hebert-kl-122ac lifts the pack" — physical act.
      # b01c20s05n05: "taylor-hebert-kl-122ac runs the ledger" — physical act.
      # b01c20s05n06: "taylor-hebert-kl-122ac exits the south gate" — physical act.
      #
      # No Taylor-subject SVO uses reads/sees/knows/feels/realizes/notices/watches/perceives/
      # observes or any perception-surrogate verb.
      #
      # Environment-actor SVOs (non-Taylor subjects):
      # "the servant-passages empty" / "the doors open" / "the Holdfast routes activate" /
      # "the succession bell rings" / "the men enter the ward junctions" / "the patron channel shifts
      # sequence" / "the gate-side routes fill" / "the faction-movement follows the passage-counts" /
      # "the burn reaches the outer wards" / "the fire traces the ward-junction catalogue" /
      # "the decommission message arrives" / "the apparatus network absorbs the coverage" /
      # "the insect-feed runs in the east-of-water-gate lanes" / "the smoke fills the east-of-water-gate lanes" /
      # "the heat disperses the insects" / "the insects scatter" / "the signal drops from the lanes" /
      # "the east-of-water-gate lanes go blank" / "the insects disperse" / "the architecture releases
      # the wards"
      # All physics-as-actor; none attribute perception to Taylor.
      #
      # POV consistent: narrator = taylor-hebert-kl-122ac; SVO subjects are Taylor + environment
      # actors. No third-party POV bleeds detected.

    - id: fault-006
      type: pass
      what: FAULT-REFERENCE check — locations, cast slugs, props
      why: n/a — pass
      criteria: null
      # Detail:
      # Locations referenced in bones: "room above the rendering works, lower city near east-of-water-gate
      # corridor" (s01 header), "east-of-water-gate lanes" (s04n01/n02/n06), "ward junctions" (s02n02),
      # "Holdfast routes" (s01n03), "gate-side routes" (s02n04), "outer wards" (s03n01),
      # "south gate" (s05n06), "Tallow Croft area" (implied by chapter context, not directly named
      # in c20 bones — not a reference fault since Tallow Croft was c19's location and c20 opens
      # in the rendering works room).
      #
      # All locations are established KL geography consistent with:
      # - cond-kl-geography-122ac (series.lore constraint): seven gates ✓ (south gate), ward junctions ✓,
      #   lower city ✓, Red Keep Holdfast routes ✓, east-of-water-gate corridors ✓.
      # - series.constraints.settings: "King's Landing — Flea Bottom anchor + Red Keep + Dragonpit +
      #   Great Sept + seven gates + three hills" — east-of-water-gate is within this geography.
      #
      # On-stage actors: taylor-hebert-kl-122ac — resolves to cast_roster entry. ✓
      # Off-stage/mediated: Wren, Otto, Jarvis — consistent with "off-stage/mediated" dispatch note.
      # Wren not named in bones (only implied via "east-of-water-gate lanes" and "coverage gap"),
      # consistent with the chapter's perceptual mechanism design (she appears only as signal-absence).
      # Otto appears as the decommission/apparatus in s03n03 rationale — not as an on-stage actor,
      # consistent with his mediated-only role.
      # Jarvis: not referenced in c20 bones. His channel was used in c19; c20 decommission arrives
      # "through a channel that is not the Jarvis route" (scene 3 chunk). No reference fault — he is
      # correctly absent.
      #
      # Props referenced:
      # "stylus" (s01n04, s04n07) — established prop from prior chapters (ledger + stylus are
      # Taylor's carried instruments); no prop-possession fault.
      # "ledger" (s01n05, s03n05/n06, s05n05) — same; established carried prop.
      # "pack" (s05n04) — Taylor's departure pack; consistent with handoff_out character_state
      # "pack and no coin above subsistence." No inventory contradiction.
      # "insect-feed / insects" — Taylor's capability instrument, not a discrete prop; consistent
      # with capability rank 8.5 "full-deployment architecture active."

    - id: fault-007
      type: pass
      what: FAULT-STATE check — actor location coherence; no actor in two places; Taylor's continuous location-through-time
      why: n/a — pass
      criteria: null
      # Detail:
      # Taylor's single continuous location-through-time:
      # s01 header: "room above the rendering works, lower city near east-of-water-gate corridor."
      # s02: lower-city feed (Flea Bottom, Hook, ward-junction lanes) — Taylor is running the feed
      # from the same room; the feed is remote-reading, not physical movement. No location conflict.
      # s03: same room (burn reaches outer wards; decommission arrives; Taylor opens ledger). No
      # location change indicated. Consistent.
      # s04: same room (insect-feed active in east-of-water-gate lanes; smoke fills those lanes).
      # Taylor is physically stationary in the room reading a remote feed. No conflict.
      # s05n04: "taylor-hebert-kl-122ac lifts the pack" — departure begins from the room.
      # s05n05: ledger run "at the gate" — Taylor has moved to the south gate between n04 and n05/n06.
      # This is a single continuous move: room → south gate. Consistent with departure narrative.
      # s05n06: "exits the south gate" — departure complete.
      # Location chain: room (s01-s04) → south gate (s05) → departure (s05n06). Coherent and
      # internally consistent. No teleportation or simultaneous dual-location fault.
      #
      # No other on-stage actors in c20 to create placement conflicts.

    - id: fault-008
      type: pass
      what: FAULT-STATE check — time/location consistency; "eleven months of KL deployment" duration
      why: n/a — pass
      criteria: null
      # Detail:
      # s05n02 rationale: "eleven months of KL deployment, the architecture she built." This is
      # consistent with the chapter chunk (memory.md line 12837): "eleven months of KL deployment."
      # The series constraints note Taylor arrived and chapter 1 begins after "three weeks" in KL.
      # "Eleven months" in s05 is explicitly the figure from the chunk; it does not contradict
      # the "three years" prohibition (the screen-writer's Phase 7 comment at the bottom of the
      # bones file explicitly notes "(NOT three years)"). No timeline fault.

    - id: fault-009
      type: pass
      what: FAULT-POV — Earth-Bet noun fence check (cond-earth-bet-noun-fence series law)
      why: n/a — pass
      criteria: null
      # Detail:
      # series.laws includes cond-earth-bet-noun-fence; the hard_fences entry: "Earth-Bet proper-noun
      # fence: parahuman jargon dialogue-banned, inner-monologue-rare."
      # The chapter has no dialogue bones (explicitly noted at file header: "No dialogue bones; no
      # on-page speech; solitary/interior chapter"). No parahuman jargon appears in any SVO.
      # "Insect-feed," "bottlefly routes," "capability" are in-world operational vocabulary, not
      # Earth-Bet jargon. No "Tinker," "Thinker," "Parahuman," "Shard," "Trigger" or equivalent
      # Earth-Bet proper nouns appear in any bone SVO or rationale.

    - id: fault-010
      type: flag
      what: b01c20s01n01 substance_delta axis_moves magnitude notation — bone magnitude 1 mapped to scene target 0.5
      why: |
        b01c20s01n01 records axis_moves[position-world direction:up magnitude:1]. The scene 1
        contract (memory.md b01c20s01 substance_delta) specifies target_delta_magnitude: 0.5 for
        position-world. Similarly b01c20s01n03 records political_register-world magnitude:1 against
        a scene target of 0.5. The bones file header comment at s01n04 clarifies: "magnitude 1 maps
        to 0.2 at bone-to-scene aggregation." The screen-writer has used bone-magnitude as a relative
        unit that aggregates to the scene-level Δ through a normalization factor, not a 1:1 mapping.
        This is internally consistent within the chapter (the same notation pattern appears throughout
        all five scenes) and is consistent with the chunk_targets schema which specifies
        bone: {delta_per_axis: 1-3} as the bone-level unit, not a scene-fraction. No fixer action
        required. Advisory only: /and-review bones should confirm the normalization convention is
        auditable at Phase 6.
      criteria: null

  verdict: CONTINUITY-OK
  # All five FAULT-class checks return PASS.
  # fault-010 is a FLAG (non-blocking advisory on bone-magnitude notation convention).
  # No FAULT-REACHABILITY, FAULT-STATE, FAULT-REFERENCE, FAULT-POV, or FAULT-HANDOFF-IN-MISMATCH
  # findings at fault or escalate severity.
  # Chapter b01c20 passes continuity audit for /and-write Phase 5.
```
