# context-ledger — b01-c12

# Licenses exposition adds past the anti-exposition penalty.
# Schema: id ctx-NNN | anchor | gap | needed_context | license: CONTEXT-REQUIRED
#         | licensed_at: 2.5 | licensed_by: context-follow-reviewer | status: open
# Produced by /and-facets b01c12 Phase 2.5 (context-follow + aliveness review), 2026-06-03.

# NO CONTEXT-REQUIRED entries.
#
# The chapter is followable end-to-end for a context-aware reader (read c01-c11).
# Both genuinely reader-facing orientation needs are already carried by the R1
# exposition facet: the @0 prior-episode-state bridge and the @1 east-of-water-gate
# first-mention place-gloss. No further exposition license is issued.
#
# DEC-0076 design-inherent opacities EXCLUDED (not licensed for exposition):
#   - @38 shape-word (Khepri cipher) — intentional proper-noun opacity, cipher
#     resident from prior chapters; carried by handoff_in + memory:2/:3 + the
#     @32-@37 referent-weight build. Routed to /and-stitch P8.5 Check 3 (register
#     must not bury the recognition), NOT exposition.
#   - s04 seam @30->@31->@32 ("why does the threshold trigger NOW") — causal chain
#     carried by the @31 bridge bone + /and-stitch P8.5; rendering an expository
#     bridge is explicitly forbidden.
#
# Two recognition-links were classed WEAVE-FIXABLE (R2 lens steering, NOT exposition):
#   @14 (scene-B irony: apparatus reads back the dawn boundary-marks) and
#   @27 (anchor-column cl-d06 debt-settlement, reader-resident from c06-c11).
#   See steering hints in the Phase 2.5 report. These are NOT ledger entries.

open-entry count: 0
