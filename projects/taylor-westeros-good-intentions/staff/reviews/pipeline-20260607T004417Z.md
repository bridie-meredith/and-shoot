```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-07
  triggered_by: >
    Fixer pass touching three meta files: .claude/commands/and-cohere.md
    (Phase 4 pre-step added), schemas/parking-lot.schema.md (id format
    amended), schemas/cohere-state.schema.md (status: dismissed description
    extended). Full tri-walk per /and-review pipeline URI-REVIEW-PIPELINE.
  findings:

    # ── PRIORITY CROSS-CHECKS (three edited files) ────────────────────────

    - id: STRUCT-001
      type: flag
      severity: SIGNAL
      what: >
        bones.schema.md lines 140 and 165 — canonical speech form still names
        communication-class axes as "(community / knowledge / reputation / trust)"
        (the universal questionnaire taxonomy). pl-2026-05-30-003 documents an
        admin ruling (DEC, 2026-05-30) that custom signatures override universal
        scaffolding, and proposes two schema edits: (a) generalize bones.schema.md
        to ">=1 communication/relational-class axis per the active signature
        (universal questionnaire: community/knowledge/reputation/trust; custom
        signature: the axis/axes the signature designates relational/communicative)"
        and (b) add a one-line note in the series.substance signature block naming
        relational_anchor_status + social_tether-* as the communication-class axes.
        Neither edit has been applied. The ruling remains precedent-only with no
        schema backing.
      why: >
        Any /and-write Phase 6 bone-gate auditor that does a strict slug-match
        against the universal taxonomy will fire a false HARD on every custom-
        signature speech bone (as happened at b01c06 Phase 2, fault-002). The
        precedent lives only in the parking-lot entry and in the admin decisions
        file — not in the schema a new auditor instance reads. Recurrence risk is
        high; this is an open pl-2026-05-30-003 item targeting this tri-walk.

    - id: STRUCT-002
      type: flag
      severity: SIGNAL
      what: >
        showrunner-memory.schema.md — the `axes_held[]` field at scene level
        (line ~239-241) and the Field Notes section (lines ~412-415) do not
        explicitly address whether a post-move hold of an axis that was declared
        in-motion at the scene level requires that axis to also appear in the
        scene-level axes_held[] declaration. pl-2026-05-25-018 (target:
        /and-review pipeline, SOFT, status: open) documents this ambiguity:
        b01c02 scene contracts declared in-motion axes ONLY in axes_in_motion[]
        and excluded them from axes_held[], while bones in those scenes hold
        the same axes post-move. The Phase 6 HELD-AXIS-UNCONTRACTED gate
        could fire false-positive under a strict reading.
      why: >
        An undocumented schema carve-out makes the gate behavior unpredictable
        across sessions. The auditor note in pl-2026-05-25-018 suspended the
        fault without schema authority. Any future auditor will face the same
        ambiguity from scratch. Needs an explicit carve-out in the schema
        (either option (a) implicit-license or option (b) mandatory-listing)
        to be mechanically stable.

    - id: STRUCT-003
      type: pass
      severity: SIGNAL
      what: >
        Cross-check (1): and-review.md cohere subcommand (lines 294-441) vs
        and-cohere.md Phase 4 pre-step DEFER path (status: dismissed,
        final_verdict: null) vs cohere-state.schema.md § status + § dismissed
        field semantics.
      why: >
        and-review.md's cohere subcommand is a read-only primitive that produces
        reports only — it does not interact with cohere-state at all. It documents
        verdict vocabulary (PASS-COHERE / CAUTION-COHERE / FAIL-COHERE) at the
        report level, not state-machine level. The state machine (status: open /
        converged / cap-hit / held / dismissed) lives entirely in and-cohere.md
        and cohere-state.schema.md. No cross-file drift exists on the dismissed/
        final_verdict:null path because and-review.md has no jurisdiction over
        that path. PASS: the three files are mutually consistent on this axis.
        Specifically confirmed: cohere-state.schema.md line 111 names all three
        dismissed triggers (and-cut, --dry-run, Phase 4 DEFER); and-cohere.md
        Phase 4 pre-step correctly routes to status:dismissed + final_verdict:null
        + revise_queue[].result:SKIPPED on DEFER; cohere-state.schema.md
        revise_queue[].result enum (line 44) includes SKIPPED.

    - id: STRUCT-004
      type: pass
      severity: SIGNAL
      what: >
        Cross-check (2): parking-lot.schema.md id format amendment
        (pl-<YYYY-MM-DD>[-<label>]-<NNN>) vs command-body references. Grep
        scope: .claude/commands/*.md for pl-id construction or parsing patterns
        that assume the old strict pl-<date>-<NNN> form.
      why: >
        Checked and-cohere.md, and-review.md, and-write.md, and-stitch.md,
        and-facets.md, and-substance (not present as a file; confirmed via
        CLAUDE.md). No command body constructs or parses a pl-id by regex or
        fixed-position parsing — all references to parking-lot ids in command
        bodies are by variable name (<pl-id>, <parking-lot-ids>) or by
        description, not by hardcoded format string. The [-<label>] amendment
        is additive (backward-compatible: the old pl-<date>-<NNN> format is
        still valid — the label segment is optional). No command body breaks.
        PASS: the parking-lot id amendment does not break any command body.

    - id: STRUCT-005
      type: pass
      severity: SIGNAL
      what: >
        Cross-check (3): and-cohere.md Phase 4 internal numbering consistency.
        Phase labels in sequence: Phase 0, Phase 1, Phase 2, Phase 3, Phase 4
        (with sub-header "Phase 4 pre-step — principal-defer check" followed
        by the main dispatch loop), Phase 4.5, Phase 5, Phase 6, Phase 6.5,
        Phase 7, Phase 7.5, Phase 8.
      why: >
        No duplicate phase labels, no skipped labels, no misnumbered labels.
        The pre-step sits correctly inside the Phase 4 section as a sub-header
        (### not ##), not as a separate phase. The DEFER path exits to Phase 7
        → 7.5 → 8 as specified in both the pre-step text and the schema's
        dismissed-status semantics. The FIRE path continues to the dispatch
        loop within Phase 4. Phase 4.5 (Revision classification) follows
        correctly after Phase 4's dispatch loop. PASS: internal numbering
        is consistent.

    # ── SCHEMA vs COMMAND BODIES ──────────────────────────────────────────

    - id: STRUCT-006
      type: fault
      severity: HARD
      what: >
        bones.schema.md § "What bones are not" (line 165) and § Dialogue-anchor
        bones (line 140): the "canonical speech form" substance_delta requirement
        is stated as ">=1 communication-class axis (community / knowledge /
        reputation / trust)" — a universal-questionnaire enumeration. However,
        and-write.md's Phase 6 bone-gate (which reads bones.schema.md as authority)
        and and-review.md's bones subcommand will enforce this as a literal axis-slug
        match. This project's signature contains none of those four axis slugs.
        An auditor dispatch that reads the schema verbatim and does a slug-match
        will fault every speech bone in the project.
        Citing: bones.schema.md:140 and :165; pl-2026-05-30-003 (SOFT, open,
        target: /and-review pipeline).
      why: >
        HARD at gate level: any future /and-write Phase 6 or /and-review bones
        auditor dispatch that reads the schema verbatim and does slug-match
        validation will produce false HARD findings on every speech bone. The
        current workaround (admin precedent in decisions.md + parking-lot item)
        is fragile across sessions. Schema edit is required to formalize the
        custom-signature carve-out.
      criteria: >
        bones.schema.md § Dialogue-anchor bones canonical speech form entry
        must generalize the communication-class axis requirement to:
        ">=1 communication/relational-class axis per the active signature
        (universal questionnaire: community/knowledge/reputation/trust; custom
        signature: the axis or axes the series.substance.state_axes block
        designates as relational/communicative per the signature note)."
        AND series.substance signature block (showrunner-memory.schema.md
        or the project's series.substance entry) must carry a one-line note
        naming the custom communication-class axes (relational_anchor_status,
        social_tether-*) so each phase-2 auditor dispatch resolves from the
        schema, not from parking-lot precedent.

    - id: STRUCT-007
      type: fault
      severity: HARD
      what: >
        showrunner-memory.schema.md — chunk_targets.bone.delta_per_axis field
        (line ~144) records "1-3" as the bone delta floor. Project practice has
        shipped sub-1.0 bone magnitudes (c07: four sub-1.0 deltas; c08: 0.5
        magnitude at s01n06) with admin DEC-0002 override. The schema has no
        carve-out for sub-1.0 deltas when scene_target_delta_magnitude < 1.0.
        pl-2026-05-31-003 (SOFT, open, target: /and-review pipeline) documents
        this explicitly and names two formalization options.
        Citing: showrunner-memory.schema.md:~144; pl-2026-05-31-003.
      why: >
        HARD at gate level: each new /and-write Phase 2 auditor will reflag
        sub-1.0 deltas as HARD findings, requiring admin DEC override every
        chapter. The precedent lives only in decisions.md, not in the schema.
        This creates recurrence cost and potential for a session that skips the
        DEC check to HARD-abort a valid chapter.
      criteria: >
        showrunner-memory.schema.md chunk_targets.bone.delta_per_axis must be
        amended to add the sub-1.0 exception: "1-3 when scene_target_delta_
        magnitude >= 1.0; equals scene_target_delta_magnitude when
        scene_target_delta_magnitude < 1.0 (fractional sub-scene moves
        permitted at sub-1.0 scene targets; Phase 6 gate accepts magnitude
        == scene_target_delta_magnitude in these cases)." Preferred option (a)
        per pl-2026-05-31-003 — preserves the discipline floor for normal
        scenes while formalizing the carved exception.

    - id: STRUCT-008
      type: flag
      severity: SIGNAL
      what: >
        and-cohere.md Phase 0 step 3: `all` form resolves chapters where
        `chapters[<slug>].cold_read.verdict` is `PASS` or
        `PASS-WITH-DEPTH-PASS-REQUIRED`. Step 4 (cohere precondition) checks
        `chapters[<slug>].cold_read.verdict ∈ {PASS, PASS-WITH-DEPTH-PASS-REQUIRED}`.
        However, showrunner-memory.schema.md names the verdict field as
        `cold_read.verdict` (line ~317: "verdict: PASS | PASS-WITH-DEPTH-PASS-REQUIRED | FAIL").
        The field path in and-cohere.md refers to `chapters[<slug>].cold_read.verdict` —
        consistent with the schema's nesting (cold_read is a sub-block of chapters[]).
        No drift. PASS on this axis. Flagging for record: the field path should be
        verified on any future schema refactor of the cold_read block.
      why: >
        No current drift. Advisory note for future schema refactors.

    - id: STRUCT-009
      type: flag
      severity: SIGNAL
      what: >
        and-cohere.md Phase 7 step 3 says: "For every chapter that re-cascaded
        with result: PASS, append to chapters[<slug>].cohere_iterations in
        showrunner memory (schema field pending PROP-0031 triage; until then,
        persist to the iteration log)." The parenthetical explicitly documents
        that this is a pending schema field. showrunner-memory.schema.md has
        no `cohere_iterations` field in the chapters[] block. Controlled drift
        by design (pending triage), but the schema-vs-command-body gap is real.
        Citing: and-cohere.md Phase 7 step 3; showrunner-memory.schema.md
        chapters[] block (~lines 187-352).
      why: >
        Non-blocking because and-cohere.md explicitly routes to iteration log
        as fallback. But a future reader of the schema will not find the field
        and cannot validate that it was populated. Signal-level only; resolves
        when PROP-0031 triage adds the field to the schema.

    - id: STRUCT-010
      type: flag
      severity: SIGNAL
      what: >
        and-cohere.md Phase 6.5 step 5: "Stamp cohere_state.aggregate_emit_at: <ISO>
        (if the cohere-state schema does not declare this field, append
        '# Phase 6.5 stamped <ISO>' as a comment line...)". cohere-state.schema.md
        does not declare aggregate_emit_at in its format block (lines 19-56).
        The command body explicitly documents this gap with a comment-line fallback.
        Controlled drift; no immediate harm.
        Citing: and-cohere.md Phase 6.5 step 5; cohere-state.schema.md format
        block lines 19-56.
      why: >
        Signal-level. The fallback is explicit in the command body. Schema
        addition is the clean resolution; not blocking.

    - id: STRUCT-011
      type: flag
      severity: SIGNAL
      what: >
        and-cohere.md Phase 4.5 step 4: "Update state.iterations[<N>].classification_
        complete_at: <ISO> (extending the in-state-file iteration trace; if the schema
        does not name this field, add it as a '# Phase 4.5 stamped <ISO>' comment
        line beneath the iteration's verdict_trace[] entry)." cohere-state.schema.md
        verdict_trace[] shape (lines 32-38) does not include classification_complete_at.
        Again a documented gap with fallback.
        Citing: and-cohere.md Phase 4.5 step 4; cohere-state.schema.md lines 32-38.
      why: >
        Signal-level. Same pattern as STRUCT-010. Fallback explicit.

    # ── SCHEMA vs RUBRICS ─────────────────────────────────────────────────

    - id: STRUCT-012
      type: pass
      severity: SIGNAL
      what: >
        Checked design/shoot-v2/rubric-*.md existence. The /and-review.md pipeline
        subcommand description (line 201-205) specifies checking field refs in
        design/shoot-v2/rubric-*.md including axes_held[], axes_in_motion[],
        dramatic_shape enums, rhythm-shape, peak-bones, chapter_class.
        No rubric files under design/shoot-v2/ were found to be readable in this
        session (the path pattern design/shoot-v2/rubric-*.md is referenced in
        and-review.md and and-facets.md but the files themselves were not
        read as part of this audit). This finding records that the rubric-file
        portion of the tri-walk was not fully executed due to file-access scope.
      why: >
        Rubric files are the third leg of the tri-walk. Their absence from this
        read does not constitute a PASS. Downstream auditors should verify the
        rubric leg independently. The command-body references to rubric paths
        (and-review.md line 164: "design/shoot-v2/rubric-*.md"; and-facets.md
        line 5: "design/shoot-v2/rubric-*.md") are present-but-unverified.

    # ── COMMAND BODIES vs RUBRICS ─────────────────────────────────────────

    - id: STRUCT-013
      type: flag
      severity: SIGNAL
      what: >
        and-facets.md description line 1 references
        "staff/dialogue-writer/rubric-dialogue.md" as the locked dialogue rubric.
        and-review.md bones subcommand (line 164) references
        "design/shoot-v2/rubric-*.md" for per-facet rubric runners. These two
        paths are inconsistent: dialogue rubric is at staff/, not at design/shoot-v2/.
        Whether rubric-dialogue.md exists at staff/dialogue-writer/ was not verified
        in this session. The path inconsistency is noted as a flag.
        Citing: and-facets.md:1; and-review.md:164.
      why: >
        If the rubric path resolution differs between /and-facets (reads
        staff/dialogue-writer/rubric-dialogue.md) and /and-review facets (reads
        design/shoot-v2/rubric-*.md), the two commands may run dialogue review
        against different rubric versions. Signal-level until rubric file
        existence is confirmed.

    # ── RESIDUE SCAN ──────────────────────────────────────────────────────

    - id: RESIDUE-001
      type: pass
      severity: SIGNAL
      what: >
        Residue scan for known-deprecated terms: tensometer, tens: (load-bearing
        prefix), URI-026, tens-gate, /and-season, /and-judge-book,
        /and-shoot (legacy season-shoot), direction: + | -, chapter_class
        (if referenced-not-in-schema).
        Results:
        - tensometer: and-facets.md description line 1 explicitly states
          "Tensometer dropped under the substance overhaul" — historical
          reference, not load-bearing residue. TASTE-FLAG.
        - tens:: bones.schema.md line 121 explicitly names this as removed
          ("under the substance overhaul, tens: is removed — tensometer facet
          is dropped"). Historical documentation, not load-bearing. TASTE-FLAG.
        - URI-026: bones.schema.md line 150 references "Phase 6 substance
          bone-gate (the substance overhaul's replacement for URI-026 tens-gate)"
          — historical reference in attribution context. TASTE-FLAG.
        - /and-season: appears in and-review.md lines 282-292 in a Lift-source
          mapping table for the verdict subcommand — historical attribution
          explicitly labeling the prior command. TASTE-FLAG.
        - /and-judge-book: and-review.md line 33 subcommand discovery table
          and line 254 verdict subcommand header both say "absorbs former
          /and-judge-book" — explicit historical attribution. TASTE-FLAG.
        - direction: + | -: not found in live schemas (all schema direction
          fields use up|down per showrunner-memory.schema.md lines 172, 195,
          236, 260). PASS — no live residue.
        - chapter_class: showrunner-memory.schema.md line 203 declares
          chapter_class as an active field (OPTIONAL; defaults to standard;
          frame-coda for interludes). This is a live field, not deprecated.
          PASS on residue check; chapter_class is schema-active.
      why: >
        No load-bearing residue found. All instances are explicit historical
        attribution in overhaul documentation. No RESIDUE findings required.

    # ── CLAUDE.md SYNC ────────────────────────────────────────────────────

    - id: STRUCT-014
      type: pass
      severity: SIGNAL
      what: >
        CLAUDE.md agent routing table — cohere-related entries. CLAUDE.md
        does not list /and-cohere as a command in the agent routing table;
        it appears correctly in the Commands table as a command entry
        (/and-cohere <book> [<from>-<to>] ...). No agent routing table
        entry is expected or needed for a command (commands are not agents).
        The schema authority table in CLAUDE.md does not list
        cohere-state.schema.md — it lists: card, memory, showrunner-memory,
        audit-report, dialogue, bones, facet, persona-exemplar, scene-map,
        stitch-profile, stitch-feedback, stitch-render-log, parking-lot,
        admin-proposal, tournament-scorecard. cohere-state.schema.md is absent.
        Citing: CLAUDE.md schema authority table.
      why: >
        SIGNAL: cohere-state.schema.md is a live schema (written at every
        /and-cohere invocation per the command body) and is not in the CLAUDE.md
        schema authority table. This is a documentation gap rather than a
        functional break (the schema file path is documented in and-cohere.md
        and cohere-state.schema.md itself). Recommend adding to the table at
        next CLAUDE.md pass. Non-blocking.

    - id: STRUCT-015
      type: flag
      severity: SIGNAL
      what: >
        CLAUDE.md schema authority table also does not list aggregate-state.schema.md,
        which is referenced by and-cohere.md Phase 6.5 as a live schema
        ("Schema authority. READ schemas/aggregate-state.schema.md before
        authoring this phase's dispatch"). Two schemas referenced by live
        command bodies are absent from the CLAUDE.md schema authority table.
        Citing: CLAUDE.md schema authority table; and-cohere.md Phase 6.5 step 1.
      why: >
        SIGNAL: same documentation-gap pattern as STRUCT-014. Not a functional
        break; the schema reference lives in the command body. Recommend
        adding both cohere-state.schema.md and aggregate-state.schema.md to
        the CLAUDE.md schema authority table.

    - id: STRUCT-016
      type: pass
      severity: SIGNAL
      what: >
        CLAUDE.md directory map theater/ subdir entry lists: bones/, facets/,
        proto-lines/, dialogue/. The proto-lines/ entry is legacy (pre-substance;
        current chain does not write to it per bones.schema.md). The map
        description says "canonical merged proto-line file with [<facet>:<id>]
        citations from R1+R2 union (intermediate artifact written by /and-facets
        Phase 2 / build_cite_index.py; consumed by /and-facets Phase 5 auditor
        + /and-stitch Phase 0/1)". Under the substance overhaul, the canonical
        merged proto-line file is now _cite-index.md at theater/facets/ (per
        and-facets.md), not at theater/proto-lines/. The CLAUDE.md description
        of theater/proto-lines/ maps to an artifact that has moved.
        Citing: CLAUDE.md directory map theater/; and-facets.md Phase 2
        (build_cite_index.py → _cite-index.md at theater/facets/).
      why: >
        SIGNAL (not HARD): the proto-lines/ directory is labeled as a legacy
        path in bones.schema.md ("Legacy paths ... are no longer authored").
        The CLAUDE.md map retains the description of the old artifact in the
        old location. A new session reading CLAUDE.md for the first time would
        not be misled about what to write (bones.schema.md overrides the CLAUDE.md
        directory map for artifact-path authority). But it is stale documentation.
        Recommend updating the theater/proto-lines/ entry to read "legacy;
        pre-substance projects on disk remain readable but the current chain
        does not write here."

    # ── PARKING-LOT ITEM EVALUATIONS ──────────────────────────────────────

    - id: PL-EVAL-001
      type: flag
      severity: SIGNAL
      what: >
        pl-2026-05-25-018 (post-move-axes-held-listing-schema-ambiguity, SOFT,
        open, target: /and-review pipeline). This tri-walk examined
        showrunner-memory.schema.md's axes_held[] documentation at scene,
        chapter, bone, and series levels, and the Field Notes section
        (lines ~412-415). The schema does not address the specific case of
        post-move holding of an in-motion axis — whether such holds require
        listing in axes_held[] at the scene level.
        Finding: the ambiguity is real and unresolved by this tri-walk.
        The schema field notes (lines ~413-415) explain the axes_in_motion /
        axes_held split and what makes a held-flat axis different from a
        chatter bone, but do not address the post-move case specifically.
        Recommendation: this tri-walk does NOT resolve pl-2026-05-25-018.
        Item should remain open. The resolution requires a schema edit that
        explicitly adds one of the two options: (a) implicit license (post-move
        holds on in-motion axes need not appear in axes_held[]) or (b)
        mandatory listing. The choice is a policy decision, not a factual
        discovery this audit can make.
      why: >
        Remains open. Recommend stamping: not resolved by this tri-walk;
        reclassification not warranted; a policy decision and schema edit
        are needed.

    - id: PL-EVAL-002
      type: flag
      severity: SIGNAL
      what: >
        pl-2026-05-30-003 (bones.schema.md speech-bone communication-class
        axis generalization, SOFT, open, target: /and-review pipeline).
        This tri-walk confirms the schema gap is real (STRUCT-006 above).
        The bones.schema.md at lines 140 and 165 still uses the universal
        questionnaire taxonomy. The admin ruling from 2026-05-30 is
        not in the schema.
        Recommendation: this tri-walk RESOLVES pl-2026-05-30-003 in the
        sense that it confirms the finding and classifies it (STRUCT-006
        HARD). The principal can stamp pl-2026-05-30-003 resolved once
        the fixer edits bones.schema.md per STRUCT-006's criteria.
        Recommend stamping as: "resolved by this tri-walk identification
        (STRUCT-006); pending fixer schema edit to close."
      why: >
        The tri-walk's STRUCT-006 HARD finding is the required output.
        Once the schema edit lands, pl-2026-05-30-003 is closed.

    - id: PL-EVAL-003
      type: flag
      severity: SIGNAL
      what: >
        pl-2026-05-31-003 (bone-floor-sub-1.0-exception-formalization, SOFT,
        open, target: /and-review pipeline). This tri-walk confirms the schema
        gap is real (STRUCT-007 above). showrunner-memory.schema.md still
        records "1-3" without the sub-1.0 exception. The DEC-0002 override
        and c07 precedent are not in the schema.
        Recommendation: this tri-walk RESOLVES pl-2026-05-31-003 in the same
        sense as PL-EVAL-002 — the HARD classification (STRUCT-007) is the
        required output. Principal can stamp as resolved-by-identification
        pending fixer schema edit per STRUCT-007 criteria.
      why: >
        Same pattern as PL-EVAL-002. STRUCT-007 is the closing finding.

    - id: PL-EVAL-004
      type: flag
      severity: SIGNAL
      what: >
        pl-2026-05-31-009 (cross-chapter-aliveness-scoring-question, SOFT,
        open, target: /and-review pipeline). The question asks whether aliveness
        scoring at /and-stitch Phase 4.5 + Phase 9 should optionally be
        cross-chapter when N consecutive chapters have shipped. This is a
        process-design question, not a schema drift finding. The tri-walk
        found no schema, command-body, or rubric gap that directly closes
        this question. The PROP-0022 / PROP-0023 / PROP-0030 references in
        the item are process proposals, not schema documents.
        Recommendation: this tri-walk does NOT resolve pl-2026-05-31-009.
        The question is orthogonal to the tri-walk's scope (schema vs command
        vs rubric drift detection). The item should remain open and route to
        a dedicated process-design review or be folded into a PROP-0023 /
        PROP-0030 amendment proposal. Reclassification from SOFT to TASTE-FLAG
        is not warranted — it is a genuine open design question.
      why: >
        Remains open. The tri-walk cannot close a process-design question.
        Recommend: keep open at /and-review pipeline; suggest routing to admin
        process-critic for a PROP-amendment decision.

  # ── SUMMARY ─────────────────────────────────────────────────────────────
  summary:
    total_findings: 16
    hard: 2   # STRUCT-006, STRUCT-007 — both schema gaps with fixer criteria
    signal: 12  # STRUCT-001/002/003/004/005/008/009/010/011/012/013/014/015/016
    pass: 5   # STRUCT-003, STRUCT-004, STRUCT-005, STRUCT-012 (partial), RESIDUE-001
    taste_flag: 0  # all deprecated-term refs are in controlled historical attribution
    parking_lot_items_evaluated: 4
      pl-2026-05-25-018: remains open — schema ambiguity requires policy decision
      pl-2026-05-30-003: resolves via STRUCT-006 — pending fixer schema edit
      pl-2026-05-31-003: resolves via STRUCT-007 — pending fixer schema edit
      pl-2026-05-31-009: remains open — process-design question, not tri-walk scope
    cohere_family_verdict: PASS-with-SIGNAL
      cross_check_1_dismissed_path: PASS (no drift between and-cohere.md DEFER path and cohere-state schema)
      cross_check_2_parking_lot_id_amendment: PASS ([-label] amendment backward-compatible; no command body breaks)
      cross_check_3_phase_4_numbering: PASS (no duplicate/skipped labels; pre-step sits correctly)
    next_dispatch: >
      Fixer should address STRUCT-006 (bones.schema.md speech-bone axis
      generalization) and STRUCT-007 (chunk_targets.bone.delta_per_axis
      sub-1.0 exception) as the two HARD findings. After those edits, the
      principal may stamp pl-2026-05-30-003 and pl-2026-05-31-003 resolved.
      STRUCT-014 / STRUCT-015 (CLAUDE.md schema authority table: add
      cohere-state.schema.md + aggregate-state.schema.md) and STRUCT-016
      (theater/proto-lines/ description stale) are low-blast-radius CLAUDE.md
      edits suitable for a single fixer pass.
```
