---
reviewer: cape-fic-reader
facet: vibes-b01-c08
phase: 5b-adversarial
round: R1
verdict: accept
generated: 2026-05-31
---

# Cape-Fic Reader — Vibes b01-c08 Adversarial Verdict

## Entry-level callouts

**[vibes:1] @24 loc:the-hook-ward — coverage-extends-through-absorbance**

The carve-out is the right move and the right scope. No library card, but the hook-ward is operationally live in bones, scene-map, and location-state — it is more present in this chapter's facet graph than some library-promoted locations are. The tokens (`watcher-network-subsumed-at-geometric-layer, two-surveillance-architectures-on-the-same-ground`) are exactly the kind of board-state reveal I come for: one surveillance architecture geometrically absorbing another without anyone on the absorbed side knowing. The SIGNAL-acceptable question is: does a downstream operator get actionable direction? Yes — studio and sensory authors know this ward is doubly-surveilled ground for b01c09+. That is a usable bias. Carve-out scoped to this entry only; no bleed.

ACCEPT. No convergence-trace overlap with auditor findings needed; carve-out was properly flagged in the facet file header.

---

**[vibes:2] @6 actor:taylor — atonement-as-repetition ++**

New tokens: `watcher-network-absorbed-without-asking, integration-uncounted-in-the-ledger`. Prior bundle carries `builds-surveillance-architecture` — that is the adjacent neighbor I would press on. But the event-frames are distinct: prior token says she builds one; new token says she absorbs an existing one without asking. Different action, different ethical weight on the ledger. The `integration-uncounted-in-the-ledger` token is the specific sin: the thing that went in without being logged. That is a debt, and I track debts. The in-entry comment justifies the event-frame distinction. String overlap: none. AP11 formal: clean.

Downstream operator: dialogue-writer reading `the-recognition-is-the-ledger-completing-not-a-revelation` (prior) alongside `integration-uncounted-in-the-ledger` (new) gets a directional pair — the ledger will complete, and this uncounted entry is one of the things it completes against. That is a live wire, not a dead token.

ACCEPT.

---

**[vibes:3] @13 actor:taylor — logistics-register-held +**

New keyword, correct `+` op. Tokens: `resentment-below-register, names-at-logistics-weight, held-discipline-visible-at-court-tier-names`. The @13 bone is the chapter's first on-screen moment where a court-tier name enters at logistics weight and the resentment stays flat. The keyword establishes a *rule*: when court-tier proper nouns appear in operational intake, the register holds. Rules that are stated early can be violated later. Held-discipline-visible-at-court-tier-names is the right signal: it is not absence of affect, it is suppressed affect. The dialogue-writer downstream knows to keep that register flat when Aemond's name or other court-tier names appear in operational context. That is clean actionable bias. AP8: tokens pass sentence-parsability test. Gate 4: state-update:10 and peak-bones :13/:15 are both on-screen.

ACCEPT.

---

**[vibes:4] @20 actor:oswyn — the-unknowing-contact ++**

Prior bundle: `trusts-taylor-as-neighborhood-fixture, does-not-know-what-the-trust-serves, ambient-not-dramatic, never-finds-out`. New tokens: `watcher-network-inside-coverage-matrix, casual-mention-as-intelligence-vector`. The adjacency to press: `does-not-know-what-the-trust-serves` vs. `watcher-network-inside-coverage-matrix`. Prior token is epistemic (he does not know what the trust serves); new token is structural (his watcher-network is inside the coverage matrix as infrastructure). These are different layers of the same ignorance. The `casual-mention-as-intelligence-vector` token names what just happened at @20 — his pass-mention delivered the courier-name, face-without-name became face-with-name-corwick. That is a political coherence moment. The trust now operates at infrastructure level AND conversational-intelligence-delivery level simultaneously. Doubled asymmetry. String overlap: none. AP11: clean.

This is exactly the faction-read that works. The reader who tracks who-knows-what-when sees the geometry: Oswyn's network is inside Taylor's without his knowledge, and his casual conversation is the delivery channel. Neither Oswyn nor corwick know what just happened. That is the information-asymmetry event I came for.

ACCEPT.

---

**[vibes:5] actor:aemond — rising-entrapment OFF-ANCHOR ++**

The entry to attack. Off-anchor (no @proto-line-id). Aemond does not appear on screen. The question: does `name-in-feed-before-body-arrives` carry the cape-fic foreshadow weight?

Yes — and here is why this is not a cheat.

The prior `rising-entrapment` bundle was authored for *appearance* events: `axis-movement-required-every-appearance, each-walk-on-tightens-the-calculation, embodied-consequence-makes-refusal-non-abstract`. These three tokens all assume a body arriving. The new token is a *pre-appearance* event: the name enters the architecture before the body does. This is not a dilution of the keyword; it is the phase that precedes the keyword's normal firing mode.

The downstream operator question: does the dialogue-writer or studio author reading the full `rising-entrapment` bundle after this `++` know what to do when Aemond first appears on screen? Yes — `name-in-feed-before-body-arrives` signals that his first appearance is a recognition-moment (Taylor's feed already has him mapped) rather than an introduction. That is a distinct and usable downstream bias. The operator would treat Aemond's physical arrival differently than they would without this token.

AP11 string overlap: none. The prior bundle has no feed-perimeter or pre-arrival tokens. Semantic-adjacency advisory: `each-walk-on-tightens-the-calculation` and `name-in-feed-before-body-arrives` are adjacent in that both track Aemond's progressive entrenchment in Taylor's architecture. But the event-frames are opposite-phase: a walk-on is embodied, this is a feed-name. The comment in the entry justifies this distinction. The advisory is addressed.

AP9 concern considered: could this be episode-scope instead of actor-scope? No. The bias is specific to how downstream operators should treat Aemond's first on-screen appearance — it is entity-bound. An episode-scope tag would lose the targeting.

Fan-out (gate 7): vibes:3 already fires on Taylor at the same event. Aemond's name enters via Jarvis logistics; the event affects Taylor (register-held, logged) and Aemond (feed-perimeter presence). Both fire. The hook-ward feed-station location has no library card and is not a target here — the event-frame is actor-bound, not location-charged.

The MARGIT NOTE axis-fence in Aemond's vibe card: `each appearance must shift a named axis; walk-on with no downstream calculation change is a fence violation`. A logistics name-entry is not a walk-on. The fence does not trigger. The `++` extension does not violate the fence; it adds a pre-appearance event-class that the fence's walk-on language does not cover.

The multi-source chain: `state-update:10` (the aemond-name logged at @13) and `canon:vhagar-handler-rotation-in-jarvis-logistics-b01c08` (the off-screen reason the name appears). Both are resolvable. Gate 4: PASS.

What I am watching for in b01c09+: when Aemond does appear on screen, does the author treat it as a recognition-moment for Taylor's architecture? If the first physical appearance is written as an introduction with no reference to the feed-entry, the `name-in-feed-before-body-arrives` token will have paid no downstream dividend. That would be a Crescent-debt situation. I am not flagging this now — the token is live and the author has been warned by the vibe itself — but I am tracking it.

ACCEPT with forward flag: the name-in-feed token must pay at Aemond's first on-screen appearance or it becomes an orphaned bias. Routing to: showrunner parking-lot attention, not a current block.

---

## Aggregated verdict

**ACCEPT**

All five entries pass the cape-fic adversarial read. The multi-source chains are defensible. The `++` extensions all distinguish event-frames from prior bundles. The off-anchor vibes:5 carries genuine cape-fic foreshadow weight — the name arriving in the feed before the body is the pre-appearance information-asymmetry event the reader wants to see established. The carve-out for vibes:1 is narrow, scoped, and operationally justified.

Forward flag (non-blocking): vibes:5 `name-in-feed-before-body-arrives` must pay at Aemond's first on-screen appearance. If his physical arrival is written without recognizing the prior feed-entry, the token is a debt. Showrunner should note this as a downstream authoring obligation.
