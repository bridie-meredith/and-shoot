# Archived rubrics

Rubrics shelved out of `design/shoot-v2/` so they're no longer consumed by the active pipeline. Preserved with full git history.

## Contents

- `rubric-tensometer-replaced-by-substance.md` — per-bone tens-rating rubric (1 / 2 / 3 charge bands; curve-shape; AP1-3 anti-patterns; frequency-band exemptions). Replaced under URI-SUBSTANCE-OVERHAUL (2026-05-17) by the substance contract — per-bone declared `substance_delta` + per-scene aggregate Δ + cost-ledger + density target. The substance contract serves the role tens was reaching for (per-bone substance-density signal with declared cause), more directly and with mandated linkage. URI-026 tens-gate is replaced by `/and-write` Phase 6 substance bone-gate.

## Reactivation

The tensometer rubric would only be reactivated if substance contracts proved insufficient as a pressure-signal — currently no plan to revive. If revived, `/and-facets` and `/and-write` would both need to add tens-author dispatches; per-bone `tens:` citation prefix would need to be re-added to `schemas/bones.schema.md`.
