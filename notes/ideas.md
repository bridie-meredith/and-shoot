# Ideas for Later
Where **brighid** places unorganized notes or thoughts that could be followed up on.
* there should be a spot in the library for procedural cards, similar to claude commands, except these are references that subagents can use at specifc moments. Below is an example of a **timeskip** card that could live in such a place. It could also hold the test harness assets for improving or creating other cards.
* [maybe not? it doesn't seem to be a need in current setup. there should be someone asking about where the interesting bits begin though.]**timeskip** is a card that can be used after an episode ends. It comes with a set of questions, such as: when will something be shown that is interesting to audience, stop there. In the period between last scene and next scene (where the **timeskip** occurs), what changes? Environment changes, character changes, state changes. The **timeskip** card can be used to replace stretches where plot is thin, these spots should be identified in initial planning between **chunks**, such as between **season chunks** when planning series or **episode chunks** when planning season. 
* ~~`/and-project` command needs an argument parser, or it needs to request an input when first called. showrunner needs to know what the active-project will be about to get going on creating it.~~ → **ADDRESSED:** and-project.md validates all five args on entry (slug, brief, three audience slugs) and prints usage + halts if any are missing or invalid.
* ~~vibe-clouds need to be tied into process. when are they generated with personas, when are they modified, when are they read by impersonator, how are they updated as episodes progress?~~ → **ADDRESSED:** actor vibes populated at cast housing (showrunner 1c), refreshed at episode start (and-shoot A2b), impersonator now uses vibes as pre-generation angle selection rather than post-hoc tint.
* ~~how does margit craft new cards, how does margit identify generic entries on catalogue?~~ → **ADDRESSED:** margit.md defines the `candidate-menu` operation (enumerates all plausible candidates including OC archetype slots; authors cards on selection) and the `provision` operation (copies cards into active-project, creates companion stubs). Generic entries are OC archetype slots always included in the menu.
* ~~how do we keep the editor from sounding like an AI? I have a list of pet-peeves of things that I noticed, but maybe we can also use something like "kill your darlings" where we take each sentence or fragment and score it on how much the AI likes it... the ones that the AI really like we kill (remove) unless it is very important (like a climax or a zinger or a character dialogue).~~ → **ADDRESSED:** editor card and agent def now include a hollow-prose-pattern pass — five named patterns cut on sight (over-qualification, told emotion, explanatory echo, thought announcements, narrator intrusion) with an explicit load-bearing exception. Score-and-kill mechanism tabled as bloat.
* ~~`/and-shoot <episode-identifier?>` defaults to most recent next episode per plan~~ → **ADDRESSED:** and-shoot.md takes an optional slug arg; defaults to `active.episode` from showrunner memory.
* ~~`/and-wrap <episode-identifier?>` defaults to most recent past episode per plan~~ → **ADDRESSED:** and-wrap.md takes an optional slug arg; defaults to the episode currently marked `shot` in showrunner memory.
* ~~some evidence points towards significant creativity biased based off initial prompt. i wonder if we're locking in too early in the planning stage, chasing a local maxima instead of a global one. there are a few ways we could test this. i'm wondering what would happen if between prompt and show-runner if there was a layer that found the bounds of the given prompt and flooded those bounds with potential ideas.~~ → **ADDRESSED:** Phase 1.5 (brief expansion) added to and-project between scaffold and showrunner dispatch. Screen-writer maps the concept-space the brief opens — alternative framings, building blocks, adjacent concepts — before any planning begins. Showrunner reads the expansion in 1a as a check against first-order lock-in.
* ~~when the bullet points are written for episode planning, what would it look like if there was zero exposition? because really, these bullets ought to be as minimalisitic as possible. xyz came, xyz saw, wyz conquered. we don't need any "... because they felt like it."~~ → **ADDRESSED:** screen-writer now has explicit episode bullet rule — action only, no motivation clauses, no because/since/wanting-to. Constraint-check step added: strip exposition from any bullet before submitting.
* ~~departure from televised series in paradigm, scene swaps within an episode aren't as well received in written media as they are in visual media. I'm wondering if the dramatist and audience would have even caught this irritation from swapping back and forth with multiple characters multiple locations multiple actions. it would be better to focus on one character (timeskips allowed between scenes) in one chapter, with mutliple perspectives sequentually instead of mixed (if at all in a single episode/chapter)~~ → **ADDRESSED:** dramatist now flags `[pov-fragmentation]` when bullets interleave multiple POVs mid-scene. Sequential perspective blocks permitted; mixed cutting is not.
* ~~general rule of thumb it seems is that AI locks in early, except the first idea is almost always not the best idea. how do we enable our agents to flush or revise context before committing?~~ → **ADDRESSED:** Phase 1.5 (brief expansion) added to and-project handles the planning-level problem. Impersonator vibe-cloud check handles the execution-level problem.
