# Bridge improvement-loop ledger

Cross-repo mining log. Each pass reads one area of `brighid-creative-writing`,
identifies the best transferable process/structure pattern, and files one admin
proposal in `staff/admin/process-proposals.md`. Persona/voice content is never
copied. Principal triages proposals; this loop only proposes.

Rotation order (advance after each pass):
1. ingrid rut-detection.plan.md + project-improvement-tracking.plan.md  ← MINED
2. critic/audience/narrator INDEX registries
3. director-cuts/
4. specs/pipeline-architecture.spec.md

---

## Pass 1 — 2026-06-11

area_mined: "staff/agents/ingrid/rut-detection.plan.md + project-improvement-tracking.plan.md"

pattern_found: >
  Ingrid's Axis D — "Improvement closure rate". At project-close, ingrid scans the
  improvement items routed since the previous project-close and reports how many were
  actually shipped (triaged + pr_ref set) before the new project opened. This is
  backward-looking accounting that complements the forward-looking per-verdict
  process-critic: "how much improvement debt are we carrying into the next book?"
  And-shoot's admin process-critic fires per-verdict (reactive) but never fires an
  aggregate at book-close asking how many accumulated open PROPs remain untriaged.
  The pattern also includes an auto-trigger that fires the closure-rate check at a
  defined milestone (project-close in brighid; book-verdict in and-shoot's analogue).

prop_filed: PROP-0053

next_area: "critic/audience/narrator INDEX registries (pass 2)"
