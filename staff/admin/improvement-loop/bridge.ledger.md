# Bridge Improvement-Loop Ledger

Cross-repo mining log. Reference harness: `/home/user/brighid-creative-writing`.
Target: `/home/user/and-shoot`. One pattern per pass; ledger rotates the mine area.

Schema: `area_mined | pattern_found | prop_filed | next_area`

---

## Pass 1 — 2026-06-12

**Area mined:** `staff/agents/ingrid/rut-detection.plan.md` + `staff/agents/ingrid/project-improvement-tracking.plan.md`

**What the area contains:**
- `rut-detection.plan.md`: a six-class rut taxonomy (exact-repeat, semantic convergence, register rut, location rut, foil-class rut, craft-gravity rut) with detection mechanisms. Oskar logs a register/location/foil summary at board close; Ingrid reads the summaries at project close; a rut fires when the same class appears in N≥2 boards across a project.
- `project-improvement-tracking.plan.md §Auto-trigger registry`: standing fire conditions at project close. Two triggers (parroting-tuning, subtraction-pass-tuning) check project artifact signals and auto-dispatch margit workshop routines when conditions are met. The mechanism is proactive — fires at project close even when all boards PASSed — and compounds the system's pattern coverage over time.

**Pattern with best transfer value:**
The **auto-trigger registry** + **rut-class detection** pattern combined. In and-shoot the admin process-critic fires REACTIVELY per chapter on non-PASS verdicts. There is NO mechanism to aggregate the same fault class across all chapters of a book and name it as a systemic defect. Brighid's pattern ports directly: after a book closes, scan all chapter audit reports for the book, count occurrences of each fault class, surface any class appearing in N≥3 chapters as a FAULT-RUT finding routed to admin process-critic as a targeted book-level proposal.

**Confirmed and-shoot does not have this:** Grep of `.claude/commands/` finds no cross-chapter fault-class aggregation, no "book-close" retrospective scan, no auto-trigger registry. PROP-0052 (sameness scan) reads bones/scene-maps for structural template repetition — distinct from reading audit reports for fault-class frequency.

**Evidence that and-shoot needs it:** DEC-0115 documents LEDGER-REGISTER running ~16 chapters before being caught. PROP-0046–PROP-0050 were each filed independently by per-chapter process-critic on per-instance verdicts — five separate proposals for the same defect class. No cross-chapter aggregator fired until the DEC-0115 diagnosis session. A fault-rut scan at book-close would have named the pattern at chapter 3.

**PROP filed:** PROP-0053

**Next area in rotation:** `critics/INDEX.md` + `audience/INDEX.md` — the four-peer persona directory structure with anxiety-axis cheat sheets and dispatch composition patterns by pipeline gate. And-shoot has `staff/audience/INDEX.md` (22 personas) but no equivalent `critics/` pool or dispatch composition pattern table. Transfer candidate: the "anxiety-axis cheat sheet" format adapted for and-shoot's auditor class library (matching fault classes to the substance axes they guard).
