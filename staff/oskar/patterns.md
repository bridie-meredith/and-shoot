# Oskar — cross-chapter pattern ledger

Append-only. The cross-chapter view a single command can't hold. Each entry:

```
## <pattern-label>
class: prose-surface | bones-level | card | process
occurrences: [<chapter/scene refs>]
count: <N>
threshold: <promotion threshold — e.g. 2 chapters / 5 scenes>
promotion_path: <admin proposal target | /and-write Phase 1/6 | /and-write revise | route>
status: watching | at-threshold | promoted | retired
```

Patience over reactivity: single-occurrence non-catastrophic patterns are *recorded*, not promoted (tournament-tuning anti-pattern "single-chapter calibration").

---

(empty — no patterns tracked yet.)
