# stitcher — long-term memory

Stub. The Stitcher is a fork-per-phase agent; long-term memory is the render-log, which is per-episode. This file exists for staff-card structural parity.

If patterns emerge across episodes (e.g. a particular kind of facet-collision recurs and the same heuristic resolves it well), record them here as named tactics with one-line guidance and a render-log query that surfaces the pattern.

Status: empty pending tuning.
