# stitcher — short-term memory

Stub. Per-phase forks do not carry state between phases — the render-log is the only inter-phase artifact. STM exists for staff-card structural parity and for any session-scoped notes the human wants the Stitcher's next dispatch to see.

Status: empty pending tuning.
