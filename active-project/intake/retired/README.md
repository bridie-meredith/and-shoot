# retired/

Archived planning artifacts superseded by an intake, each in `<date>-<name>/` with a one-line
tombstone (what it was · why retired · what replaced it). **Nothing is ever deleted.** Empty until
the first re-baseline retires something.
