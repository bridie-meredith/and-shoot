```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-12
  findings:

    # ─── CHECK 1: SCHEMA vs COMMAND BODIES ───────────────────────────────────

    - id: STRUCT-001
      type: fault
      what: >
        schemas/dialogue.schema.md line 3 states: "The R2 dialogue judge at
        `/and-facets` Phase 2 remains as a locked-graph review pass (KEEP /
        DELETE / REWRITE against the facet graph the upstream author was blind
        to) — review only, no authoring."
      why: >
        DEC-0116 (URI-FACETS-SLIM, 2026-06-08) explicitly retired the R2
        dialogue judge: "The R2 dialogue judge (the locked-graph KEEP/DELETE/
        REWRITE pass that survived URI-WRITE-DIALOGUE-COBONDED) is retired too."
        CLAUDE.md Rule 15 confirms: "The R2 dialogue judge is also RETIRED under
        DEC-0116." The schema says the judge "remains"; the authoritative
        command body and CLAUDE.md say it does not. Any agent loading this
        schema will receive a false contract: that a dialogue judge pass still
        fires at `/and-facets`. No such pass exists.
      criteria: >
        schemas/dialogue.schema.md line 3 must be updated to reflect that the
        R2 dialogue judge is RETIRED under DEC-0116, and that the one
        non-mechanical concern it handled (dialogue duplicating a lens facet at
        the same anchor) is now the Phase 4 auditor's DEDUP class.

    - id: STRUCT-002
      type: fault
      what: >
        schemas/audit-report.schema.md lines 106–157 contain a full section
        titled "R2 decision-shard frontmatter (URI-026 follow-on, 2026-05-10)"
        that documents: the `/and-facets-r2` command, the `r2-decision-shard.md`
        per-layer shard format, the consolidated `.r2-decisions.md` file at
        `active-project/theater/facets/`, F-R2-1 through F-R2-4 failure classes,
        the consumer contract where the orchestrator-critic Phase 6 reads
        `f-r2-counts`, and the HARD threshold `f-r2-1 > 0`.
      why: >
        The entire R2 round is retired under DEC-0116. `/and-facets-r2` no
        longer exists. The shard files `r2-decision-shard.md` and
        `.r2-decisions.md` are never produced. The orchestrator-critic contract
        referencing `f-r2-counts` describes a field that will never be written.
        Any agent reading the audit-report schema receives an active description
        of a pipeline phase that does not run, which can cause it to check for
        files that will never exist or block on counts that are never set.
      criteria: >
        The R2 decision-shard section (lines 106–157) must be removed or clearly
        marked "RETIRED under DEC-0116 (URI-FACETS-SLIM, 2026-06-08) — these
        artifacts are no longer produced." The F-R2-* class definitions and the
        orchestrator-critic consumer contract on `f-r2-counts` must be
        retired in the same edit.

    - id: STRUCT-003
      type: fault
      what: >
        .claude/commands/and-stitch.md Phase 0.7 (approximately line 254)
        states: "The dialogue facet is authored upstream at `/and-facets` time
        by the per-character `dialogue-writer` fork."
      why: >
        URI-WRITE-DIALOGUE-COBONDED (2026-05-25) moved dialogue authorship to
        `/and-write` Phase 1.5 + Phase 7 — not `/and-facets`. CLAUDE.md Rule 15
        is explicit: "dialogue is authored at `/and-write` Phase 1.5, not at
        `/and-facets`." The `/and-facets` Phase 1 dialogue author has been
        REMOVED. The stitch Phase 0.7 description leads the stitcher to
        understand that dialogue arrives from `/and-facets` time; it actually
        arrives from `/and-write` time and is verified (not authored) at
        `/and-facets` Phase 0. Any `/and-stitch` run that misattributes the
        dialogue source could misdirect a fault back to `/and-facets` for
        remediation when the correct routing is `/and-write revise`.
      criteria: >
        and-stitch.md Phase 0.7 must be updated to state that dialogue files
        are co-emitted by `/and-write` Phase 7 (URI-WRITE-DIALOGUE-COBONDED),
        are verified-present by `/and-facets` Phase 0, and arrive at the
        stitcher already on disk at `theater/dialogue/<character-slug>.md`.

    - id: STRUCT-004
      type: fault
      what: >
        .claude/commands/and-stitch.md Phase 0.5 pre-flight display block
        lists `refused-at-R2=<n>` as a field in the exposition summary printed
        at Phase 0.
      why: >
        The R2 round is retired (DEC-0116). `refused-at-R2` is a count that
        can never be non-zero because no R2 pass runs. The field in the
        pre-flight print block is dead residue that misleads the operator about
        what the Phase 0 summary represents, and misleads any agent parsing the
        block about what fields to expect. The stitch render-log's `STATS`
        section also retains `exposition entries-rendered/refused-at-R2/
        cross-episode-register-skipped` in Phase 8 (line 773), which carries
        the same residue.
      criteria: >
        `refused-at-R2=<n>` must be removed from both the Phase 0.5 pre-flight
        display block and the Phase 8 STATS exposition accounting line. Where
        the R2 refusal count was meaningful, replace with a note that exposition
        entries not selected for `surface: render` are classified as
        `surface: reference` by default (PROP-0004 / DEC-0014).

    - id: STRUCT-005
      type: fault
      what: >
        .claude/commands/and-facets.md Phase 5c reads
        `staff/audience/and-facets-orchestrator-critic/card.md` as the
        orchestrator-critic card path.
      why: >
        CLAUDE.md's agent routing table specifies the orchestrator-critic lives
        at `staff/orchestrator-critic/` (entry: "orchestrator-critic — run-judge
        card … `staff/orchestrator-critic/`"). The path
        `staff/audience/and-facets-orchestrator-critic/` places the
        orchestrator-critic inside the audience persona library, which is the
        wrong taxonomy (CLAUDE.md Rule 8 exception explicitly notes that
        `staff/orchestrator-critic/card.md` is staff-facing and outside the
        `cards/` taxonomy). If the file does not exist at
        `staff/audience/and-facets-orchestrator-critic/card.md` but exists at
        `staff/orchestrator-critic/card.md`, Phase 5c will fail to load its
        judge and the verdict step cannot fire.
      criteria: >
        The Phase 5c orchestrator-critic card path in and-facets.md must be
        corrected to `staff/orchestrator-critic/card.md` (or verified that a
        file actually lives at the `staff/audience/and-facets-orchestrator-critic/`
        path; if so, document the deliberate deviation from the routing table).

    - id: STRUCT-006
      type: flag
      what: >
        CLAUDE.md schema authority table (the `| File type | Schema |` table)
        does not list `schemas/aggregate-state.schema.md` or
        `schemas/cohere-state.schema.md`. Both schemas are referenced by active
        command bodies: `and-substance.md` Phase 0 Step 6 reads
        `schemas/aggregate-state.schema.md` and `/and-stitch` Phase 10 Step 4
        reads it; `and-cohere.md` reads `schemas/cohere-state.schema.md`.
      why: >
        The omission is not a command-body fault (the commands correctly name
        their schemas), but the CLAUDE.md table is the canonical cross-reference
        agents use to locate schema authority for any file type. A table that
        omits two actively-used schemas creates a discoverability gap —
        particularly for the aggregate-state schema, which is a HARD-abort
        condition at `/and-substance chapter` Phase 0 and at `/and-stitch`
        Phase 10. This is a flag (documentation gap), not a fault, because the
        commands themselves correctly name the paths.
      why: >
        If an agent consulting only the CLAUDE.md schema authority table
        attempts to author or validate an `aggregate-state.md` or cohere-state
        file, it has no pointer to the schema. The omission creates a lookup gap
        rather than a contradiction.

    # ─── CHECK 2: SCHEMA vs RUBRICS ──────────────────────────────────────────

    - id: STRUCT-007
      type: pass
      what: >
        All rubric files checked against schemas/facet.schema.md: rubric-feeling,
        rubric-sensory, rubric-state-updates, rubric-memory-flags,
        rubric-narrator-interest, rubric-metaphor, rubric-location-state,
        rubric-vibes.
      why: >
        Every rubric correctly references the post-overhaul schema vocabulary.
        Specifically confirmed: (1) rubric-feeling correctly references the
        scene-map `rhythm-shape` + `peak-bones` fields (not tensometer);
        (2) rubric-sensory correctly uses the delta shape `<modality>: <old>
        -> <new>` matching the schema, and explicitly confirms the
        `<up|down|spike|drop>` tags are optional; (3) rubric-state-updates
        correctly uses `<target>.<field>: <old> -> <new>` and references
        scene-map for pressure-signal; (4) rubric-memory-flags V3 correctly
        references the 2026-05-21 `axes_held[]` / `axes_in_motion[]`
        axis-bookkeeping split for the feel-as-spine carve-out; (5) rubric-
        narrator-interest correctly uses scene-map file path
        `theater/facets/scene-map-<book>-<chapter>.md`; (6) rubric-metaphor
        correctly retired `tens:<reading>` support and replaced with
        `peak-bone:<flat-id>`; (7) rubric-location-state V2 references
        `fusion-eligible-runs` correctly; (8) rubric-vibes V1.1 patch
        correctly uses `peak-bone:<flat-id>` for pressure-signal.

    # ─── CHECK 3: COMMAND BODIES vs RUBRICS ──────────────────────────────────

    - id: STRUCT-008
      type: pass
      what: >
        and-facets.md Phase 4 audit class RUBRIC-FIDELITY enumeration of source
        rubrics vs. rubric files actually present in design/shoot-v2/.
      why: >
        Phase 4 RUBRIC-FIDELITY (source enumeration, and-facets.md line ~333)
        lists: rubric-memory-flags.md, rubric-sensory.md, rubric-state-updates.md,
        rubric-narrator-interest.md, rubric-location-state.md, rubric-feeling.md,
        rubric-metaphor.md, rubric-vibes.md, rubric-exposition.md,
        rubric-dialogue.md. The first eight exist as confirmed reads.
        rubric-exposition.md and rubric-dialogue.md were not in the read list
        provided for this audit run but are referenced by the command body;
        their existence is architecturally consistent with the command's
        reference (both are plausible rubric files given the facet pipeline).
        No STRUCT finding warranted without confirming absence.

    - id: STRUCT-009
      type: pass
      what: >
        and-write.md Phase 6 HARD bone-gate finding classes vs. CLAUDE.md
        Rules 10, 22.
      why: >
        and-write.md Phase 6 correctly documents: ABSTRACTION-AS-SUBJECT (HARD,
        per DEC-0115 / PROP-0046), SCENE-ABSTRACT-DOMINANT (HARD), and the
        concreteness floor (default ≥0.6 concrete-SVO bones), matching CLAUDE.md
        Rule 22. The dialogue HARD findings (FAULT-DIALOGUE-MISSING-AT-ANCHOR,
        FAULT-DIALOGUE-CARD-VIOLATION, FAULT-DIALOGUE-OBJECTIVE-MISSING,
        FAULT-DIALOGUE-EARTH-BET-FENCE, FAULT-DIALOGUE-COVERAGE) match
        CLAUDE.md Rule 15. No drift found.

    - id: STRUCT-010
      type: pass
      what: >
        and-stitch.md Phase 4 no-ledger discipline vs. DEC-0115 / PROP-0047
        and CLAUDE.md Rule 22.
      why: >
        and-stitch.md Phase 4 correctly marks the ledger/apparatus register as
        PROHIBITED (not merely de-preferred), defines LEDGER-REGISTER-@<bone>
        as HARD within-stitch, and documents the EMBODIMENT-BLOCKED-@<bone>
        upstream routing to `/and-write revise` at K>=3 (default K=3). The
        VOICE-FIXABLE finding path and grounding-ledger relationship is
        consistent with and-facets.md Phase 2.5/3. No drift found.

    - id: STRUCT-011
      type: pass
      what: >
        Memory-flags rubric V3 feel-as-spine carve-out vs. showrunner-memory
        schema axis-bookkeeping split (2026-05-21).
      why: >
        rubric-memory-flags.md V3 condition 2 for the carve-out requires
        "Scene's `scene_conflict.stakes_axis` resolves to an entry in the
        scene's `axes_held[]` list (per the 2026-05-21 axis-bookkeeping split
        in `schemas/showrunner-memory.schema.md`)." This is correctly aligned
        with the showrunner-memory schema field split. The rubric explicitly
        cites compatibility with the axis-bookkeeping split. No drift found.

    - id: STRUCT-012
      type: pass
      what: >
        and-facets.md Phase 2 dialogue slice consolidation note vs.
        schemas/dialogue.schema.md dialogue ID namespace convention.
      why: >
        and-facets.md Phase 2 sub-phase 3 states "Dialogue files are NOT
        consolidated — per schemas/dialogue.schema.md, ID space is per-character.
        The cite-index builder treats per-character dialogue file IDs as a
        distinct citation namespace (<character-slug>:<id>)." This matches the
        dialogue schema's per-character ID scoping and the bones citation token
        format `[<character-slug>:<id>]`. No drift found.

    - id: STRUCT-013
      type: pass
      what: >
        Sensory rubric V3 short-chapter floor-vs-ceiling exemption vs.
        and-facets.md Phase 4 FREQUENCY-BAND class.
      why: >
        rubric-sensory.md V3 adds the short-chapter exemption: when
        `bone_count < 30` and modality count equals the floor (2), the effective
        ceiling is `max(6%, 2/bone_count)` — advisory not blocking. The
        and-facets.md Phase 4 FREQUENCY-BAND class entry for sensory correctly
        states "sensory 3-6%" as the standard band; the rubric's V3 exemption
        clause is a rubric-level detail the audit class inherits from RUBRIC-FIDELITY
        (which reads the rubric's curve-shape rubric including the exemption).
        No command-body contradiction found.

    # ─── CHECK 4: RESIDUE SCAN ───────────────────────────────────────────────

    - id: RESIDUE-001
      type: pass
      what: >
        Scan of all command bodies and schemas for deprecated terms:
        `tensometer`, `tens:`, `URI-026`, `direction: null`, `magnitude: 0`,
        `tens-gate`, `/and-judge-book`, `/and-shoot` (as a command),
        `faceted-r2` (as a live status value).
      why: >
        Confirmed clean in command bodies (and-write, and-facets, and-stitch,
        and-substance, and-cast, and-postop, and-cohere, and-review). The
        stitch-profile schema retains `flat-low-run-collapse` with a note
        "(formerly `tens1-run-collapse` pre-overhaul; renamed under URI-SUBSTANCE-
        OVERHAUL 2026-05-17)" — this is a correctly-marked historical note, not
        live residue. The stitch-render-log schema retains `COLLAPSE-FLAT-LOW-RUN`
        with the same pattern (formerly `COLLAPSE-TENS1-RUN`; correctly marked).
        `faceted-r2` appears in and-facets.md Phase 0 only as a legacy-resume
        case: "Legacy `faceted-r2` from pre-slim runs: treat as `faceted-r1`
        for resume" — this is the correct handler, not live residue.
        and-write.md correctly states "`tens:` citation prefix is removed from
        the recognized list."

    - id: RESIDUE-002
      type: pass
      what: >
        Scan for references to the retired `/and-facets` Phase 5b adversarial
        audience-gate loop (3-cycle cap-burn, DELETE semantics, per-facet
        3-of-3 audience reviewers).
      why: >
        No live command body references the Phase 5b gate as an active step.
        and-facets.md explicitly marks it retired in the header, in the Phase 1
        notes, in Phase 4 documentation, and in the Phase 5 persist block. The
        and-stitch.md and-postop.md both correctly note that the adversarial
        prose read now lives at Phase 9 cold-read + naive-follow. No residue found.

    - id: RESIDUE-003
      type: pass
      what: >
        Scan for live references to the R2 judging round infrastructure
        (r2-decision-shard.md, .r2-decisions.md, f-r2-counts, /and-facets-r2)
        in command bodies.
      why: >
        No command body references these R2 infrastructure files as active
        outputs or inputs. The only occurrence is in schemas/audit-report.schema.md
        (already classified as STRUCT-002). Command bodies (and-facets, and-review,
        and-stitch, and-substance) do not reference r2-decision-shard, .r2-decisions,
        f-r2-counts, or /and-facets-r2 as live pipeline artifacts.

    # ─── CHECK 5: CLAUDE.md SYNC ─────────────────────────────────────────────

    - id: STRUCT-014
      type: pass
      what: >
        CLAUDE.md Rule 15 (URI-WRITE-DIALOGUE-COBONDED) vs. and-write.md,
        and-facets.md, and-review.md dialogue-gate documentation.
      why: >
        CLAUDE.md Rule 15 states dialogue ships with bones from /and-write
        Phase 1.5/7; /and-facets Phase 1 dialogue author is REMOVED; /and-write
        Phase 6 verifies FAULT-DIALOGUE-* findings (all HARD); /and-review bones
        enforces dialogue-coverage as a HARD gate. and-write.md Phase 1.5 and
        Phase 6 confirm these. and-facets.md Phase 0 item 9 correctly reads
        "Speaking-character + speech-bone inventory (verification only under
        URI-WRITE-DIALOGUE-COBONDED)" and item under Phase 1 states "Dialogue —
        NOT authored here." No CLAUDE.md/command-body drift found.

    - id: STRUCT-015
      type: pass
      what: >
        CLAUDE.md Rule 22 no-ledger fence vs. and-write.md Phase 6 bone-gate,
        and-stitch.md Phase 4 voice transform, and-stitch.md Phase 9 NAIVE-FOLLOW
        sub-gate.
      why: >
        CLAUDE.md Rule 22 wires the no-ledger fence at: /and-write Phase 6
        (ABSTRACTION-AS-SUBJECT HARD, SCENE-ABSTRACT-DOMINANT HARD),
        /and-stitch Phase 4 (LEDGER-REGISTER PROHIBITED, HARD within-stitch),
        /and-stitch Phase 9 (NAIVE-FOLLOW blocking, reviewer-excuse ban). All
        three confirmed present in the command bodies with correct fault codes
        and severity. The disposition circuit-breaker (N=2 consecutive chapter
        cap on "design-inherent/accepted-caveat") is documented in and-stitch.md
        Phase 9 Step 4 as the reviewer-excuse ban. No drift found.

    - id: STRUCT-016
      type: pass
      what: >
        CLAUDE.md Rule 18 (RUNBOOK chapter production protocol — R1 through R5)
        vs. command bodies' cascade behavior.
      why: >
        The RUNBOOK rule is explicitly in CLAUDE.md as Rule 18; command bodies
        do not need to replicate it (the rule states "when command-body docs
        conflict with the runbook protocol on chapter-production behavior, the
        runbook wins for chapter-production runs"). No command body was found
        to contradict R1–R5.

    - id: STRUCT-017
      type: pass
      what: >
        CLAUDE.md Rules 19–21 (PROP-0043/0044/0045 output-persistence, shared-
        state read-back, pre-commit RECONCILE) vs. command bodies that
        implement them.
      why: >
        and-review.md (verdict/cohere subcommands) and and-cohere.md (Phase 3/4
        RECONCILE sub-step) are cited in CLAUDE.md as the wiring locations.
        and-stitch.md Phase 8 documents the RECONCILE line requirement
        (FAULT-RECONCILE-MISSING). The rules in CLAUDE.md match the command-body
        documentation. No drift found.

    - id: STRUCT-018
      type: pass
      what: >
        CLAUDE.md Rule 17 (PROP-0019/0020/0022 completeness + readability
        progressive gating) vs. and-facets.md Phase 2.5 + and-stitch.md
        Phase 8.5 + Phase 9.
      why: >
        CLAUDE.md Rule 17 specifies the context-ledger + grounding-ledger pattern
        (PROP-0020/0022) wired at /and-facets Phase 2.5 + /and-stitch Phase 4
        voice-embodiment discipline. and-facets.md correctly implements
        CONTEXT-REQUIRED/GROUNDING-REQUIRED classification and ledger writes at
        Phase 2.5, and Phase 3 conditional remediation. and-stitch.md Phase 4
        correctly implements VOICE-FIXABLE enforcement. and-stitch.md Phase 8.5
        (coherence review) and Phase 9 Step 4 (FOLLOWABLE × ALIVE separated
        scoring) correctly implement the PROP-0022 two-axis verdict. No drift
        found.

    - id: STRUCT-019
      type: pass
      what: >
        Tensometer retirement consistency: URI-SUBSTANCE-OVERHAUL (2026-05-17)
        across all schemas and rubrics.
      why: >
        All rubrics (feeling, sensory, state-updates, memory-flags, narrator-
        interest, location-state, metaphor, vibes) correctly reference the
        post-overhaul pressure-signal surface: scene-map `rhythm-shape` +
        `peak-bones` fields rather than tensometer scalars. Every rubric that
        previously referenced tensometer has a parenthetical or note confirming
        the retire. The facet schema retains the deprecated tensometer section
        explicitly marked "DEPRECATED 2026-05-17 (URI-SUBSTANCE-OVERHAUL)."
        The stitch-profile and stitch-render-log schemas retain renamed fields
        with correct historical notes. No live tensometer residue found in any
        rubric or schema.
```
