```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-12
  triggered_by: >
    Improvement-loop test routine (first run; ledger initialized). Lens (a):
    /and-review pipeline tri-walk. Focus: post-DEC-0115 (no-ledger overhaul,
    2026-06-08) + post-DEC-0116 (slim /and-facets, retire R2 + Phase 5b
    audience-gate, 2026-06-12) drift that was not captured by the prior
    pipeline runs (pipeline-20260607T004417Z.md + pipeline-legs23-20260607T010305Z.md).
    Those runs predated both major refactors; this run re-walks the three legs
    scoped to the newly touched surfaces. Legs 4 (residue scan) and 5
    (CLAUDE.md sync) are also executed for the changed sections.

  open_parking_lot_items:
    - id: pl-2026-05-25-018
      severity: SOFT
      summary: >
        axes_held ambiguity: showrunner-memory.schema.md does not specify
        whether post-move holding of an in-motion axis requires listing in
        axes_held[]. Still unresolved; surfaced per Rule 14.

  findings:

    # ── LEG 1: SCHEMA vs COMMAND BODIES ─────────────────────────────────────

    - id: STRUCT-NEW-001
      type: fault
      severity: HARD
      what: >
        schemas/audit-report.schema.md §"R2 decision-shard frontmatter"
        (lines 106-157) defines the F-R2 class taxonomy, the `.r2-decisions.md`
        consolidated file format, and F-R2 thresholds. staff/orchestrator-critic/card.md
        B7 (lines 119-124) reads `f-r2-counts` from `active-project/theater/facets/.r2-decisions.md`
        and gates on it; F7-r2 (lines 196-209) fires a HARD-FAIL verdict trigger
        (`f-r2-1 > 0`); the verdict template (lines 288-292) includes the F-R2
        counts block. All of this infrastructure is tied to the R2 judging round,
        which is retired under DEC-0116 (the R2 round no longer exists in
        /and-facets). The `.r2-decisions.md` file is never produced by the current
        pipeline. B7 always reports `not-fired` for all new chapters; F7-r2 FAIL
        can never trigger. Dead HARD verdict trigger + dead schema section.
      why: >
        HARD: an active verdict card (orchestrator-critic) carries a HARD-FAIL
        trigger (F7-r2) tied to a file the current pipeline will never write.
        This is zombie infrastructure: it cannot cause a false-positive FAIL
        (the absent-file path resolves to `not-fired`), but it (a) occupies
        reasoning context in every verdict run, (b) misleads a fresh auditor
        into thinking F-R2 tracking is live, and (c) the schema section it
        references is stale specification that contradicts the post-DEC-0116
        pipeline description. Schema drift at a verdict-card level.
      criteria: >
        schemas/audit-report.schema.md §"R2 decision-shard frontmatter"
        (lines 106-157) must be marked RETIRED (DEC-0116) with a tombstone
        note: "The R2 judging round is retired. This section describes
        infrastructure that is no longer produced. Preserved for archive
        reference only."
        staff/orchestrator-critic/card.md B7 + F7-r2 blocks must be marked
        RETIRED (DEC-0116) with a note that F7-r2 cannot fire because the
        consolidated file is never produced; or removed in a follow-up card
        edit. The B7 verdict-template block (lines 288-292) must be removed
        or replaced with a "not-applicable post-DEC-0116" line.
        Owner: fixer (schema + card edits). Card content change → route to
        the card's owner (principal) per card-revise conventions if the
        principal has standing over orchestrator-critic card mutations.

    - id: STRUCT-NEW-002
      type: fault
      severity: HARD
      what: >
        and-stitch.md Phase 8 STATS line (line 773): the render-log STATS
        format includes `refused-at-R2` in the exposition entries field —
        "exposition entries-rendered/refused-at-R2/cross-episode-register-skipped".
        and-stitch.md line 236 also surfaces "refused-at-R2 entries listed for
        the auditor." schemas/stitch-render-log.schema.md has no `refused-at-R2`
        field (confirmed: grep returns no output). Under DEC-0116, no exposition
        entries are refused at R2 (there is no R2 round). The command body
        references a STATS field the schema does not define.
      why: >
        HARD: schema-vs-command-body drift on an emitted artifact field. The
        render-log schema is the contract between and-stitch (the emitter) and
        any consumer reading render-log files (the /and-review facets subcommand,
        the orchestrator-critic, the auditor). An undefined field in the emitted
        STATS format produces either (a) a confusing but harmless `refused-at-R2: 0`
        annotation, or (b) a consumer that parses the STATS block and fails to
        find the field in the schema. Either way: schema and command body disagree
        on what the emitted file contains.
      criteria: >
        Drop `refused-at-R2` from the STATS format in and-stitch.md Phase 8
        line 773. Replace with either (a) nothing (the field is irrelevant
        post-DEC-0116 and was only useful for tracking R2 deletions), or
        (b) a note such as `surface:render-entries` / `surface:reference-entries`
        to track the distinction that replaced the R2 decision (entries are
        now reference-or-render by default per PROP-0004/DEC-0014, not
        refuse-at-R2). Line 236 "refused-at-R2 entries listed for the auditor"
        must be updated to reflect the current field name or removed.
        schemas/stitch-render-log.schema.md must be checked for any similar
        stale exposition-stats field definitions and updated to match.
        Owner: fixer.

    - id: STRUCT-NEW-003
      type: fault
      severity: HARD
      what: >
        and-cut.md Phase 1 (line 18): chapter status enum listed as
        "planned, bones-written, audited-r1, faceted-r2, stitched" — includes
        `faceted-r2` without a retirement note.
        and-cut.md Phase 1 (line 39): pipeline_position derivation: "pre-stitch
        — active chapter is `audited-r1` or `faceted-r2`" — treats `faceted-r2`
        as a current valid status without a retirement note.
        schemas/showrunner-memory.schema.md line 368 explicitly marks
        `faceted-r2` as "RETIRED (URI-FACETS-SLIM / DEC-0116) — Legacy value
        from the pre-slim two-round pipeline; the R2 judging round is retired.
        New runs never set it; treat as `faceted-r1` on resume."
        The schema retired the status; the command body still presents it as
        a live option with no annotation.
      why: >
        HARD: the status enum is the command's authoritative read surface for
        chapter state. If a session-fresh instance reads and-cut.md to determine
        valid chapter statuses, it will treat `faceted-r2` as a current pipeline
        status and route it identically to `audited-r1` — which happens to be
        correct behavior (and-facets.md Phase 0 says "treat as faceted-r1 for
        resume") but for the wrong reason. A naive auditor reading and-cut.md
        without cross-referencing the schema would not know `faceted-r2` is
        retired. Schema and command body are out of sync on the status enum.
      criteria: >
        and-cut.md Phase 1 line 18 must add a note marking `faceted-r2` as
        RETIRED, e.g. "planned, bones-written, audited-r1, faceted-r2 (RETIRED;
        treat as audited-r1 on resume per DEC-0116), stitched."
        and-cut.md Phase 1 line 39 must include a parenthetical:
        "(Note: `faceted-r2` is a RETIRED status from the pre-slim pipeline;
        treat as `audited-r1` if encountered — pre-stitch position applies.)"
        Owner: fixer.

    # ── LEG 5: CLAUDE.md SYNC ────────────────────────────────────────────────

    - id: STRUCT-NEW-004
      type: flag
      severity: SIGNAL
      what: >
        CLAUDE.md schema authority table (lines 116-136): does NOT list
        `schemas/cohere-state.schema.md` or `schemas/aggregate-state.schema.md`.
        Both are active schemas:
        - cohere-state: referenced by .claude/commands/and-cohere.md at lines
          17, 53, 282, 313 ("schemas/cohere-state.schema.md" as the state file
          schema); also referenced in Rule 20's example ("cohere-state files").
        - aggregate-state: referenced by and-cohere.md at lines 239, 243, 250,
          251, 264 ("schemas/aggregate-state.schema.md"). The file it governs
          (active-project/staff/showrunner/aggregate-state.md) is the forward-feed
          channel for the post-cohere convergence pass.
        The table also does not list the theater/_inflight/ and theater/_archive/
        subdirectories that are created and used by /and-facets Phase 0 (steps 5
        and 8) and are not described in the CLAUDE.md directory map.
      why: >
        SIGNAL: incomplete schema authority table means a session-fresh instance
        reading CLAUDE.md to find schema authority for cohere-state or
        aggregate-state will not find a pointer and may read the schema directly
        (acceptable) or skip schema validation entirely (risky). The directory
        map omission is a minor orientation gap for new sessions — the
        _inflight/ and _archive/ subdirs are created at runtime by /and-facets
        and are expected to be absent at session start, but not documenting them
        creates confusion ("is this normal?") when a session scans the theater/
        directory and finds unexpected subdirs.
      criteria: >
        CLAUDE.md schema authority table must add:
        "| Cohere iteration state | schemas/cohere-state.schema.md |"
        "| Aggregate cross-chapter state | schemas/aggregate-state.schema.md |"
        CLAUDE.md directory map theater/ section should note that
        theater/facets/_inflight/ (R1 authoring scratch space; created per-run
        by /and-facets Phase 0) and theater/facets/_archive/ (prior-chapter
        facet archive; created by /and-facets Phase 0 cross-chapter clearance)
        are runtime-created subdirectories, not persistent.
        Owner: fixer.

    - id: STRUCT-NEW-005
      type: flag
      severity: SIGNAL
      what: >
        and-stitch.md Phase 0.6 (line 204): "The exposition-author's R1+R2
        should have rendered in the profile's voice, but the stitcher re-checks."
        and-stitch.md Phase 3 (line 349): "The exposition-author's R1+R2 already
        shaped the gloss for audience-fit, anti-jargon, hollow-prose, asinine,
        voice."
        Under DEC-0116, exposition is authored in a single R1 pass at
        /and-facets Phase 1. There is no R2 exposition judge or R2 exposition
        pass. The "R1+R2" description at both lines is factually wrong about the
        current pipeline — it describes the pre-slim two-round authoring process.
      why: >
        SIGNAL: the "R1+R2" text is a command-body fact error about the
        exposition authoring pass, not a schema-contract drift. But it is
        misleading: a stitcher dispatch reading this would believe the exposition
        has been through two author/review passes and so its content is
        doubly-vetted; under the slim pipeline it has been through one pass and
        the mechanical Phase 4 auditor. The Phase 0.6 voice-check (auto-reword
        on 3rd-person-in-1st-person frame) is correct and unaffected; only the
        "R1+R2" label is wrong.
      criteria: >
        and-stitch.md line 204: replace "R1+R2" with "R1 (single authoring pass
        per DEC-0116)".
        and-stitch.md line 349: replace "R1+R2" with "R1 (the exposition author's
        single-pass authoring per DEC-0116 + the Phase 4 mechanical audit)".
        Owner: fixer.

    # ── CARRY-FORWARD FROM PRIOR RUNS (not re-examined here) ─────────────────

    - id: STRUCT-CARRY-PRIOR-001
      type: flag
      severity: SIGNAL
      what: >
        pl-2026-05-25-018 (axes_held schema ambiguity) remains open. The schema
        does not specify whether post-move holding of an in-motion axis requires
        listing in axes_held[]. Two options: (a) implicit license, (b) mandatory
        listing. This is the same item from the prior pipeline runs; it was not
        fixed by the DEC-0115/0116 refactors.
      why: >
        SIGNAL: the ambiguity exists and causes inconsistent Phase 6 auditor
        behavior across sessions. Not escalated to HARD here — the prior pipeline
        runs classified it SIGNAL and no new evidence changes that.
      criteria: >
        Resolve pl-2026-05-25-018 by choosing option (a) or (b) and adding an
        explicit carve-out to showrunner-memory.schema.md Field Notes
        §"axes_in_motion vs axes_held." Owner: fixer (schema edit).

  verdict: >
    3 HARD (schema-vs-command-body drift; all post-DEC-0116 R2-retirement
    residue or command-body enum staleness), 2 SIGNAL (CLAUDE.md sync gap,
    exposition authoring description error). No RESIDUE-class findings
    (prior runs closed all tensometer residue). CLAUDE.md Rules 19/20/21
    wiring (PROP-0043/0044/0045) not re-walked here — confirmed as clean by
    the implementing commit (e011809). Parking-lot item pl-2026-05-25-018
    (SOFT) remains open and was surfaced per Rule 14.

  new_parking_lot_items:
    - pl-2026-06-12-pipeline-001  # STRUCT-NEW-001 → fixer: audit-report.schema.md + orchestrator-critic card
    - pl-2026-06-12-pipeline-002  # STRUCT-NEW-002 → fixer: and-stitch.md + stitch-render-log.schema.md
    - pl-2026-06-12-pipeline-003  # STRUCT-NEW-003 → fixer: and-cut.md
    - pl-2026-06-12-pipeline-004  # STRUCT-NEW-004 → fixer: CLAUDE.md schema table + dir map
    - pl-2026-06-12-pipeline-005  # STRUCT-NEW-005 → fixer: and-stitch.md lines 204 + 349
```
