---
report: pipeline
scope: pipeline
timestamp: 2026-06-12T00:00:00Z
verdict: HARD-FINDINGS
hard_count: 5
signal_count: 1
taste_count: 10
---

# Pipeline Audit — 2026-06-12T00:00:00Z

Schema-vs-command-body-vs-rubric tri-walk. Auditor: schema-audit mode (no Agent dispatches — single-fork read walk).

---

## HARD FINDINGS

---

### STRUCT-001 — `schemas/audit-report.schema.md` undeprecated R2 section

**File:** `schemas/audit-report.schema.md` lines 106–158
**Finding:** Section `## R2 decision-shard frontmatter (URI-026 follow-on, 2026-05-10)` documents `/and-facets-r2` as an active command, describes the `.r2-decisions.md` artifact as actively produced and consumed, and specifies a live consumer contract in `staff/orchestrator-critic/card.md` (Phase 6 reads `f-r2-counts`). The R2 round is RETIRED under DEC-0116 (URI-FACETS-SLIM, 2026-06-08). The section carries no DEPRECATED marker and no tombstone annotation. It is formatted and positioned as normative documentation.

**Confirmed stale references inside this section:**
- `/and-facets-r2` — command retired under DEC-0116
- `Phase 6 of /and-facets` — the slim pipeline's final phase is Phase 5 (persist + orchestrator-critic verdict); Phase 6 no longer exists
- `active-project/theater/facets/.r2-decisions.md` — no longer produced by the pipeline
- `F-R2-*` counts read by `/and-review verdict` — confirmed: the `verdict` subcommand in `.claude/commands/and-review.md` lines 254–295 contains zero references to F-R2 counts or `.r2-decisions.md`; the orchestrator-critic dispatch section does not mention them

**Why it matters:** a consumer of the schema reading this section will attempt to produce or consume `.r2-decisions.md` and surface `F-R2-*` counts. Neither artifact exists. The section's `Consumer contract` paragraph asserts that `staff/orchestrator-critic/card.md` Phase 6 reads this file — any audit or reviewer that loads the schema and follows that contract will either hard-fault on a missing file or author a ghost artifact. This is a live-authoring trap, not dead documentation.

**Owning agent for fix:** schema edits belong to the principal or a principal-dispatched fixer. The fix is to prepend a `**DEPRECATED (URI-FACETS-SLIM, DEC-0116, 2026-06-08):** The R2 round is retired. This section is preserved as historical reference only. No command in the current pipeline produces or consumes these artifacts.` block to the section heading.

**Criteria:** the section must carry a visible DEPRECATED annotation at its heading before its body; the consumer-contract paragraph must note that `/and-review verdict` does not read `f-r2-counts` under the current pipeline.

---

### RESIDUE-001 — `URI-026` in undeprecated schema section heading

**File:** `schemas/audit-report.schema.md` line 106
**Finding:** Section heading `## R2 decision-shard frontmatter (URI-026 follow-on, 2026-05-10)` uses `URI-026` as the primary label for an undeprecated normative section. URI-026 is a known-deprecated term (the pre-substance tens-gate URI; superseded by URI-SUBSTANCE-OVERHAUL and the substance bone-gate). The heading presents URI-026 as the canonical authority for the section rather than as a historical annotation. This is not an `archive/`, `design/substance/plan-holes-*.md`, or rubric pre-overhaul reference — it is in the schema's normative body.

**Why it matters:** any consumer reading the schema as a reference will parse this section as the current specification for URI-026-based decision-shards. The URI is dead. This is RESIDUE in the residue-scan sense: a deprecated term load-bearing in a live location.

**Owning agent for fix:** same as STRUCT-001 — the DEPRECATED annotation in STRUCT-001 resolves this simultaneously if the section heading itself is annotated.

**Criteria:** URI-026 must not appear as the primary authority label of an undeprecated schema section. Either tombstone the heading or annotate it `(DEPRECATED)`.

---

### STRUCT-002 — CLAUDE.md schema authority table missing two active schemas

**File:** `CLAUDE.md` lines 116–136 (schema authority table)
**Finding:** Two schemas are operational and actively referenced in command bodies but absent from the schema authority table.

**(a) `schemas/cohere-state.schema.md`**
- File confirmed present: `/home/user/and-shoot/schemas/cohere-state.schema.md`
- Referenced by: `.claude/commands/and-review.md` cohere subcommand Phase 4 persist (reads state file per this schema); `.claude/commands/and-cohere.md` (PROP-0031 wiring — the iteration loop reads and writes this file)
- Not in the table

**(b) `schemas/aggregate-state.schema.md`**
- File confirmed present: `/home/user/and-shoot/schemas/aggregate-state.schema.md`
- Referenced by: `/and-stitch` Phase 10 forward-thread (emits to this file); `/and-cohere` Phase 6.5 aggregate-emit; `/and-substance chapter` Phase 0 reads it (aggregate value wins over `handoff_in` on conflict)
- Not in the table

**Why it matters:** CLAUDE.md Rule 1 states "Read the relevant schema before writing any new schema-typed file." The table is the authority surface for what schemas exist. Agents and orchestrators dispatched for `/and-cohere` or `/and-stitch` Phase 10 work will not find these schemas in the table and may not read them before authoring, producing schema-invalid files. The omission is especially load-bearing for `cohere-state` because the RECONCILE rule (CLAUDE.md Rule 21) requires cross-checking the report against the state file — if the schema is not in the authority table, the check is under-specified.

**Owning agent for fix:** showrunner or principal-level CLAUDE.md edit.

**Criteria:** both `schemas/cohere-state.schema.md` and `schemas/aggregate-state.schema.md` must appear in the schema authority table with a one-line descriptor of the file type they govern.

---

### STRUCT-003 — `rubric-exposition.md` referenced but does not exist

**File:** `.claude/commands/and-facets.md` Phase 4, audit class 12 (RUBRIC-FIDELITY), source enumeration, line 333
**Finding:** The Phase 4 audit RUBRIC-FIDELITY source enumeration names `rubric-exposition.md` as a seeded rubric source:

> "Seeded from each facet rubric's §Anti-patterns + §Curve-shape (file-level) + §Cross-axis tests + §ACCEPT/REJECT signatures + §Cross-facet contract of: `rubric-memory-flags.md`, `rubric-sensory.md`, `rubric-state-updates.md`, `rubric-narrator-interest.md`, `rubric-location-state.md`, `rubric-feeling.md`, `rubric-metaphor.md`, `rubric-vibes.md`, `rubric-exposition.md`, `rubric-dialogue.md`"

`design/shoot-v2/rubric-exposition.md` does not exist on disk. The path was verified absent.

**Why it matters:** the RUBRIC-FIDELITY class states "The auditor enumerates these at audit time; new rubric rules get picked up automatically." An auditor dispatched per Phase 4 will attempt to read `rubric-exposition.md`, fail, and either abort or silently skip the exposition rubric class — which means exposition entries receive no rubric-fidelity check. The exposition facet is one of the ten R1-authored facets; its rubric discipline (`AP-SCAN` for hollow-prose, anti-jargon, source-traceability) is documented in `and-facets.md`'s CONSTRAINT class inline, but no dedicated rubric file organizes the ACCEPT/REJECT signatures.

**Owning agent for fix:** the rubric must either be authored at `design/shoot-v2/rubric-exposition.md` or the RUBRIC-FIDELITY source enumeration must remove `rubric-exposition.md` and explicitly note that exposition's rubric-fidelity checks live inline in the CONSTRAINT class. This is an episode-scope fix.

**Criteria:** either `design/shoot-v2/rubric-exposition.md` exists at `and-facets.md` Phase 4 audit time, or the source enumeration explicitly removes the reference and documents where exposition ACCEPT/REJECT signatures are defined.

---

### STRUCT-004 — `rubric-dialogue.md` referenced but does not exist

**File:** `.claude/commands/and-facets.md` Phase 4, audit classes 9 (AP-SCAN) and 12 (RUBRIC-FIDELITY), lines 324 and 333
**Finding:** `rubric-dialogue.md` is cited twice in Phase 4:
- AP-SCAN class: "Dialogue anti-patterns (from `rubric-dialogue.md`):" lists AP-chassis-contamination, AP-modern-hr-speak, AP-deposition-cadence, AP-nominalization-substituting-plain-English
- RUBRIC-FIDELITY source enumeration: lists `rubric-dialogue.md`

`design/shoot-v2/rubric-dialogue.md` does not exist on disk. Also referenced in `/and-write` Phase 1.5b: "staff/dialogue-writer/rubric-dialogue.md (the v1 round-trip writer-pattern rubric)" — a different path (`staff/dialogue-writer/rubric-dialogue.md`), which is a separate location. The rubric does not exist at either location.

**Why it matters:** dialogue is HARD-gated throughout the pipeline (FAULT-DIALOGUE-CARD-VIOLATION, FAULT-DIALOGUE-EARTH-BET-FENCE at `/and-write` Phase 6; DEDUP and dialogue-coverage-sanity HARD classes at `/and-facets` Phase 4 CONSTRAINT). The AP-SCAN dialogue anti-patterns (AP-chassis-contamination, AP-modern-hr-speak) are named in the command body but have no canonical rubric source. When the Phase 4 auditor runs RUBRIC-FIDELITY and attempts to enumerate `rubric-dialogue.md` anti-patterns, the file is absent — the four named AP-SCAN anti-patterns exist only as inline text in the command body. Dialogue writers dispatched at Phase 1.5b also have no rubric file to load.

**Owning agent for fix:** rubric must be authored at the canonical path (`design/shoot-v2/rubric-dialogue.md`), or the command body must be updated to name the actual path where dialogue rubric discipline lives (if it was authored under a different path). This is an episode-scope fix.

**Criteria:** `design/shoot-v2/rubric-dialogue.md` exists and is loadable by the Phase 4 auditor; OR the command body corrects the path to the actual rubric location; AND the four AP-SCAN anti-patterns have a canonical home discoverable at audit time.

---

## SIGNAL FINDINGS

---

### STRUCT-005 — `chapters[].cohere_review` schema gap (acknowledged-pending)

**File:** `.claude/commands/and-review.md` line 438 (cohere subcommand Phase 4)
**Finding:** The cohere subcommand's Phase 4 mandatory-step record writes `chapters[<slug>].cohere_review = {reviewed_at, report_path, verdict, range, failed_axes}` to showrunner memory. This field does not appear in `schemas/showrunner-memory.schema.md`.

**However**, the command body explicitly annotates this as pending: "(Schema-level memory.md addition; documented here, gated by principal triage of PROP-0030 before going live in the schema.) Until the memory.md field is schema-blessed, persist this record to the report front-matter only."

This is a **known gap with a declared fallback**, not a silent drift. The command body's behavior is correct under the current instruction (front-matter only). Classified as SIGNAL rather than HARD because the drift is acknowledged, the fallback is documented, and no agent is expected to read this field from memory before PROP-0030 is triaged.

**Why it matters (signal):** if PROP-0030 is accepted and the field begins being written to memory without the schema being updated, downstream agents and the RECONCILE check (Rule 21) will operate against an out-of-spec field. Principal should triage PROP-0030 and update `schemas/showrunner-memory.schema.md` before `/and-cohere` is in active use.

**Owning agent for resolution:** showrunner or principal; once PROP-0030 is triaged and accepted, the schema update is a bounded edit.

---

## TASTE-FLAG FINDINGS

The following references look like drift but are correctly annotated historical documentation. No action required.

| ID | Location | Content | Annotation status |
|---|---|---|---|
| TF-001 | `schemas/facet.schema.md` § tensometer | Tensometer section with DEPRECATED marker | Explicitly marked "DEPRECATED 2026-05-17 (URI-SUBSTANCE-OVERHAUL)... retained as a historical reference only" |
| TF-002 | `schemas/scene-map.schema.md` | Tensometer references | Framed as "pre-overhaul" in the Pressure-signal translation reference table |
| TF-003 | `schemas/stitch-profile.schema.md` | Tensometer references | Marked "removed under URI-SUBSTANCE-OVERHAUL" in multiple places |
| TF-004 | `schemas/stitch-render-log.schema.md` | Tensometer reference | Marked "Pre-overhaul..." |
| TF-005 | `schemas/bones.schema.md` | "`tens:` is removed" note | Appears in the recognized-artifact-prefixes section as a tombstone annotation ("under the substance overhaul, `tens:` is removed — tensometer facet is dropped") |
| TF-006 | `design/shoot-v2/rubric-sensory.md` | Tensometer "pre-overhaul" annotation | Pre-overhaul V2 context |
| TF-007 | `design/shoot-v2/rubric-state-updates.md` | Tensometer references | Framed as "formerly @64-class beats in tensometer language"; explicitly historical |
| TF-008 | `.claude/commands/and-review.md` | `Lift basis: pre-overhaul /and-season...` notes | Explicit lift-basis annotations in the subcommand lift-source tables — historical record of what was replaced |
| TF-009 | `.claude/commands/and-write.md` | `Replaces URI-026 tens-gate` note | Historical rationale for Phase 6 design in the command description |
| TF-010 | `.claude/commands/and-review.md` verdict subcommand | `Lift-source mapping (Hole G): /and-season.md Phase 6...` table | Explicit lift-basis table with `Lift-source mapping` heading — not an active invocation |

---

## Scope notes

- **STRUCT-001 / RESIDUE-001** are both in `schemas/audit-report.schema.md` and share a single fix surface. A DEPRECATED annotation on the section heading resolves both simultaneously.
- **STRUCT-002** is in `CLAUDE.md` (the schema authority table); it is principal-level or showrunner-level to update.
- **STRUCT-003 and STRUCT-004** are missing rubric files. STRUCT-004 is double-cited (both `and-facets.md` Phase 4 AP-SCAN anti-patterns and RUBRIC-FIDELITY source, and `and-write.md` Phase 1.5b). Both are episode-scope fixes — each requires authoring the missing rubric or correcting the reference path.
- **STRUCT-005** is a known-acknowledged schema gap with a documented fallback; no immediate blocking action required.

---

## Tri-walk coverage summary

| Walk axis | Result |
|---|---|
| Schema vs command bodies | 5 findings (STRUCT-001 stale section; STRUCT-002 missing table entries; STRUCT-005 schema-gap-acknowledged; chapter_class field confirmed present in schema, absent from bones file — correct; bones_review.follow_check confirmed in schema at line 304) |
| Schema vs rubrics | No additional findings beyond TF-001–TF-010 (all correctly annotated historical references) |
| Command bodies vs rubrics | 2 findings (STRUCT-003 rubric-exposition.md absent; STRUCT-004 rubric-dialogue.md absent) |
| Residue scan | 1 finding (RESIDUE-001); all other tensometer / URI-026 / /and-season occurrences are correctly annotated TASTE-FLAGs |
| CLAUDE.md sync | 1 finding (STRUCT-002); agent routing table matches `.claude/agents/` (renderer-minimal and admin confirmed present); directory map matches active filesystem layout; theater/ subdirs correct |
