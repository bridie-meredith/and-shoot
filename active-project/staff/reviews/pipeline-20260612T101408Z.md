```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-12
  triggered_by: >
    Improvement-loop test routine (lens a: /and-review pipeline).
    Two major command-body changes since the last tri-walk (2026-06-07):
    DEC-0116 (slim /and-facets — retired R2 round + Phase 5b audience-gate,
    2026-06-08) and DEC-0115 (no-ledger overhaul — retired cold ledger/
    apparatus register as a prose mode, added ABSTRACTION-AS-SUBJECT +
    SCENE-ABSTRACT-DOMINANT + LEDGER-REGISTER + EMBODIMENT-BLOCKED +
    NAIVE-FOLLOW fault classes, 2026-06-08). Also added /and-reoutline
    since the prior run.
  prior_run_hardsresolved:
    - STRUCT-006: "bones.schema.md communication-class carve-out — CONFIRMED FIXED"
    - STRUCT-007: "showrunner-memory.schema.md sub-1.0 bone delta exception — CONFIRMED FIXED"
    - STRUCT-017: "rubric-metaphor.md tens: multi-justification — CONFIRMED FIXED"
    - STRUCT-018: "rubric-vibes.md tens: cross-facet contract — CONFIRMED FIXED"
    - STRUCT-019: "rubric-metaphor.md 'proposed' framing — CONFIRMED FIXED"

  findings:

    # ── PRIOR HARD RESOLUTION CONFIRMATION ────────────────────────────────

    - id: STRUCT-006-resolved
      type: pass
      severity: SIGNAL
      what: >
        bones.schema.md § Dialogue-anchor bones (line 140) and § "What bones
        are not" (line 165): the canonical speech form now reads ">=1
        communication/relational-class axis per the active signature (universal
        questionnaire: community / knowledge / reputation / trust; custom
        signature: the axis or axes the series.substance.state_axes block
        designates as relational/communicative per the signature note)." The
        custom-signature carve-out from STRUCT-006 is applied. PASS.
      why: >
        Confirms the prior HARD is no longer a risk. Any phase-6 auditor reading
        the schema will get the generalized language, not the universal-taxonomy
        enumeration.

    - id: STRUCT-007-resolved
      type: pass
      severity: SIGNAL
      what: >
        showrunner-memory.schema.md line 144: chunk_targets.bone.delta_per_axis
        now reads "conditional: 1-3 when scene_target_delta_magnitude >= 1.0;
        magnitude = scene_target_delta_magnitude when scene_target_delta_magnitude
        < 1.0 (fractional sub-scene moves permitted at sub-1.0 scene targets; Phase 6
        gate accepts magnitude == scene_target_delta_magnitude in these cases).
        Precedent: DEC-0002 (c07 + c08 sub-1.0 bone deltas accepted)." PASS.
      why: >
        The sub-1.0 exception is formalized in the schema. Auditor dispatches
        no longer need admin DEC override for sub-1.0 scenes.

    # ── DEC-0116 IMPACT: FACET-SLIM (SCHEMA vs COMMAND BODIES) ───────────

    - id: STRUCT-023
      type: pass
      severity: SIGNAL
      what: >
        DEC-0116 impact on showrunner-memory.schema.md: the `faceted-r2` status
        value is documented as RETIRED at line 368: "Legacy value from the pre-slim
        two-round pipeline; the R2 judging round is retired. New runs never set it;
        treat as `faceted-r1` on resume." The retirement notice is present. PASS.
      why: >
        No ghost field risk. Any agent reading the schema will find the RETIRED
        annotation and handle a `faceted-r2` status on resume correctly.

    - id: STRUCT-024
      type: pass
      severity: SIGNAL
      what: >
        DEC-0116 impact on and-facets.md Phase 4 "12 classes": enumeration
        confirmed (STRUCTURAL / FREQUENCY-BAND / METADATA-INCONSISTENCY /
        CURVE-SHAPE / CONTRADICTION / DEDUP / SUPERFLUOUS / CONSTRAINT /
        AP-SCAN / TASTE-FLAG / PILE-UP REVIEW / RUBRIC-FIDELITY). The CLAUDE.md
        commands table entry for /and-facets names "12 classes incl. scene-map
        coverage + dialogue dedup/coverage sanity + Earth-Bet fence" — these are
        sub-items of CONSTRAINT (class 8), consistent with the command body.
        Class count is correct. PASS.
      why: >
        No mismatch between CLAUDE.md summary and command body enumeration.

    # ── DEC-0115 IMPACT: NO-LEDGER (SCHEMA vs COMMAND BODIES) ────────────

    - id: STRUCT-025
      type: fault
      severity: SIGNAL
      what: >
        and-write.md Phase 6 (DEC-0115, 2026-06-08) defines two new HARD fault
        classes: ABSTRACTION-AS-SUBJECT-<bone> (line 300: "A bone whose grammatical
        subject is the apparatus or an abstraction...") and SCENE-ABSTRACT-DOMINANT
        (line 301: "If grounding-class bones number fewer than ceil(0.6 × ...)").
        Neither class is documented in schemas/bones.schema.md or in
        schemas/audit-report.schema.md. The bones.schema.md has no section
        enumerating HARD bone-gate fault class names.
      why: >
        A session that reads bones.schema.md to understand valid gate fault classes
        will not find ABSTRACTION-AS-SUBJECT or SCENE-ABSTRACT-DOMINANT. The command
        body is the sole authority. Low blocking risk (agents dispatched via and-write.md
        get the right brief), but schema is self-incomplete on DEC-0115 additions.
        SIGNAL: documentation gap, not a live gate mismatch.
      criteria: >
        schemas/bones.schema.md should add a § Gate fault classes note under the
        Phase 6 bone-gate section documenting ABSTRACTION-AS-SUBJECT-<bone> (HARD),
        SCENE-ABSTRACT-DOMINANT (HARD: grounding-class bones < ceil(0.6 × non-chatter
        bones)), and EVENT-NOT-CONCRETE-<event> (HARD) — the DEC-0115 and
        URI-WRITE-EVENT-CONCRETENESS additions. The 0.6 threshold should be
        explicitly named here as the canonical schema source (currently it exists
        only in CLAUDE.md Rule 22 and and-write.md Phase 6 inline).

    - id: STRUCT-026
      type: fault
      severity: SIGNAL
      what: >
        stitch-render-log.schema.md Phase 4 move class table (lines 188-199)
        lists: TENSE-SHIFT, PERSON-SHIFT-POV, POV-PRONOUN-RESOLVE,
        PRESERVE-THIRD-PARTY, SENSORY-PROSE-FIT, SENSORY-DROP-COVERED,
        BONE-OBJECT-IDIOM-FIT, CONTRACTION. It does NOT include LEDGER-REGISTER
        or EMBODIMENT-BLOCKED, which are new Phase 4 move classes added by
        DEC-0115 (and-stitch.md Phase 4, line 698-700: "Log each transform as...
        LEDGER-REGISTER / EMBODIMENT-BLOCKED"). and-stitch.md Phase 4 explicitly
        says these are logged to the render-log but the schema's Phase 4 table
        does not enumerate them.
      why: >
        Any session reading the stitch-render-log schema to interpret a Phase 4
        log entry will encounter LEDGER-REGISTER or EMBODIMENT-BLOCKED and not
        find them in the taxonomy. Low blocking risk; schema is incomplete for
        post-DEC-0115 renders.
      criteria: >
        stitch-render-log.schema.md Phase 4 move class table should add:
        LEDGER-REGISTER — bone/facet rendered in ledger/apparatus register; the
        Phase 4 prohibition fires and the passage is re-rendered concrete;
        EMBODIMENT-BLOCKED — bone that cannot be rendered concretely without
        inventing content (bone itself is an abstraction-as-subject); accumulation
        counter toward the K=3 threshold for upstream routing to /and-write revise.

    - id: STRUCT-027
      type: fault
      severity: SIGNAL
      what: >
        stitch-render-log.schema.md has no Phase 9 section. and-stitch.md Phase 9
        (DEC-0115 + PROP-0047) defines a cold-read terminal gate with fault classes
        NAIVE-FOLLOW, FOLLOW-FAIL (blocking → /and-write revise), READABLE,
        AIRLESS, and the PASS-WITH-DEPTH-PASS-REQUIRED verdict. The schema's move
        class taxonomy only covers Phase 1 through Phase 7 (scene-window fallback).
        Phase 9 has no schema representation for its verdict vocabulary or fault
        classes.
      why: >
        A session that reads the stitch-render-log schema to understand what
        Phase 9 produces will find no Phase 9 section. The chain's terminal gate
        is schema-invisible. SIGNAL: the command body is authoritative; the schema
        is incomplete for the current pipeline's final gate.
      criteria: >
        stitch-render-log.schema.md should add a Phase 9 section documenting:
        verdict vocabulary (PASS / PASS-WITH-DEPTH-PASS-REQUIRED / FAIL);
        NAIVE-FOLLOW sub-gate (FOLLOW-FAIL = blocking); readability axis
        (READABLE / AIRLESS); fault class VOICE-APPARATUS-DEFAULT (from Phase 4
        EMBODIMENT-BLOCKED accumulation, as referenced in and-stitch.md line 939).

    # ── CLAUDE.md SYNC ────────────────────────────────────────────────────

    - id: STRUCT-028
      type: fault
      severity: SIGNAL
      what: >
        /and-reoutline is a live command at .claude/commands/and-reoutline.md
        (frontmatter: "description: Intake a tentative outline (whole-book/series)
        into the and-experiment design track and adapt-or-retire the existing planning
        to receive it. A re-baseline, not a chapter note. Runs the OUTLINE-INTAKE
        RUNBOOK. Usage: /and-reoutline"). It is NOT listed in the CLAUDE.md
        Commands section (lines 225-235 enumerate the full command table; /and-reoutline
        is absent).
      why: >
        CLAUDE.md is the authoritative operator document. A session-start reader
        who consults CLAUDE.md to find available commands will not discover
        /and-reoutline. Low functional risk (command file is discoverable), but
        the gap breaks the CLAUDE.md completeness invariant. SIGNAL.
      criteria: >
        Add /and-reoutline to the CLAUDE.md Commands section table with its
        correct usage: "/and-reoutline" and purpose: "Intake a tentative outline
        (whole-book/series) into the and-experiment design track and adapt-or-retire
        existing planning to receive it. A re-baseline, not a chapter note."

    - id: STRUCT-029
      type: fault
      severity: SIGNAL
      what: >
        CLAUDE.md schema authority table still does not list cohere-state.schema.md
        or aggregate-state.schema.md. This was first noted as STRUCT-014 and
        STRUCT-015 in the 2026-06-07 pipeline report. Neither the DEC-0116 nor
        DEC-0115 passes added these entries. The two schemas are live: cohere-state
        is written by every /and-cohere invocation; aggregate-state is written by
        /and-cohere Phase 6.5 (which explicitly reads schemas/aggregate-state.schema.md
        before authoring its dispatch). Neither appears in the CLAUDE.md
        "Schema authority" table.
      why: >
        SIGNAL (re-surfacing from STRUCT-014/015): a session-start reader using
        CLAUDE.md's schema authority table as the index to find all live schemas
        will not find these two. The gap has survived two major command-body
        passes without being fixed. Should be added to prevent the pattern from
        persisting through further book-boundary passes.
      criteria: >
        Add to CLAUDE.md schema authority table:
        - cohere-state.schema.md | /and-cohere state file (per /and-cohere invocation)
        - aggregate-state.schema.md | /and-cohere Phase 6.5 aggregate emit

    - id: STRUCT-030
      type: flag
      severity: SIGNAL
      what: >
        CLAUDE.md Rule 22 (DEC-0115) states "each scene must hold a concreteness
        floor (default ≥ 0.6 concrete-SVO bones)" and and-write.md Phase 6
        encodes the formula as "grounding-class bones < ceil(0.6 × (bone_count −
        chatter_count))." The 0.6 threshold is named in two command-facing
        documents (CLAUDE.md Rule 22 + and-write.md Phase 6) but is NOT in
        bones.schema.md or any schema. If a future session overrides this threshold
        (e.g. by admin DEC for a particular chapter type), there is no schema field
        to record the override — it would live only in decisions.md precedent (the
        same fragility pattern that produced STRUCT-007 for sub-1.0 bone deltas).
      why: >
        Advisory. The threshold is stable today; this is a forward-looking note.
        If an exception is made (e.g. a dialogue-heavy chapter where physical
        grounding bones naturally run lower), the proper resolution is to add a
        schema carve-out (like the sub-1.0 precedent), not to rely on decisions.md.
        Flag for awareness at next /and-substance series or book boundary.

    # ── RUBRIC RESIDUE SCAN ───────────────────────────────────────────────

    - id: RESIDUE-002
      type: fault
      severity: SIGNAL
      what: >
        rubric-sensory.md anti-pattern #14 (line 217): "Cycle-N ADD without
        pre-validation (V3, 2026-05-21). A fixer ADD operation introduced at
        audience-gate cycle N must satisfy the full per-entry rubric..." and
        line 10 (changelog): "Anti-patterns — new entry #14: 'Cycle-N ADD
        without pre-validation.' Fixer ADD operations introduced at audience-gate
        cycle N must satisfy the full per-entry rubric... BEFORE the ADD commits
        — the cycle-N audit slot is too late to catch ADD-introduced HARDs.
        Promoted from URI-FACETS-V3-CYCLE-N-ADD."
        The "audience-gate cycle N" mechanism was retired under DEC-0116 (the
        Phase 5b adversarial audience-gate with 3-cycle cap is gone). In the
        current slim-facets pipeline there is no "cycle N" — there is a single
        Phase 4 mechanical auditor pass and a Phase 3 conditional spine-hole
        remediation. The principle (don't introduce new HARDs in a remediation
        pass) is valid, but the vocabulary is now stale.
      why: >
        SIGNAL: a remediation author reading rubric-sensory.md AP#14 will
        encounter "audience-gate cycle N" and have no referent in the current
        pipeline. The structural analogue is Phase 3 conditional remediation:
        a fixer ADD to clear a Phase 4 HARD must not introduce a new HARD that
        Phase 4's subsequent re-check cannot then resolve. The principle maps;
        the vocabulary needs updating.
      criteria: >
        rubric-sensory.md anti-pattern #14 should be reframed for the slim-facets
        pipeline: "Fixer ADD operations introduced at Phase 3 conditional
        remediation must satisfy the full per-entry rubric BEFORE the ADD commits —
        if a remediation ADD introduces a new HARD, the Phase 4 auditor will
        catch it, but the conditional remediation only runs once and there is no
        second remediation pass." Historical attribution and URI-FACETS-V3-CYCLE-N-ADD
        reference can be retained for provenance. Related command-body context
        (formerly "A3 in run-action-plan... command-body Phase 5b iteration logic")
        should be updated to reference the current remediation mechanism in
        and-facets.md Phase 3.

    - id: RESIDUE-003
      type: flag
      severity: TASTE-FLAG
      what: >
        rubric-sensory.md lines 120, 124 and rubric-narrator-interest.md line 141,
        rubric-memory-flags.md lines 5, 156, 192 contain historical attribution
        references "(URI-FACETS-CYCLE-1, 2026-05-19 — promoted from audience-gate
        cycle-1 attack on b01c01...)" These are provenance traces explaining when
        each anti-pattern was promoted. They reference the prior pipeline's
        audience-gate cycle vocabulary but do so in a historical-attribution context
        (past tense, attributing the original detection mechanism), not as live
        process instructions. TASTE-FLAG: they look like residue but are legitimate
        historical documentation.
      why: >
        No action required. Distinguishing legitimate historical attribution from
        live vocabulary confusion is the purpose of TASTE-FLAGs. These traces
        support auditability of when each rubric clause was added and what
        evidence motivated it.

    - id: RESIDUE-004
      type: pass
      severity: SIGNAL
      what: >
        Rubric check for cold-ledger/apparatus-register as a permitted prose mode
        (DEC-0115 residue check). rubric-state-updates.md and rubric-narrator-interest.md
        use "ledger" to refer to the story-world prop (district-ledger, Taylor's
        administrative record) — not the ledger/apparatus prose register. No rubric
        approves abstract bookkeeping narration as a valid mode. rubric-state-updates.md
        explicitly authors from a "concrete physical event" basis consistent with
        DEC-0115. PASS: no rubric licenses the retired apparatus-register prose mode.
      why: >
        No conflict with DEC-0115 found in rubrics. Story-world references to
        fictional ledgers (prop:district-ledger) are correctly distinct from the
        prose-mode prohibition.

    - id: RESIDUE-005
      type: pass
      severity: SIGNAL
      what: >
        R2 / Phase 5b residue scan in rubrics. rubric-metaphor.md, rubric-vibes.md:
        confirmed FIXED in prior run (STRUCT-017/018/019); not rechecked in full.
        rubric-feeling.md: STRUCT-020 from prior run noted tensometer vocabulary
        in "Tens-distribution" table and "Cross-facet contract / Tensometer" as
        SIGNAL (observation-only, soft). That finding stands unresolved but was
        already classified SIGNAL in the prior run. Not re-escalated here.
        rubric-sensory.md AP#14: live vocabulary issue → see RESIDUE-002 above.
        Other rubric R2/Phase 5b historical attribution: TASTE-FLAG per RESIDUE-003.
        PASS on scanning for any rubric that treats Phase 5b or R2 as a currently
        active gate process the author should satisfy.
      why: >
        No rubric currently instructs authors to satisfy an R2 round or Phase 5b
        audience-gate. The mechanism is gone from all live rubric clauses except
        the stale vocabulary in rubric-sensory.md AP#14 (addressed in RESIDUE-002).

    # ── AUDITOR FORK SUPPLEMENTARY FINDINGS (post-completion merge) ──────

    - id: STRUCT-031
      type: fault
      severity: HARD
      what: >
        schemas/audit-report.schema.md lines 106-157: the "R2 decision-shard
        frontmatter" section (URI-026 follow-on, 2026-05-10) describes an active
        contract for /and-facets-r2 emitting per-layer decision shards to
        `active-project/staff/<facet>/r2-decision-shard.md`, a consolidated
        `.r2-decisions.md`, f-r2-counts frontmatter, and the F-R2-1 through
        F-R2-4 failure class definitions. The section closes with the orchestrator-
        critic consumer contract: "f-r2-1 > 0 is HARD; f-r2-2 + f-r2-3 + f-r2-4
        > 2 is SIGNAL. HARD trips F7 with /and-facets attribution." There is NO
        retirement notice on this section. Under DEC-0116, the R2 judging round
        is entirely retired — /and-facets-r2 never runs, no shard files are emitted,
        and .r2-decisions.md is never created. The consumer contract references
        a metric that is never populated post-DEC-0116.
        Citing: audit-report.schema.md lines 106-157;
        staff/admin/decisions.md (DEC-0116).
      why: >
        HARD: the schema is the authoritative reference for what audit pipeline
        artifacts look like. Any fork reading this section to understand what
        /and-facets produces will (a) attempt to emit r2-decision-shard.md files
        that nothing reads, and (b) expect the orchestrator-critic to read f-r2-counts
        thresholds that are never populated. If the orchestrator-critic card reads
        the schema to find its consumer contract, it finds an active-looking HARD
        threshold (f-r2-1 > 0) against a file that does not exist — this will either
        silently pass (file absent = 0, threshold not met) or error, neither of
        which is the intended behavior. The contract is live-specified for a retired
        mechanism with no guard.
      criteria: >
        audit-report.schema.md §"R2 decision-shard frontmatter" (lines 106-157)
        must be marked RETIRED under DEC-0116 / URI-FACETS-SLIM. The section should
        read: "RETIRED — /and-facets-r2 and the R2 round were retired under
        DEC-0116 (URI-FACETS-SLIM, 2026-06-08). No shard files are emitted on
        new runs. The f-r2-counts consumer contract in orchestrator-critic/card.md
        Phase 6 is superseded by the Phase 4 mechanical auditor's HARD/SIGNAL count
        (reported in the /and-facets Phase 5 orchestrator-critic verdict)." The
        F-R2-* class definitions may be preserved for historical provenance with a
        RETIRED header.

    - id: STRUCT-032
      type: fault
      severity: SIGNAL
      what: >
        CLAUDE.md Rule 17 spine-legibility pair entry (line 192) names the finding
        class as `ABSTRACTION-DOMINANT (SIGNAL)`. CLAUDE.md Rule 22 (DEC-0115,
        same document, line 211) names the same-concept finding class as
        `SCENE-ABSTRACT-DOMINANT` and classifies it **HARD**. The command body
        (and-write.md Phase 6 lines 300-301) is correct and consistent with Rule 22:
        `SCENE-ABSTRACT-DOMINANT (HARD)`. Rule 17 is stale: it names the pre-DEC-0115
        class (ABSTRACTION-DOMINANT) with the pre-DEC-0115 severity (SIGNAL).
        Citing: CLAUDE.md lines 192 + 211; and-write.md Phase 6 line 301.
      why: >
        SIGNAL: a session-start reader using CLAUDE.md Rule 17 to understand the
        Phase 6 bone-gate will configure the wrong fault-class name AND the wrong
        severity for one of the two new DEC-0115 HARD gates. Rule 22 is correct;
        Rule 17 needs to be updated to match. The contradiction is within the same
        document between two rules written at different times.
      criteria: >
        CLAUDE.md Rule 17 spine-legibility pair must replace `ABSTRACTION-DOMINANT
        (SIGNAL)` with `SCENE-ABSTRACT-DOMINANT (HARD, DEC-0115)`. Rule 17 should
        reference Rule 22 for the full DEC-0115 fence specification rather than
        independently classifying this finding.

    - id: STRUCT-033
      type: flag
      severity: SIGNAL
      what: >
        design/shoot-v2/rubric-feeling.md line 207: "Independent gates. Both must
        pass for ACCEPT. They cannot substitute. Voice-fidelity is the dialect
        audience's exclusive scope; mechanic does not adjudicate voice; dialect
        does not adjudicate firing decision."
        design/shoot-v2/rubric-memory-flags.md line 302: "Verdict combination.
        Mechanic + dialect verdicts are independent gates. Both must pass; either
        reject = revise. They cannot substitute."
        design/shoot-v2/rubric-narrator-interest.md line 231: "Verdict combination.
        Mechanic + dialect verdicts are independent gates. Both must pass; either
        reject = revise."
        All three rubrics prescribe a dialect-audience gate as an active mandatory
        independent gate. Under DEC-0116, the facet-level dialect-audience gate
        (Phase 5b adversarial audience-gate) was retired. The Phase 4 mechanical
        auditor is now the sole facet-layer gate. No dialect-audience dispatch fires
        at /and-facets on any facet type.
      why: >
        SIGNAL: a fork authoring or reviewing the feeling, memory, or narrator-
        interest facets under these rubrics believes a dialect-audience verdict is
        still required for the facet to pass. This will cause the fork to attempt
        a gate dispatch that no longer exists, or to declare the chapter reviewable
        only after a dialect-audience pass that the pipeline no longer runs.
        Three rubrics carry this residue — particularly concerning for narrator-
        interest (the spine facet for memory co-citation, consulted most frequently).

    - id: STRUCT-034
      type: flag
      severity: SIGNAL
      what: >
        schemas/facet.schema.md line 42: "interest flags — audience
        (`facets/interest-aud-<persona>.md`)" is documented as an active facet type
        authored by "the named audience persona." Under the slim /and-facets pipeline
        (DEC-0116), the R1 fanout dispatches 9 + 1 (exposition) authoring agents;
        audience interest-flag authoring is not among them. No `interest-aud-<persona>.md`
        files are emitted on new runs. The schema still defines this as an active type.
      why: >
        SIGNAL: any fork or consumer that reads the schema's facet type list
        will find `interest-aud-<persona>.md` as a file it should expect to exist
        at `theater/facets/`. Under the slim pipeline, these files are not created.
        A downstream consumer (stitcher Phase 1, /and-facets Phase 4 STRUCTURAL check)
        that looks for these files will find them absent and may fault incorrectly.

    - id: STRUCT-035
      type: flag
      severity: SIGNAL
      what: >
        .claude/agents/audience.md frontmatter description field (line 4) still
        names "facet-adversarial review (per-reviewer verdicts, 3-of-3 accept
        required)" as an active override mode. The agent body (lines 97-101)
        correctly marks this mode as RETIRED under DEC-0116. The frontmatter
        is what a dispatcher reads to understand what modes the agent supports.
      why: >
        Minor flag: the body is authoritative and correctly reflects the retirement.
        A dispatcher reading only the frontmatter description will see an active mode
        listing. Low risk since dispatchable mode is in the body, but the frontmatter
        inconsistency should be corrected.

  summary:
    verdict: "1 HARD + 8 SIGNAL faults (audit-report.schema.md R2 contract active, CLAUDE.md Rule 17/22 conflict, schema gaps from DEC-0115, rubric residue from DEC-0116)"
    hard_count: 1
    signal_count: 8
    taste_flag_count: 2
    pass_count: 8
    key_findings:
      - "STRUCT-031 (HARD): audit-report.schema.md R2 decision-shard section active-looking with no retirement notice — orchestrator-critic consumer contract references a metric never populated post-DEC-0116"
      - "STRUCT-032 (SIGNAL): CLAUDE.md Rule 17 says ABSTRACTION-DOMINANT (SIGNAL) but Rule 22 says SCENE-ABSTRACT-DOMINANT (HARD) — same-document contradiction"
      - "STRUCT-025 (SIGNAL): ABSTRACTION-AS-SUBJECT + SCENE-ABSTRACT-DOMINANT (DEC-0115 HARDs) not in bones.schema.md"
      - "STRUCT-026/027 (SIGNAL): stitch-render-log.schema.md missing Phase 4 LEDGER-REGISTER/EMBODIMENT-BLOCKED + no Phase 9 section"
      - "STRUCT-033 (SIGNAL): rubric-feeling/memory/narrator-interest all prescribe retired dialect-audience gate (DEC-0116 residue in 3 rubrics)"
    all_prior_hardsresolved: true
    recommended_next:
      - "Dispatch fixer to mark audit-report.schema.md R2 section RETIRED (STRUCT-031 — highest priority HARD)"
      - "Fix CLAUDE.md Rule 17 ABSTRACTION-DOMINANT → SCENE-ABSTRACT-DOMINANT (HARD) to match Rule 22 (STRUCT-032)"
      - "Dispatch fixer to add Phase 9 + Phase 4 LEDGER-REGISTER/EMBODIMENT-BLOCKED to stitch-render-log.schema.md (STRUCT-026/027)"
      - "Add DEC-0115 fault classes to bones.schema.md gate-fault note (STRUCT-025)"
      - "Add /and-reoutline + two missing schemas to CLAUDE.md (STRUCT-028/029)"
      - "Update 3 rubrics to remove retired dialect-audience gate prescription (STRUCT-033)"
      - "Mark facet.schema.md interest-aud type as retired (STRUCT-034)"
      - "Reframe rubric-sensory AP#14 vocabulary for slim-facets pipeline (RESIDUE-002)"
```
