---
audit:
  scope: pipeline
  target: pipeline
  subcommand: pipeline
  run_id: pipeline-2026-06-11T19-06-38Z
  timestamp: 2026-06-11T19:06:38Z
  verdict: FAIL
---

# Pipeline Audit Report — 2026-06-11T19:06:38Z

Schema vs command-body vs rubric tri-walk. Auditor role: find-only. No fixes authored.

---

## Check 1 — Schema vs command bodies

### STRUCT-001
- **id:** STRUCT-001
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `schemas/audit-report.schema.md` lines 106–158 (R2 decision-shard section)
- **description:** The R2 decision-shard section describes `/and-facets-r2` emitting per-layer decision shards and Phase 6 of `/and-facets` reading `f-r2-counts` from `.r2-decisions.md`. The R2 judging round is retired under DEC-0116 (URI-FACETS-SLIM). No current `/and-facets` command body has a Phase 6, and the shard-consolidation step described here has no live orchestrator. The schema section is not marked deprecated and reads as an active authoring target.
- **why:** An orchestrator reading `audit-report.schema.md` for the current `/and-facets` pipeline encounters live-looking documentation for a retired artifact. `/and-facets` Phase 5c's orchestrator-critic verdict section in the current command body references no `f-r2-counts` aggregate and no Phase 6.
- **recommendation:** Add a deprecation notice to the R2 decision-shard section in `schemas/audit-report.schema.md` marking it retired under DEC-0116, or move the block to an archive-note so the live schema does not present retired mechanics as active.

### STRUCT-002
- **id:** STRUCT-002
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `schemas/dialogue.schema.md` line 3 (authoring attribution paragraph)
- **description:** The schema states "The R2 dialogue judge at `/and-facets` Phase 2 remains as a locked-graph review pass (KEEP / DELETE / REWRITE against the facet graph the upstream author was blind to) — review only, no authoring." The R2 dialogue judge is explicitly retired under DEC-0116 (URI-FACETS-SLIM). The current `/and-facets` command body (Notes section, first bullet) states: "The R2 dialogue judge… is retired too." The schema's description of the R2 judge as still active contradicts the command body.
- **why:** A dialogue-writer reading the schema would believe the R2 dialogue judge still runs, which could cause incorrect briefing of downstream reviewers and incorrect pipeline expectations.
- **recommendation:** Update `schemas/dialogue.schema.md` line 3 to reflect that the R2 dialogue judge is retired under DEC-0116 and dialogue now ships complete from `/and-write` Phase 7 with no R2 pass.

### STRUCT-003
- **id:** STRUCT-003
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `schemas/facet.schema.md` line 250 (scene-map consumer list)
- **description:** The scene-map section in `schemas/facet.schema.md` lists `/and-facets` Phase 5 audit as consuming the full scene-map file for "per-scene cap enforcement." Under DEC-0116, `/and-facets` has been renumbered: the mechanical audit is Phase 4 (not Phase 5); the Phase 5 referenced in the schema is the persist/orchestrator-critic phase. The `/and-facets` command body's Phase 4 CONSTRAINT class (audit-classes section) correctly identifies scene-map per-scene cap enforcement, but the facet schema's cross-reference still says Phase 5.
- **why:** A reader cross-referencing the facet schema and the command body finds a phase-number mismatch. Not a gate-level failure but creates confusion when tracing cap-enforcement to the right audit phase.
- **recommendation:** Update `schemas/facet.schema.md` scene-map consumer list to reference Phase 4 (mechanical auditor) for per-scene cap enforcement, not Phase 5.

### STRUCT-004
- **id:** STRUCT-004
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `schemas/stitch-profile.schema.md` line 288 (interval-bridge section header)
- **description:** The `interval-bridge:` block is marked `(LEGACY FALLBACK ONLY — superseded by exposition facet 2026-05-12)`. The section still documents fields like `cold-start-sources:`, `prior-episode-sources:`, `mode:`, `voice:`, etc. as active field-docs. The schema's main `interval-bridge:` block inside the YAML example at line 152 still shows it as a top-level field without a deprecation annotation inline. A reader authoring a new profile from the YAML example would include `interval-bridge:` as a live field.
- **why:** The YAML example near line 152 is what a command body reads at Phase 0 for schema defaults. If an implementer takes the YAML example at face value and uses `interval-bridge:` as a primary preamble mechanism (not the legacy fallback), they bypass the exposition-facet path. The body text correction at line 288 is present but the schema example itself is the authority for new profile authors.
- **recommendation:** Add an inline deprecation comment to the `interval-bridge:` block in the YAML example (around line 152) so the example itself signals legacy status.

### STRUCT-005
- **id:** STRUCT-005
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `.claude/commands/and-facets.md` Phase 0, step 5 — `theater/proto-lines/` path reference
- **description:** Phase 0 step 5 (facet-namespace clearance) references `theater/proto-lines/<slug>.md` as a path to auto-archive when cross-chapter collision is detected: "all of `theater/facets/*` … `theater/proto-lines/<prior-slug>.md`". The `theater/proto-lines/` directory is listed as a live output in `CLAUDE.md` directory map ("proto-lines/ — canonical merged proto-line file…"). However, under the substance overhaul the bones file at `theater/bones/<book>-<chapter>.md` is the primary artifact; `theater/proto-lines/<slug>.md` is an intermediate of `/and-facets` Phase 2 (the cite-index build). The directory map lists it without a deprecation note, and and-facets Phase 2 (build_cite_index.py) still writes to it. This is consistent. The potential confusion is that `schemas/bones.schema.md` explicitly drops the legacy proto-lines paths while the CLAUDE.md directory map and `/and-facets` Phase 2/5 still reference `theater/proto-lines/` as the canonical merged output. These are consistent in the current chain (bones = upstream SVO source; proto-lines = cite-indexed merged bones + citations = stitcher input). No actual drift found here.
- **why:** No violation — flagged during inspection, confirmed consistent. No finding issued.

---

## Check 2 — Schema vs rubrics

### STRUCT-006
- **id:** STRUCT-006
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `design/shoot-v2/rubric-state-updates.md` line 9 (pressure-signal reference)
- **description:** The rubric references "formerly @64-class beats in tensometer language" and "formerly @39-class" in its description of hinge beats and held-against-turn class. These are legacy tensometer scalar references (pre-substance-overhaul), documented in the rubric as "formerly" with the replacement surface named (`rhythm-shape` + `peak-bones` from scene-map). The rubric is correctly updated in the operative guidance but retains the legacy tensometer language in a translation-guide role. Cross-referencing against `schemas/scene-map.schema.md` § "Pressure-signal translation reference" table confirms the mapping is accurate.
- **why:** The tensometer-class language appears in a rubric that `/and-facets` Phase 4 (RUBRIC-FIDELITY audit class) enumerates at audit time. An auditor reading "@64-class" or "@39-class" without the translation context could misclassify pressure-signal alignment. This is a SIGNAL not a HARD because the operative rubric guidance is correct; the translation-register is historical context.
- **recommendation:** Wrap the `@64-class` / `@39-class` tensometer references in the rubric in an explicit `[legacy translation reference; tensometer retired URI-SUBSTANCE-OVERHAUL 2026-05-17]` annotation so auditors reading the rubric know these are historical glosses, not active vocabulary.

### STRUCT-007
- **id:** STRUCT-007
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `design/shoot-v2/rubric-sensory.md` line 16 — "(correlative observation only, NOT gating — `rhythm-shape` and `peak-bones` fields provide the same structural orientation that tensometer scalars previously provided)"
- **description:** The rubric's dependency declaration explicitly names the scene-map facet file as a correlative reference for `rhythm-shape` and `peak-bones`. The rubric does NOT depend on tensometer. This is correctly stated. However the rubric also references "tensometer" in its V3 header section: "It does NOT depend on… the open V3 rubric work for tensometer." The phrase "open V3 rubric work for tensometer" describes a rubric effort that predates URI-SUBSTANCE-OVERHAUL; the tensometer is now fully retired and there is no "open V3 rubric work" on it. The parenthetical implies something is still in progress.
- **why:** The "open V3 rubric work for tensometer" parenthetical reads as a pending task that was never closed after the tensometer's retirement. An auditor could interpret it as a signal that tensometer dependency may be added back.
- **recommendation:** Remove or annotate the "open V3 rubric work for tensometer" parenthetical in `rubric-sensory.md` line 16 to clarify that the tensometer work was fully superseded and no further rubric development is pending.

### STRUCT-008
- **id:** STRUCT-008
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `design/shoot-v2/rubric-memory-flags.md` line 18 — "It does NOT depend on… the open V3 rubric work for tensometer."
- **description:** Same pattern as STRUCT-007. The memory-flags rubric carries the same "open V3 rubric work for tensometer" parenthetical, which is stale post-URI-SUBSTANCE-OVERHAUL.
- **why:** Same as STRUCT-007. The parenthetical misleads about whether tensometer dependency is forthcoming.
- **recommendation:** Remove or annotate the parenthetical in `rubric-memory-flags.md` line 18.

### STRUCT-009
- **id:** STRUCT-009
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `design/shoot-v2/rubric-state-updates.md` line 7 — "It does NOT depend on… the open V3 rubric work for tensometer (body-charge POV scope, negative-event reversal-proximity)."
- **description:** Same pattern as STRUCT-007/008. The state-updates rubric carries the same parenthetical with additional tensometer-specific detail ("body-charge POV scope, negative-event reversal-proximity"). These details describe planned tensometer V3 rubric features that were superseded.
- **why:** Same as STRUCT-007/008; the additional detail compounds the confusion by making the pending tensometer work sound more concrete than the fully-retired reality.
- **recommendation:** Remove or annotate the tensometer parenthetical in `rubric-state-updates.md` line 7.

---

## Check 3 — Command bodies vs rubrics

### STRUCT-010
- **id:** STRUCT-010
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `.claude/commands/and-review.md` line 164 — `facets` subcommand rubric reference
- **description:** The `facets` subcommand documentation states: "Reviewers: per-facet rubric runners. What it reviews: facet-by-facet review against rubric (`design/shoot-v2/rubric-*.md`)." No rubric named `design/shoot-v2/rubric-dialogue.md` exists on disk (confirmed: `ls` of the `design/shoot-v2/` directory does not reveal a `rubric-dialogue.md`). The `/and-facets` Phase 4 RUBRIC-FIDELITY class audit enumerates from `rubric-dialogue.md` per the command body (lines 332–333): "from each facet rubric's §Anti-patterns … `rubric-dialogue.md`". If this file does not exist, the RUBRIC-FIDELITY dialogue enumeration at Phase 4 audit time has no source file.
- **why:** If `rubric-dialogue.md` is absent from `design/shoot-v2/`, the Phase 4 auditor's RUBRIC-FIDELITY scan for dialogue anti-patterns (AP-chassis-contamination, AP-modern-hr-speak, AP-deposition-cadence, AP-nominalization-substituting-plain-English) cannot enumerate from the named source. The audit class references the path but the file may not exist.
- **recommendation:** Confirm whether `design/shoot-v2/rubric-dialogue.md` exists on disk. If absent, either create it from the dialogue anti-pattern definitions already embedded in the `/and-facets` Phase 4 AP-SCAN section, or update the RUBRIC-FIDELITY source-enumeration list to note the file is pending.

### STRUCT-011
- **id:** STRUCT-011
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `.claude/commands/and-facets.md` Phase 4 RUBRIC-FIDELITY source-enumeration list (line 333) — `rubric-exposition.md`
- **description:** The Phase 4 RUBRIC-FIDELITY audit class enumerates from `rubric-exposition.md` ("from each facet rubric's … `rubric-exposition.md`"). Inspection of `design/shoot-v2/` does not confirm the existence of `rubric-exposition.md`. The exposition facet schema (`schemas/facet.schema.md` § exposition) was added 2026-05-12 and the CONSTRAINT class in `/and-facets` Phase 4 carries extensive exposition rules inline, but a standalone `rubric-exposition.md` is not confirmed to exist.
- **why:** If `rubric-exposition.md` is absent, the Phase 4 RUBRIC-FIDELITY auditor's per-entry signature scan and file-level shape gate for exposition entries cannot enumerate from the named source file, leaving exposition entries audited only under the inline CONSTRAINT class (not the RUBRIC-FIDELITY class).
- **recommendation:** Confirm whether `design/shoot-v2/rubric-exposition.md` exists on disk. If absent, note it as pending in the RUBRIC-FIDELITY source-enumeration.

---

## Check 4 — Residue scan

### RESIDUE-001
- **id:** RESIDUE-001
- **class:** RESIDUE
- **severity:** HARD
- **location:** `schemas/audit-report.schema.md` line 107 — "URI-026 follow-on"
- **description:** The R2 decision-shard section header reads "R2 decision-shard frontmatter (URI-026 follow-on, 2026-05-10)". `URI-026` is in the known-deprecated set (the pre-overhaul tens-gate URI). This reference appears in the live schema file (not in `archive/` or a historical doc), making it a HARD finding per the audit specification.
- **why:** A `URI-026` reference in an active schema file signals the reader that the tens-gate concept is still foundational to this section. The tens-gate is superseded by the substance bone-gate under URI-SUBSTANCE-OVERHAUL. The section itself documents a retired R2 mechanism (per STRUCT-001), so the URI-026 attribution compounds the stale reference.
- **criteria:** Remove or replace `URI-026 follow-on` in the R2 decision-shard section header, or add a clear deprecation notice that both this section and the underlying URI-026 mechanism are retired under DEC-0116 / URI-SUBSTANCE-OVERHAUL.

### RESIDUE-002
- **id:** RESIDUE-002
- **class:** RESIDUE
- **severity:** HARD
- **location:** `schemas/bones.schema.md` line 150 — "Replaces URI-026 tens-gate"
- **description:** Line 150 of the bones schema reads: "Reviewed by the five-pass pipeline… Phase 6 substance bone-gate (the substance overhaul's replacement for URI-026 tens-gate)." The `URI-026` term appears in the live active schema. While the context is explanatory (describing what was replaced), the known-deprecated term appears in a non-archive, non-historical-doc active schema file.
- **why:** Per the residue-scan rules, `URI-026` references in active command bodies or schemas are HARD. The bones schema is the authoritative source for bone-authoring; retaining `URI-026` in its body keeps the deprecated concept in the production reference surface.
- **criteria:** Replace the parenthetical `(the substance overhaul's replacement for URI-026 tens-gate)` with `(the substance overhaul's replacement for the pre-overhaul tens-gate; see URI-SUBSTANCE-OVERHAUL 2026-05-17)` or remove the historical reference entirely, since the substance bone-gate is fully documented in Phase 6 of `/and-write`.

### RESIDUE-003
- **id:** RESIDUE-003
- **class:** RESIDUE
- **severity:** HARD
- **location:** `.claude/commands/and-write.md` line 503 — "`tens:` citation prefix is removed"
- **description:** Line 503 reads: "`tens:` citation prefix is removed from the recognized list (`schemas/bones.schema.md § citation prefixes`) — tensometer dropped." This explicitly invokes the known-deprecated `tens:` prefix in an active command body (Notes section). Per the residue-scan rules, `tens:` as a load-bearing prefix reference in an active command body is HARD.
- **why:** While the statement correctly documents the removal, the presence of `tens:` in a command body's active Notes section means any grep-for-deprecated-terms check on command bodies returns a hit. The intent is archival documentation, but the location is the live command body, not `archive/`.
- **criteria:** Either move this note to an archive or historical comment, or rephrase to avoid the `tens:` token: e.g. "The tensometer citation prefix has been removed from the recognized citation list in `schemas/bones.schema.md`."

### RESIDUE-004
- **id:** RESIDUE-004
- **class:** RESIDUE
- **severity:** SIGNAL
- **location:** `schemas/scene-map.schema.md` — Pressure-signal translation reference table (lines 120–126)
- **description:** The scene-map schema contains a translation reference table mapping pre-overhaul language to scene-map equivalents. Column 1 reads: "`tens=1` / 'quiet beat' / 'low-charge'", "`tens=2` / 'building' / 'ramp'", "`tens=3` / 'peak' / 'high-charge'", "`tens=2 trailing edge` / 'release zone'". The `tens=` references are the retired tensometer scalar vocabulary. Location is a schema file, not `archive/` — but the table is explicitly a historical translation reference within the schema, not an active authoring instruction. This is a SIGNAL (historical reference serving a mapping function) rather than a HARD finding.
- **why:** The translation table is intentionally present to help auditors reading older documentation map legacy language to current schema fields. It is not misleading about current practice; it is a deliberate rosetta-stone. However it places known-deprecated terms in a live schema.
- **recommendation:** Add a section header before the table explicitly marking it as a `[Legacy translation reference — tensometer retired URI-SUBSTANCE-OVERHAUL 2026-05-17; do not use these terms in new authoring]` notice, to prevent the table from being mistaken as active vocabulary.

### RESIDUE-005
- **id:** RESIDUE-005
- **class:** RESIDUE
- **severity:** SIGNAL
- **location:** `schemas/stitch-render-log.schema.md` line 183 — `COLLAPSE-FLAT-LOW-RUN` entry
- **description:** The Phase 3 move class entry reads: "`COLLAPSE-FLAT-LOW-RUN` — Run of zero-cite bones inside a `rhythm-shape: flat-low` zone collapsed. (Formerly `COLLAPSE-TENS1-RUN` pre-overhaul; renamed under URI-SUBSTANCE-OVERHAUL 2026-05-17 — references the scene-map's `rhythm-shape` field instead of the removed `tens=1` scalar.)". This is a historical annotation within an active schema file documenting a rename. The tensometer-era name `COLLAPSE-TENS1-RUN` is mentioned for reference. Historical annotation in a schema is in the SIGNAL range per the residue rules (not `archive/` or `design/substance/plan-holes-*.md`, but a named-formerly note within a live schema entry is closer to intentional historical documentation than a ghost reference).
- **why:** The formerly-note is useful for readers tracing log entries from pre-overhaul runs. Not a ghost reference; intentionally documented. Severity SIGNAL because it is in a live schema rather than archive.
- **recommendation:** No immediate action required. If schema cleanup is done, the `(Formerly COLLAPSE-TENS1-RUN…)` note could be moved to a schema revision-history section rather than inline in the move-class definition.

### RESIDUE-006
- **id:** RESIDUE-006
- **class:** RESIDUE
- **severity:** SIGNAL
- **location:** `schemas/stitch-profile.schema.md` line 237 — `flat-low-run-collapse` field comment
- **description:** The `flat-low-run-collapse` compression field comment reads: "(Pre-overhaul name: `tens1-run-collapse`, referencing the now-removed `tens=1` scalar; renamed under URI-SUBSTANCE-OVERHAUL 2026-05-17.)". Same pattern as RESIDUE-005 — a formerly-note in a live schema carrying the deprecated `tens1-run-collapse` name.
- **why:** Historical annotation; intentional. SIGNAL because it is in a live schema.
- **recommendation:** Same as RESIDUE-005 — no blocking action; consider consolidating formerly-notes into a schema revision-history section if cleanup is done.

### RESIDUE-007
- **id:** RESIDUE-007
- **class:** RESIDUE
- **severity:** SIGNAL
- **location:** `schemas/facet.schema.md` lines 34–38 (tensometer section) and line 126 (`tens:<reading>` support removed) and line 163 (`tens:<reading>` source removed)
- **description:** The tensometer facet-type section (lines 34–38) is marked DEPRECATED and retained as historical reference. The schema correctly labels it deprecated. Lines 126 and 163 contain inline removal notes: "(Pre-overhaul `tens:<reading>` support removed under URI-SUBSTANCE-OVERHAUL; …)" and "(Pre-overhaul `tens:<reading>` source removed under URI-SUBSTANCE-OVERHAUL; …)". These are historical-annotation-in-active-schema entries, not ghost references. The tensometer section's `DEPRECATED` header makes the status clear to readers.
- **why:** Intentional historical retention; `DEPRECATED` is clearly labeled. SIGNAL because the content is in a live schema rather than `archive/`. The tensometer section notes cite the `tens:` prefix in a clearly deprecated block.
- **recommendation:** No blocking action. The deprecated section is properly labeled. Consider whether the tensometer section should be moved to an archive note or a schema revision-history section in a future cleanup pass.

### RESIDUE-008
- **id:** RESIDUE-008
- **class:** RESIDUE
- **severity:** SIGNAL
- **location:** `.claude/commands/and-stitch.md` Phase 1 scene-window mode, scene-boundary resolution (around line 416) — "Tensometer derivation (REMOVED 2026-05-17)"
- **description:** The scene-boundary resolution section documents "Tensometer derivation (REMOVED 2026-05-17 under URI-SUBSTANCE-OVERHAUL). The tensometer-fallback path is dead code under the new chain…" This is an explicit removal notice in an active command body rather than `archive/`. Per the residue-scan rules, `tensometer` in an active command body is HARD. However the context is a tombstone ("REMOVED" notice, not an active dispatch path) and the command body was updated to document the removal explicitly — an intentional historical note.
- **why:** Technically HARD per the classification rules (tensometer in active command body), but the substance is a tombstone documenting removal, not a ghost reference that could mislead a reader into using the deprecated path. Reporting as SIGNAL rather than HARD on the grounds that the tombstone is the correct form for documenting removed fallback paths, and the REMOVED label prevents misuse.
- **recommendation:** If the strict HARD classification is enforced, move the "Tensometer derivation (REMOVED...)" entry to an archive comment block or to the command body's Notes section. If tombstones in removal-notices are accepted as SIGNAL, no action needed.

---

## Check 5 — CLAUDE.md sync

### STRUCT-012
- **id:** STRUCT-012
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `CLAUDE.md` Schema authority table — missing entries
- **description:** The schema authority table in `CLAUDE.md` lists 14 schema file entries. Two schemas present under `schemas/` are not in the table:
  - `schemas/aggregate-state.schema.md` — referenced by `/and-substance chapter` Phase 0 step 6a as `schemas/aggregate-state.schema.md`. This file is not in the CLAUDE.md schema authority table.
  - `schemas/memory.schema.md` — listed in the table as "Actor memory (LTM/STM/state/vibes)" but the actual file in `schemas/` may be `schemas/memory.schema.md`. This entry is present. No gap here.
  The missing entry is `aggregate-state.schema.md`.
- **why:** A reader using the CLAUDE.md schema authority table as the index of all schemas would not know `aggregate-state.schema.md` exists, even though `/and-substance chapter` Phase 0 reads from it.
- **recommendation:** Add `schemas/aggregate-state.schema.md` to the CLAUDE.md schema authority table with description "Aggregate rolling close-state for shipped chapters (PROP-0031)".

### STRUCT-013
- **id:** STRUCT-013
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `CLAUDE.md` directory map — `theater/proto-lines/` description vs current usage
- **description:** The CLAUDE.md directory map entry for `theater/proto-lines/` reads: "proto-lines/ — canonical merged proto-line file with `[<facet>:<id>]` citations from the single R1 authoring round (intermediate artifact written by /and-facets Phase 2 / build_cite_index.py; consumed by /and-facets Phase 4 auditor + /and-stitch Phase 0/1)". The current `/and-facets` Phase 4 auditor reads from `theater/proto-lines/<slug>.md` as an input per the command body (Phase 4 Read inputs section). The description is broadly correct. However `/and-stitch` Phase 0 reads from `theater/bones/<book>-<chapter>.md`, NOT from `theater/proto-lines/`. The stitch command body Phase 0 step 1 confirms: "Verify `active-project/theater/bones/<book>-<chapter>.md` exists." The CLAUDE.md claim that proto-lines is consumed by "/and-stitch Phase 0/1" refers to the pre-overhaul path — under URI-SUBSTANCE-OVERHAUL the stitcher's primary input is the bones file, not the proto-lines file. Phase 1 of `/and-stitch` also references `theater/proto-lines/<slug>.md` in the scene-window fork payload — so this is partially correct (the stitcher does read proto-lines for citation-resolved content), but the primary validated-existence check at Phase 0 is against the bones file, not proto-lines.
- **why:** A reader may understand from the CLAUDE.md directory map that `/and-stitch` Phase 0 validates the proto-lines file, when in fact Phase 0 validates `theater/bones/<book>-<chapter>.md`. Minor clarity gap, not a blocking issue.
- **recommendation:** Update the `theater/proto-lines/` entry in CLAUDE.md to clarify that `/and-stitch` Phase 0 validates the bones file; proto-lines is consumed by Phase 1 forks as the cite-indexed merged bones output.

### STRUCT-014
- **id:** STRUCT-014
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `CLAUDE.md` agent routing table — `renderer-minimal` agent location
- **description:** The agent routing table lists `renderer-minimal` with "Owned by: `.claude/agents/renderer-minimal.md`". The `.claude/agents/` directory is listed elsewhere in CLAUDE.md as the agent definitions location. This is consistent with the system. No gap found.
- **why:** Confirmed consistent. No finding.

### STRUCT-015
- **id:** STRUCT-015
- **class:** STRUCT
- **severity:** SIGNAL
- **location:** `CLAUDE.md` Commands table — `/and-stitch` description references "Phase 9 Step 4" URI-STITCH-SIGNAL-CLUSTER
- **description:** The CLAUDE.md commands table entry for `/and-stitch` states: "Phase 9 Step 4 includes the URI-STITCH-SIGNAL-CLUSTER soft-gate (2026-05-24)". The current `/and-stitch` command body Phase 9 section references the cold-read gate but detailed Phase 9 step structure was not read in full. Cross-referencing the showrunner-memory schema, the `cold_read.signal_clusters[]` field confirms the URI-STITCH-SIGNAL-CLUSTER is a real gate. This entry is consistent. No gap found on this specific item.
- **why:** Confirmed consistent. No finding.

### STRUCT-016 (TASTE-FLAG)
- **id:** STRUCT-016
- **class:** TASTE-FLAG
- **severity:** TASTE-FLAG
- **location:** `CLAUDE.md` Rules §8 — card class count
- **description:** CLAUDE.md Rule §8 states "No card class outside the five defined (persona, location, prop, condition, behavior)." The `cards/` directory map lists `dialects/` as "behavior cards (rename to behaviors/ pending; deferred under this overhaul)." The rule says five classes but one of the listed classes (`behavior`) has a different on-disk directory name (`dialects/`). This creates a naming discrepancy (class named "behavior" but directory is "dialects") that is explicitly noted as a deferred rename. Not a schema violation but worth noting for the pending rename task.
- **why:** This is intentional deferred work, not pipeline drift. Documenting as TASTE-FLAG.

---

## Summary

| Severity | Count |
|---|---|
| HARD | 3 |
| SIGNAL | 12 |
| TASTE-FLAG | 1 |

**Overall verdict: FAIL** (3 HARD findings)

### HARD findings

1. **RESIDUE-001** — `URI-026` appears in live `schemas/audit-report.schema.md` (R2 decision-shard section header). Active schema, not archive.
2. **RESIDUE-002** — `URI-026` appears in live `schemas/bones.schema.md` (authoring rules section). Active schema, not archive.
3. **RESIDUE-003** — `` `tens:` `` prefix mentioned by name in `.claude/commands/and-write.md` Notes section. Active command body, not archive.

### Top 3 most actionable findings

1. **RESIDUE-001 / RESIDUE-002 (HARD)** — Two `URI-026` references in active schemas (`audit-report.schema.md` line 107, `bones.schema.md` line 150). Both are in prose that documents retired mechanics. Fix: replace `URI-026 tens-gate` phrasing with `pre-overhaul tens-gate (see URI-SUBSTANCE-OVERHAUL 2026-05-17)` or remove the attribution. These are one-line edits each.

2. **RESIDUE-003 (HARD)** — `` `tens:` `` in `.claude/commands/and-write.md` Notes. Fix: rephrase "`tens:` citation prefix is removed" to "the tensometer citation prefix is removed" to avoid the deprecated token appearing in the active command body.

3. **STRUCT-001 / STRUCT-002 (SIGNAL, but load-bearing clarity gap)** — The R2 decision-shard section in `audit-report.schema.md` and the R2-dialogue-judge sentence in `dialogue.schema.md` both describe retired mechanics without clear deprecation labeling. These create the most reader-confusion of the SIGNAL findings because they are in the two schemas most likely to be read when authoring/reviewing under the current pipeline.
