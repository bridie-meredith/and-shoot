# /and-review pipeline — 2026-05-21 18:40

**Subcommand:** pipeline (URI-REVIEW-PIPELINE, A10)
**Scope:** pipeline (schema ↔ command-body ↔ rubric tri-walk)
**Target:** entire pipeline meta-state
**Trigger:** first real-stakes run of the `pipeline` subcommand after PR #48 merge (14 files touched across schemas/, .claude/commands/, design/shoot-v2/rubric-*.md, cite-index builder)
**Auditor:** staff/auditor (single fork, schema-audit mode)
**Prior reactive audit:** `active-project/staff/auditor/pipeline-adaptation-audit-2026-05-21.md` (5 HARDs + 4 SIGNALs, all resolved same-session)

---

## Verdict

**BLOCK.** Six HARDs present. `/and-substance chapter b01c02` dispatch is blocked until HARDs are resolved.

| Severity | Count | IDs |
|---|---|---|
| HARD | 6 | STRUCT-001, STRUCT-002, STRUCT-003, STRUCT-004, STRUCT-005, STRUCT-006 |
| SIGNAL | 7 | STRUCT-007, STRUCT-008, STRUCT-009, STRUCT-010, STRUCT-011, RESIDUE-001, RESIDUE-002 |
| TASTE-FLAG | 3 | RESIDUE-003, RESIDUE-004, RESIDUE-005 |

---

## HARD findings

### STRUCT-001 — `/and-cut` body is pre-substance legacy

**file:** `.claude/commands/and-cut.md` (entire command body)

**what:** Phase 1 file table references `show.md`, `shoot-log.md`, `episode-plan.md`, `episode-plan-log.md`, `wrap-structure-log.md`, `wrap-audience-log.md`, `polish/<episode-slug>.md`. Phase 2 checkpoint writes `active_season`, `active_episode`, `episode_status`. Phase 3 annotates `memory.md` with `cut: … — <active_episode>`. Phase 4 prints `season: <slug>` / `episode: <slug>`. Resume instructions reference `/and-shoot` and `/and-wrap`. Pipeline-position states include `pre-shoot`, `mid-shoot`, `shoot-complete`, `mid-wrap`.

**why HARD:** Whole body is pre-substance. CLAUDE.md describes `/and-cut` as "Mid-pipeline stop. Saves resume checkpoint; prints 'you are here' with `next:` and (if cascade was in-progress) `resume:` lines." The current body surveys a theater pipeline that no longer exists. A cascade-in-progress that calls `/and-cut` will write a checkpoint pointing at `/and-shoot`, which is not a current command.

**criteria:** Replace `/and-cut` body with one that reads `cascade-checkpoint.md` (written by `/and-substance --cascade`), reports active book/chapter/cascade state from `staff/showrunner/memory.md`, and produces a resume command in the form `/and-substance <root> --cascade --resume` or the appropriate next single-chapter command. Remove the show/season/episode survey phase and `/and-shoot`/`/and-wrap` resume instructions.

---

### STRUCT-002 — audit-report schema `scope` enum is pre-substance

**file:** `schemas/audit-report.schema.md`

**what:** Line 11: `scope: episode | season | series`. Line 12: `target: <episode slug, season slug, or series>`.

**why HARD:** Substance overhaul replaced episode/season with book/chapter/scene (per showrunner-memory schema revision history). Current-chain audits (including this very `pipeline` run) cannot honestly populate `scope` — the closest enum values are wrong. The `pipeline` subcommand requires a `scope: pipeline` value that doesn't exist.

**criteria:** Update `scope` enum to add `book | chapter | scene | pipeline`. Decide whether to retain `episode | season` as deprecated-legacy or replace outright. Update `target` description to reference current slug forms (`b<NN>`, `b<NN>c<MM>`, `b<NN>c<MM>s<KK>`, etc.).

---

### STRUCT-003 — `/and-stitch` Phase 0 step 5 references "proto-lines header"

**file:** `.claude/commands/and-stitch.md` line 100

**what:** Step 5: "Read `narrator:` from **proto-lines** header."

**why HARD:** Under URI-SUBSTANCE-OVERHAUL, `/and-stitch` Phase 0 step 1 reads from `theater/bones/<book>-<chapter>.md`. The seven-field extended header (`narrator:`, `goal:`, etc.) lives in the bones file, not a proto-lines file. Step 5 is operational dispatch — a stitcher fork following it literally looks in the wrong file.

**criteria:** Step 5 must read: "Read `narrator:` from the bones file header (`theater/bones/<book>-<chapter>.md` § `narrator:` field)."

---

### STRUCT-004 — `/and-stitch` Phase 0 step 6 uses narrator-interest as scene-boundary source

**file:** `.claude/commands/and-stitch.md` line 101

**what:** Step 6: "Parse `active-project/theater/facets/interest-narrator.md` for the sparsity-gradient section; extract scene labels and anchor ranges."

**why HARD:** Under URI-SCENE-WINDOW (2026-05-13), default `phase-1.mode` is `scene-window` and scene boundaries are sourced from `theater/facets/scene-map-<book>-<chapter>.md`. Narrator-interest sparsity-gradient parsing as boundary source is legacy. Step 6 conflicts with Phase 2a and Phase 1 scene-window mode — same boundaries derived twice from two different sources.

**criteria:** Update Step 6 to read scene boundaries from `theater/facets/scene-map-<book>-<chapter>.md` in the default scene-window flow. Either remove narrator-interest sparsity-gradient parsing or demote to per-anchor-mode-only (verify whether per-anchor mode still needs it; remove if not).

---

### STRUCT-005 — `/and-stitch` misattributes scene-map emission

**file:** `.claude/commands/and-stitch.md` line 367 (Phase 1 scene-window mode § Scene-boundary resolution step 1)

**what:** "The scene-map is emitted at `/and-facets` Phase 4d as a derived structural facet."

**why HARD:** Under URI-SUBSTANCE-OVERHAUL (2026-05-17), `/and-write` Phase 7 emits the scene-map facet directly from `chapters[].scenes[]`. `/and-facets` Phase 4d was downgraded from derivation to validation (explicit in `/and-facets` header line 9). The stitcher's description misattributes the emission source.

**criteria:** Replace with: "emitted by `/and-write` Phase 7 from `chapters[].scenes[]` in showrunner memory; validated (not derived) at `/and-facets` Phase 4d."

---

### STRUCT-006 — `/and-facets` Notes references `/and-season` in load-bearing promotion-path description

**file:** `.claude/commands/and-facets.md` line 703

**what:** "Shared reviewer assets … are authored once and consumed from both `/and-season` and `/and-facets`. Patterns the audience flags at `/and-season` bone-gate graduate into AP-SCAN entries via the shared auditor's TASTE-FLAG → AP-SCAN promotion path."

**why HARD:** `/and-season` does not exist. The TASTE-FLAG → AP-SCAN promotion-path description is the load-bearing claim, so the dropped-command reference isn't merely historical — it misroutes the promotion path. Current bone-gate consumer is `/and-write` Phase 6; current post-hoc reviewer is `/and-review`.

**criteria:** Replace `/and-season` references in this note: "consumed from both `/and-write` and `/and-facets`. Patterns the audience flags at `/and-write` Phase 6 bone-gate (per-scene audience review) graduate into AP-SCAN entries via the auditor's TASTE-FLAG → AP-SCAN promotion path."

---

## SIGNAL findings

### STRUCT-007 — `/and-review` lift-basis citations to `/and-season` (four occurrences)

**file:** `.claude/commands/and-review.md` (subcommands `chunk`, `contract`, `signature`, `consistency`)

**what:** Each subcommand's "Lift basis:" line cites `/and-season` (Phase 1 step 1e; Pass S1 + Pass S3.5; Phase 3 sweep-A; Phase 1 step 1g).

**why SIGNAL:** Provenance documentation, not operational dispatch. No reviewer would try to invoke `/and-season`; they'd confuse future maintainers.

**criteria:** Replace with closest current-chain equivalents OR add note "lift basis: pre-overhaul `/and-season`; adapted to current substance-chain hierarchy."

---

### STRUCT-008 — `/and-stitch` notes section references `/and-protolines-v2`

**file:** `.claude/commands/and-stitch.md` line 624

**what:** "Source pipeline is upstream (`/and-protolines-v2`, `/and-facets`)."

**why SIGNAL:** In a notes/limitations section, not operational dispatch. CLAUDE.md confirms `/and-write` replaces `/and-protolines`.

**criteria:** Replace `/and-protolines-v2` with `/and-write`.

---

### STRUCT-009 — `/and-facets` Phase 5 dispatch reads "season plans"

**file:** `.claude/commands/and-facets.md` line 299

**what:** "Series + **season** plans (showrunner memory) — for series-law constraint checks."

**why SIGNAL:** Current memory schema has `books[]`, not `seasons[]`. Series plans live in `series.chunk`, `series.substance`, `series.laws`, `series.lore`.

**criteria:** Replace with: "Series plans (showrunner memory: `series.chunk`, `series.laws`, `series.lore`, `series.behaviors`)."

---

### STRUCT-010 — audit-report schema R2 shard description uses `<episode slug>`

**file:** `schemas/audit-report.schema.md` lines 107, 122

**what:** Shard frontmatter description: `episode: <episode slug>`.

**why SIGNAL:** The `episode:` field name is retained for downstream-compat per bones schema, but the value is now a chapter slug. Description should clarify.

**criteria:** Update to `episode: <chapter slug>  # field name kept for downstream-compat; value is the chapter slug (e.g. b01c01)`.

---

### STRUCT-011 — CLAUDE.md schema authority table missing `stitch-profile.schema.md`

**file:** `CLAUDE.md` schema authority section

**what:** `/and-review` pipeline subcommand (`and-review.md` line 176) instructs auditor to verify against `stitch-*.schema.md`, but CLAUDE.md schema authority table does not list it.

**why SIGNAL:** Coverage gap, not a block. File exists and is reachable from `/and-stitch`.

**criteria:** Add `stitch-profile.schema.md` row to CLAUDE.md schema authority table.

---

### RESIDUE-001 — `/and-protolines-v2` residue (cross-ref STRUCT-008)

**file:** `.claude/commands/and-stitch.md` line 623

**why SIGNAL:** Residue inventory of STRUCT-008.

---

### RESIDUE-002 — `/and-season` residue (cross-ref STRUCT-006)

**file:** `.claude/commands/and-facets.md` line 703

**why SIGNAL:** Residue inventory of STRUCT-006.

---

## TASTE-FLAG findings

### RESIDUE-003 — `/and-review` lift-basis `/and-season` provenance (cross-ref STRUCT-007)

**why TASTE-FLAG:** Provenance documentation in historical-lift notes. Not operational.

---

### RESIDUE-004 — `URI-026` provenance tag in audit-report schema R2 shard section header

**file:** `schemas/audit-report.schema.md` line 96

**what:** Header: "## R2 decision-shard frontmatter (URI-026 follow-on, 2026-05-10)"

**why TASTE-FLAG:** URI-026 is the pre-overhaul tens-gate replaced by the substance bone-gate. Reference is purely provenance documentation in a section header, not an operational instruction. Same classification rationale as TASTE-FLAG-001 in the prior reactive audit.

---

### RESIDUE-005 — `rubric-sensory.md` calibration anchors scene-map vocabulary

**file:** `design/shoot-v2/rubric-sensory.md` lines 286-296

**why TASTE-FLAG:** Scan found no `tens=N` scalar residue. Calibration anchors use scene-map vocabulary correctly. Clean — listed for inventory completeness.

---

## CLAUDE.md sync check

**Directory map:** PASS. `theater/proto-lines/` added by prior reactive audit (line 88).
**Schema authority table:** SIGNAL (STRUCT-011: missing `stitch-profile.schema.md`).
**Agent routing table:** PASS.
**Commands table:** PASS. `/and-cut` description is correct; command body (STRUCT-001) is the defect, not the CLAUDE.md description.

---

## Next-action recommendations

**Most consequential HARD for the immediate chain:** STRUCT-001 (`/and-cut` body non-functional for cascade checkpoint/resume) and STRUCT-002 (audit-report schema can't classify this pipeline run's own scope).

**Latent (don't block `/and-substance chapter b01c02` directly, but will misroute `/and-stitch` when b01c02 reaches it):** STRUCT-003, STRUCT-004, STRUCT-005.

**Cleanup HARD (operational misroute in shared-reviewer-assets doc):** STRUCT-006.

To unblock the chain: resolve all six HARDs, then re-run `/and-review pipeline`. On clean re-run, proceed to `/and-substance chapter b01c02`.
