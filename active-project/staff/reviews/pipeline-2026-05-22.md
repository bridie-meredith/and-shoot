audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-05-22
  invocation: /and-review pipeline
  context: b01c02 postmortem command-body + schema edits (8 files); tri-walk before /and-write b01c01 redo
  findings:

    - id: STRUCT-001
      type: fault
      what: |
        `and-stitch.md` flowchart header (line ~20) lists the dialogue input path as
        `theater/dialogue/<book>-<chapter>.md` — a single per-chapter file.
        `schemas/dialogue.schema.md` defines the canonical path as
        `theater/dialogue/<character-slug>.md` — one file per speaking character.
        All body text in and-stitch (Phase 0.7), and-facets (Phase 1 author #11, Phase 5 CONSTRAINT),
        and and-facets Phase 2/3/4 all correctly use the `<character-slug>.md` convention.
        The flowchart header is the only location that uses the wrong shape.
      why: |
        A fork dispatched by `/and-stitch` that reads the flowchart header as its path spec
        would look for a nonexistent file and either fault-abort or miss all dialogue.
      criteria: |
        The flowchart header path must read `theater/dialogue/<character-slug>.md`
        (or `theater/dialogue/*.md` for the glob form), consistent with the schema
        and all body-text references in the same command.

    - id: STRUCT-002
      type: fault
      what: |
        `and-stitch.md` flowchart header (line ~20) lists `_cite-index-<book>-<chapter>.md`
        as the cite-index path. `and-facets.md` Phase 2 step 5 writes (and `and-stitch.md`
        Phase 0 body text reads at step 2) `theater/facets/_cite-index.md` — no chapter suffix.
      why: |
        A fork that reads the flowchart for the cite-index path would construct
        `_cite-index-<book>-<chapter>.md` and receive a file-not-found.
      criteria: |
        The flowchart header must read `theater/facets/_cite-index.md`
        (matching the path used by and-facets Phase 2 and and-stitch Phase 0 step 2 body text).

    - id: STRUCT-003
      type: fault
      what: |
        `and-facets.md` Phase 5 audit output section specifies twelve audit classes
        (STRUCTURAL, FREQUENCY-BAND, METADATA-INCONSISTENCY, CURVE-SHAPE, CONTRADICTION,
        DEDUP, SUPERFLUOUS, CONSTRAINT, AP-SCAN, TASTE-FLAG, PILE-UP REVIEW, RUBRIC-FIDELITY),
        but the "Audit summary" block says
        "HARD classes: STRUCTURAL <n>, CONTRADICTION <n>, DEDUP <n>, SUPERFLUOUS <n>, CONSTRAINT <n>"
        — listing only 5 HARD classes. RUBRIC-FIDELITY is defined as "HARD by default"
        under URI-RUBRIC-FIDELITY (2026-05-19) but is absent from the Audit summary HARD class list.
      why: |
        The orchestrator-critic reading the audit summary to count HARD findings would
        undercount RUBRIC-FIDELITY HARDs, which are non-blocking-absent but blocking-present.
      criteria: |
        The "HARD classes" line in the Audit summary block must include
        `RUBRIC-FIDELITY <n>` alongside STRUCTURAL, CONTRADICTION, DEDUP, SUPERFLUOUS, CONSTRAINT.

    - id: STRUCT-004
      type: fault
      what: |
        `schemas/facet.schema.md` § metaphor flags: the `<support>` field in the
        `licensed-by:` syntax still lists `tens:<reading>` as a valid support value.
        The tensometer (`tens:`) citation prefix was removed under URI-SUBSTANCE-OVERHAUL.
      why: |
        A facet author or R2 judge would find `tens:<reading>` listed as a valid support
        value and attempt to cite a facet that no longer exists, producing an unresolvable
        `licensed-by:` anchor that the Phase 5 auditor's CONSTRAINT check would flag as HARD.
      criteria: |
        `tens:<reading>` must be removed from the metaphor flags `<support>` enum in
        `schemas/facet.schema.md`. Pressure-signal substitute is scene-map `peak-bones` membership.

    - id: STRUCT-005
      type: fault
      what: |
        `schemas/facet.schema.md` § vibes-updates: the `<source>` field in
        `licensed-by:` still lists `tens:<reading>` as a valid source value.
      why: |
        Same downstream consequence as STRUCT-004: vibes authors would attempt to source
        a `tens:` reading that no longer exists.
      criteria: |
        `tens:<reading>` must be removed from the vibes-updates `<source>` enum in
        `schemas/facet.schema.md`.

    - id: RESIDUE-001
      type: fault
      what: |
        `design/shoot-v2/rubric-location-state.md` § Anti-patterns entry #2:
        "Fix: only fire on movement; atmospherics belong to dramatist (tensometer) or are deleted."
        Anti-pattern entry #3: "Fix: delete or recast as a tensometer beat."
        Both fix-prescriptions route correction work to "tensometer" — a dropped concept.
      why: |
        Live load-bearing authoring instructions; the rubric is read by studio and reviewers
        at every `/and-facets` R1 / Phase 5b invocation.
      criteria: |
        Anti-pattern #2 fix must read "delete the entry; atmospherics with no perceptible
        focus-element are not authored in this pipeline." Anti-pattern #3 fix must read
        "delete the entry; persistence-of-state is location-card content, not a facet."

    - id: RESIDUE-002
      type: fault
      what: |
        `design/shoot-v2/rubric-location-state.md` § Transition-run continuity license uses
        `tens=1` as a classification term. The `tens=1` scalar is removed; the correct
        vocabulary is `rhythm-shape: flat-low` or `fusion-eligible-runs` membership from
        the scene-map facet.
      why: |
        A new studio author reading this rubric would look for a tensometer scalar that
        doesn't exist; the license's scope condition currently references a non-existent field.
      criteria: |
        "tens=1 transition runs" must be replaced with "flat-low zone bones
        (scene-map `rhythm-shape: flat-low` or `fusion-eligible-runs` membership)".

    - id: RESIDUE-003
      type: fault
      what: |
        `design/shoot-v2/rubric-narrator-interest.md` § Cross-axis tests / earning test:
        "*Tens transition into peak*" — uses deprecated tensometer vocabulary.
        Scene-map equivalent: `rhythm-shape` transitioning from `flat-low`/`rising` into
        `peak-bones` membership.
      why: |
        This cross-axis test is a rubric gate used at authoring and review time.
        The calibration example is inoperative as written.
      criteria: |
        The calibration example must be replaced: "Tens transition into peak" →
        "scene-map transition flat-low → rising, or peak-bones entry".

    - id: STRUCT-006
      type: flag
      what: |
        `schemas/stitch-profile.schema.md` still defines the `interval-bridge:` block as an
        active profile field, while `schemas/facet.schema.md` § exposition says it is
        "replaced/subsumed by upstream-authored exposition facets."
      criteria: N/A — non-blocking; deprecation note should be added to stitch-profile schema.

    - id: STRUCT-007
      type: flag
      what: |
        CLAUDE.md § schema authority table omits `stitch-feedback.schema.md` and
        `stitch-render-log.schema.md` despite both files existing under `schemas/`.
      criteria: N/A — non-blocking convenience reference.

    - id: STRUCT-008
      type: flag
      what: |
        CLAUDE.md § directory map omits `theater/proto-lines/` even though the path is
        a real working file used by and-facets Phase 2 / and-stitch.
      criteria: N/A — non-blocking convenience reference.

    - id: RESIDUE-004
      type: taste-flag
      what: |
        `design/shoot-v2/rubric-memory-flags.md` § what memory-flags is for has a historical
        "It does NOT depend on (and must not be folded into) the open V3 rubric work for
        tensometer." — ghost reference to a dropped artifact. Technically still satisfied.
      criteria: N/A

    - id: RESIDUE-005
      type: taste-flag
      what: |
        `schemas/facet.schema.md` § scene-map: "Consumed by: `/and-wrap` editor (future) —
        scene-cut marker placement." — deliberate forward-reference; `/and-wrap` is deferred.
      criteria: N/A

    - id: RESIDUE-006
      type: taste-flag
      what: |
        `schemas/audit-report.schema.md` § R2 decision-shard frontmatter: a "pre-overhaul
        `/and-season` is replaced by `/and-facets` Phase 6 + `/and-review verdict`"
        explanatory note — explicitly historical.
      criteria: N/A

summary:
  hard: 8
  signal: 0
  flag: 3
  taste-flag: 3
  blocks_next_dispatch: true
  next_dispatch: /and-write b01c01 redo
