---
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-12T081039Z
  lens: /and-review pipeline (schema ↔ command-body ↔ rubric tri-walk)
  triggered_by: improvement-loop/test routine — first pass, lens-a
  verdict: FAIL
  hard: 1
  signal: 2
  taste: 3
---

# Pipeline Audit — 2026-06-12T081039Z

**Verdict: FAIL (HARD: 1, SIGNAL: 2, TASTE: 3)**

Tri-walk scope: `schemas/` × `.claude/commands/*.md` × `design/shoot-v2/rubric-*.md` × `staff/orchestrator-critic/card.md` × `CLAUDE.md` sync.

---

## Walk summary

### 1. Schema vs command bodies — CLEAN (with two SIGNAL flags below)

All field references in command bodies resolve correctly in schemas:
- `axes_in_motion[]`, `axes_held[]`, `substance_delta`, `axis_moves`, `chapter_class` — verified present in `schemas/showrunner-memory.schema.md`.
- `rhythm-shape`, `peak-bones`, `peak-shadow-bones`, `fusion-eligible-runs`, `protected-patterns` — verified present in `schemas/scene-map.schema.md`.
- `dramatic_shape` enum values (`rising | climax | falling | hinge`) — verified in schema.
- `episode:` field in bones header — preserved as legacy name per explicit downstream-compat annotation in both schema and command body. TASTE-FLAG only.
- `direction: null` references in `and-write.md` / `and-substance.md` — used as the name of the malformed form, not as an active value. CLEAN.

### 2. Schema vs rubrics — CLEAN

All rubric field references verified:
- `axes_held[]`, `axes_in_motion[]` — in `schemas/showrunner-memory.schema.md`.
- `rhythm-shape`, `peak-bones` — in `schemas/scene-map.schema.md`.
- `dramatic_shape` — in schema.
- Pre-overhaul translation reference table in `rubric-memory-flags.md` (`flat-low = tens=1`, etc.) — explicitly labeled historical; TASTE-FLAG.

### 3. Command bodies vs rubrics — CLEAN

All rubric paths referenced in command bodies resolve on disk:
- `design/shoot-v2/rubric-feeling.md` ✓
- `design/shoot-v2/rubric-location-state.md` ✓
- `design/shoot-v2/rubric-memory-flags.md` ✓
- `design/shoot-v2/rubric-metaphor.md` ✓
- `design/shoot-v2/rubric-narrator-interest.md` ✓
- `design/shoot-v2/rubric-sensory.md` ✓
- `design/shoot-v2/rubric-state-updates.md` ✓
- `design/shoot-v2/rubric-vibes.md` ✓
- `design/shoot-v2/rubric-vibes-v1.1-patch.md` ✓
- `design/shoot-v2/rubric-exposition.md` — DOES NOT EXIST. Noted as deliberately deferred per `pl-2026-06-07-pipeline-001` (de-referenced in `and-facets.md` Phase 1 item 10; operating on schema + context-ledger authority instead). Prior parking-lot item covers this. NOT a new finding.

### 4. Residue scan — HARD + SIGNAL findings

See §"Findings" below.

### 5. CLAUDE.md sync — SIGNAL finding

See §"Findings" below.

---

## Findings

---

### STRUCT-001 (HARD) — orchestrator-critic card calibrated against retired `/and-season`; incompatible with current chain

**What:** `staff/orchestrator-critic/card.md` was authored 2026-05-10 against the then-active `/and-season` orchestration command. It has never been updated post-substance-overhaul (2026-05-17) or post-DEC-0116 (2026-06-08). The card in its entirety judges a `/and-season` run:

- **Invocation protocol (lines 27-45):** "At end of `/and-season` Phase 5…At end of `/and-season-plan` (when implemented)…At end of `/and-protolines-v2`." No reference to `/and-review verdict <book>`, which is the actual current dispatch point.
- **Category A (convergence) criteria:** evaluate `/and-season` Phases 2/3/4 (split) iteration counts. None of these phases exist in the current chain.
- **Category B7 (lines 119-125):** reads `f-r2-counts` from `active-project/theater/facets/.r2-decisions.md`. This file is never produced by the current chain — R2 was retired under DEC-0116. B7 will always report `not-fired` (the absent-file branch), which the card treats as non-blocking. A permanently absent gate.
- **Category B6 (lines 112-117):** checks `tens-gate-residual-HARD` findings and references `URI-026`. The tens-gate was superseded by the substance bone-gate under URI-SUBSTANCE-OVERHAUL (2026-05-17). No current command emits `tens-gate-residual-HARD` findings.
- **Failure mode F7-bone (lines 196, 341):** fires on `tens-gate-residual-HARD`. Dead trigger.
- **Failure mode F7-r2 (lines 196-209):** fires when `f-r2-counts.f-r2-1 > 0`. Never fires because `.r2-decisions.md` is never produced.
- **Category C memory checks (lines 140-144):** reads `seasons[<slug>].protolines_complete`, `seasons[<slug>].episodes[]`, `seasons[<slug>].orchestrator_verdict` from showrunner memory. Current chain uses `books[<slug>].*`, not `seasons[<slug>].*`.
- **Verdict persistence (line 323):** writes to `seasons[<slug>].orchestrator_verdict`. Current chain writes to `books[<slug>].orchestrator_critic_verdict` (per `and-review.md` verdict subcommand Phase 4).
- **Verdict cross-check (line 209):** says "the verdict is still emitted by `/and-season`."
- **Runtime budgets (lines 151-157):** calibrated for `/and-season` dispatch counts (100 dispatches/run), `/and-season` Phase 2/3/4 iteration caps. Not applicable to the current multi-command chain.

**Why this matters:** `/and-review verdict <book>` dispatches this card as its reviewer (per `and-review.md` §verdict: "Reviewers: orchestrator-critic `staff/orchestrator-critic/card.md`"). When the card is dispatched, it will:
  1. Look for `seasons[<slug>].*` memory fields → not found
  2. Apply `/and-season` pass-label verdicts → not applicable
  3. Check for `tens-gate-residual-HARD` → never fires (dead trigger)
  4. Check F-R2-* counts → always `not-fired` (file never exists)
  5. Write verdict to `seasons[<slug>].orchestrator_verdict` → wrong memory location

The book-level quality gate (`/and-review verdict`) currently applies dead criteria. The two existing b01 verdict reports (`verdict-b01-2026-06-06T04-08-37Z.md` and `verdict-rebuilt-b01-2026-06-08.md`) were produced under these stale criteria. This is the highest-priority structural finding — the terminal quality gate is broken.

**Criteria for resolution:** `staff/orchestrator-critic/card.md` rewritten to evaluate `/and-review verdict <book>` runs against current substance-overhaul criteria: substance bone-gate convergence (`chapters[].substance_bone_gate_verdict = PASS`), `/and-facets` Phase 4 mechanical audit clean (`0 HARD`), `/and-stitch` Phase 9 cold-read (`PASS` or `PASS-WITH-NOTES`), `/and-review bones` fidelity verdicts, showrunner memory `books[<slug>].orchestrator_critic_verdict` persistence. B7 (F-R2-* check) and F7-r2 removed; F7-bone updated to check `SUBSTANCE-FLAT-*` / `SUBSTANCE-SUSPECT-*` residuals, not `tens-gate-residual-HARD`. Category A updated to substance-overhaul phase/iteration model. Category C updated to `books[<slug>].*` memory fields. Runtime budgets removed or recalibrated to the multi-command chain.

**Parking-lot item:** `pl-2026-06-12-pipeline-001`

---

### STRUCT-002 (SIGNAL) — `schemas/audit-report.schema.md` R2 decision-shard section references retired architecture

**What:** `schemas/audit-report.schema.md` §"R2 decision-shard frontmatter" (lines 106-157) presents the R2 decision-shard format and `.r2-decisions.md` consolidated file as active schema. Under DEC-0116, the R2 round was retired in full. The section references:
- `/and-facets-r2` (command does not exist in current chain)
- `/and-facets` Phase 6 orchestrator-critic verdict (current `/and-facets` goes to Phase 5 — no Phase 6 exists)
- `staff/orchestrator-critic/card.md` Phase 6 verdict reads `f-r2-counts` — cross-reference to STRUCT-001's dead B7 block

The section is presented as a live schema (not marked deprecated or historical-only), creating reader confusion about whether `.r2-decisions.md` is a format the pipeline still produces. It also creates a circular residue loop with STRUCT-001: the schema references the orchestrator-critic card's B7 block, and the card references this schema section.

**Why this matters:** A pipeline engineer reading the schema will believe `.r2-decisions.md` files and `f-r2-counts` frontmatter are current artifacts. The section is not merely legacy documentation — it's a complete format specification with "consumer contract" instructions that direct the orchestrator-critic to read these files.

**Criteria for resolution:** The §"R2 decision-shard frontmatter" section should be marked clearly as **retired/legacy** with a `> **RETIRED 2026-06-08 under DEC-0116 (URI-FACETS-SLIM).** R2 round eliminated; this format is no longer produced.` notice at the section top, OR the section should be removed entirely and replaced with a note pointing to DEC-0116. Either way, the "consumer contract" text directing the orchestrator-critic to read `f-r2-counts` should be removed.

**Parking-lot item:** `pl-2026-06-12-pipeline-002`

---

### STRUCT-003 (SIGNAL) — CLAUDE.md schema authority table missing two active schemas

**What:** `schemas/` contains `aggregate-state.schema.md` and `cohere-state.schema.md`, both referenced by `/and-cohere.md`. Neither appears in CLAUDE.md's schema authority table. The table currently ends with:
```
| Tournament scorecard (per-scene cherry-pick scoring) | `schemas/tournament-scorecard.schema.md` |
```

**Why this matters:** The schema authority table is the canonical map for what schemas govern which file types. A file format without a table entry creates an undiscoverable schema — engineers may author cohere-state or aggregate-state files without knowing the schema exists.

**Criteria for resolution:** Add two rows to CLAUDE.md's schema authority table:
```
| Aggregate state (cross-chapter coherence tracking) | `schemas/aggregate-state.schema.md` |
| Cohere state (/and-cohere iteration state) | `schemas/cohere-state.schema.md` |
```

**Parking-lot item:** `pl-2026-06-12-pipeline-003`

---

## TASTE-FLAGS (noted for reference; not actionable)

- **TASTE-001:** `schemas/bones.schema.md` / `schemas/audit-report.schema.md` uses `episode:` as field name (kept for downstream-compat) while chapter is the operational concept. Explicit compat annotation present in both files; no reader confusion risk given annotations.
- **TASTE-002:** `and-review.md` verdict subcommand lift-source table (lines 284-293) references `/and-season.md Phase 6 (lines 577-609)` as the upstream lift source. This is intentional historical documentation; the table explains migration, not current invocation. Not a RESIDUE finding.
- **TASTE-003:** `staff/orchestrator-critic/card.md` §Versioning (lines 340-344) references `/and-season` in the version history. Clearly historical; part of the version log, not active criteria.

---

## Summary

| Class | Finding | Severity | Parking-lot item |
|-------|---------|---------|-----------------|
| STRUCT-001 | Orchestrator-critic card calibrated against retired `/and-season`; book-level quality gate broken | HARD | pl-2026-06-12-pipeline-001 |
| STRUCT-002 | `schemas/audit-report.schema.md` R2 section not marked retired | SIGNAL | pl-2026-06-12-pipeline-002 |
| STRUCT-003 | CLAUDE.md schema authority table missing `aggregate-state` + `cohere-state` schemas | SIGNAL | pl-2026-06-12-pipeline-003 |
| TASTE-001 | `episode:` field name for backward-compat | TASTE | — |
| TASTE-002 | Historical lift-source reference to `/and-season.md` in `and-review.md` | TASTE | — |
| TASTE-003 | `/and-season` version history in orchestrator-critic card | TASTE | — |

**HARD findings block the immediate next `/and-review verdict` dispatch** (per `and-review.md` pipeline subcommand: "HARD findings block the immediate next chain dispatch").

---

## admin-process-critic

_STRUCT-001 constitutes a non-PASS verdict. Admin process-critic dispatch deferred: this is the improvement-loop/test routine (finds, does not fix); admin dispatch belongs to the next chain-invoked `/and-review pipeline` run after these findings are presented to the principal._
