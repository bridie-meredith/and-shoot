---
report: pipeline
subcommand: pipeline
timestamp: 2026-06-11T23:10:29Z
lens: /and-review pipeline (schema ↔ command-body ↔ rubric tri-walk)
verdict: HARD-FINDINGS
hard: 3
signal: 1
taste: 4
---

# /and-review pipeline — 2026-06-11

Schema-vs-command-body-vs-rubric tri-walk per `.claude/commands/and-review.md § pipeline`. Dispatched by the improvement-loop/test routine (first pass; ledger absent → defaulted to lens a).

**Scope:** `.claude/commands/*.md`, `schemas/*.md`, `design/shoot-v2/rubric-*.md`, `CLAUDE.md` (sync check). Residue scan covers all four areas.

**Verdict:** 3 HARD, 1 SIGNAL, 4 TASTE-FLAGs. No rubric path errors. No stale enum values in rubrics.

---

## HARD Findings

### STRUCT-001 — `schemas/audit-report.schema.md` § R2 decision-shard: zombie R2 machinery (HARD)

**File:** `schemas/audit-report.schema.md` lines 106–157  
**Class:** STRUCT (schema vs command body)

The `§ R2 decision-shard frontmatter` section documents a pipeline that no longer exists:
- `/and-facets-r2` command emitting `staff/<facet>/r2-decision-shard.md` files
- "Phase 6 of `/and-facets`" consolidating `.r2-decisions.md`
- `staff/orchestrator-critic/card.md` Phase 6 reading `f-r2-counts` from the consolidated file
- Thresholds: `f-r2-1 > 0 = HARD; f-r2-2 + f-r2-3 + f-r2-4 > 2 = SIGNAL`

The current `/and-facets.md` command runs through **Phase 5** only. The R2 judging round and Phase 6 are **RETIRED under DEC-0116** (2026-06-08). The `.r2-decisions.md` consolidated file is no longer produced; `staff/auditor/facets-final-audit.md` from Phase 4 is the facet-layer gate artifact. Any agent (or the pipeline itself) reading this schema section to understand the orchestrator-critic's judgment basis will look for a `.r2-decisions.md` file that doesn't exist and a Phase 6 that doesn't fire.

**Owning agent:** fixer (schema text update — add `RETIRED (DEC-0116, 2026-06-08)` header to the section; redirect the consumer note to `staff/auditor/facets-final-audit.md` as the current facet-layer gate artifact).

**Parking-lot item:** `pl-2026-06-11-pipeline-001`

---

### STRUCT-002 — `and-stitch.md` body text: stale `proto-lines` path references (HARD)

**File:** `.claude/commands/and-stitch.md` (9 occurrences)  
**Class:** STRUCT (command body vs bones schema)

Phase 0's own mutation note (line 8) explicitly states: "Phase 0 reads `theater/bones/<book>-<chapter>.md` (not `theater/proto-lines/<slug>.md`)." Yet the body text in Phases 0.5, 0.7, 1 was not updated to reflect the rename:

| Line | Stale reference |
|------|-----------------|
| 186 | `"speaks-to" bones in proto-lines` |
| 193 | `If proto-lines contains any 'speaks to' bones` |
| 259 | `the proto-lines file contains any <X> speaks to <Y> bones` |
| 281 | `Cross-validate against proto-lines.` |
| 282 | `Every <X> speaks to <Y> bone in proto-lines` |
| 284 | `@anchor not present in proto-lines` |
| 419 | `every bone in proto-lines/<slug>.md` |
| 427 | `verbatim from proto-lines/<slug>.md` |
| 1157 | `missing inputs (proto-lines, cite-index, profile)` |

An agent executing and-stitch will attempt to read `proto-lines/<slug>.md` as the authoritative bone-list when validating the scene-map and dialogue gate, but the actual file is at `theater/bones/<book>-<chapter>.md`. Lines 419 and 427 (Phase 1 scene-window validation) explicitly reference `proto-lines/<slug>.md` as the file from which bone IDs are read — this is a functional error, not just a terminology gap.

**Owning agent:** fixer (text replacement: `proto-lines/<slug>.md` → `theater/bones/<book>-<chapter>.md`; `proto-lines` → `bones file` in prose context).

**Parking-lot item:** `pl-2026-06-11-pipeline-002`

---

### STRUCT-003 — `theater/proto-lines/` path status: three-way inconsistency (HARD)

**Files:** `schemas/bones.schema.md`, `.claude/commands/and-facets.md`, `CLAUDE.md`  
**Class:** STRUCT (schema vs command body vs CLAUDE.md sync)

The path `theater/proto-lines/<slug>.md` is declared inconsistently across three authoritative documents:

1. **`schemas/bones.schema.md` (lines 14-16):** "Legacy paths (`theater/proto-lines/<slug>.md`, `theater/proto-lines/<season-slug>.aggregate.md`, top-level `theater/proto-lines.md`) are **no longer authored**."

2. **`and-facets.md` Phase 0 Step 5 (lines 114-117):** The cross-chapter archive clearance explicitly includes `theater/proto-lines/<prior-slug>.md` in the `git mv` archive set, implying the file exists in a live run. Also, Phase 1 body text (lines 152-154) references "the base proto-lines file" that R1 authors copy to `_inflight/proto-lines-<facet>.md`.

3. **`CLAUDE.md` directory map (`theater/proto-lines/` entry):** "canonical merged proto-line file with `[<facet>:<id>]` citations from the single R1 authoring round (intermediate artifact written by `/and-facets` Phase 2 / `build_cite_index.py`; consumed by `/and-facets` Phase 4 auditor + `/and-stitch` Phase 0/1)." — A **present-tense active description**.

These three cannot all be right. If the bones schema is authoritative (proto-lines is dead), then:
- `and-facets.md` Phase 0 Step 5 archive clearance contains dead-code for a non-existent path
- `and-facets.md` Phase 1 "base proto-lines file" reference is orphaned (what file is it?)
- `CLAUDE.md` directory map entry is stale and actively misleading

If CLAUDE.md + and-facets are authoritative (proto-lines is live), then:
- `schemas/bones.schema.md`'s "no longer authored" declaration is wrong

**Resolution requires principal determination.** The fixer cannot resolve this without knowing which document reflects ground truth. A principal-dispatched session should inspect whether `theater/proto-lines/<slug>.md` files exist on disk for a chapter that has been through the full pipeline, and reconcile all three documents against that fact.

**Owning agent:** principal-dispatched schema-fix session (cannot be auto-fixed; requires determination of ground truth).

**Parking-lot item:** `pl-2026-06-11-pipeline-003`

---

## SIGNAL Finding

### STRUCT-004 — `and-facets.md` Phase 1: "base proto-lines file" terminology without path resolution (SIGNAL)

**File:** `.claude/commands/and-facets.md` lines 152–157  
**Class:** STRUCT (command body internal consistency; depends on STRUCT-003 resolution)

Phase 1's R1 authoring dispatch instructions repeatedly reference "the base proto-lines file" as the input each author copies to `_inflight/proto-lines-<facet>.md`. The phrase appears four times in that section. No path is given. If `theater/proto-lines/<slug>.md` is legacy (STRUCT-003 resolution → bones schema correct), the "base proto-lines file" must be `theater/bones/<book>-<chapter>.md` — but the command body doesn't say so, leaving any agent executing Phase 1 to guess. If proto-lines is live, the base file exists and this is only a description gap.

**Blocked on STRUCT-003 resolution.** Once the principal determines ground truth, the fixer should either:
- Replace "base proto-lines file" with "bones file (`theater/bones/<book>-<chapter>.md`)" if proto-lines is dead, or
- Add an explicit path reference to `theater/proto-lines/<slug>.md` if proto-lines is live.

**Parking-lot item:** (Deferred — depends on STRUCT-003. Not filed separately; STRUCT-003's parking-lot item covers this.)

---

## TASTE-FLAGs (historical documentation, not load-bearing errors)

**T-01 — `schemas/audit-report.schema.md` § R2 decision-shard references `design/shoot-v2/r2-judge-tuning/A-corpus.md`.** The file and directory exist. Path resolves. But the feature is retired (DEC-0116). The schema section (covered by STRUCT-001) is the real issue; the path reference itself is accurate for historical content.

**T-02 — `/and-season` references in command bodies.** Appears in `and-review.md` (lift-basis descriptions), `and-substance.md` (rerun-protocol reference), and `schemas/stitch-feedback.schema.md`. All framed as "pre-overhaul" or "replaced by" — historical documentation explaining what was superseded. No load-bearing usage.

**T-03 — `URI-026` / `tens-gate` references in command bodies and schemas.** `and-write.md` line 270, `schemas/bones.schema.md` line 150. Both framed as "replaces URI-026 tens-gate" — historical provenance documentation. Not load-bearing.

**T-04 — `design/shoot-v2/phase2-memory-flags-output.md` load-order includes "locked tensometer".** Outside the residue scan's primary scope (not a rubric file, schema, command body, or active-project file). Historical design artifact from the pre-overhaul tuning phase; not read by any current command or agent.

---

## Tri-walk summary

| Walk | Status |
|------|--------|
| Schema vs command bodies | 3 HARD findings (R2 zombie schema; stale proto-lines paths in stitch; proto-lines status contradiction) |
| Schema vs rubrics | CLEAN — `axes_held[]`, `axes_in_motion[]`, `dramatic_shape`, `rhythm-shape`, `peak-bones` all validate; `chapter_class` in schema per `showrunner-memory.schema.md` line 203 |
| Command bodies vs rubrics | CLEAN — all rubric paths in commands resolve to existing files; no stale rubric clause references |
| Deprecated-term residue | CLEAN in commands/schemas/rubrics (all `tensometer`/`tens:`/`URI-026` references are explicitly historical) |
| CLAUDE.md sync | 1 HARD (STRUCT-003 — proto-lines directory map status contradicts bones schema) |

---

## Next steps

1. **STRUCT-001** → fixer: add `RETIRED (DEC-0116, 2026-06-08)` header to `schemas/audit-report.schema.md` §R2 decision-shard; redirect consumer note.
2. **STRUCT-002** → fixer: replace `proto-lines/<slug>.md` with `theater/bones/<book>-<chapter>.md` throughout `and-stitch.md`.
3. **STRUCT-003** → principal session: determine whether `theater/proto-lines/<slug>.md` is live or dead; reconcile `schemas/bones.schema.md`, `and-facets.md` Phase 0/1, and `CLAUDE.md` directory map.
4. **STRUCT-004** → deferred; blocked on STRUCT-003.

Parking-lot items filed: `pl-2026-06-11-pipeline-001`, `pl-2026-06-11-pipeline-002`, `pl-2026-06-11-pipeline-003`.
