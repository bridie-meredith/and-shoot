# /and-review pipeline — 2026-05-21 22:15

**Subcommand:** pipeline (URI-REVIEW-PIPELINE, A10)
**Scope:** pipeline (schema ↔ command-body ↔ rubric tri-walk)
**Target:** entire pipeline meta-state
**Trigger:** re-run after commit 4f1e012, which addressed the 2 HARDs + 13 SIGNALs from pipeline-2026-05-21-2055.md
**Auditor:** staff/auditor (single fork, schema-audit mode)
**Prior audit:** `active-project/staff/reviews/pipeline-2026-05-21-2055.md` (2 HARDs + 13 SIGNALs)

---

## Verdict

**PROCEED.** Zero HARDs. `/and-substance chapter b01c02` dispatch is not blocked.

| Severity | Count | IDs |
|---|---|---|
| HARD | 0 | — |
| SIGNAL | 3 | STRUCT-021, RESIDUE-012, RESIDUE-013 |
| TASTE-FLAG | 3 | (RESIDUE-003/004/005 carry forward from prior audits; not re-listed here) |

---

## Verification status — round-2 findings

### HARD findings

| Finding | Status | Notes |
|---|---|---|
| STRUCT-012 (HARD) | RESOLVED | `/and-stitch.md` line 78: `$1` arg description now reads "Chapter slug (e.g. `b01-c01` or `b01c01`; normalized to `<book>-<chapter>` form at Phase 0). If omitted, use `active.chapter` from `active-project/staff/showrunner/memory.md`." Correct field name, correct example slug. |
| STRUCT-013 (HARD) | RESOLVED | `schemas/stitch-profile.schema.md` line 69: `boundary-source: scene-map-facet` with inline note `(only valid value post-URI-SUBSTANCE-OVERHAUL, 2026-05-17; tensometer-derive \| hybrid removed)`. Line 215: field rule rewritten to `scene-map-facet is the only valid value`; credits `/and-write` Phase 7 as emitter and `/and-facets` Phase 4d as validator-not-deriver. Both `tensometer-derive` and `hybrid` removed from enum and field rule. |

### SIGNAL findings

| Finding | Status | Notes |
|---|---|---|
| STRUCT-014 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 115: `boundary-source: <scene-map-facet \| per-anchor-fallback>`. The two old values (`tensometer-derive`, `hybrid`) are gone; current values correctly name the two live outcomes. |
| STRUCT-015 (SIGNAL) | RESOLVED | `/and-stitch.md` line 122: pre-flight comment now reads `# from scene-map facet (theater/facets/scene-map-<book>-<chapter>.md)`. Adjacent `anchors:` comment on line 121 now reads `# from bones file (theater/bones/<book>-<chapter>.md)`. Both updated. |
| STRUCT-016 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 34 (per-anchor Phase 1 header) and line 118 (scene-window Phase 1 header): both `input:` lines now read `theater/bones/<book>-<chapter>.md + facets/* + _cite-index.md + profile + persona`. Old `theater/proto-lines/<slug>.md` path gone from both. |
| STRUCT-017 (SIGNAL) | RESOLVED | `schemas/stitch-profile.schema.md` line 14: per-scene override sentence now reads "Scene labels resolve via `theater/facets/scene-map-<book>-<chapter>.md` (per-scene `label:` field; the scene-map is upstream-emitted by `/and-write` Phase 7)." Old `interest-narrator.md` sparsity-gradient reference removed. |
| STRUCT-018 (SIGNAL) | RESOLVED | `schemas/stitch-profile.schema.md` line 198: `pov` field rule now reads "defaults to the bones file's `narrator:` field (`theater/bones/<book>-<chapter>.md` header)." Old "proto-lines file" language removed. |
| STRUCT-019 (SIGNAL) | RESOLVED | `schemas/stitch-profile.schema.md`: line 205 (peak/nonpeak priority rule) now references `peak-bones[]` from scene-map rather than `tens=3` scalar; line 215 no longer mentions `tensometer-derive` as a boundary-source option (resolved with STRUCT-013); line 237 field name is now `flat-low-run-collapse` with inline provenance note `(Pre-overhaul name: tens1-run-collapse, referencing the now-removed tens=1 scalar; renamed under URI-SUBSTANCE-OVERHAUL 2026-05-17.)`. All three tensometer-scalar references corrected. |
| STRUCT-020 (SIGNAL) | RESOLVED | `schemas/stitch-profile.schema.md` line 215 field rule: "reads `theater/facets/scene-map-<book>-<chapter>.md` (emitted by `/and-write` Phase 7 directly from `chapters[].scenes[]` in showrunner memory; validated — not derived — at `/and-facets` Phase 4d)." Emitter attribution corrected; validate-not-derive distinction explicit. Resolved jointly with STRUCT-013. |
| RESIDUE-006 (SIGNAL) | RESOLVED | `/and-stitch.md` line 463 (failure modes table, `FAULT-BONE-AUDIT-MISS` row): recovery note now reads "surfaces upstream as an `/and-write` Phase 6 bone-gate / SVO rubric pass." Old `/and-protolines-v2` reference gone. |
| RESIDUE-007 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 164 (`FAULT-BONE-AUDIT-MISS` fault description): "surfaces upstream as an `/and-write` Phase 6 bone-gate / SVO rubric pass." Old `/and-protolines-v2` reference gone. |
| RESIDUE-008 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 59: `lenses-loaded:` example now reads `narrator:12, memory:6, sensory:3 (tag=drop), feel:8`. The `tens=3` entry removed. |
| RESIDUE-009 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 181: Phase 3 compression move-class now reads `COLLAPSE-FLAT-LOW-RUN \| Run of zero-cite bones inside a rhythm-shape: flat-low zone collapsed. (Formerly COLLAPSE-TENS1-RUN pre-overhaul; renamed under URI-SUBSTANCE-OVERHAUL 2026-05-17 — references the scene-map's rhythm-shape field instead of the removed tens=1 scalar.)` Renamed with provenance note. |
| RESIDUE-010 (SIGNAL) | RESOLVED | `schemas/stitch-render-log.schema.md` line 162: `FAULT-PHASE-1-NO-SCENE-MAP` description now reads "Scene-map facet absent or coverage-invalid; no usable boundary set from scene-map. Triggers fallback or escalation per `phase-1.scene-window.fallback-on-no-scene-map`. (Pre-overhaul this fault also covered tensometer-derive failure; tensometer-derive was removed under URI-SUBSTANCE-OVERHAUL 2026-05-17.)" Tensometer-derive removed as trigger; provenance note added. |
| RESIDUE-011 (SIGNAL) | RESOLVED | `schemas/stitch-profile.schema.md`: `tens: false` field removed from `render.facet-types` block (line 57 in prior schema no longer exists). The field rules note (line 203) now reads "The `tens:` field was removed under URI-SUBSTANCE-OVERHAUL (2026-05-17); the tensometer facet no longer exists in the pipeline." `FAULT-PROFILE-FORBIDDEN-RENDER` description (line 333) updated similarly. Field is gone; provenance documented. |

---

## New findings — full tri-walk pass 3

### Stage 1 — Schema vs command bodies

All schema-vs-command-body pairs that produced findings in rounds 1 and 2 now read clean. Scanning for newly introduced drift:

**`schemas/stitch-render-log.schema.md` § Audit handle (line 332):** The audit handle section lists "Every bone in `proto-lines/<slug>.md`" as the auditor's check point 1. This is a pre-substance path reference (`proto-lines/<slug>.md`) that should read `bones/<book>-<chapter>.md`. The check in scene-window mode is correctly re-stated inline ("`bone-walk:` block") but the initial path reference remains stale.

---

### STRUCT-021 — render-log schema audit handle references `proto-lines/<slug>.md`

**file:** `schemas/stitch-render-log.schema.md` line 332

**what:**
```
1. Every bone in `proto-lines/<slug>.md` has a corresponding `render-bone`, `MERGE-*`, `CUT-BONE`, or `DROP-*` entry (per-anchor mode), or appears in exactly one scene-fork's `bone-walk:` block with a non-empty disposition (scene-window mode).
```

**why SIGNAL:** The auditor's primary input check specifies `proto-lines/<slug>.md` as the bones source. Under URI-SUBSTANCE-OVERHAUL (2026-05-17), bones live at `theater/bones/<book>-<chapter>.md`; `theater/proto-lines/<slug>.md` is no longer authored. An auditor reading this schema as authoritative for stitch-review would attempt to load a file that does not exist under the current chain. Not HARD because this is in the audit-handle documentation section (advisory to the auditor, not a runtime dispatch path), but the path mismatch creates a misleading reference.

**criteria:** Update `proto-lines/<slug>.md` to `theater/bones/<book>-<chapter>.md` in audit handle check 1.

---

### Stage 2 — Schema vs rubrics

No new schema-vs-rubric drift found.

`rubric-sensory.md` calibration anchors correctly use `scene-map` vocabulary (`rhythm-shape` and `peak-bones` fields). No tensometer references in sensory rubric.

`rubric-narrator-interest.md` is at V1 draft status and contains no tensometer references. The rubric's `Curve-shape rubric` section (not read here fully but the header confirms it exists) does not reference `tens:` scalars in the portions examined.

---

### Stage 3 — Command bodies vs rubrics

No new command-body-vs-rubric drift found.

`/and-facets.md` Notes line 703: shared-reviewer-resources note correctly references `/and-write` Phase 6 bone-gate. Phase 5b cycle-cap origin comment (line 525) correctly annotated as pre-overhaul `/and-season` convention carried forward — this is a TASTE-FLAG from prior passes, not a new finding.

`/and-stitch.md` Phase 5 local-flow cycle-cap convention comment (line 524): correctly notes the convention is `pre-overhaul /and-season audience-loop convention; carried forward` — same TASTE-FLAG class as above, already logged.

---

### Stage 4 — Residue scan

Scanning `.claude/commands/*.md`, `schemas/`, `design/shoot-v2/rubric-*.md` for: `tensometer`, `tens:` (as load-bearing prefix), `URI-026`, `tens-gate`, `/and-season`, `/and-judge-book`, `/and-shoot`, `/and-protolines`, `direction: +|-`, `direction: null`.

**`.claude/commands/and-stitch.md`:**
- Line 78: CLEAN (`active.chapter` — STRUCT-012 resolved).
- Line 122: CLEAN (`# from scene-map facet` — STRUCT-015 resolved).
- Line 134: Contains "tensometer fallback removed under URI-SUBSTANCE-OVERHAUL 2026-05-17" — this is a legitimate historical provenance note in a user-visible pre-flight comment. TASTE-FLAG class (intentional historical reference), not a fault.
- Line 463: CLEAN (`/and-write` — RESIDUE-006 resolved).
- Line 496: CLEAN (`COLLAPSE-FLAT-LOW-RUN` — RESIDUE-009 resolved in command body).
- Line 624: CLEAN (`/and-write` — STRUCT-008/RESIDUE-001 resolved in prior run).

**`schemas/stitch-profile.schema.md`:**
- Line 14: CLEAN (STRUCT-017 resolved).
- Line 69: CLEAN (STRUCT-013 resolved).
- Line 100: CLEAN (`flat-low-run-collapse` — STRUCT-019 resolved).
- Line 198: CLEAN (STRUCT-018 resolved).
- Line 203: CLEAN (RESIDUE-011 resolved; field removed, provenance documented).
- Line 205: CLEAN (STRUCT-019 resolved; `peak-bones[]` reference).
- Line 215: CLEAN (STRUCT-013/STRUCT-020 resolved).
- Line 237: CLEAN (STRUCT-019 resolved; field renamed).
- Line 333: CLEAN (FAULT-PROFILE-FORBIDDEN-RENDER updated; `tens:` removed).

**`schemas/stitch-render-log.schema.md`:**
- Line 34: CLEAN (STRUCT-016 resolved; `theater/bones/<book>-<chapter>.md`).
- Line 59: CLEAN (RESIDUE-008 resolved; `tens=3` removed from example).
- Line 115: CLEAN (STRUCT-014 resolved; `<scene-map-facet | per-anchor-fallback>`).
- Line 118: CLEAN (STRUCT-016 resolved; `theater/bones/<book>-<chapter>.md`).
- Line 162: CLEAN (RESIDUE-010 resolved; tensometer-derive removed as trigger).
- Line 164: CLEAN (RESIDUE-007 resolved; `/and-write`).
- Line 181: CLEAN (RESIDUE-009 resolved; `COLLAPSE-FLAT-LOW-RUN`).
- **Line 332:** `proto-lines/<slug>.md` — SIGNAL (STRUCT-021, new finding above).

**`schemas/stitch-feedback.schema.md`:**
- Line 182: CLEAN. "authored by `/and-write` (the substance-overhaul replacement for the pre-overhaul `/and-protolines` + `/and-season` bone-gate chain)" — this is a legitimate provenance note naming what was replaced. Not a load-bearing residue reference. TASTE-FLAG class.

**`schemas/audit-report.schema.md`:**
- Line 108 (R2 decision-shard reference): CLEAN. "pre-overhaul `/and-season` is replaced by `/and-facets` Phase 6 + `/and-review verdict`" — correct attribution, no live residue.
- Scope enum: CLEAN (`bone | scene | chapter | book | series | pipeline`).

**`schemas/bones.schema.md`:**
- CLEAN. No tensometer, no `/and-protolines`, no `/and-season` in load-bearing context.

**`schemas/showrunner-memory.schema.md`:**
- CLEAN. `direction: up | down` throughout (no `+|-|null`). `active.chapter` is the field name (STRUCT-012 resolved).

**`schemas/scene-map.schema.md`:**
- CLEAN throughout. The `pressure-signal translation reference` table on lines 121-127 is an intentional historical mapping table (maps pre-overhaul `tens=*` vocabulary to current scene-map equivalents). This is TASTE-FLAG class (historical reference serving as translation guide), not a fault.

**`.claude/commands/and-facets.md`:**
- Line 8-11 (URI-SUBSTANCE-OVERHAUL mutations block): CLEAN. Pre-overhaul paths named only in the context of "replaced by" statements.
- Line 703: CLEAN (STRUCT-006/RESIDUE-002 resolved; `/and-write` Phase 6 bone-gate).

**`.claude/commands/and-review.md`:**
- Lines 72, 100, 115, 167: CLEAN. All four `/and-season` lift-basis citations carry "pre-overhaul `/and-season`; adapted to ..." qualifiers (STRUCT-007 resolved).

**`.claude/commands/and-cut.md`:**
- CLEAN. Reads `active.book`, `active.chapter`, `active.cascade_in_progress` — all valid schema fields (STRUCT-001 resolved).

**`.claude/commands/and-write.md`:**
- Line 3 description: CLEAN. "Replaces `/and-protolines` as the shoot-v2 chain entry" — legitimate replacement attribution.
- No tensometer, no `tens:`, no `direction: null` in load-bearing contexts.

**`.claude/commands/and-substance.md`:**
- CLEAN on the deprecated-term set.

**`design/shoot-v2/rubric-sensory.md`:**
- Line 16: "the scene-map facet file ... `rhythm-shape` and `peak-bones` fields provide the same structural orientation that tensometer scalars previously provided" — TASTE-FLAG class (intentional historical orientation note).
- No load-bearing tensometer references.

**`design/shoot-v2/rubric-narrator-interest.md`:**
- No tensometer references in the portion examined.

---

### RESIDUE-012 — stitch-render-log Phase 7 worked example uses pre-substance episode slug

**file:** `schemas/stitch-render-log.schema.md` line 285

**what:** Worked example Phase 7 header:
```
output: draft/s01e01.phase-7.draft.md
```
The Phase 7 output path in the worked example uses `s01e01` (pre-substance `season-episode` vocabulary) rather than the current `b01-c01` chapter-slug form.

**why SIGNAL:** Under URI-SUBSTANCE-OVERHAUL (2026-05-17), phase output paths use `<book>-<chapter>` slug form (e.g. `b01-c01`). The worked example is a schema illustration; an author reading it to understand the file naming convention would see `s01e01` and may apply the old convention to a new project. Not HARD because worked examples are not operational dispatch paths. However, this is the same class as the round-2 RESIDUE-008 fix (render-log example `tens=3` removed); the example should use current-vocabulary slugs for consistency with that fix.

**criteria:** Update `draft/s01e01.phase-7.draft.md` to `draft/b01-c01.phase-7.draft.md` (or a similarly current-vocabulary slug) in the Phase 7 worked example.

---

### RESIDUE-013 — stitch-render-log schema file-path comment uses `<episode-slug>` vocabulary

**file:** `schemas/stitch-render-log.schema.md` line 13

**what:**
```
`active-project/staff/stitcher/render-log-<episode-slug>.md`
```

**why SIGNAL:** The schema file-path section names the file using `<episode-slug>` vocabulary. Under the substance overhaul, the chapter slug is the unit (`<book>-<chapter>` form, e.g. `b01-c01`). The command body at `/and-stitch.md` line 8 uses "Slug-arg shape: `<book>-<chapter>`" consistently; the schema file-path should match. Not HARD — the variable is descriptive, not machine-read — but it is pre-substance vocabulary in a schema authority file.

**criteria:** Update to `render-log-<chapter-slug>.md` with a parenthetical example `(e.g. render-log-b01-c01.md)`, matching the command-body slug form.

---

### Stage 5 — CLAUDE.md sync

**Schema authority table:** PASS. Rows are:
- `Stitcher profile | schemas/stitch-profile.schema.md` — present; content verified clean (this pass).
- `Stitcher feedback | schemas/stitch-feedback.schema.md` — present; content verified clean (this pass).
- `Stitcher render-log | schemas/stitch-render-log.schema.md` — present; STRUCT-021, RESIDUE-012, RESIDUE-013 are SIGNALs in the schema content, not in the authority table listing.

**Agent routing table:** PASS.

**Directory map:** PASS. `theater/` subdirs (`bones/`, `facets/`, `proto-lines/`, `dialogue/`) correctly mapped.

**Commands table:** PASS. All command rows present and accurate.

**Rules section:** PASS. Rule 11 correctly references `/and-write` Phase 6 bone-gate and `/and-facets` Phase 5b.

**Primary pattern block:** PASS. `/and-facets` row annotation "(tensometer dropped)" is accurate. `/and-stitch` output slug `draft/b01-c01.md` is correct form.

---

## Tri-walk coverage summary

**Stage 1 — Schema vs command bodies:**
- `showrunner-memory.schema.md` vs `and-stitch.md`: PASS (STRUCT-012 resolved; `active.chapter` correct).
- `stitch-profile.schema.md` vs `and-stitch.md`: PASS (STRUCT-013/019/020 resolved; tensometer enum, peak-bones attribution, field name all corrected).
- `stitch-render-log.schema.md` vs `and-stitch.md`: PARTIAL — STRUCT-021 (SIGNAL: `proto-lines/<slug>.md` in audit handle).
- `bones.schema.md` vs `and-write.md` / `and-facets.md` / `and-stitch.md`: PASS.
- `audit-report.schema.md` vs `and-review.md` / `and-facets.md`: PASS.
- `scene-map.schema.md` vs `and-write.md` / `and-facets.md` / `and-stitch.md`: PASS.

**Stage 2 — Schema vs rubrics:**
- All schema fields referenced in rubric-sensory.md, rubric-narrator-interest.md checked: PASS.
- No new schema-vs-rubric drift.

**Stage 3 — Command bodies vs rubrics:**
- PASS. No new command-body-vs-rubric drift.

**Stage 4 — Residue scan:**
- `.claude/commands/and-stitch.md`: CLEAN on deprecated-term set.
- `schemas/stitch-profile.schema.md`: CLEAN.
- `schemas/stitch-render-log.schema.md`: STRUCT-021, RESIDUE-012, RESIDUE-013 (SIGNALs; no new HARDs).
- `schemas/stitch-feedback.schema.md`: CLEAN (provenance note is TASTE-FLAG).
- All other command bodies + schemas: CLEAN.
- Rubrics: CLEAN on load-bearing deprecated terms (historical orientation notes are TASTE-FLAG).

**Stage 5 — CLAUDE.md sync:** PASS.

---

## Findings summary

```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-05-21
  findings:
    - id: STRUCT-021
      type: flag
      what: schemas/stitch-render-log.schema.md line 332 — audit handle check 1 references proto-lines/<slug>.md
      why: Pre-substance path in a schema authority file; an auditor reading this would look for a file the current chain does not write. Not a runtime dispatch path, so not HARD.
      criteria: Update to theater/bones/<book>-<chapter>.md

    - id: RESIDUE-012
      type: flag
      what: schemas/stitch-render-log.schema.md line 285 — Phase 7 worked example uses s01e01 slug form in output path
      why: Pre-substance episode-slug vocabulary in a schema worked example; inconsistent with the round-2 RESIDUE-008 fix (same file, same section).
      criteria: Update draft/s01e01.phase-7.draft.md to draft/b01-c01.phase-7.draft.md in the worked example

    - id: RESIDUE-013
      type: flag
      what: schemas/stitch-render-log.schema.md line 13 — file-path convention uses <episode-slug> variable name
      why: Pre-substance vocabulary in a schema file-path declaration; inconsistent with command body's <book>-<chapter> slug form.
      criteria: Update <episode-slug> to <chapter-slug> with example e.g. render-log-b01-c01.md
```

---

## Bottom-line verdict

**PROCEED.** Zero HARDs. All STRUCT-012 through STRUCT-020 and RESIDUE-006 through RESIDUE-011 verified RESOLVED. Three new SIGNALs found (STRUCT-021, RESIDUE-012, RESIDUE-013), all in `schemas/stitch-render-log.schema.md`. None are blocking. All three are in documentation/example sections, not in runtime dispatch paths or operational field enumerations.

The gate condition for `/and-substance chapter b01c02` dispatch is met.
