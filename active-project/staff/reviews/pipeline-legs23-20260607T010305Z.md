```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-07
  triggered_by: >
    Completion pass for legs 2 and 3 of the tri-walk initiated in
    pipeline-20260607T004417Z.md. That prior run recorded STRUCT-012 (pass,
    schema-vs-rubric leg not executed) and STRUCT-013 (flag, command-body-
    vs-rubrics leg partially noted). This report executes both legs fully.
    Legs 1, 4, and 5 are NOT re-examined here.

  findings:

    # ── LEG 2: SCHEMA vs RUBRICS ─────────────────────────────────────────────

    - id: STRUCT-017
      type: fault
      severity: HARD
      what: >
        rubric-metaphor.md §"Multi-justification" (line ~89): "Multi-justification:
        ≥2 layers from `{memory, feeling, tens}`." §"Support layers" (line ~85):
        lists `tens:<reading>` as a valid support layer alongside sensory:<id>
        and ni:<id>. §"Tens-curve discipline" section (lines ~92-98): defines
        tens=1/2/3 as the governing scale for metaphor fire eligibility.
        The active facet.schema.md § metaphor flags (line 126) reads:
        "Pre-overhaul `tens:<reading>` support removed under URI-SUBSTANCE-
        OVERHAUL; pressure-signal substitute is scene-map `peak-bones` membership."
        The schema's live form replaces tens with `peak-bone:<flat-id>` as a
        named support type. The rubric's multi-justification rule still requires
        "≥2 layers from `{memory, feeling, tens}`" — an enumeration the schema
        has retired.
        Citing: rubric-metaphor.md lines ~85-98; facet.schema.md line 126.
      why: >
        HARD: an R1 metaphor author following the rubric's multi-justification
        requirement would (a) cite `tens:<reading>` as a support source in
        licensed-by, and (b) apply the tens=1/2/3 scale when determining AP7
        eligibility — both of which contradict the live schema. A Phase 5
        mechanic auditor reading the schema would flag every tens-sourced
        metaphor entry as a schema violation, while the rubric would accept it.
        The multi-justification rule is the gate; its enumeration is wrong.
      criteria: >
        rubric-metaphor.md §"Multi-justification" must replace `tens` with the
        schema's substitute: `peak-bone` (referencing scene-map `peak-bones`
        membership). The corrected form is: "≥2 layers from `{memory, feeling,
        peak-bone}`." §"Support layers" must replace `tens:<reading>` with
        `peak-bone:<flat-id>`. §"Tens-curve discipline" must be renamed and
        re-specified in terms of scene-map `peak-bones` membership and
        `rhythm-shape` zones, matching the pressure-signal translation
        reference in scene-map.schema.md (flat-low/resolving = quiet;
        rising = pressure; peak-bones = peak). AP7 must be restated in terms
        of `peak-bones` membership (default-refuse if bone is in `peak-bones`
        array), not tens=3.

    - id: STRUCT-018
      type: fault
      severity: HARD
      what: >
        rubric-vibes.md §"Cross-facet contract — Read side" (lines ~110-118):
        lists `tens:<reading>` as a valid licensed-by source type: "tens:<reading>
        (peak-tension as supporting licensing)". The active facet.schema.md
        § vibes updates (line 163) reads: "Pre-overhaul `tens:<reading>` source
        removed under URI-SUBSTANCE-OVERHAUL; pressure-signal substitute is
        scene-map `peak-bones` membership." The schema's live form lists
        `peak-bone:<flat-id>` as the substitute source type; `tens:<reading>`
        is not in the live source enumeration.
        rubric-vibes-v1.1-patch.md does not address this field.
        Citing: rubric-vibes.md lines ~110-118 (Cross-facet contract);
        facet.schema.md line 163.
      why: >
        HARD: a vibes-updates author following the rubric would write
        `licensed-by: tens:3` or similar, which the Phase 5 mechanic auditor
        reading the schema would flag as a schema violation (unrecognized source
        type). The schematic enumeration of valid source types is the gate; the
        rubric's enumeration is stale. Any vibe entry using `tens:` as its sole
        support source is malformed under the live schema.
      criteria: >
        rubric-vibes.md §"Cross-facet contract — Read side" must replace
        `tens:<reading>` with `peak-bone:<flat-id>` as the pressure-signal
        source type, matching the facet.schema.md § vibes updates live source
        enumeration. The description "(peak-tension as supporting licensing)"
        should be rephrased to "(scene-map peak-bones membership as supporting
        licensing)."

    - id: STRUCT-019
      type: fault
      severity: HARD
      what: >
        rubric-metaphor.md §"Schema content-shape revision (proposed; ship at
        Phase 5 if rubric holds)" (lines ~24-42): the section header and
        internal framing describe the `licensed-by:` field shape and the
        `peak-bone:` / `tens:` support enumeration as "proposed" and
        "pending Phase 5 ship." In fact, facet.schema.md § metaphor flags
        (lines 122-132) has already shipped the live form with `peak-bone:
        <flat-id>` as the support type and `tens:<reading>` explicitly retired.
        The rubric's "proposed" framing misrepresents the schema status as
        future-pending when it is current.
        The rubric §"Notes on schema-revision-at-ship" (lines ~246-249)
        confirms the expected ship behavior but the header "proposed; ship at
        Phase 5 if rubric holds" is now materially false.
        Citing: rubric-metaphor.md lines ~24-42, ~246-249;
        facet.schema.md lines 122-132 (metaphor flags section, live form).
      why: >
        HARD: a rubric reader seeing "proposed" schema content-shape will
        not treat the `licensed-by:` field as mandatory or the `peak-bone:`
        support type as authoritative. They may omit the field or use the
        pre-ship free-text form, which the schema now requires to be the
        `licensed-by:` form. The section's framing actively misleads authors
        about what the schema requires today. The proposed form in the rubric
        also still includes `tens:<reading>` in the support enumeration — the
        schema shipped a different form (without tens). The "proposed" framing
        both misrepresents timing and preserves a now-wrong support type.
      criteria: >
        rubric-metaphor.md §"Schema content-shape revision" must be updated
        to remove the "proposed" framing and reflect that this is the CURRENT
        schema (shipped). The support enumeration in the rubric's documented
        form must match the live schema form: `peak-bone:<flat-id>` replaces
        `tens:<reading>`. The section title should read "Schema content-shape
        (SHIPPED 2026-05-07)" or similar to accurately date the promotion.

    - id: STRUCT-020
      type: flag
      severity: SIGNAL
      what: >
        rubric-feeling.md §"Curve-shape rubric (tens-independent)" (lines ~111-116):
        references the tensometer explicitly: "Tens-independence rule: feeling-flags
        do NOT gate on tensometer ≥ 2 (sensory precedent). Correlation-only
        observation. The expected file-shape distributes fires across t=1, t=2,
        t=3 per the natural distribution..." The per-bone scalar t=1/t=2/t=3 is
        a tensometer concept; the pressure-signal substitute is scene-map
        `rhythm-shape` zones and `peak-bones` membership.
        rubric-feeling.md §"Cross-facet contract / Tensometer" (last cross-facet
        bullet): "Tensometer (observation-only, like sensory) — Independent.
        Tens-correlation observed at audit but never gated." This section
        continues to list tensometer as an active cross-facet consideration.
        rubric-feeling.md §"Sparsity / coverage targets" table (lines ~213-223):
        row "Tens distribution | distribute across t=1, t=2, t=3 | soft" —
        references tensometer scalar.
        Citing: rubric-feeling.md §"Curve-shape rubric (tens-independent)";
        §"Cross-facet contract / Tensometer"; §"Sparsity / coverage targets" table.
      why: >
        SIGNAL: the tensometer references in rubric-feeling.md are
        characterized as "observation-only" and "soft" — no gate depends on
        them mechanically. An author reading the rubric is told to distribute
        across t=1/t=2/t=3; the correct instruction is to distribute across
        scene-map zones (`flat-low`/`resolving` = quiet; `rising` = pressure;
        `peak-bones` = peak). The rubric's intent is correct (do not
        concentrate fires at peaks) but the vocabulary is stale. Non-blocking
        because the gate is explicitly "never gated" and "soft."
      # no criteria: SIGNAL only

    - id: STRUCT-021
      type: pass
      severity: SIGNAL
      what: >
        Schema field verification — axes_held[], axes_in_motion[], dramatic_shape
        enum, rhythm-shape, peak-bones, chapter_class:
        - axes_held[]: present in showrunner-memory.schema.md at book, chapter,
          scene, and bone levels. All rubrics referencing it (rubric-memory-flags.md
          V3 carve-out; rubric-state-updates.md cross-facet contract) use the
          field name correctly.
        - axes_in_motion[]: present in showrunner-memory.schema.md at book,
          chapter, and scene levels. No rubric uses this as a field reference
          (rubrics read `rhythm-shape` and `peak-bones` from the scene-map file
          for pressure-signal; they do not read axes_in_motion[] directly). PASS.
        - dramatic_shape enum: showrunner-memory.schema.md line 207 lists
          `rising | climax | falling | hinge`. rubric-memory-flags.md §V3
          carve-out conditions cite `dramatic_shape: hinge` — matches schema.
          PASS.
        - rhythm-shape: scene-map.schema.md enumerates all eight values
          (flat-low, flat-mid, rising, rising-to-peak, peak-and-release,
          double-peak, resolving, release-only). All rubrics reference a
          subset of these values correctly. PASS.
        - peak-bones: scene-map.schema.md defines this field. All rubrics
          use "peak-bones" as the field name. PASS.
        - chapter_class: showrunner-memory.schema.md line 203 defines
          `chapter_class: standard | frame-coda`. No rubric references this
          field directly (it is a substance-chain field, not a facet-authoring
          field). PASS on schema-vs-rubric for this field.
      why: >
        All six schema-field targets from the audit brief verified present
        and correctly referenced. No stale enum values, no missing fields.

    - id: STRUCT-022
      type: flag
      severity: SIGNAL
      what: >
        rubric-feeling.md §"Locked notation" (last paragraph, lines ~236-239):
        "Schema edit (caveat-pre-ship): `schemas/facet.schema.md` § feeling flags
        content shape will be revised to drop `feels: <feeling>` field; ships with
        Phase 5 facet file commit (sensory precedent)." The facet.schema.md
        § feeling flags (lines 98-114) no longer carries a `feels: <feeling>`
        field — the current schema shape is:
        `<id> @<proto-line-id> <character-slug>: <somatic-tell-one-clause> |
        expressed: <yes|partial|no>` with no `feels:` field. The schema edit
        described as "caveat-pre-ship" has been completed, but the rubric's
        "caveat-pre-ship" notice was never cleared.
        Citing: rubric-feeling.md §"Locked notation" lines ~236-239;
        facet.schema.md lines 98-114.
      why: >
        SIGNAL: a rubric reader sees "caveat-pre-ship" and may incorrectly
        infer the schema change is still pending. The practical consequence is
        low (the rubric's content shape definition already forbids `feels:` in
        the §"Form discipline" section) but the stale notice is a documentation
        lag. Same pattern as STRUCT-019 for metaphor.

    - id: STRUCT-023
      type: pass
      severity: SIGNAL
      what: >
        rubric-sensory.md §"Schema rename (COMPLETE)" (lines ~335-344):
        all four schema rename items are stamped as DONE and explicitly
        references a prior pipeline-adaptation-audit-2026-05-21 STRUCT-007.
        The facet.schema.md § sensory flags (lines 80-94) shows the renamed
        form (`<modality>: <old-state> -> <new-state>`). PASS — the rename
        is complete, the rubric correctly records completion, no stale
        pending notices remain in this rubric.
      why: >
        No action needed. The rubric's completion record is consistent with
        the schema's current state.

    - id: STRUCT-024
      type: flag
      severity: SIGNAL
      what: >
        rubric-memory-flags.md §"Curve-shape rubric" § "Scene-level shape"
        (line ~226): "Sparsity. 5-12% of bones fire." But §"Licensing-discipline
        axis" (line ~166) states "The file fires on 1-5% of bones for chapter-
        length corpora." There is an internal inconsistency within the rubric
        itself — the episode-level shape section says 5-12% and the per-entry
        axis says 1-5%. Both appear in the same document. (The 5-12% is in
        the "Episode-level shape" subsection of §"Curve-shape rubric"; the
        1-5% is in the main §"Licensing-discipline axis" ACCEPT signatures.)
        No schema field is involved; this is an intra-rubric inconsistency
        surfaced by the schema-vs-rubric pass.
        Citing: rubric-memory-flags.md §"Curve-shape rubric / Episode-level
        shape" (Sparsity bullet); §"Licensing-discipline axis" ACCEPT signatures
        (Sparsity discipline bullet).
      why: >
        SIGNAL: the two bands are contradictory (1-5% and 5-12% overlap only
        at exactly 5%). An author or mechanic auditor reading both sections
        will encounter conflicting sparsity targets. No schema field is broken,
        but the intra-rubric conflict is a legitimate finding. Not a schema
        drift — a rubric self-consistency defect.

    # ── LEG 3: COMMAND BODIES vs RUBRICS ──────────────────────────────────────

    - id: STRUCT-025
      type: fault
      severity: HARD
      what: >
        and-facets.md Phase 1 item 10 (exposition author, lines ~219-220):
        "rubric-exposition.md" is named as the authority rubric for the
        exposition facet author dispatch. `design/shoot-v2/rubric-exposition.md`
        does NOT exist on disk. The file is absent. Every other rubric named
        in the and-facets.md R1 fanout exists under design/shoot-v2/; the
        exposition rubric alone is missing.
        The and-review.md § facets subcommand (line 164) references
        "design/shoot-v2/rubric-*.md" as the rubric glob for per-facet
        rubric runners — the glob would not match a non-existent file, meaning
        the exposition facet has no rubric runner in /and-review facets either.
        Citing: and-facets.md Phase 1 item 10; and-review.md line 164;
        filesystem: design/shoot-v2/rubric-exposition.md does not exist.
      why: >
        HARD: the exposition R1 author dispatch at and-facets.md Phase 1
        names a rubric that does not exist on disk. An R1 exposition author
        following the dispatch would fail to load the named rubric, falling
        back to no rubric or an arbitrary substitute. This makes exposition
        authoring unanchored — exactly the discipline failure the rubric
        system is designed to prevent. The Phase 5 mechanical auditor's
        RUBRIC-FIDELITY class cannot verify exposition entries against a
        non-existent rubric. /and-review facets has no exposition rubric
        runner because the file is absent from the glob.
      criteria: >
        Either (a) author rubric-exposition.md at design/shoot-v2/ covering
        the exposition facet's form, per-anchor cap, sparsity, scope-kinds,
        renders-as directives, scene-open-orient conditional fire-rule, and
        anti-exposition-penalty / new-plot-content / over-explain gates —
        authoring discipline defined in facet.schema.md § exposition
        (lines 175-232) serves as the authoritative starting point; OR
        (b) if the exposition author is intended to operate without a
        standalone rubric (relying solely on facet.schema.md), update
        and-facets.md Phase 1 item 10 to remove the "rubric-exposition.md"
        reference and replace it with "schema § exposition (facet.schema.md
        lines 175-232)" as the authority. Option (a) is strongly preferred
        because the schema's exposition section does not carry the anti-pattern
        / review-gate content that the other facet rubrics carry.

    - id: STRUCT-026
      type: fault
      severity: HARD
      what: >
        and-facets.md Phase 1 item 8 (metaphor author) dispatches with
        "rubric-metaphor.md" and references "AP7 default-refuse on bones
        outside hinge-magnitude band." AP7 in rubric-metaphor.md is defined
        as "Peak-zone fire. Metaphor at tens=3 peak default-refuse." The
        command body maps this to "hinge-magnitude band" (a substance-delta
        concept) but the rubric's AP7 still uses tens=3 as its trigger
        condition. An R1 metaphor author dispatched with rubric-metaphor.md
        reads AP7 as "refuse if tens=3" — a tensometer-based gate — while
        the command body intends "refuse if bone is in the scene's peak-bones
        array." These are different gates: the tensometer scalar and peak-bones
        membership overlap but are not identical (peak-bones is a 75th-percentile
        or ≥0.15 magnitude threshold, not a discrete scalar level).
        STRUCT-017 above identifies the root schema mismatch. This is the
        command-body-vs-rubric expression of the same fault: the command body
        attempts to apply the substance-delta translation to AP7 but the rubric
        it dispatches still defines AP7 in tensometer terms.
        Citing: and-facets.md Phase 1 item 8; rubric-metaphor.md §"AP7 —
        Peak-zone fire."
      why: >
        HARD: a metaphor author dispatched with the un-updated rubric-metaphor.md
        applies AP7 using tens=3 as the trigger, which cannot be evaluated
        without a tensometer file (dropped under URI-SUBSTANCE-OVERHAUL). The
        author has no tensometer to read; in the absence of the scalar, AP7 is
        either ignored or applied incorrectly. The substance-delta substitute
        (peak-bones membership) is defined in the scene-map schema but not
        in the rubric's AP7 clause. This is the same root as STRUCT-017.
        Fixer criteria for STRUCT-017 (update rubric-metaphor.md AP7 to use
        peak-bones) also closes this finding.
      criteria: >
        See STRUCT-017 criteria. After rubric-metaphor.md AP7 is updated to
        use peak-bones membership as the trigger, the command body's "hinge-
        magnitude band" phrasing should also be updated to "peak-bones
        membership (scene-map `peak-bones` array)" for consistent vocabulary.
        Both the rubric and the command body must name the same gate condition.

    - id: STRUCT-027
      type: flag
      severity: SIGNAL
      what: >
        and-facets.md Phase 1 items 2 (narrator-interest), 6 (memory),
        7 (feeling), and 8 (metaphor): each dispatch includes "per-chapter
        `substance_delta` from showrunner memory" as the "pressure-signal
        substitute" for the tensometer. However, rubric-narrator-interest.md,
        rubric-memory-flags.md, rubric-feeling.md, and rubric-metaphor.md each
        define their pressure-signal reads differently:
        - rubric-narrator-interest.md: correctly reads `scene-map facet file
          (`theater/facets/scene-map-<book>-<chapter>.md`) for `rhythm-shape` +
          `peak-bones`."
        - rubric-memory-flags.md: correctly reads "the scene-map's `rhythm-shape`
          and `peak-bones` fields."
        - rubric-feeling.md: uses tensometer language (t=1/t=2/t=3 — STRUCT-020).
        - rubric-metaphor.md: uses tensometer language (tens=1/2/3 — STRUCT-017).
        The command body's phrase "per-chapter `substance_delta` from showrunner
        memory (pressure-signal substitute)" is a partial misdirection: the
        actual pressure-signal surface for rubric-reading agents is the scene-map
        file (`theater/facets/scene-map-<book>-<chapter>.md`), not the raw
        `substance_delta` block in showrunner memory. The scene-map is derived
        from substance_delta but rubrics read the scene-map, not the raw memory
        block directly. For the two rubrics that still use tensometer language
        (STRUCT-017 + STRUCT-020), this dispatch note cannot bridge the gap.
        Citing: and-facets.md Phase 1 items 2, 6, 7, 8; scene-map.schema.md
        §"Pressure-signal translation reference."
      why: >
        SIGNAL: the command body should route agents to the scene-map file
        as the pressure-signal surface, not to showrunner memory's raw
        substance_delta block. The two are not equivalent for rubric reads:
        the scene-map has pre-computed `rhythm-shape` and `peak-bones` fields
        that the rubrics expect to load directly. For rubrics that still
        reference tensometer scalars (STRUCT-017, STRUCT-020), dispatching
        with raw substance_delta does not provide the tens=N equivalent even
        partially. The STRUCT-017 and STRUCT-026 rubric fixes are the primary
        remediation; updating dispatch language is secondary.

    - id: STRUCT-028
      type: pass
      severity: SIGNAL
      what: >
        Rubric path existence check (leg 3 prerequisite):
        - design/shoot-v2/rubric-feeling.md: EXISTS
        - design/shoot-v2/rubric-location-state.md: EXISTS
        - design/shoot-v2/rubric-memory-flags.md: EXISTS
        - design/shoot-v2/rubric-metaphor.md: EXISTS
        - design/shoot-v2/rubric-narrator-interest.md: EXISTS
        - design/shoot-v2/rubric-sensory.md: EXISTS
        - design/shoot-v2/rubric-state-updates.md: EXISTS
        - design/shoot-v2/rubric-vibes.md: EXISTS
        - design/shoot-v2/rubric-vibes-v1.1-patch.md: EXISTS
        - design/shoot-v2/rubric-exposition.md: MISSING (see STRUCT-025)
        - design/shoot-v2/and-season-tuning-r1/rubric-and-season.md: this is
          the noted legacy artifact; treated as TASTE-FLAG per dispatch
          instructions. Not a command-body reference; no active dispatch reads
          it. TASTE-FLAG (historical).
        All nine current-chain rubric files exist. The exposition rubric is
        the sole missing file (STRUCT-025).
      why: >
        Reference check only. Nine of ten rubrics exist; exposition is
        the gap. No additional path-existence faults beyond STRUCT-025.

    - id: STRUCT-029
      type: flag
      severity: SIGNAL
      what: >
        and-facets.md Phase 1 item 8 (metaphor, lines ~215-216): "R1 metaphor
        entries may carry **provisional** `licensed-by:` notes naming the
        intended anchor by description; resolution to machine-readable
        `<prefix>:<id>` form happens in R2." rubric-metaphor.md defines the
        `licensed-by:` field as requiring exactly one of `memory:<id>` or
        `feeling:<id>` as the mandatory anchor — no provisional form is
        described in the rubric. The command body permits a provisional
        description anchor at R1 that the rubric's form discipline requires
        to be a machine-readable id.
        This is a controlled divergence (R1 authors write provisional, R2
        resolves to machine-readable) and the rubric is the authority for
        the FINAL form. However, the rubric's form discipline section does
        not acknowledge the provisional path, which means an R1 author
        strictly following the rubric would refuse to write a provisional
        anchor even when the command body explicitly invites it.
        Citing: and-facets.md Phase 1 item 8; rubric-metaphor.md §"Form"
        §"Licensing layer".
      why: >
        SIGNAL: if a future R1 metaphor author reads the rubric strictly
        (as the command body instructs — "reads only its rubric + the inputs
        the rubric names"), the rubric's absence of a provisional-anchor form
        will cause the author to attempt full resolution at R1 before other
        facet IDs exist, which is structurally impossible in a parallel R1
        block. The command body's provisional provision should be noted in
        the rubric's form discipline section (e.g., "R1 provisional form
        permitted: `licensed-by: <description of intended memory or feeling
        anchor>` — resolution to `<prefix>:<id>` form happens at R2").

    - id: STRUCT-030
      type: pass
      severity: SIGNAL
      what: >
        and-review.md § facets subcommand (line 164-166): "per-facet rubric
        runners against rubric (`design/shoot-v2/rubric-*.md`). Lift basis:
        `/and-facets` R1 fanout per-facet rubric runners (extracted into named
        subroutine; called from both `/and-facets` and `/and-review facets`)."
        The glob `design/shoot-v2/rubric-*.md` would match all nine existing
        rubric files (including rubric-vibes-v1.1-patch.md as a separate file).
        The V1.1 patch file being a separate file rather than applied content
        is an authoring-discipline concern (and-facets.md Phase 1 item 9
        correctly names both "rubric-vibes.md + rubric-vibes-v1.1-patch.md"
        as the combined authority); and-review.md's glob-reference would
        include the patch file in the per-facet runner, which is correct
        behavior. PASS on the and-review.md glob reference.
        STRUCT-025 (missing rubric-exposition.md) means the glob also has no
        exposition rubric to run — already captured above.
      why: >
        No additional findings on the and-review.md rubric-reference mechanism.

    - id: TASTE-FLAG-001
      type: flag
      severity: SIGNAL
      what: >
        design/shoot-v2/and-season-tuning-r1/rubric-and-season.md — legacy
        tuning artifact for the dropped /and-season command. No current
        command body references this path. It is a historical artifact.
        Per dispatch instructions: treat as TASTE-FLAG historical, not HARD.
        No action required.
      why: >
        Historical. No live dispatch reads this file. Orphan by design
        (dropped command's rubric retained for reference).

  # ── SUMMARY ──────────────────────────────────────────────────────────────

  summary:
    legs_covered: [2, 3]
    legs_excluded: [1, 4, 5]  # covered by pipeline-20260607T004417Z.md
    total_findings_this_report: 14
    hard: 4   # STRUCT-017, STRUCT-018, STRUCT-019, STRUCT-025, STRUCT-026
              # (count: 5 — corrected below)
    signal: 8  # STRUCT-020, STRUCT-021, STRUCT-022, STRUCT-023, STRUCT-024,
               # STRUCT-027, STRUCT-028, STRUCT-029, STRUCT-030 (count: 9 — see note)
    pass: 3   # STRUCT-021, STRUCT-023, STRUCT-028, STRUCT-030
    taste_flag: 1  # TASTE-FLAG-001

    # Corrected counts (pass and signal overlap in presentation above; clarifying):
    hard_findings: [STRUCT-017, STRUCT-018, STRUCT-019, STRUCT-025, STRUCT-026]
    hard_count: 5
    signal_findings: [STRUCT-020, STRUCT-022, STRUCT-024, STRUCT-027, STRUCT-029]
    signal_count: 5
    pass_findings: [STRUCT-021, STRUCT-023, STRUCT-028, STRUCT-030]
    pass_count: 4
    taste_flag_findings: [TASTE-FLAG-001]
    taste_flag_count: 1

    leg_2_verdict: >
      FAIL-WITH-HARD. Three HARD findings (STRUCT-017, STRUCT-018, STRUCT-019)
      identify live schema-vs-rubric drift on the tensometer → peak-bones
      migration: rubric-metaphor.md's multi-justification gate still requires
      `tens` as a valid layer after the schema retired it; rubric-vibes.md's
      licensed-by source list still names `tens:<reading>` after the schema
      retired it; and rubric-metaphor.md's schema-content-shape section
      misrepresents the live schema form as "proposed." Two SIGNAL findings
      identify softer residue (rubric-feeling.md tensometer vocabulary
      STRUCT-020; rubric-feeling.md stale "caveat-pre-ship" notice STRUCT-022).
      One additional SIGNAL finds an intra-rubric sparsity inconsistency in
      rubric-memory-flags.md (STRUCT-024).

    leg_3_verdict: >
      FAIL-WITH-HARD. Two HARD findings: STRUCT-025 (rubric-exposition.md
      named by and-facets.md Phase 1 exposition dispatch does not exist on
      disk — the exposition facet's R1 author has no rubric, and /and-review
      facets has no exposition rubric runner); STRUCT-026 (rubric-metaphor.md
      AP7 uses tensometer trigger condition that cannot be evaluated without
      the dropped tensometer, while the command body attempts to bridge to
      peak-bones vocabulary; the bridge is incomplete). Two SIGNAL findings:
      STRUCT-027 (command-body pressure-signal dispatch note routes to raw
      substance_delta instead of the scene-map file the rubrics actually read);
      STRUCT-029 (rubric-metaphor.md form discipline does not acknowledge the
      R1 provisional anchor form that and-facets.md Phase 1 permits).

    cross_references:
      prior_report: active-project/staff/reviews/pipeline-20260607T004417Z.md
      prior_STRUCT-012: >
        Closed. This report fully executes the schema-vs-rubric leg that
        STRUCT-012 recorded as not executed. The leg returned 5 HARD and
        5 SIGNAL findings (not a clean pass).
      prior_STRUCT-013: >
        Partially superseded. STRUCT-013 flagged the path inconsistency
        between and-facets.md (staff/dialogue-writer/rubric-dialogue.md) and
        and-review.md (design/shoot-v2/rubric-*.md) on the dialogue rubric.
        This report does not re-examine that specific flag but notes: the
        dialogue rubric path (staff/dialogue-writer/rubric-dialogue.md) is
        outside the design/shoot-v2/ glob and is correctly excluded from the
        scope of this completion pass. STRUCT-013 remains an open SIGNAL
        from the prior report.

    next_dispatch: >
      Priority 1 (HARD, fixer scope): STRUCT-017 + STRUCT-019 + STRUCT-026
      are related — all require updating rubric-metaphor.md to replace
      tensometer vocabulary with peak-bones vocabulary throughout (multi-
      justification gate, AP7, §Schema content-shape section). A single
      fixer pass on rubric-metaphor.md closes all three.
      Priority 2 (HARD, fixer scope): STRUCT-018 requires updating
      rubric-vibes.md Cross-facet contract to replace `tens:<reading>` with
      `peak-bone:<flat-id>`. The V1.1 patch file does not need to change
      (it did not address this section). One targeted rubric-vibes.md line
      edit closes STRUCT-018.
      Priority 3 (HARD, author or fixer scope): STRUCT-025 requires either
      authoring rubric-exposition.md at design/shoot-v2/ OR updating
      and-facets.md Phase 1 item 10 to remove the rubric-exposition.md
      reference. The rubric-authoring option is strongly preferred.
      SIGNAL findings STRUCT-020, STRUCT-022, STRUCT-027, STRUCT-029 are
      lower-priority rubric cleanup suitable for a subsequent pass.
      STRUCT-024 (intra-rubric sparsity inconsistency in rubric-memory-flags.md)
      requires a policy decision on the authoritative band (1-5% vs 5-12%)
      before a fixer can act.
```
