---
scope: pipeline
target: pipeline
timestamp: 2026-06-11T00:00:00Z
lens: /and-review pipeline (improvement-loop/test run 1)
verdict: PASS-WITH-SIGNALS
fault_count: 3
flag_count: 8
---

# Pipeline Tri-Walk Audit — 2026-06-11

**Verdict: PASS-WITH-SIGNALS**
- `fault` (STRUCT — actionable drift): 3
- `flag` (advisory, no fixer dispatch): 8
- `RESIDUE` findings: 0 detected in live files
- Rubric axis: **not completed** — see scope note below

> **Scope note.** The rubric files under `design/shoot-v2/rubric-*.md` were not read in this audit pass. The schema-vs-rubric and command-body-vs-rubric axes of the tri-walk are therefore incomplete. The three faults below are cross-schema and cross-command-body findings. Rubric-specific drift may exist and is not reflected in this report. A follow-on rubric pass is warranted before this audit is considered fully closed on all five tri-walk axes.

---

## Faults (actionable — route to fixer)

```yaml
audit:
  scope: pipeline
  target: pipeline
  timestamp: 2026-06-11
  findings:

    - id: fault-001
      type: fault
      what: >
        schemas/audit-report.schema.md — R2 decision-shard frontmatter section.
        Defines live schema objects `report: r2-decision-shard`,
        `report: r2-decisions-consolidated`, `.r2-decisions.md` consolidated file
        format, `f-r2-counts` frontmatter block, and the four `F-R2-*` failure
        classes (F-R2-1 through F-R2-4). Also references "Phase 6 verdict reads
        f-r2-counts" — but Phase 6 in the current /and-facets is renumbered to Phase
        5; the Phase 6 label is stale. The producing command `/and-facets-r2` is the
        R2 round retired by DEC-0116. The `cite_index_hash: <sha>` REQUIRED field is
        part of an artifact that no command in the pipeline currently emits.
      why: >
        The schema authority file is the canonical contract for what auditor produces
        and what downstream consumers expect. Keeping live schema definitions for a
        retired pipeline path creates risk that a command body or agent dispatch will
        attempt to emit or validate these fields, or that a future rubric author will
        treat them as an active requirement. The stale Phase 6 reference misdirects
        any implementation that reads the schema as an orchestrator-critic consumer
        contract.
      criteria: >
        The R2 decision-shard section must be clearly marked RETIRED/DEPRECATED with
        a reference to DEC-0116 and the retirement date, or moved to an archive schema
        file. The Phase 6 reference in the consumer contract note must be corrected to
        Phase 5 (the current orchestrator-critic verdict phase in /and-facets). No new
        audit reports should be expected to carry `f-r2-counts` or `r2-decision-shard`
        format fields.

    - id: fault-002
      type: fault
      what: >
        .claude/commands/and-write.md — Phase 4 (Trim) audience dispatch context block.
        Loads `series.theme` as part of the persona context: "series.theme + chapter
        substance_delta + per-scene substance_delta + scene_conflict."
      why: >
        `series.theme` is not a field in `schemas/showrunner-memory.schema.md`. The
        series block defines `series.chunk`, `series.substance`, `series.laws`,
        `series.lore`, `series.behaviors`, `series.vibe_cloud` — no `series.theme`
        field at any path. An audience persona dispatch that loads `series.theme` will
        either error on a missing key, silently load nothing, or load an unrelated
        field if the key collides. The thematic content the load is likely intended to
        provide lives in `series.chunk.path.{motivation, trade, irony}` or
        `series.substance.state_axes[]`, not a `theme` field.
      criteria: >
        The `series.theme` reference in Phase 4's audience dispatch context must be
        replaced with the correct showrunner memory field(s) that carry the
        series-level thematic/motivational content (likely `series.chunk.path`
        components, or `series.substance.state_axes[]`, or removed if the intent is
        covered by the vibe-cloud already loaded in the same block). The field used
        must match a key defined in `schemas/showrunner-memory.schema.md`.

    - id: fault-003
      type: fault
      what: >
        .claude/commands/and-facets.md Phase 5c (orchestrator-critic verdict).
        Reads from `staff/audience/and-facets-orchestrator-critic/card.md`. The path
        places the orchestrator-critic inside the audience directory (`staff/audience/`).
      why: >
        CLAUDE.md routing table places the orchestrator-critic at
        `staff/orchestrator-critic/card.md` (not under `staff/audience/`). The schema
        authority table and directory map both use `staff/orchestrator-critic/`. The
        `staff/audience/and-facets-orchestrator-critic/` path does not correspond to
        any established directory. A Phase 5c dispatch that reads the wrong path will
        silently load an absent or wrong card, producing an invalid or default verdict.
        Because the orchestrator-critic verdict is the facet-layer success standard
        (SUCCESS / SHIPPABLE-WITH-CAVEATS / NOT-SUCCESSFUL), a silent card-miss here
        invalidates the gate.
      criteria: >
        Phase 5c must read the orchestrator-critic card from
        `staff/orchestrator-critic/card.md` (per CLAUDE.md agent routing table). The
        path `staff/audience/and-facets-orchestrator-critic/` must be corrected or
        verified to match an actual directory. If a facet-specific orchestrator-critic
        variant is intentional, it must be explicitly registered in the CLAUDE.md
        routing table.
```

---

## Flags (advisory — no fixer dispatch; noted for future sessions)

```yaml
    - id: fault-004
      type: flag
      what: >
        schemas/scene-map.schema.md — line ~86: "Names inherited from
        `chapters[<slug>].scenes[].scene_id` in showrunner memory." Field name used
        is `scene_id`.
      why: >
        `schemas/showrunner-memory.schema.md` defines scenes under
        `chapters[].scenes[]` with the field `slug: b01c01s01` — not `scene_id`.
        A scene-map author reading this will look for `scenes[].scene_id` and find
        nothing, falling through to bones-file detection for all scenes.
      criteria: >
        `scene-map.schema.md` line ~86 must reference `chapters[<slug>].scenes[].slug`
        to match the actual showrunner-memory field name.

    - id: fault-005
      type: flag
      what: >
        CLAUDE.md schema authority table does not list `schemas/aggregate-state.schema.md`
        or `schemas/cohere-state.schema.md`. Both files exist on disk and are referenced
        by `/and-stitch` Phase 10, `/and-cohere`, and `/and-review`. CLAUDE.md also
        does not list `/and-cohere` in the commands table.
      why: >
        CLAUDE.md § "Schema authority" is declared as the registry for all file
        formats. Missing entries create session-start gaps. `/and-cohere` omission
        means the command table is incomplete for an actively-used command.
      criteria: >
        Add `schemas/aggregate-state.schema.md` and `schemas/cohere-state.schema.md`
        to CLAUDE.md schema authority table. Add `/and-cohere` to the commands table.

    - id: fault-006
      type: flag
      what: >
        schemas/dialogue.schema.md — example header uses old season/episode slug
        format `s01e01` (pre-URI-SUBSTANCE-OVERHAUL vocabulary).
      why: >
        Contradicts bones.schema.md's `episode:` field note ("the chapter slug —
        matches filename"). A dialogue-file author reading the schema example will
        emit `episode: s01e01` rather than `episode: b01c01`, causing cite-index /
        /and-facets Phase 0 matching failures.
      criteria: >
        Update the dialogue schema example to use current chapter-slug format
        (e.g. `episode: b01c01`).

    - id: fault-007
      type: flag
      what: >
        schemas/facet.schema.md — retains `tensometer` section at schema-definition
        depth (has deprecated notice but still provides field-level detail). Also uses
        `@<proto-line-id>` anchor label throughout — pre-substance vocabulary for
        what is now `flat_id` per `schemas/bones.schema.md`.
      why: >
        (1) Tensometer section at definition depth is a copy-from surface even with
        a deprecated notice. (2) `@<proto-line-id>` vs `flat_id` vocabulary drift
        can cause facet authors to produce anchor labels inconsistent with what the
        cite-index parser expects.
      criteria: >
        Compress tensometer section to a single-line retirement notice referencing
        DEC-0116 and the archive. Annotate `@<proto-line-id>` anchor label to
        clarify it refers to `flat_id` as defined in bones.schema.md.

    - id: fault-008
      type: flag
      what: >
        .claude/commands/and-stitch.md Phase 0.6 — references "exposition-author's
        R1+R2" when describing what /and-facets authored. The R2 round is retired
        under DEC-0116.
      why: >
        Misrepresents the current pipeline state. A stitcher fork reading this may
        incorrectly assume the exposition received R2 judgment-pass validation.
      criteria: >
        Remove "R2" from Phase 0.6's exposition-author characterization. Current
        pipeline: R1 authoring + Phase 4 mechanical audit only.

    - id: fault-009
      type: flag
      what: >
        .claude/commands/and-review.md `cohere` subcommand Phase 4 writes
        `chapters[<slug>].cohere_review` to showrunner memory. This field is not
        defined in `schemas/showrunner-memory.schema.md`. The command body itself
        acknowledges: "(Schema-level memory.md addition; documented here, gated by
        principal triage of PROP-0030 before going live in the schema)."
      why: >
        Schema authority violated for this field until triage completes.
      criteria: >
        Either update schema to include `chapters[<slug>].cohere_review`, or note
        in the command body that the write is deferred until schema is updated.
        Blocked on PROP-0030 principal triage.

    - id: fault-010
      type: flag
      what: >
        schemas/showrunner-memory.schema.md — missing `coherence_review` field
        under `chapters[]`. But `/and-stitch` Phase 8.5 actively writes
        `chapters[<slug>].coherence_review` (subfields: `reviewed_at`, `verdict`,
        `weave_gaps`, `followability_breaks`, `cold_read_risk_high`,
        `cold_read_risk_advisory`, `findings`, `report_path`).
      why: >
        Unlike fault-009 (pending triage), this is a fully wired, actively executed
        phase. The schema is missing a field that is being written on every chapter
        production run. Schema-validation checks will surface it as an unknown field.
      criteria: >
        `schemas/showrunner-memory.schema.md` must be updated to include
        `chapters[<slug>].coherence_review` with all subfields as declared in
        /and-stitch Phase 8.5's memory-write block.

    - id: fault-011
      type: flag
      what: >
        .claude/commands/and-project.md Phase 1 scaffold writes `series.substance: ~`
        as null placeholder. `schemas/showrunner-memory.schema.md` defines
        `series.substance` as a complex object with subfields `state_axes[]`,
        `actor_baselines[]`, `cost_ledger[]`, `antagonist_pressure[]`, `chunk_targets{}`.
      why: >
        Minor shape mismatch; not a hard breakage since the field starts null and is
        populated by /and-substance series. Documentation alignment only.
      criteria: >
        and-project.md Phase 1 stub should note "populated by /and-substance series;
        null until then." Schema should note the expected two-phase state.
```

---

## Residue scan

No deprecated-term residue found in live schema or command files for the searched terms:
`tensometer`, `URI-026`, `tens-gate`, `/and-judge-book`, `direction: +`, `direction: -`,
`seasons[`, `episodes[`.

Note: `tensometer` appears in `schemas/facet.schema.md` with a DEPRECATED notice (flagged as fault-007 above — not a residue HARD since it carries its own deprecation notice, but warrants compression).

---

## CLAUDE.md sync (walk-5)

- Agent routing table: accurate against current agent file set.
- Schema authority table: **missing** `aggregate-state.schema.md` and `cohere-state.schema.md` (fault-005).
- Directory map: `theater/` subdir listing matches actual subdirs.
- Command table: **missing** `/and-cohere` (fault-005).
- No agent or schema in CLAUDE.md corresponds to a missing file.

---

## Parking-lot items filed

See `active-project/staff/showrunner/parking-lot.md`:
- `pl-2026-06-11-pipeline-001` → fault-001 (audit-report.schema.md R2 section) — SOFT
- `pl-2026-06-11-pipeline-002` → fault-002 (and-write.md `series.theme`) — HARD
- `pl-2026-06-11-pipeline-003` → fault-003 (and-facets.md Phase 5c wrong OC path) — HARD
- `pl-2026-06-11-pipeline-004` → fault-010 (memory schema missing `coherence_review`) — SOFT
