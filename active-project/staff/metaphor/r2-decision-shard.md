cite_index_hash: 0e12ee9c89ee2002e4b5a6b751c9c393e02bbbf5941a58c6b61ba06b45468e30
generated: 2026-06-03
phase: /and-facets b01c13 Phase 3 R2 — metaphor JUDGE (graph-aware; add review)
judge: editor (judge mode)

---

## Verdict

REFUSAL-HOLDS

f-r2-counts:
  K: 0
  D: 0
  A: 0

---

## Add-review rationale

R1 refused all 31 bones. R2 reviewed all five memory/feeling anchors visible in the locked graph
({mem:1 @16, mem:2 @23, mem:3 @30, feel:1 @31, feel:2 @21}) against the full rubric.
None clears all required gates simultaneously.

### @16 — mem:1 (road-to-hell-chain; cond-override-architecture-residue)

Cite-index confirms @16 carries: mem:1, state:16, state:17. No feeling, no NI, no sensory fire.
Multi-just requires ≥2 layers from {memory, feeling, tens}. mem:1 = layer 1. No second layer
available from the locked graph. tens at @16 is mid-scene-B (blowfly grip; not a quiet zone —
pressure is building toward @15/@17 peaks, making this tens=2 at best). A tens=2 reading does not
satisfy multi-just as a clean second layer (trailing-edge exception requires post-3-cluster; @16
is mid-cluster). Multi-just: 1 of 2. FAIL. Refuse upheld.

### @23 — mem:2 (b01c05 + dynastic-war-smallfolk-cost foreknowledge)

Cite-index confirms @23 carries: mem:2, narrator:5. No feeling fire. @23 is post-hinge quiet
(tens=1 after the naming peak at @19). Multi-just: mem:2 (layer 1) + tens=1 (layer 2) = 2 layers.
Multi-just technically clears.

AP4 check: mem:2's content — "the people the apparatus spends are spent the way a coming fire will
spend them, as weather, never as a column anyone keeps" — is itself an explicit simile (X spent
the way a coming fire will spend them, as weather). The figurative move mem:2 provides is already
an explicit comparison. Any metaphor entry at @23 would restate or elaborate the fire/weather figure
already present in the memory content. AP4 fires (figurative-already-in-memory). Q1 fails: the
figure is already in the memory flag.

Refuse upheld. AP4 is the operative block.

### @30 — mem:3 (b01c07 + b01c09; same counter, route unbent)

Cite-index confirms @30 carries: mem:3, narrator:7, state:20. No feeling fire. @30 is post-peak
walking-out (scene-D peaks at @28/@29; @30 is trailing). tens=1 quiet. Multi-just: mem:3 (layer 1)
+ tens=1 (layer 2) = 2 layers. Clears technically.

Q1 check: mem:3 content — "it is the same counter she has run at the trough before... nothing in
the running has ever bent the route" — is plain statement (no explicit comparator; not itself
figurative). AP4 does not fire. Q1 requires the metaphor to add what the proto-line + cited facets
don't already carry. Proto-line: "taylor-hebert-kl-122ac walks the route." mem:3 already carries
the figurative-adjacent weight (same counter, same route, nothing bent). A callback metaphor at @30
would be elaborating what mem:3's plain statement implies — the figure would either restate the
"same route" content or import a new comparator that mem:3 has deliberately withheld in plain
register. The memory content holds the pattern in cold-ledger prose; overlaying a metaphor imports
emotional register the chapter's cold-contempt-as-verdict discipline excludes.

Additionally: functional-register check. Callback register licensed by mem:3. But the callback
maps to b01c07/b01c09 (prior circuit-runs). This would be a self-referential loop-callback without
a clear figurative vehicle not already present in mem:3's plain enumeration. The Q1 defensibility
gate fails: the figurative-adjacent content is already present in the memory + proto-line union;
a metaphor would add rhetorical warmth, not a distinct figure.

Refuse upheld. Q1 failure (redundant with mem:3) + chapter register discipline are the operative
blocks.

### @21 — feel:2 (taylor-hebert-kl-122ac takes the two-breaths)

Cite-index confirms @21 carries: feel:2 only (lonely entry, no co-location). No memory anchor.
Multi-just: feel:2 (layer 1) + tens (post-hinge quiet, tens=1, layer 2) = 2 layers. Clears
technically.

Functional-register check: without a memory anchor, the only licensed functional register is
dark-humor (sardonic deflation). The beat is two controlled breaths in the lane after the naming
hinge — a cold stillness beat, not a sardonic irony beat. Dark-humor deflation requires
recognition-of-pattern the in-scene characters do not share; "two breaths" is interior steadying,
not sardonic. Functional-register: FAIL. No callback register (no memory anchor). No dark-humor
register (wrong affective mode for the beat).

Refuse upheld. Functional-register fail is the operative block.

### @31 — feel:1 (septon-halvard-flea-bottom fills the water-skin)

Cite-index confirms @31 carries: feel:1, sensory:4, state:13, vibes:16, vibes:17. No memory
anchor at @31. @31 is post-peak scene-D tail (scene-D peaks @28/@29; @31 is trailing, tens=1).
Multi-just: feel:1 (layer 1) + tens=1 (layer 2) = 2 layers. Clears technically.

Functional-register check: feel:1 at @31 is the principled-slower register — the septon continuing
his circuit unbroken after she has left. Without a memory anchor, the only licensed functional
register is dark-humor. The septon filling the water-skin as dark-humor deflation? The beat is
moral-mirror-not-following — principled continuance, not sardonic. No ironic recognition available;
the beat's register is the opposite of sardonic (it is the earnest-slower answer continuing
unanswered). Functional-register: FAIL.

AP12 consideration: feel:1 at @31 is the septon's beat (non-POV). A metaphor here would require
editor-frame third-person rendering. Even setting AP12 aside, functional-register fail blocks.

Refuse upheld. Functional-register fail is the operative block.

---

## Chapter-register confirmation

Cold contempt-as-verdict chapter. The dominant prose discipline is ledger-language and cold
enumeration. The three memory anchors (mem:1, mem:2, mem:3) carry the figurative-adjacent weight
already — mem:2 with an explicit simile, mem:1 and mem:3 in plain cold-ledger register. Figurative
overlay at any of the five anchor beats would import a warmth or ironic distance the chapter
deliberately withholds. The refuse-by-default discipline and the AP discipline together correctly
hold at 0 entries.

---

## File-shape audit

- Sparsity: 0 fires / 31 bones = 0%. Within 0-3% band (zero acceptable). PASS.
- Per-scene cap: all scenes at 0. PASS.
- Add-cap exhaustion: 0 of 3 add-cap used. PASS (no adds).
- Schema: no entries to audit. PASS.

---

## Seams flagged

One seam for downstream awareness (informational; not a refusal override):

**@23 AP4 note:** mem:2 contains an explicit simile (fire/weather). At stitch time, if the stitcher
renders mem:2's content in prose, the fire/weather figure will appear in the draft. This is not
a metaphor-facet entry — it is the memory content itself. The stitcher should ensure this figure
does not get amplified or doubled in the render pass; it should appear once, in the memory's voice,
without figurative echo in adjacent narrator prose.
