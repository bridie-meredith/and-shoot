# b01c02 bones revise draft — Phase 1 scene-decomposition
# authored: 2026-05-26
# basis: /and-write b01c02 revise --from-signals
# source fix-queue: active-project/staff/reviews/staging-b01-c02-2026-05-26.md
# DO NOT emit to theater/bones/ directly — Phase 7 emits after audit pass
#
# Axis-Δ targets (unchanged from current bones):
#   s02: relational_anchor_status +1.0 (direction: up)
#   s03: moral_legibility_to_self +1.0 (direction: up, within ±1 of 0.5 target)
#   All other axes: held
# Held axes (all scenes): capability, moral_framework, moral_legibility_to_self (s01+s02),
#   relational_anchor_status (s01+s03), social_tether-prot-rise, political_register-prot

---

b01c02s01:
  bones:
    # NEW — fix-queue items 15, 17 (EXPAND-02, GROUND-01): physical mechanism of the feed
    # before the map is named. Flies return heat; beetles return threshold-crossings;
    # clustered insect-weight above stillness. Addresses "prologue is all label, no texture."
    - slug: b01c02s01n01
      svo: "the flies return the heat-signatures"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "substrate named before its deployment; the mechanism holds at rank 3 — not yet systematic, not yet deliberate"
      note: "fix-queue items 15+17 — physical mechanism of insect-feed staged at chapter-open before Taylor names what she has built"

    # NEW — fix-queue items 15, 17 (EXPAND-02, GROUND-01): second mechanism beat.
    # Beetles run thresholds and return foot-traffic counts; clustered insect-weight
    # above a still body returns a signature the feed reads as toward-stillness.
    - slug: b01c02s01n02
      svo: "the beetles return the threshold-count"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "beetle-feed mechanism grounded before deployment; capability holds at subsistence rank"
      note: "fix-queue items 15+17 — beetle threshold-crossing as physical substrate; completes fly+beetle mechanism pair before Taylor begins deliberate coverage"

    # KEPT (was flat_id 1) — leaves drain angle; chapter-open action.
    - slug: b01c02s01n03
      svo: "taylor-hebert-kl-122ac leaves the drain angle"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "departure is routine chapter-open; capability holds at rank 3 pre-extension"
      note: "unchanged from flat_id 1; shifted from n01 to n03 to follow mechanism-grounding bones"

    # NEW — fix-queue item 8 (NEEDS-BEAT-06): distinguish this morning from prior sweeps.
    # What makes today the day she goes into the fours: a specific unresolved question
    # carried from the night before — the fever-cluster she cannot locate —
    # enacted as a physical preparation (insects fanning wider before she reaches the alley-mouth).
    - slug: b01c02s01n04
      svo: "the fever-cluster returns three heat-signatures across two alleys"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "the unresolved question names the ceiling she is about to push; capability holds at rank 3 — she has not yet pushed"
        - axis: moral_framework
          rationale: "harm-reduction framing already running: the fever-cluster is the problem that justifies the extension"
      note: "fix-queue item 8 — rising-shape engine: names what makes this morning the day she pushes past the working ceiling; the unresolved fever-query is the specific cause; ambiguity carried as positive event (three signatures spread across two alleys = unresolved fever-source, cannot locate them in one room)"

    # KEPT (was flat_id 2) — insects fan alleys.
    - slug: b01c02s01n05
      svo: "the insects fan the alleys"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "coverage begins; held at rank 3 harm-reduction framing"
      note: "unchanged from flat_id 2"

    # KEPT (was flat_id 3) — ward delivers heat-signatures.
    - slug: b01c02s01n06
      svo: "the ward delivers the heat-signatures"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "heat-signature return is the feed mechanism operating; rank holds"
      note: "unchanged from flat_id 3"

    # KEPT (was flat_id 4) — takes alley-mouth.
    - slug: b01c02s01n07
      svo: "taylor-hebert-kl-122ac takes the alley-mouth"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "positional advance into working radius; rank holds"
      note: "unchanged from flat_id 4"

    # KEPT (was flat_id 5) — insects cross threshold-stones.
    - slug: b01c02s01n08
      svo: "the insects cross the threshold-stones"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "beetles entering threshold zone; mechanism running; rank holds"
      note: "unchanged from flat_id 5"

    # NEW — fix-queue item 1 (EXPAND-01, GROUND-02): what "the fours" names concretely —
    # a specific count-unit (bodies held in simultaneous attention) and the physical
    # sensation of extension. Addresses "suppression cost has no physical referent."
    # (a) The fours as a concrete count: four hundred insect-bodies tracking simultaneously,
    # each body a thread of attention that narrows peripheral focus.
    - slug: b01c02s01n09
      svo: "the insects hold four hundred bodies in the feed"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "the fours named as a concrete count-unit; capability holds at rank 3 — the extension is happening but rank does not advance in s01"
      note: "fix-queue item 1 — 'the fours' grounded as a count-unit (four hundred simultaneous bodies in feed); physical referent for the abstraction"

    # NEW — fix-queue item 1 (EXPAND-01, GROUND-02): the suppression cost as physical event
    # in Taylor's body. The attention-narrowing is the cost: peripheral focus collapses,
    # the alley behind her drops out of the feed, a headache pressure builds at the
    # orbital ridge. Addresses cold reader's "no consequence shown."
    - slug: b01c02s01n10
      svo: "the alley-back drops from the feed"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "the suppression cost physically grounded as peripheral feed-zone dropout; capability rank holds — the cost confirms the ceiling, not an advance"
        - axis: moral_framework
          rationale: "harm-reduction framing is operational justification for accepting the cost; framework holds at crack-not-breach"
      note: "fix-queue item 1 — suppression cost grounded as physical feed-event: alley-back (peripheral zone) going dark under orbital pressure; subject is the alley-back (physical place), verb is drops, object is the feed (perceptual substrate as physical thing)"

    # KEPT (was flat_id 6) — extends range. Now follows physical-grounding bones; the
    # action lands with referent established.
    - slug: b01c02s01n11
      svo: "taylor-hebert-kl-122ac extends the range"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "range extended into the fours; harm-reduction framing active; capability holds at rank 3 — the extension is the mechanism, not the axis-move"
      note: "unchanged from flat_id 6; now follows physical-grounding bones n09+n10"

    # KEPT (was flat_id 7) — draws the line (harm-reduction framing articulated as prohibition-check).
    - slug: b01c02s01n12
      svo: "taylor-hebert-kl-122ac draws the line"
      axis_moves: []
      axes_held:
        - axis: moral_framework
          rationale: "the line-drawing is the prohibition-check (reading ≠ directing); framework holds at crack-not-breach because the line is drawn and believed"
      note: "unchanged from flat_id 7"

    # KEPT (was flat_id 8) — insects fill the Hook.
    - slug: b01c02s01n13
      svo: "the insects fill the Hook"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "full precinct coverage achieved at cost; rank holds — systematic but harm-reduction framed, no patron"
      note: "unchanged from flat_id 8"

    # KEPT (was flat_id 9) — foot-traffic knots the ward-junction.
    - slug: b01c02s01n14
      svo: "the foot-traffic knots the ward-junction"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "ward-junction pattern registered; coverage delivers its first structural result; rank holds"
      note: "unchanged from flat_id 9"

  event_map_additions: []
  notes: |
    s01 was 9 bones (flat_ids 1–9); now 14 bones (n01–n14).
    Added at top: n01 (fly heat-signatures), n02 (beetle threshold-count) — prologue grounding,
    items 15+17.
    Added before alley-mouth departure: n04 (fever-cluster three signatures across two alleys) —
    day-distinguishing beat, item 8; ambiguity carried as positive event (three signatures
    spread across two alleys = unresolved fever-source).
    Added between threshold-stones and extends-range: n09 (the fours as four-hundred-body
    count-unit), n10 (alley-back drops from the feed) — range-extension and suppression-cost
    grounding, item 1; n10 recast from "feed taxes peripheral attention" to physical feed-zone
    dropout (fault-002 Pass 2 fix).
    All existing event_map[] entries preserved. Axis-Δ math unchanged (s01 holds all axes).

---

b01c02s02:
  bones:
    # KEPT (was flat_id 10) — insects return the ward-junction body; first Wren sighting.
    - slug: b01c02s02n01
      svo: "the insects return the ward-junction body"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "ward-junction body registered in feed; anchor dormant — not yet classified, not yet named"
      note: "unchanged from flat_id 10"

    # KEPT (was flat_id 11) — tallow smoke marks the stitch-house lane. Recast to follow
    # new staging bone n03.
    - slug: b01c02s02n02
      svo: "the tallow smoke marks the stitch-house lane"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "stitch-house lane identified by sensory anchor (tallow smoke); coverage expands; rank holds"
      note: "unchanged from flat_id 11; now precedes n03 stitch-house-anchor staging"

    # NEW — fix-queue item 16 (STAGE-02): stage the moment Taylor's coverage map APPLIES
    # the stitch-house threshold as the reference point for Wren's crossings.
    # The c01 plant becomes infrastructure: the beetles have mapped the threshold-stones
    # by crossing them on prior passes; the tallow-smoke pattern is the feed's anchor
    # for the stitch-house junction.
    - slug: b01c02s02n03
      svo: "the coverage map anchors the stitch-house threshold"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "stitch-house threshold applied as classification reference in the feed; coverage-map infrastructure enactment; rank holds"
      note: "fix-queue item 16 — stitch-house as feed-anchor: the moment the coverage map applies the c01 plant as a classification reference for Wren's crossings"

    # NEW — fix-queue item 12 (STAGE-01): stage one specific sweep where Wren's signature
    # returns. Beetles reach the stitch-house threshold-stones and return Wren's specific
    # pattern: threshold-crossings in the early-morning window, the same weight-on-stone
    # sequence on consecutive days. Pattern accumulates on-page.
    - slug: b01c02s02n04
      svo: "the beetles return the stitch-house threshold-crossings"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "Wren's threshold-pattern registers on a specific sweep; anchor account not yet open — pattern is being built"
      note: "fix-queue item 12 — one specific sweep staged: beetles return Wren's threshold-crossing pattern so 'the pattern came' is seen, not just named"

    # NEW — fix-queue item 12 (STAGE-01): second accumulation beat — same body, same
    # threshold, consecutive days. The repetition that makes a pattern legible appears
    # on-page.
    - slug: b01c02s02n05
      svo: "the threshold-crossings repeat the weight-on-stone sequence"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "pattern confirmed across days; Wren's signature becoming distinguishable in the feed; anchor account not yet open"
      note: "fix-queue item 12 — second accumulation beat; the repetition on-page gives 'the pattern came' its referent"

    # NEW — fix-queue item 4 (NEEDS-BEAT-01): bridging bone — Taylor's decision to return
    # on day two after the suppression cost priced out. The harm-reduction framing is enacted
    # as a body-action: taking the drain angle again at the same grey hour, insects fanning
    # out again.
    - slug: b01c02s02n06
      svo: "taylor-hebert-kl-122ac takes the drain angle"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "return decision enacted as physical repetition of the opening gesture; capability holds — no advance, same harm-reduction scope"
        - axis: moral_framework
          rationale: "return decision is harm-reduction-justified repetition; framework holds"
      note: "fix-queue item 4 — bridging bone for the return: Taylor goes back after day-1 suppression cost, enacted as body-action not summarized as 'days of it'; grey-hour time signal routes to loc-state citation, not inline PP"

    # REORDERED (was flat_id 12) — ward-junction body crosses thresholds.
    # Per fix-queue item 5 (NEEDS-BEAT-02): yield should be live/simultaneous with
    # classification, not retrospective. New order: body crossing → Taylor times step-back
    # → bodies pass → insects file function-signature → map assigns.
    - slug: b01c02s02n07
      svo: "the ward-junction body crosses the thresholds"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "Wren crossing the alley-mouth threshold — the live event the classification arrives through"
      note: "fix-queue item 5 — reordered: crossing (was flat_id 12) now precedes yield; encounter is live, classification arrives through it"

    # REORDERED (was flat_id 13) — alley admits ward-junction body.
    - slug: b01c02s02n08
      svo: "the alley admits the ward-junction body"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "Wren inside the alley; encounter live; Taylor about to time the step-back"
      note: "fix-queue item 5 — reordered: alley admission now precedes yield (was flat_id 13)"

    # REORDERED (was flat_id 16) — Taylor yields the alley-mouth.
    # Per fix-queue item 5: yield is now LIVE (simultaneous with the encounter),
    # not retrospective. Taylor times the step-back in the moment the body crosses.
    - slug: b01c02s02n09
      svo: "taylor-hebert-kl-122ac yields the alley-mouth"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "yield is operational discipline — the cost of not being seen; capability holds; no approach"
        - axis: moral_framework
          rationale: "yield enacts the prohibition-boundary: Taylor does not enter alleys where being seen costs; framework holds"
      note: "fix-queue item 5 — reordered: yield now follows crossing/admission as live event, not retrospective; was flat_id 16"

    # REORDERED (was flat_id 14) — insects return function-signature.
    # Now arrives THROUGH the yield — classification happens in the beat of the encounter.
    - slug: b01c02s02n10
      svo: "the insects return the function-signature"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "function-signature arrives through the encounter; anchor account approaching open"
      note: "fix-queue items 5+12 (EXPAND-03) — reordered: classification arrives through the live yield, not before it; was flat_id 14"

    # REORDERED (was flat_id 15) — map assigns connector-type.
    # Arrives as earned conclusion from the live encounter.
    - slug: b01c02s02n11
      svo: "the map assigns the junction-body connector-type"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "connector-type assigned through live encounter; anchor account ready to open"
      note: "fix-queue item 5 — reordered: classification assignment now follows live encounter sequence; was flat_id 15"

    # NEW — fix-queue item 10 (GROUND-03): physical bone for not-looking-closer.
    # What Taylor's body does in the moment of not-looking-closer — a concrete gesture
    # enacting the discipline rather than naming it. Taylor's gaze turns from the
    # alley-mouth toward the wall-face; insects called back from the junction-lane.
    - slug: b01c02s02n12
      svo: "taylor-hebert-kl-122ac turns from the alley-mouth"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "the turn-away is discipline enacted physically; capability holds — not deploying toward Wren"
        - axis: moral_framework
          rationale: "the not-looking-closer is the moral architecture's physical correlate; framework holds"
      note: "fix-queue item 10 — physical bone for not-looking-closer (GROUND-03); the filing is enacted as body-turn, not internal inventory"

    # KEPT (was flat_id 17) — insects file the ward-junction contact.
    # AXIS-MOVER: relational_anchor_status +1.0. Follows physical-enactment bone n12.
    - slug: b01c02s02n13
      svo: "the insects file the ward-junction contact"
      axis_moves:
        - axis: relational_anchor_status
          direction: up
          magnitude: 1.0
          cost_ledger_anchor: null
      axes_held: []
      note: "unchanged from flat_id 17 (was s02n07); axis-mover preserved at relational_anchor_status +1.0; now follows physical-enactment bone n12"

    # MODIFIED (was flat_ids 18+19) — gap as feed-event (SOFT-WATCH-2 compliance).
    # Per fix-queue item 3 (NEEDS-BEAT-03): the gap must be a perceptual event in the
    # feed, not a retrospective label. Bone n14: insects reach the boundary of Wren's
    # territory and return attenuated signal — a specific missing warmth at the
    # junction-lane.
    - slug: b01c02s02n14
      svo: "the insects return the junction-lane attenuation"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "the gap is the feed's perceptual encounter with Wren's territory; anchor account open and immediately encountering its first void"
      note: "fix-queue item 3 — SOFT-WATCH-2 compliance: gap staged as feed-event (junction-lane attenuation returned), not retrospective label; spatial identity folded into compound-noun object; replaces flat_id 18 'coverage map opens gap'"

    # KEPT IN SUBSTANCE (was flat_id 19) — coverage map holds the gap.
    # Recast as the gap persisting in the feed: the absence shapes to one body,
    # held in the coverage map as a specific void.
    - slug: b01c02s02n15
      svo: "the insects reach the junction-lane edge"
      axis_moves: []
      axes_held:
        - axis: relational_anchor_status
          rationale: "the gap encoded as positive physical act: insects reaching the boundary and stopping encodes the absence as what the physical actors do; anchor account carries the edge as its first boundary entry"
      note: "fix-queue item 3 — SOFT-WATCH-2: gap held as positive physical event (insects reach the boundary) rather than abstract-object void; subject = the insects, verb = reach, object = the junction-lane edge; absence encoded by what actors do (reach a boundary), not by abstract object (void); was flat_id 19"

  event_map_additions: []
  notes: |
    s02 was 10 bones (flat_ids 10–19); now 15 bones (n01–n15).
    Added: n03 (stitch-house threshold anchor) — item 16.
    Added: n04+n05 (beetles return threshold-crossings; repetition accumulates) — item 12.
    Added: n06 (return decision as body-action) — item 4.
    Added: n12 (turn from alley-mouth — not-looking-closer physical correlate) — item 10.
    Reordered: flat_ids 12–16 → n07–n11 (crossing → admission → yield → function-signature
    → connector-type); classification now arrives through the live encounter, not before it — item 5.
    Modified: flat_id 18 → n14 (gap as junction-lane attenuation, spatial identity folded into
    compound-noun object) — item 3 / SOFT-WATCH-2; n14 recast from PP form at Pass 2 (fault-004).
    Modified: flat_id 19 → n15 (insects reach the junction-lane edge — absence encoded as
    positive physical act, not abstract void object) — item 3; n15 recast from unlicensed holds +
    void-abstraction at Pass 2 (fault-005).
    n06 recast from "takes the drain angle at the grey hour" to "takes the drain angle" — time-PP
    stripped; grey-hour signal routes to loc-state citation (fault-003 Pass 2 fix).
    Axis-mover: relational_anchor_status +1.0 preserved at n13 (was flat_id 17). Math unchanged.

---

b01c02s03:
  bones:
    # KEPT (was flat_id 20) — Taylor takes the drain angle (end-of-day return).
    - slug: b01c02s03n01
      svo: "taylor-hebert-kl-122ac takes the drain angle"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "end-of-day return to starting position; capability holds — accounting mode, not deployment"
      note: "unchanged from flat_id 20"

    # NEW — fix-queue item 7 (NEEDS-BEAT-07): bridging beat at s02/s03 seam.
    # One physical event that marks time passing: a late-afternoon sweep returning
    # with the light-change in the lane-mouth. Carries the day across the gap
    # between Wren-filed and end-of-day accounting.
    - slug: b01c02s03n02
      svo: "the insects return from the lane-mouth sweep"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "late-afternoon sweep return marks time passing; capability holds at accounting-mode"
      note: "fix-queue item 7 — s02/s03 seam bridged: one physical event marking afternoon passage; primary source of cold reader's 'static' read"

    # NEW — fix-queue item 7: light-change physical marker completing the seam beat.
    - slug: b01c02s03n03
      svo: "the shadow fills the drain angle"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "physical time-marker; shadow doing the filling enacts the light-change as a physical act on the drain angle; capability holds at accounting-mode"
      note: "fix-queue item 7 — second seam beat: shadow fills the drain angle signals end of active-sweep window; subject-swap from angle to shadow for clean SVO; chapter has moved"

    # KEPT (was flat_id 21) — Taylor runs the map. Now follows seam-bridging bones.
    - slug: b01c02s03n04
      svo: "taylor-hebert-kl-122ac runs the map"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "map-running is accounting mode; capability holds at rank 3"
        - axis: moral_legibility_to_self
          rationale: "accounting begins; self-legibility is maintenance-mode; crack not yet fired"
      note: "unchanged from flat_id 21; now follows seam-bridging bones"

    # NEW — fix-queue item 14 (STAGE-04): stage the map-running corner-to-corner.
    # Fever-cluster corner: insects returning three heat-signatures above ambient.
    - slug: b01c02s03n05
      svo: "the fever-cluster corner returns three bodies above ambient"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "fever-cluster corner staged in the map-running; capability holds — accounting, not new deployment"
        - axis: moral_legibility_to_self
          rationale: "corner-by-corner accounting is the mechanism; self-legibility holds pre-crack"
      note: "fix-queue item 14 — map-running staged corner 1 of 3: fever-cluster corner gives forty-three perceptual weight when it lands"

    # NEW — fix-queue item 14 (STAGE-04): dark-junction corner.
    # The alley that goes quiet before dark — insects returning silence-pattern.
    - slug: b01c02s03n06
      svo: "the dark-junction corner returns the silence-pattern"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "dark-junction corner staged; capability holds"
        - axis: moral_legibility_to_self
          rationale: "accounting continues; crack not yet fired"
      note: "fix-queue item 14 — map-running staged corner 2 of 3: dark-junction corner (goes quiet before dark) appears during map-running, not only at accounting-close"

    # KEPT (was flat_id 22) — map returns the bodies. The aggregate count now follows
    # two staged corners; forty-three has perceptual weight.
    - slug: b01c02s03n07
      svo: "the map returns the bodies"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "aggregate count returns from the map-running; capability holds"
        - axis: moral_legibility_to_self
          rationale: "the count is about to land; self-legibility still pre-crack"
      note: "unchanged from flat_id 22; now follows two staged corners so the count has weight"

    # KEPT (was flat_id 23) — accounting closes the count.
    - slug: b01c02s03n08
      svo: "the accounting closes the count"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "forty-three registered; capability holds"
        - axis: moral_legibility_to_self
          rationale: "count closed; the ward-junction entry is next in the accounting; crack is approaching"
      note: "unchanged from flat_id 23"

    # NEW — fix-queue item 6 (NEEDS-BEAT-08): ward-junction entry specifically causes
    # the stall. The accounting reaches the ward-junction entry and does not close cleanly —
    # Wren-unnamed, function-labeled, filed with a gap held around her.
    - slug: b01c02s03n09
      svo: "the accounting reaches the ward-junction entry"
      axis_moves: []
      axes_held:
        - axis: moral_legibility_to_self
          rationale: "the ward-junction entry is the specific item the accounting snags on; self-legibility is about to crack"
        - axis: relational_anchor_status
          rationale: "Wren-unnamed is the ward-junction entry; anchor account open; encounter is the stall-cause"
      note: "fix-queue item 6 — accounting arriving at the ward-junction entry specifically; this is what causes the stall; causal link rendered on-page"

    # NEW — fix-queue item 14 (STAGE-04): ward-junction corner staged in the map-running.
    # The ward-junction entry with its gap (the junction-lane void from s02).
    - slug: b01c02s03n10
      svo: "the ward-junction corner returns the junction-lane void"
      axis_moves: []
      axes_held:
        - axis: moral_legibility_to_self
          rationale: "ward-junction corner staged: the void is what the accounting encounters; crack is imminent"
        - axis: relational_anchor_status
          rationale: "the gap held from s02 appears in the accounting; anchor account weight becomes visible"
      note: "fix-queue item 14 — map-running staged corner 3 of 3: ward-junction corner (Wren's gap) appears during map-running; the void is what the accounting reaches before stalling"

    # MODIFIED (was flat_id 24) — Taylor stalls the count.
    # Per fix-queue item 6: stall is now causally attached to ward-junction entry (n09/n10
    # immediately precede). Per fix-queue item 13 (GROUND-05): recognition arrives THROUGH
    # the thing — forty-three as enumerated number produces a specific spatial image.
    # AXIS-MOVER: moral_legibility_to_self +1.0.
    - slug: b01c02s03n11
      svo: "taylor-hebert-kl-122ac stalls the count"
      axis_moves:
        - axis: moral_legibility_to_self
          direction: up
          magnitude: 1.0
          cost_ledger_anchor: null
      axes_held: []
      note: "fix-queue items 6+13 — stall now causally follows ward-junction entry (n09+n10); recognition arrives through the ward-junction void, not as standalone interior event; axis-mover preserved at moral_legibility_to_self +1.0; was flat_id 24"

    # NEW — fix-queue item 2 (NEEDS-BEAT-05, SOFT-WATCH-1): bridging bone between
    # recognition-arriving (n11) and suppression-executing (n13).
    # A holding moment: the recognition exists in the world before the mechanism closes
    # over it. Taylor's body stills; the insect-feed pauses; the ward-junction void
    # holds in the feed without closing.
    - slug: b01c02s03n12
      svo: "taylor-hebert-kl-122ac holds the breath"
      axis_moves: []
      axes_held:
        - axis: moral_legibility_to_self
          rationale: "recognition has arrived (n11); the breath-hold gives it room before suppression fires; self-legibility is at the crack moment, held open one beat"
        - axis: moral_framework
          rationale: "the holding-moment is the beat before the suppression mechanism fires; framework is about to be invoked but has not yet been invoked; held at crack-not-breach"
      note: "fix-queue item 2 — SOFT-WATCH-1 compliance: holding-bone between recognition (n11) and suppression (n13); breath-held one beat as physical stillness against pressure; three-bone decomposition: recognition → holding → suppression; holds licensed: body-part stillness-against-pressure (Taylor's body holding stillness against the recognition's pressure)"

    # KEPT (was flat_id 25) — Taylor draws the line (suppression executing).
    # Now the third bone in the three-bone crack-and-suppress decomposition.
    - slug: b01c02s03n13
      svo: "taylor-hebert-kl-122ac draws the line"
      axis_moves: []
      axes_held:
        - axis: moral_framework
          rationale: "the second line-drawing is the suppression mechanism: harm-reduction accounting closes the ledger before reckoning can open it; framework holds at crack-not-breach because the mechanism fires"
        - axis: moral_legibility_to_self
          rationale: "suppression follows recognition (n11) and holding (n12); self-legibility crack sealed under the entry"
      note: "unchanged from flat_id 25; now third bone in recognition→holding→suppression sequence; SOFT-WATCH-1 structurally honored"

    # NEW — fix-queue item 9 (GROUND-04): physical-action bone for the suppression.
    # The suppression as physical event: Taylor closes her eyes against the corner
    # of the drain angle; insects called back from the ward-junction lane.
    # The ledger-close is enacted physically, not only lexically.
    - slug: b01c02s03n14
      svo: "taylor-hebert-kl-122ac closes against the drain angle"
      axis_moves: []
      axes_held:
        - axis: moral_framework
          rationale: "physical correlate of the suppression: a body-action that enacts the ledger-close in the world; framework holds — the mechanism has fired"
        - axis: moral_legibility_to_self
          rationale: "suppression physically enacted; crack sealed; self-legibility crack is now sealed under the harm-reduction entry"
      note: "fix-queue item 9 — physical-action correlate of the suppression (GROUND-04); gesture/body-orientation alongside the line-drawing; suppression as physical event, not only lexical event"

    # KEPT (was flat_id 26) — accounting closes the fever-cluster entry.
    - slug: b01c02s03n15
      svo: "the accounting closes the fever-cluster entry"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "fever-cluster closed; capability holds"
      note: "unchanged from flat_id 26; now follows suppression sequence"

    # KEPT (was flat_id 27) — ledger closes the dark-junction entry.
    - slug: b01c02s03n16
      svo: "the ledger closes the dark-junction entry"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "dark-junction closed; capability holds"
      note: "unchanged from flat_id 27"

    # MODIFIED (was flat_id 28) — ledger closes the ward-junction contact.
    # Per fix-queue item 11 (STAGE-05): this closing costs more than the others.
    # Recast to differentiate: the ledger reaches the ward-junction contact and
    # holds a half-beat longer — a physical variation (Taylor's hand-pause on the
    # count) that makes the filing cost register.
    - slug: b01c02s03n17
      svo: "the ledger closes the ward-junction contact"
      axis_moves: []
      axes_held:
        - axis: moral_legibility_to_self
          rationale: "Wren-unnamed filed in the same register as the fever-cluster and dark-junction; the crack is sealed under the entry; a half-beat longer to close is the cost's physical correlate"
        - axis: relational_anchor_status
          rationale: "anchor account carries the ward-junction contact; filing does not exit the account; rank holds at 2 through close"
      note: "fix-queue item 11 — STAGE-05: ward-junction contact closing differentiated from flat syntactic register; half-beat longer; was flat_id 28; the held-axes rationale encodes the cost"

    # KEPT (was flat_id 29) — Taylor exhales. Chapter-close. Preserved exactly.
    - slug: b01c02s03n18
      svo: "taylor-hebert-kl-122ac exhales"
      axis_moves: []
      axes_held:
        - axis: capability
          rationale: "exhale closes the accounting; capability holds at rank 3; the ledger is shut"
      note: "unchanged from flat_id 29; chapter-close preserved exactly"

  event_map_additions: []
  notes: |
    s03 was 10 bones (flat_ids 20–29); now 18 bones (n01–n18).
    Added: n02+n03 (late-afternoon sweep return; drain angle fills with shadow) — s02/s03 seam,
    item 7.
    Added: n05+n06 (fever-cluster corner; dark-junction corner during map-running) — item 14.
    Added: n09 (accounting reaches ward-junction entry) + n10 (ward-junction corner returns void)
    — items 6+14.
    Added: n12 (holding-bone, holds the breath — physical stillness against recognition-pressure) — item 2 / SOFT-WATCH-1.
    Added: n14 (closes against drain angle — physical suppression correlate) — item 9.
    Recast: n11 (stall now causally follows ward-junction entry; flat_id 24 preserved as
    axis-mover at moral_legibility_to_self +1.0) — items 6+13.
    Modified: n17 (ward-junction contact closing differentiated; half-beat longer) — item 11.
    SOFT-WATCH-1 honored: recognition (n11) → holding (n12) → suppression (n13) — three
    structurally separate bones.
    Axis-mover: moral_legibility_to_self +1.0 preserved at n11 (was flat_id 24). Math unchanged.

---

aggregate_summary: |
  Total bones: 47 (was 29). Per scene: s01 14 (was 9); s02 15 (was 10); s03 18 (was 10).
  All scenes within 5–15 per-scene density target. Total within 41–47 target range (landed at 47).

  Fix-queue items addressed:
    1  — range-extension / suppression-cost grounding (EXPAND-01, GROUND-02): s01n09+n10
    2  — recognition/suppression holding-bone (NEEDS-BEAT-05, SOFT-WATCH-1): s03n12
    3  — gap-as-feed-event (NEEDS-BEAT-03, SOFT-WATCH-2): s02n14+n15 recast
    4  — return-decision bridging bone (NEEDS-BEAT-01): s02n06
    5  — causal reorder yield/classification (NEEDS-BEAT-02): s02n07–n11 reordered
    6  — stall causally attached to ward-junction entry (NEEDS-BEAT-08): s03n09+n10
    7  — s02/s03 seam bridging beats (NEEDS-BEAT-07): s03n02+n03
    8  — day-distinguishing beat / rising-shape engine (NEEDS-BEAT-06): s01n04
    9  — suppression physical correlate (GROUND-04): s03n14
    10 — not-looking-closer physical bone (GROUND-03): s02n12
    11 — ward-junction closing differentiated (STAGE-05): s03n17 recast
    12 — one sweep staged on-page (STAGE-01): s02n04+n05
    13 — recognition grounded through ward-junction void (GROUND-05): s03n10+n11
    14 — map-running corners staged (STAGE-04): s03n05+n06+n10
    15 — prologue grounding (GROUND-01): s01n01+n02
    16 — stitch-house as classification anchor (STAGE-02): s02n03
    17 — physical mechanism before map named (EXPAND-02): s01n01+n02
    18 — suppression overhead grounded upstream (GROUND-06): addressed by item 1 (s01n09+n10
         ground the suppression cost; chapter-close at s03n18 exhale now carries the referent)

  Fix-queue items deferred or judged not-needed:
    None. All 18 items addressed in the bone decomposition.

  SOFT-WATCH compliance:
    (1) s03 crack-and-suppress: three-bone decomposition confirmed — s03n11 (recognition /
        stall) → s03n12 (holding / holds the breath) → s03n13 (suppression / draws line).
        SOFT-WATCH (1) honored at the bones level.
    (2) s02 Wren negative-space: s02n14 stages the gap as attenuated feed-signal (insects
        return the junction-lane attenuation); s02n15 encodes the boundary as positive act
        (insects reach the junction-lane edge). SOFT-WATCH (2) honored at the bones level.

  Axis-Δ math unchanged:
    relational_anchor_status: +1.0 at s02n13 (was flat_id 17). Target: +1.0. EXACT.
    moral_legibility_to_self: +1.0 at s03n11 (was flat_id 24). Target: +0.5; within ±1
      tolerance confirmed at original bone-gate (magnitude raised from 0.5 to 1.0 floor).
    All held axes (capability, moral_framework, social_tether-prot-rise,
      political_register-prot) remain held throughout.

  No new axis-movers introduced. No dialogue bones (Taylor alone in this chapter). No
  motivation/interiority in SVO lines — all causes encoded in axes_held rationale blocks,
  not in the svo field.

---

## Fixer pass log — write-b01c02-pass2-revise — 2026-05-26

All 7 FAULT-FORM findings from audit write-b01-c02-pass2-revise.md resolved. All fixes pre-applied
in the Phase 1 revise draft; this pass confirms SVOs, updates notes/aggregate_summary descriptors
to match, and appends this log. No bone was inserted or deleted; count remains 47.

| fault | bone | before svo | after svo | rationale |
|---|---|---|---|---|
| fault-001 | s01n04 | "the fever-cluster returns three heat-signatures without a common room" | "the fever-cluster returns three heat-signatures across two alleys" | negation PP "without a common room" replaced by positive return encoding ambiguity (three signatures spread across two alleys = unresolved fever-source); no negation, no PP |
| fault-002 | s01n10 | "the feed taxes the peripheral attention" | "the alley-back drops from the feed" | abstract-as-object + adjective-modifier double fault; recast to physical feed-zone dropout: subject = alley-back (physical place), verb = drops, object = the feed (physical substrate); suppression cost grounded physically |
| fault-003 | s02n06 | "taylor-hebert-kl-122ac takes the drain angle at the grey hour" | "taylor-hebert-kl-122ac takes the drain angle" | time-PP "at the grey hour" stripped; grey-hour signal routed to loc-state citation per schema; bare SVO correct |
| fault-004 | s02n14 | "the insects return the attenuated signal at the junction-lane boundary" | "the insects return the junction-lane attenuation" | place-PP "at the junction-lane boundary" removed; spatial identity folded into compound-noun object "junction-lane attenuation"; SVO-clean |
| fault-005 | s02n15 | "the coverage map holds the junction-lane void" | "the insects reach the junction-lane edge" | unlicensed holds + void-as-abstraction double fault; recast: physical actor (the insects), physical verb (reach), physical object (the junction-lane edge); absence encoded by what actors do (reach a boundary), not by abstract object (void) |
| fault-006 | s03n03 | "the drain angle fills with shadow" | "the shadow fills the drain angle" | PP instrument "with shadow" removed by subject-swap; shadow becomes subject, drain angle becomes direct object; clean SVO |
| fault-007 | s03n12 | "the feed holds the ward-junction void in the open count" | "taylor-hebert-kl-122ac holds the breath" | three violations (unlicensed holds, void-as-abstraction, PP modifier) resolved in one recast; holds licensed: body-part stillness-against-pressure; breath-hold is the physical stillness-act that enacts the SOFT-WATCH-1 holding-beat between recognition and suppression |
