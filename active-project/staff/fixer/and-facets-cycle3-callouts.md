# /and-facets b01c01 — Phase 5b cycle-2 → cycle-3 callouts

Cycle-2 post-arbitration aggregate: 9 ACCEPT (6 cleared 3-of-3 cycle-2 + 3 deferred-with-
attribution via pulp-enthusiast taste-arbiter ruling A) / 3 FAIL. Cycle 3 is the LAST
remediation budget before cap-burn per URI-FACETS-CAP-BURN-SEMANTICS A2. URI-FACETS-CYCLE-N-ADD
A3 applies strictly: cycle-3 ADDs that fail pre-validation are REFUSED; DELETE is the
fallback path.

Three facets carry to cycle-3 fixer: **interest-narrator** (content recast, 2 entries),
**vibes** (token + license trim, 5 entries), **dialogue-coll** (facet-license cross-anchor,
1 entry). All three are tractable; no high-risk ADDs required for #1 or #2. Callout C
(dialogue-coll) requires an ADD pre-validation OR a rubric-side ruling — see options.

The three deferred-with-attribution facets (memory mem:1, sensory shape-level,
dialogue-taylor taylor:2) ship as-is — arbiter ruling A holds, dissents on disk.

---

## Callout A — interest-narrator content recast (cycle-2 FAIL, dark-fantasy)

File: `active-project/theater/facets/interest-narrator-b01-c01.md`
Verdict source: `active-project/staff/audience/dark-fantasy-reader/interest-narrator-r2-verdict.md`

Two entries with unresolved content complaints (dark-fantasy: "two entries with unresolved
content complaints in a six-entry file is a revise trigger"). Both were raised cycle-1,
unaddressed in cycle-2 rewrites (cycle-2 fixed §Form / semicolon / "power" Earth-Bet but
did NOT change the content register on either). Cape-fic + worm-canon ACCEPT cycle-2.

### A1 — `[narrator:7] @20` — ledger-cost recast

Current text:
```
7 @20 the day closed under the count she had been running with nothing moved that needed not to be moved
```

Problem: reads as **ledger-satisfaction** (the rule held; competence confirmed; the count
ran; nothing moved that shouldn't have). For a hinge chapter whose substance lives in
held-discipline, the day-close NI fire should carry the specific weight of the prohibition's
labor — the **ledger-cost**, not the ledger-result.

Recast target (dark-fantasy direction): surface (a) the labor of the maintained prohibition,
(b) what almost moved, (c) the specific weight of what was held. Not "what didn't break" —
"what held under what." The cost-tracking channel (per the entry's channel-note header)
demands the COST register at day-close, not the AUDIT-RESULT register.

Hard constraints:
- Anchor stays @20 (the day-close chatter bone).
- Stay in cost-tracking / ledger-close channel (per file header annotation).
- Stay in Taylor-POV interior register (no author-annotation; no third-person commentary).
- Stay within the "one observation per fire" §Form rule (post-directive — semicolons + em-dashes
  now governed by base-card cadence, not by spine constraint; but no clause-stacking that
  registers as two observations).
- Do not violate the inverted-predicate cap (narrator:6 holds the single licensed instance).
- Preserve the structural function: day-close ledger fire (this is the chapter's denouement
  beat per G5 position-gate; must "hold live" not "archive").
- No new Earth-Bet category-nouns. No "power."

### A2 — `[narrator:5] @24` — interior-register-in-motion recast

Current text:
```
5 @24 observation-radius confirmed before she named it a radius; the circuit count was already in the entry before she decided not to enter it
```

Problem: reads as **author-annotation** on a Taylor behavior ("confirmed before she named
it" = description of the reflex from outside, not the reflex firing in the interior
register). The doubled before-she-knew-it structure amplifies the annotation register.

Rubric calibration anchor (per dark-fantasy citation, `rubric-narrator-interest.md`):
> she had already counted the four spans of dirt to the gate

Pre-calc surfaces as a **completed fact**, NOT as a **report on the fact's generation**.

Recast target: the pre-calc reflex fires in interior register **in motion** — as a completed
fact Taylor already has, not as an external description of the fact's generation. The two
parallel "before she named/decided" clauses both need to collapse into the form of the
completed-fact register.

Hard constraints:
- Anchor stays @24 (the assessment-runs-and-is-caught held beat).
- Stay in pre-calc surfacing channel (per file header annotation: "pre-calc surfaces as
  completed fact").
- Stay in Taylor-POV interior register; eliminate author-annotation phrasing.
- One observation per fire (post-directive §Form).
- Preserve the structural earning: the assessment is the scene's collision; her attention
  is here because the rule is under load. The held label signals something fired without
  permission. The CONTENT of the pre-calc (observation-radius + circuit count = the
  assessment of Wren-as-node) must remain readable.
- The radius / circuit-count subject matter is the right substance — the prose register
  is what needs to change. Match the calibration anchor's "she had already X-ed" form.

---

## Callout B — vibes token + license trim (cycle-2 FAIL, cape-fic + dark-fantasy)

File: `active-project/theater/facets/vibes-b01-c01.md`
Verdict sources:
- `active-project/staff/audience/cape-fic-reader/vibes-r2-verdict.md`
- `active-project/staff/audience/dark-fantasy-reader/vibes-r2-verdict.md`

Five direct edits in one pass; no ADDs; no entry deletions. Worm-canon ACCEPT cycle-2.

### B1 — `[vibes:2] @6` — `licensed-by` chain trim (cape-fic)

Current `licensed-by`: `state-update:1, state-update:2, proto:6`

Problem: `state-update:2` is time-of-day null→morning. A tallow-smoke king's-landing
register extension does not license off the time-of-day initialization (the smell is not
morning-specific; the quality is not conditional on the hour). Downstream operators cannot
determine which source is doing licensing work vs which is freight — tactical coherence
failure on an information-asymmetry-core project.

Fix: remove `state-update:2` from the `licensed-by` chain. Keep `state-update:1` (location
arrival) + `proto:6` (sensory confirmation at the stall). Operative licenses only.

Convergence: auditor fault-017 (TASTE-FLAG: vibes:1/2 shared-license advisory) — direct
overlap.

### B2 — `[vibes:5] @13` — provenance → disposition token (dark-fantasy)

Current token chain:
```
[every-warm-body-in-the-block-legible, density-at-level-that-used-to-mean-work, confirmed-on-screen-b01c01]
```

Problem: `confirmed-on-screen-b01c01` is provenance-as-vibe. Gate-6 failure (per
`rubric-vibes.md` and `rubric-vibes-v1.1-patch.md`): the `keyword: [tokens]` must imply
**downstream operator behavior**. No dialogue-writer or feeling-fork can act on "this was
confirmed in episode 1." The two flanking tokens (`every-warm-body...`, `density-at-level...`)
are functional — they carry the suppression-overhead as ongoing cost. The third token is
dead weight in the operator-facing record.

Fix: replace `confirmed-on-screen-b01c01` with a disposition token encoding the **durable
behavioral consequence of carrying idle-surveillance-overhead** (a full-block read that
demands no response — the cost of watching without acting as its own suppression-texture).
Token must encode operator behavior: how does a downstream feeling-fork or dialogue-writer
select differently knowing this disposition is active?

Hard constraints:
- Token form: hyphenated lowercase phrase per existing token convention in the file.
- Must imply at least one specific downstream-operator decision (e.g., a feeling-fork
  selecting a particular somatic register; a dialogue-writer governing register of
  reference to ambient density).
- No proper nouns. No Earth-Bet hard-fence terms.
- Length comparable to flanking tokens (the kind of mid-length compound phrase already
  used).

### B3 — `[vibes:7] @26` — TWO provenance → disposition tokens (dark-fantasy)

Current token chain:
```
[first-on-screen-naming-of-what-she-saw, reported-before-interpreted, the-flies-report-as-demonstration]
```

Problem: `first-on-screen-naming-of-what-she-saw` and `the-flies-report-as-demonstration`
are both provenance-as-vibe (gate-6 failures). The middle token `reported-before-interpreted`
IS functional (tells a dialogue-writer that Wren names what she sees before she evaluates it;
governs voice register, timing, register of silence) — that one survives.

Fix: REPLACE both provenance tokens with disposition tokens encoding Wren's
**observation-habit as durable behavioral disposition**: what she notices, in what order,
with what withheld. Keep `reported-before-interpreted` as the middle anchor of the chain.

Hard constraints:
- Token #1 (replacing `first-on-screen-naming-of-what-she-saw`): the dispositional shape
  of WHAT Wren notices preferentially — the noticing-class that defines her observation
  habit.
- Token #3 (replacing `the-flies-report-as-demonstration`): the dispositional shape of
  HOW she reports — what she renders aloud about what she has seen vs what she keeps
  back (without it being the silence; the silence is vibes:8's job).
- Both must imply distinct downstream operator behaviors (not the same disposition twice
  in different words).
- Hyphenated lowercase; comparable length to retained token.

### B4 — `[vibes:8] @26` — provenance → disposition token (dark-fantasy)

Current token chain:
```
[named-observation-did-not-ask-the-question, the-follow-up-withheld-on-screen, first-confirmed-shape-of-the-mutual-silence]
```

Problem: `the-follow-up-withheld-on-screen` is provenance-as-vibe (gate-6 failure). The
viable token is `first-confirmed-shape-of-the-mutual-silence` (encodes relational
disposition), but that one ALSO carries provenance-flavor ("first-confirmed") — verify
whether it actually encodes operator behavior or also needs strengthening.

Fix: REPLACE `the-follow-up-withheld-on-screen` with a disposition token encoding the
**dispositional shape of the mutual silence**: what will not be asked, what will not be
explained — the rule of the relationship going forward. The mutual silence is the
relationship-shape that downstream operators in subsequent chapters must inherit.

Hard constraints:
- Encodes a rule-of-the-relationship-going-forward (what governs subsequent Taylor↔Wren
  dynamics).
- If `first-confirmed-shape-of-the-mutual-silence` reads as also-provenance, consider a
  light revision (drop `first-confirmed`, keep `shape-of-the-mutual-silence`-class
  encoding) — judgment call; do not force a rewrite if the token already implies
  operator behavior.
- Hyphenated lowercase; comparable length.

### B5 — `[vibes:9] @27` — provenance → disposition token (dark-fantasy)

Current token chain:
```
[the-flies-report-landed, the-anomaly-confirmed-on-screen, holds-the-eyes-does-not-file]
```

Problem: `the-anomaly-confirmed-on-screen` is provenance-as-vibe at the chapter's PAYLOAD
beat (gate-6 failure). The flanking tokens are load-bearing — `the-flies-report-landed`
encodes the relational event; `holds-the-eyes-does-not-file` is the operative dispositional
verb (Taylor's choice not to administer Wren as a node). The provenance token sits between
two functional tokens at the densest beat in the chapter.

Fix: REPLACE `the-anomaly-confirmed-on-screen` with a disposition token encoding the
**consequence of not-filing** (the gap in the ledger; the relationship that is not
administered; the cost that accumulates outside the accounting). This is the chapter's
load-bearing relational disposition — the vibe-token must carry it as a durable shape
that subsequent episodes inherit.

Hard constraints:
- Encodes Taylor's relational-administration-gap as durable disposition
  (unadministered-relationship-as-accumulating-cost; the not-filed entry that does not
  go away because it was not filed).
- Hyphenated lowercase; comparable length to flanking tokens.
- This is the most load-bearing of the four token replacements — the densest beat,
  the chapter's payload. Care.

---

## Callout C — dialogue-coll facet-license cross-anchor (cycle-2 FAIL, dark-fantasy)

File: `active-project/theater/dialogue/coll-net-mender-flea-bottom.md`
Sidecar: `active-project/staff/dialogue-writer/coll-net-mender-flea-bottom.drafts.md`
Verdict source: `active-project/staff/audience/dark-fantasy-reader/dialogue-coll-net-mender-flea-bottom-r2-verdict.md`

### C0 — Path-error dismissal (orchestrator note)

Dark-fantasy's Seam 2 (behavior-card absence at `cards/dialects/coll-net-mender-flea-bottom.card.md`
and `cards/personas/coll-net-mender-flea-bottom.card.md`) is **DISMISSED** as path-search
error. The card EXISTS at `active-project/actors/coll-net-mender-flea-bottom/card.md` per
the `/and-cast` Phase 4 provisioning convention (`active-project/actors/<slug>/card.md`).
The Q1 affirmative-demonstration §-section verifiability concern dissolves once the correct
path is read; the §Voice citations in the sidecar are verifiable against the real card.

The fixer's action on C0: confirm the card's existence (read it; quote the relevant §Voice
sections), then **record the dismissal in the fixer log** with the correct path and a brief
note that the Q1 surface-verifiability axis closes once card is read at the active-project
path. **No file edit required.**

### C1 — Facet-license cross-anchor at @8 (standing structural finding)

The standing structural finding STANDS. The sidecar's `Facet-licenses:` field cites
`exposition:2 @4` as the on-disk locked-graph fact licensing Coll's "offer work without
asking" card-signature. Cite-index walk for @8 (`coll-net-mender-flea-bottom speaks to
taylor-hebert-kl-122ac`) returns only `[coll-net-mender-flea-bottom:1]` — no co-located
lens facets at the speaking anchor. The rubric's CONSTRAINT § citation-completeness
(URI-FACETS-CYCLE-1 promotion) classifies cross-anchor non-resolving citation as HARD
per entry.

**This is a real structural fault.** The sidecar's R2 resolution note acknowledges the
cross-anchor problem and explicitly tries to defend it (`exposition:2 @4 is the same-scene,
on-card license`) but the rubric language is unambiguous: cite-index walk must resolve at
the citation's anchor. The fixer must choose between two paths:

#### C1 Path (a) — ADD a lens facet at @8 (cycle-3 ADD pre-validation REQUIRED)

The speaking bone is:
```
8 coll-net-mender-flea-bottom speaks to taylor-hebert-kl-122ac
```

Candidate lens fires at @8 (the fixer must pre-validate before commit per A3):

- **state-updates** — at @8 a new state field is created or updated for one of the
  actors (Coll: relationship-to-Taylor field shifts null → offer-extended; OR Taylor:
  work-status field shifts arriving → offered-work). State-updates rubric check: must
  use `<target>.<field>: <old> → <new>` form per `schemas/facet.schema.md`; target is an
  actor slug; per-character state-updates author is the impersonator. **Cross-facet
  contract check:** if the actor-state update is for Taylor and Taylor is POV, this
  REQUIRES a co-cited narrator-interest fire at @8 per RUBRIC-FIDELITY (c) — which would
  then ALSO require an NI ADD at @8 (chain pre-validation). If for Coll (non-POV), the
  NI co-cite contract does not apply. Coll's state update is the safer ADD candidate.

- **feeling-coll** — at @8 Coll's somatic-tell during the offer (rubric: per-character
  per-scene cap ≤1; multi-justification ≥3 of 5; sparsity 2-5%; forbidden vocab; somatic
  tells only, no named feelings). Check feeling-coll-net-mender-flea-bottom.md for any
  existing scene-A entries (cap check). Feeling is a high-care ADD — adversarial review
  would scrutinize the somatic justification.

- **exposition** — ADD at @8 would gloss something at the speech anchor that wasn't
  glossed at @4. Possible candidates: glossing the offer-as-Flea-Bottom-courtesy register
  (Coll's "asking if you can hold a needle" as the local stranger-vouching protocol). Must
  pass exposition rubric: license-completeness, fire-rule, source-traceability, no
  re-gloss against `staff/exposition-author/glossed-terms.md`. The exposition author also
  cannot ADD on audience-side push without an audience-flagged ADD (this is structurally
  an audit-side push, not audience-flagged) — verify the routing path is legal.

- **metaphor** — default-refuse on bones outside hinge-magnitude band (AP7). @8 is the
  cost-bearer establishment beat; check substance_delta to see whether @8 carries a
  hinge-magnitude axis_move. Probably NOT — the chapter hinge is in scene C. Metaphor
  ADD here would likely fail AP7.

- **narrator-interest** — only fires for POV (Taylor). At @8 Taylor receives the offer.
  NI ADD candidate: Taylor's cost-tracking ledger registers the social-cover-as-arrived
  fact. But NI cap is 15-25% (current: 6/27 = 22%); ADD would bring 7/27 = 26% — over
  band. Likely refused on FREQUENCY-BAND.

- **vibes** — fixer should NOT ADD to vibes during Callout B's edit pass (vibes is
  showrunner-authored; cycle-3 ADDs require pre-validation; vibes already has 10 entries
  at @1-@27 establishing operator-facing bias). Risky.

- **memory** — POV-only; mem cap 5-12% (current: 2/27 = 7.4%); @8 is not a callback
  beat. Likely fails.

**Recommended pre-validation ordering** (fixer's call): start with **state-updates Coll
(relationship-to-Taylor: null → offer-extended)** — simplest ADD, non-POV avoids NI
co-cite chain, anchors the speech-act licensing structurally. If that pre-validates
clean, the dialogue sidecar's `Facet-licenses:` updates to cite the new `state:NEW @8`
entry alongside (or replacing) `exposition:2 @4`. If state-updates pre-validation fails,
fall back to Path (b).

**A3 cycle-3 ADD pre-validation discipline:** load `rubric-state-updates.md` (or the
relevant facet rubric for whichever ADD candidate is chosen); walk every REJECT signature,
anti-pattern, file-level shape gate, and cross-facet co-citation requirement; verify the
ADD does not violate any. If pre-validation fails, do NOT commit the ADD — fall back to
Path (b) OR recommend DELETE-on-cycle-3 of the dialogue entry.

#### C1 Path (b) — rubric-side ruling permitting upstream-same-scene cross-anchor (out-of-band)

The rubric language (`design/shoot-v2/r2-judge-tuning/B-locked-rubric.md` G2/G5 and
`rubric-dialogue.md` CONSTRAINT § citation-completeness, URI-FACETS-CYCLE-1 promotion):

Path (b) would propose a rubric edit explicitly permitting an upstream-same-scene
cross-anchor citation when (i) the cited facet entry is in the same scene as the speech-act
(per scene-map), (ii) the citation is upstream (earlier flat_id, not later), (iii) no
co-located lens facet exists at the speech anchor.

This is an OUT-OF-BAND INTERVENTION — a rubric-authority edit changes the cycle-3
mechanical landscape (RUBRIC-FIDELITY auto-promotion picks it up). It would require
margit / authoring-staff review of the dialogue rubric, which is heavier than a fixer
pass.

**Recommended:** the fixer should NOT take Path (b) directly. If Path (a) ADD pre-validation
fails, the fixer should recommend DELETE-on-cycle-3 (the dialogue entry is deleted; the
chapter goes one speech-bone underdialogued; cap-burn report records the trade-off) AND
flag the rubric-edit option as a follow-on issue for separate authoring-staff resolution.
This is the safer disposition under cycle-3 budget constraints.

---

## Fixer log requirements

Per `.claude/commands/and-facets.md` § "Cycle-N ADD pre-validation (URI-FACETS-CYCLE-N-ADD
A3)", the fixer must log every ADD attempt + pre-validation result to:
```
active-project/staff/fixer/and-facets-cycle3-fixes.md
```
(Note: stale c02-cycle3 files at this path were archived to
`active-project/staff/fixer/_archive/c02-cycle3-leftovers/` before this dispatch — the
canonical cycle3 log path is clear for the c01 cycle-3 run.)

ADD verdict markers: `ADD-LANDED` / `ADD-LANDED-AFTER-UPSTREAM-EDIT` / `ADD-REFUSED-CYCLE-3`.

For Callouts A (interest-narrator) and B (vibes), the disposition will be `FIXED-DIRECT`
(content edits, no ADDs). For Callout C (dialogue-coll), the disposition will be one of:
`FIXED-VIA-ADD` (Path a succeeded) / `ADD-REFUSED-CYCLE-3` + `RECOMMENDED-DELETE-CYCLE-3`
(Path a failed pre-validation, DELETE fallback) / `DEFERRED-TO-RUBRIC-EDIT` (Path b
out-of-band flagged).

## Deferred-with-attribution facets (DO NOT TOUCH)

The following facets cleared via pulp-enthusiast taste-arbiter ruling A. **Do not dispatch
fixer work on these in cycle 3** — arbiter ruling holds, dissents stay on disk as
attribution trail:

- **memory** mem:1 @9 — dark-fantasy "is no longer doing" callout. Ship as-is.
- **sensory** shape-level — modality-coverage scene-C zero-fire. Ship as-is.
- **dialogue-taylor** taylor:2 @25 — dark-fantasy cost-show-at-anchor. Ship as-is.

Arbiter verdict file:
`active-project/staff/audience/pulp-enthusiast/orchestrator-judgment-cycle2-arbiter-verdict.md`
