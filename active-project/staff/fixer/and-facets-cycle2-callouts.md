# /and-facets b01c02 — Phase 5b cycle-1 consolidated audience callouts

Aggregation: 3-of-3 ACCEPT required per facet. metaphor PASSED 3-of-3. The other
10 targets failed cycle 1. Routes to fixer for remediation. Cycle 2 follows.

## Mechanical fixes (fixer applies directly)

1. **vibes:14 @20** — rename keyword `earning-collapse` → `wren-layer-actualization`
   (token bundle unchanged). All 3 vibes reviewers + dialogue-taylor reviewers:
   `earning-collapse` biases downstream operators toward a deflationary register at
   a crystallization beat. Edit theater/facets/vibes.md entry 14 keyword only.

2. **vibes:1 @5** — retarget token `routing-without-contact` from `actor:taylor-hebert-kl-122ac`
   (book-wide permanent) to `episode` scope. 3-of-3 vibes reviewers: a constraint-mode
   is not a permanent actor property; it will go stale as the deployment architecture
   evolves. Move the `routing-without-contact` token to an `episode + first-deployment`
   entry (or retire it from the actor bundle). Adjust theater/facets/vibes.md; if this
   changes the entry's target line, keep the @5 anchor + licensing chain.

3. **dialogue sidecars — facet-license resolution.** Update R1 `DEFERRED-TO-R2`
   placeholders to the R2-resolved concrete citations:
   - staff/dialogue-writer/wren-stitch-maker-flea-bottom-ward.drafts.md — Draft B chosen
     block `facet-licenses:` → `vibes:13 @19`
   - staff/dialogue-writer/taylor-hebert-kl-122ac.drafts.md — both chosen-mark blocks
     `facet-licenses:` → `vibes:14 @20`
   (worm-canon-pedant blocked both dialogue files solely on this stale-sidecar seam;
   no utterance content change.)

4. **exposition:4 @4** — final sentence "In Flea Bottom there is rarely anyone who can"
   → "In Flea Bottom, the city's poorest ward, there is rarely anyone who can."
   3-of-3 exposition reviewers: the gloss names the place without orienting a cold-join
   reader. Inline appositive; ~6 words; within first-mention-term cap. Edit
   theater/facets/exposition-b01-c02.md entry 4.
   ALSO confirm "the Hook" in exposition:1 @0 bridge text: if not on the b01c01
   glossed-terms register, add an inline appositive in the bridge text (no new entry).

## Prose-revision fixes (route to the facet author)

5. **feeling — route to feeling author (impersonator forks).** Two one-clause form
   violations:
   - feel:1 @28 (taylor): "closes short of the page and does not finish the reach" is
     two predicates; second clause subjects the abstraction-noun "the reach". Recast as
     ONE clause whose subject is Taylor's body, disclosing a withholding-cost vector not
     already given by proto-line "holds the hand" + narrator:6.
   - feel:2 @15 (wren): three sequential motion units / compound-complex sentence.
     Compress to ONE clause (subject = Wren's body) capturing the deliberate reanchoring
     ("stay"); drop the reflex eye-lead subordination.

6. **interest-narrator — route to NI author.** narrator:4 @15: (a) first clause ends on
   softener-tail preposition "about" ("a thing you have decided about") — terminate on
   "decided"; (b) surface the age-mismatch channel (adult interpersonal pattern-reading
   in an 11-year-old body) BEFORE the ledger frame, so it does not read as a third
   cost-tracking fire after narrator:3.

7. **location-state — route to studio.** loc-state:6 @6: sensory note names a relational
   quantity ("remaining clear ground between her and the sealed lane-mouths") — spatial
   arithmetic, not a perceptible focus-element. Reword to name a concrete perceptible
   thing Wren's arrival makes visible (her figure against the blocked exit; the sealed
   mouth from that angle), OR delete if loc-state:3 + loc-state:5 carry inherited coverage.

8. **state-updates — route to studio (env) + impersonator forks (actor slices).**
   - state:10 @12 (coll): resolve the hard-fence acknowledgement comment — name which
     fence is in question and whether deferred or honored; replace "substrate-level"/
     "registered-as-anomaly" cape-architecture vocabulary with a Westerosi-register
     value for a non-cape character's passive noticing.
   - state:12 @15 (taylor.social-tether-wren): `crystallized-observer-bond` overclaims
     the speed of bond formation after one encounter — revise to a more provisional
     attachment value.
   - state:13 @25 (taylor.knowledge): `categorical-structural` overclaims structural
     comprehension from a single sweep + one accounting — revise to a more provisional
     knowledge value (2-of-3 reviewers; worm-canon-pedant partially defended via Taylor's
     parallel-processing capacity — fixer adjudicates).
   - state:15 @15 (wren.taylor_awareness): `attachment-crystallized-deliberate-observer`
     strips the Westerosi uncanny/dread register a smallfolk child would carry after
     witnessing flies seal an alley — revise to encode dread alongside the rational track.

### 8b. sensory — route to studio (sensory author)

The sensory facet failed 2-of-3 specialists (modality-coverage ACCEPTED). The two
findings INTERACT — resolve together, do not fix in isolation:
- **sensory:1 @7** — old-state `watch-press-alley-ambient` is unanchored: no loc-state
  sensory/conditions note in loc-state:1–4 (@2–@6) establishes the ambient baseline.
  Fix: either backfill a loc-state conditions note naming the watch-press ambient sound,
  or revise the old-state to what loc-state actually establishes.
- **sensory:2 @22** — two grounds: (a) action-verb self-charge — proto-line "lights the
  lamp" IS the light-onset; the flag `light: unlit -> lamp-lit` doubles the verb;
  (b) old-state `unlit-lodging-interior` unanchored (no loc-state entry covers interior
  darkness before @22).
- **INTERACTION:** cutting sensory:2 outright drops the file to 1 entry / 1 modality —
  breaks the ≥2-modality floor that modality-coverage accepted. So the fixer must EITHER
  (i) anchor sensory:2's old-state via a loc-state backfill AND re-cut the entry so it
  fires on a non-self-charged perceptible light inflection (not the lamp-lighting verb
  itself), OR (ii) relocate the light-modality fire to a different scene-C bone with an
  anchored old-state. Any ADD/relocation must pre-validate per A3 (load the upstream
  loc-state anchor FIRST, then commit). Coordinate with the loc-state fix (item 7).

## Card-resolution fix (route to margit's workshop)

9. **memory mem:2 @25** — the peak-bone displacement-clamp exception depends on the
   monument `monument-conquest-charter-language` being a real behavior-pack family.
   worm-canon-pedant blocks: the exception's justification rests on an unresolved card.
   Route to margit: confirm or author the `monument-conquest-charter-language` card
   (mechanism-descriptive Westerosi-monument). Also `monument-cost-borne-by-the-unconsenting`
   (mem:1 target-reference) — same margit referral path (mem:1 itself is non-blocking,
   advisory, but resolve while in the workshop). If the conquest-charter monument cannot
   be confirmed, re-anchor mem:2 to a non-peak resolving-zone bone or cull it.

## ADD-pre-validation note (A3)

No facet ADDs are required by these callouts (all are reword / retarget / value-revision /
card-resolution). The exposition "the Hook" item is an in-gloss-text reword, NOT a new
numbered entry. If sensory remediation ends up needing an ADD, pre-validate per A3.
