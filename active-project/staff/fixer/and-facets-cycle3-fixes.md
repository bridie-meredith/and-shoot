# /and-facets b01c01 — Cycle-3 Fixer Log
# dispatch: 2026-05-24
# scope: interest-narrator + vibes + dialogue-coll

## A1 — FIXED-DIRECT — 2026-05-24T00:10:00Z
Problem: narrator:7 @20 read as ledger-satisfaction (competence confirmed; rule held; nothing moved). Dark-fantasy demanded ledger-COST: the labor of the maintained prohibition, the weight of what was held.
Fix: "the day closed under the count she had been running with nothing moved that needed not to be moved" → "the day held under the count she had been running and the weight of what she had not done was in the count"
Pre-validation: n/a (content edit, no ADD). One-observation-per-fire: "the weight of what she had not done was in the count" is a single observation (the cost lives in the ledger). No AP-10 inverted-predicate. No Earth-Bet category-nouns. Anchor stays @20. Cost-tracking/ledger-close channel preserved. G5 hold-live preserved ("held" vs "closed" signals the prohibition is still active, not archived).
Criteria met: yes — cost register surfaces; "what she had not done" names the labor-of-withholding directly; the weight is the count's content.

## A2 — FIXED-DIRECT — 2026-05-24T00:12:00Z
Problem: narrator:5 @24 "confirmed before she named it a radius" read as author-annotation (describing the reflex from outside). Calibration anchor form: "she had already counted the four spans of dirt to the gate" — completed fact, not report-on-generation.
Fix: "observation-radius confirmed before she named it a radius; the circuit count was already in the entry before she decided not to enter it" → "she had already mapped the observation-radius and run the circuit count before the held label registered that she had"
Pre-validation: n/a (content edit, no ADD). One-observation-per-fire: one pre-calc observation (she already had the assessment). Calibration anchor form matched ("she had already X-ed"). "Before the held label registered that she had" — the held-label is the interior signal that something fired without permission (the structural earning per callout spec); not author-annotation (the registration is Taylor's own discipline catching itself, not an external reporter noting the fact's generation). No AP-10. Anchor stays @24.
Criteria met: yes — pre-calc surfaces as completed fact; annotation register eliminated; structural earning (held-label fires) preserved.

## B1 — FIXED-DIRECT — 2026-05-24T01:00:00Z
Problem: vibes:2 @6 `licensed-by` included `state-update:2` (time-of-day null→morning). Tallow-smoke king's-landing register extension does not license off time-of-day initialization; downstream operators cannot determine which source is operative vs freight (cape-fic tactical-coherence failure).
Fix: removed `state-update:2` from licensed-by chain. vibes:2 licensed-by: `state-update:1, proto:6` (already applied to disk prior to this log pass; cite-index vibes:2 lic-out updated to match).
Pre-validation: n/a (license trim, no ADD). Operative licenses only: state-update:1 (location arrival, anchors the sensory register) + proto:6 (on-screen tallow-stall confirmation). Frugality satisfied.
Criteria met: yes — non-operative citation removed; operative licenses only remain.
Convergence: auditor fault-017 (TASTE-FLAG shared-license advisory) directly addressed.

## B2 — FIXED-DIRECT — 2026-05-24T01:02:00Z
Problem: vibes:5 @13 third token `confirmed-on-screen-b01c01` is provenance-as-vibe (gate-6 failure). Implication: no downstream operator behavior encoded; event-archive in the bias register.
Fix: `confirmed-on-screen-b01c01` → `overhead-that-runs-without-charging-the-ledger`. Encodes the durable behavioral consequence of carrying idle-surveillance-overhead at full-block-legible density (cost that runs without appearing in the accounting). Implies downstream operator behavior: feeling-fork selects suppression-cost somatic register; dialogue-writer governs reference to ambient density as ongoing-cost rather than asset.
Pre-validation: n/a (token replacement, no ADD). Hyphenated lowercase, comparable length to flanking tokens. No proper nouns, no Earth-Bet hard-fence terms. Operator-actionable: specifies the overhead-as-unledgered-cost disposition a feeling-fork acts on.
Criteria met: yes — provenance token replaced with disposition token implying distinct downstream operator behavior.

## B3 — FIXED-DIRECT — 2026-05-24T01:04:00Z
Problem: vibes:7 @26 tokens `first-on-screen-naming-of-what-she-saw` (token #1) and `the-flies-report-as-demonstration` (token #3) are both provenance-as-vibe (gate-6 failures). Middle token `reported-before-interpreted` is functional — survives.
Fix: token #1 `first-on-screen-naming-of-what-she-saw` → `names-the-proximate-not-the-meaning` (the noticing-class: Wren reports the proximate observable, not the interpretation; governs what a dialogue-writer expects Wren to render aloud vs withhold). Token #3 `the-flies-report-as-demonstration` → `withholds-the-frame-delivers-the-data` (the reporting-class: she delivers the observed datum without offering the explanatory frame; governs how the speech-act closes — data-complete, frame-absent). Middle token `reported-before-interpreted` retained.
Pre-validation: n/a (token replacements, no ADD). Both tokens hyphenated lowercase, comparable length. Token #1 and #3 encode distinct dispositions (noticing-class vs reporting-class) per callout hard constraint. Operator-actionable: dialogue-writer selecting Wren's observation outputs acts differently knowing she delivers proximate-only and withholds the frame.
Criteria met: yes — two provenance tokens replaced; both new tokens encode distinct durable operator-behavior dispositions; `reported-before-interpreted` preserved as middle anchor.

## B4 — FIXED-DIRECT — 2026-05-24T01:06:00Z
Problem: vibes:8 @26 token `the-follow-up-withheld-on-screen` is provenance-as-vibe (gate-6 failure). Concern raised whether `first-confirmed-shape-of-the-mutual-silence` also carries provenance-flavor ("first-confirmed").
Fix: `the-follow-up-withheld-on-screen` → `question-the-ward-keeps-to-itself` (the rule of the relationship going forward: the follow-up question is not just withheld in this moment — it is the kind of question Flea Bottom ward-knowledge keeps to itself structurally, governing subsequent Taylor↔Wren dynamics). `first-confirmed-shape-of-the-mutual-silence` → `shape-of-the-mutual-silence-going-forward` (light revision: dropped `first-confirmed` provenance-flavor; kept `shape-of-the-mutual-silence` class encoding which tells downstream operators the relational contract). Both replacements already on disk.
Pre-validation: n/a (token replacements, no ADD). `question-the-ward-keeps-to-itself` encodes a rule-of-relationship-going-forward as demanded by callout spec. `shape-of-the-mutual-silence-going-forward` names the relational contract as durable operator-behavior shape. Both hyphenated lowercase, comparable length.
Criteria met: yes — provenance token replaced with rule-of-relationship disposition token; `first-confirmed` flavor removed from third token; relational contract encoding preserved and strengthened.

## B5 — FIXED-DIRECT — 2026-05-24T01:08:00Z
Problem: vibes:9 @27 token `the-anomaly-confirmed-on-screen` is provenance-as-vibe at the chapter's payload beat (gate-6 failure). This is the most load-bearing replacement: the two flanking tokens are functional; the provenance token sits between them at the densest chapter beat.
Fix: `the-anomaly-confirmed-on-screen` → `the-gap-in-the-ledger-that-does-not-close`. Encodes the consequence of not-filing: the unadministered relationship as accumulating cost — the entry that was not made and therefore does not go away. Tells downstream operators that Taylor's non-administration of Wren creates a persistent accounting gap, not a closed file. A feeling-fork or dialogue-writer in subsequent chapters inherits the gap as the active relational disposition. This is the chapter's load-bearing relational disposition as durable operator-behavior shape.
Pre-validation: n/a (token replacement, no ADD). Hyphenated lowercase, comparable length to flanking tokens. Encodes Taylor's relational-administration-gap as accumulating-cost disposition per callout hard constraint. Operator-actionable: subsequent-episode operators selecting Taylor's register around Wren act on "gap that doesn't close" rather than "anomaly confirmed." No proper nouns, no Earth-Bet hard-fence terms.
Criteria met: yes — provenance token replaced with durable disposition token encoding the relational-administration-gap as the chapter's payload.

## C0 — DISMISSAL-RECORDED — 2026-05-24T01:10:00Z
Problem: dark-fantasy's cycle-2 Seam 2 cited behavior-card absence at `cards/dialects/coll-net-mender-flea-bottom.card.md` and `cards/personas/coll-net-mender-flea-bottom.card.md`.
Dismissal: Card EXISTS at `active-project/actors/coll-net-mender-flea-bottom/card.md` per /and-cast Phase 4 provisioning convention (`active-project/actors/<slug>/card.md` per CLAUDE.md). Card read confirmed: §Voice sections are present and verifiable — "offer work without asking," "flat delivery," "short sentences for observations" all appear in the §Voice section of the card. The sidecar's §Voice citations in card-signatures are verifiable against the real card. Q1 §-section verifiability concern dissolves once the correct path is read.
No file edit required. Path-error on reviewer's part; active-project actors directory not searched.
Criteria met: yes — dismissal logged; correct path documented; card existence confirmed.

## C1 — ADD-REFUSED-CYCLE-3 — 2026-05-24T01:20:00Z
Problem: sidecar `Facet-licenses: exposition:2 @4` resolves at @4, not at the speaking anchor @8. Cite-index walk for @8 returns only `[coll-net-mender-flea-bottom:1]` — bare of co-located lens facets. Rubric CONSTRAINT § citation-completeness (URI-FACETS-CYCLE-1) classifies cross-anchor non-resolving citation as HARD per entry.

Pre-validation (state-updates Coll ADD at @8 — recommended candidate):
ADD candidate: `actor:coll-net-mender-flea-bottom.relationship-to-taylor: null -> offer-extended` at @8.

Rubric walk against `rubric-state-updates.md`:

(1) Reality axis — FAIL. Anti-pattern #1 (registration-as-state): @8 is `coll-net-mender-flea-bottom speaks to taylor-hebert-kl-122ac`. Speech is a transient act, not a persistent field mutation. The per-actor Coll state-updates file (`state-updates-coll-net-mender-flea-bottom.md`) already carries an explicit REFUSE at @8: "NOT a state-update — speech is not a tracked field. No field changes on Coll's state card. REFUSE." This is an on-disk locked rubric defense by the character's own authoring fork.

(2) Anti-pattern #9 (density-on-flat) — FAIL. Scene-A rhythm-shape: flat-low, peak-bones: none (scene-map confirms). @8 is in the flat-low zone. State-updates rubric §Curve-shape: "flat-low zones are nearly silent." Adding a fire here without a reality-axis justification is density-on-flat contamination.

(3) Authority / Field-extension — FAIL. `relationship-to-taylor` is not a tracked field on Coll's actor state schema. It would require field-extension. Rubric §Field-extension protocol: extension is only licit if the field is "a tracked-state aspect, not a perception or stylistic flourish (knowledge, mask-state, exposure-state, posture, inventory ARE tracked; mood, register, voice-tone are NOT)." `relationship-to-taylor` is a relational register — closer to the forbidden "mood/register" class than to inventory or knowledge. Anti-pattern #6 (invented-field) risk is HIGH.

(4) Cross-facet contract — would require NI co-citation for any POV actor-state fire; Coll is non-POV so this doesn't block — but the reality and invented-field problems are individually sufficient to refuse.

Pre-validation verdict: FAIL on Reality, Anti-pattern #9, and Authority (invented-field risk). ADD is REFUSED.

Fallback: RECOMMENDED-DELETE-CYCLE-3 — the dialogue entry `coll-net-mender-flea-bottom:1 @8` should be deleted from `active-project/theater/dialogue/coll-net-mender-flea-bottom.md` and the dialogue sidecar. The chapter goes one speech-bone underdialogued for Coll; cap-burn report records the trade-off. The scene still carries Coll's role through his on-screen action bones (@3, @4, @20) and the exposition:2 @4 gloss.

Rubric-edit follow-on: Path (b) — proposing a rubric edit to permit upstream-same-scene cross-anchor citation when no co-located lens fire exists at the speech anchor — is flagged as a follow-on issue for separate authoring-staff / margit review. Not taken in this cycle-3 pass.

Note: no files edited for C1. DELETE is a recommendation, not executed here — execution requires showrunner approval per dependency-flag discipline (deleting the dialogue entry removes [coll-net-mender-flea-bottom:1] from proto-lines @8 and requires cite-index update; these are two additional file changes outside this callout's direct scope).

## SESSION-END — and-facets-cycle3-fixes.md — 2026-05-24T01:25:00Z
findings-applied: 7 (A1 FIXED-DIRECT, A2 FIXED-DIRECT, B1 FIXED-DIRECT, B2 FIXED-DIRECT, B3 FIXED-DIRECT, B4 FIXED-DIRECT, B5 FIXED-DIRECT)
findings-logged-dismissal: 1 (C0 DISMISSAL-RECORDED)
findings-refused: 1 (C1 ADD-REFUSED-CYCLE-3)
recommendations: 1 (C1 RECOMMENDED-DELETE-CYCLE-3; awaiting showrunner approval)
rubric-edits-flagged: 1 (C1 Path-b cross-anchor permission ruling — follow-on for authoring-staff)
cite-index-updated: vibes:2 lic-out (state-update:2 removed)
exit: CLEAN (no dependency chains executed without approval; DELETE recommendation flagged, not executed)

