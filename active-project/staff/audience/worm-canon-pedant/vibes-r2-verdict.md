---
reviewer: worm-canon-pedant
facet: vibes
cycle: 2
episode: b01c02
date: 2026-05-21
verdict: accept
---

# Verdict reasoning

Two items from cycle-1 tally. Checking both directly. Then running the file clean.

**vibes:14 — keyword fix verified.**

Entry 14 now reads `actor:taylor-hebert-kl-122ac ++ wren-layer-actualization: [speech-exchange-as-the-first-actualized-wren-layer, the-moment-the-un-priced-speaks-back]`.

The keyword `earning-collapse` is gone. My cycle-1 concern was specific: Taylor's voice register at crystallization-and-redirect beats is not deflationary in canon. When a relationship she has been observing from distance becomes operational — when the unpriced speaks back — her interior register is precise acknowledgment followed by recalibration, not collapse. `earning-collapse` was biasing the dialogue-writer fork toward a diminishment register at exactly the moment the canon register is redirect-and-absorption. That is the specific misdirection.

`wren-layer-actualization` corrects this. The keyword indexes the event as actualization — a layer becoming real. The dialogue-writer reading this keyword before drafting @20 will set voice register toward Taylor absorbing a new operational constraint, which is her canonical mode at moments like this. The @20 speech lines ("The watch came through. They did not stop. That is all it was." / "Go home, Wren. The street is quiet now.") read as redirection-while-absorbing, not as retreat. The keyword now supports that reading instead of working against it.

One tally item: `licensed-by:` still carries `world-build:earning-collapse-three-mistakes`. The world-build gloss slug uses the discarded keyword name. Formally this is not a gate issue — the gloss is a named context pointer, and it resolves to the world-build document regardless of what the vibe keyword says. Functionally: a dialogue-writer fork that follows the `licensed-by` chain before reading the keyword will encounter `earning-collapse` in the gloss name and may carry that register into generation. This is a soft mechanics-encoding concern, not a formal fault in the facet under review. Advisory only. The world-build gloss should be renamed at next maintenance pass, but this facet entry does not require revision on that basis alone.

**vibes:1 — target fix verified.**

Entry 1 now reads `episode + first-deployment-routing-mode: [routing-without-contact, first-deployment-constraint-intact, the-lane-that-closes-as-tool-not-command]`.

The actor-level placement was the fault. My cycle-1 framing: actor-level vibes describe capability properties; constraint-imposed deployment modes are not capability properties; `routing-without-contact` on actor:taylor conflates the constraint with the capability. Taylor's power is full insect-control with tactile feedback and kinesthetic awareness. Routing is a constraint-mode, not an intrinsic power property. Permanently encoding it at actor level was a mechanics-encoding error — three chapters from now, a dialogue-writer reading that actor-level vibe might write Taylor describing her power as if contact-avoidance is intrinsic rather than situationally imposed.

That conflation is now corrected. `routing-without-contact` sits on episode scope, correctly framing b01c02's first-deployment mode as an episode-ambient quality. Taylor's actor-level carries `override-architecture-residue` (entry 2), `insects` extension (entry 2 carries the `++` on that keyword, with `the-constraint-tested-not-broken` and `rhyme-with-prior-architecture-made-visible-in-action`), and `wren-layer` extensions (entries 7, 14). These are correct actor-level marks: they describe Taylor's capability relationship with prior override architecture, and her operational relationship with Wren. None of them conflate constraint-mode with capability property.

**Full-file fresh read:**

One new item I am checking: entries 1 and 4 are now both on episode scope at @5. Entry 1 keyword is `first-deployment-routing-mode`; entry 4 keyword is `first-deployment`. These are semantically adjacent. The rubric AP11 governs `++` token-overlap on a single keyword — it does not govern two separate `+` entries on different keywords. So AP11 does not apply. What I am checking is whether the dual episode-scope fire from the same event is mechanically justified. Entry 1 tokens: `routing-without-contact, first-deployment-constraint-intact, the-lane-that-closes-as-tool-not-command` — mode-description signals. Entry 4 tokens: `prohibition-tested-in-scene, routing-not-override-confirmed, the-constraint-holds-at-cost` — arc-confirmation signals. These are distinct signal categories. A downstream operator reading both episode vibes gets mode-description plus arc-confirmation — layered, not duplicated. No mechanics error.

The rest of the file was accepted in cycle-1 by my tally. I am running it again without deference to that prior tally. Entries 2-3 (Taylor's override-architecture-residue and flea-bottom's operational-substrate): both correct. Entry 5-7 (Wren crystallization arc): all `++` ops on pre-existing keywords; `anomaly-perceived-without-vocabulary` is correct ignorance-played-correctly encoding for a Flea Bottom ward; `trust-holds-after-direct-encounter` is correct encoding for trust formed by direct evidence rather than given cheaply; `wren-layer-from-theoretical-to-operational` tracks with how Taylor codes interpersonal relationships as assets with activation conditions — this is canonically Taylor. Entries 8-9 (near-witnesses): vocabulary-gap tokens correct for Westerosi witnesses who have no schema for Earth-Bet parahumans. Entry 10 (Coll never-names): behavioral encoding for structural differentiation. Entries 11-12 (ledger): cost-accounting cluster canonically correct for Taylor's psychological register — she tracks costs, runs ledgers, identifies gaps. Entry 13 (Wren silence breaking): `voice-used-once-in-the-scene-that-uses-insects` — constraint noted correctly.

The tally is clean. Both flagged items resolved. No new mechanics errors introduced by the fix. Voice register on entry 14 is now Taylor-compatible.

---

# Entry-level callouts

`[vibes:14] @20 — RESOLVED. keyword renamed to wren-layer-actualization; dialogue-writer voice-register bias corrected from deflationary to actualization-and-redirect. Residual advisory: licensed-by field carries world-build:earning-collapse-three-mistakes; gloss slug embeds discarded keyword; formally resolves (pointer is a named context reference, not a string match); soft mechanics-encoding concern for downstream authoring passes; not a revise trigger in this facet.`

`[vibes:1] @5 — RESOLVED. target moved from actor:taylor to episode scope; constraint-mode/capability-property conflation corrected. Actor:taylor now carries correct permanent marks (override-architecture-residue, wren-layer extensions). No capability-property conflation present. Dual episode-scope entries at @5 (vibes:1 first-deployment-routing-mode + vibes:4 first-deployment) are mechanically distinct: mode-description signals vs arc-confirmation signals; no AP11 issue (separate keywords, not ++ append); no mechanics error.`

---

# Convergence trace

- `[vibes:14] @20` — cycle-1 convergence with auditor flag-006 (TASTE-FLAG, TF-001). Auditor framed as keyword-ambiguity advisory. Worm-pedant framed as voice-register fault (dialogue-writer fork receives wrong canon register). Fix resolves both framings. Residual on world-build gloss slug (`earning-collapse-three-mistakes`) is new in cycle-2; not covered by any prior auditor finding; does not reactivate flag-006 (which was keyed to the keyword in the entry, now corrected).

- `[vibes:1] @5` — cycle-1 convergence with auditor flag-007 (PILE-UP-REVIEW, PU-001). Auditor framed as staleness concern (permanent target may go stale as deployment architecture evolves). Worm-pedant framed as mechanics-encoding error (constraint-mode conflated with capability-property). Fix resolves both framings. Dual episode-scope observation (vibes:1 + vibes:4) is new in cycle-2; not covered by prior findings; no revise trigger.
