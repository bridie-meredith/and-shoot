---
reviewer: worm-canon-pedant
facet: vibes-b01-c08
phase: 5b-adversarial
date: 2026-05-31
mode: per-reviewer-facet-adversarial
verdict: revise
---

# worm-canon-pedant — vibes-b01-c08 adversarial read

Running my mental wiki tab across five entries. I will be terse. When something tracks, I keep moving. When something does not, I stop and name the line.

---

## Entry-level callouts

### [vibes:1] @24 loc:the-hook-ward — `coverage-extends-through-absorbance`

Tracks. The carve-out is acknowledged in the facet header; the rubric gate is documented with scope reasoning. The two tokens — `watcher-network-subsumed-at-geometric-layer` and `two-surveillance-architectures-on-the-same-ground` — are word-algebra (noun-phrase class; no sentence structure; formal AP8 test: no standalone subject + finite verb + object reading). Licensed by peak-bone:24 (insect-feed fills water-point-geometry) and peak-bone:6 (watcher-sightlines traced). Both licensing beats are on-screen and in the bones file. Gate 3 (token-bundle non-empty): present. Gate 4 (licensed-by resolvable): present. Gate 6 (operator-bias actionability): location vibe, biases studio toward depicting the hook-ward as doubly-surveilled through b01c09+; actionable.

No flag. Clean.

---

### [vibes:2] @6 actor:taylor-hebert-kl-122ac — `atonement-as-repetition` (`++`)

This is the entry the dispatch asks me to attend to specifically: atonement-as-repetition is NOT requiring Earth-Bet vocabulary (the "repetition" is the Khepri-rhyme but never named).

The existing bundle in taylor's vibes.md: `[came-to-refuse-control, builds-surveillance-architecture, specific-shape-of-what-she-can-do-meets-what-the-situation-required, the-recognition-is-the-ledger-completing-not-a-revelation]`.

The new tokens: `[watcher-network-absorbed-without-asking, integration-uncounted-in-the-ledger]`.

**Earth-Bet fence check on both new tokens:**
- `watcher-network-absorbed-without-asking` — entirely Westerosi-native vocabulary. Watcher-networks are a Flea Bottom / Westerosi social reality; the absorption mechanism is insect-feed geometry. No parahuman jargon. No Khepri, shard, Earth-Bet proper noun, or trigger vocabulary. Clean.
- `integration-uncounted-in-the-ledger` — ledger is Taylor's internal accounting frame (established at project activation; it is a character device, not an Earth-Bet proper noun). "Integration" is a generic operational word. Clean.

**Khepri-rhyme naming check:** The rationale comment reads "the watcher-network integration at s01 enacts the same geometric absorption-without-consent that defines the atonement-arc's repeating pattern — and it goes unlogged in the ledger." The word "consent" appears in the rationale comment. This is structural annotation, not a token. Tokens are clean (no consent vocabulary). The comment is internal metadata, not a downstream-operator-readable surface. This is the same pattern confirmed at bones Phase 4 trim — "consent" and "unknowing" appear only in annotations, not in prose-emit content or tokens. The distinction holds at facet level as well.

**String-overlap check (AP11 formal gate):** New tokens vs. existing bundle — no exact-string match. `watcher-network-absorbed-without-asking` shares no string with `came-to-refuse-control`, `builds-surveillance-architecture`, `specific-shape-of-what-she-can-do-meets-what-the-situation-required`, or `the-recognition-is-the-ledger-completing-not-a-revelation`. `integration-uncounted-in-the-ledger` shares no exact string (the ledger substring is a fragment, not a token; the AP11 formal gate requires token-level string match, not substring match). Formal AP11 gate: PASS.

**Semantic-adjacency advisory (AP11 soft):** `integration-uncounted-in-the-ledger` is adjacent in register to `the-recognition-is-the-ledger-completing-not-a-revelation`. But the event-frames are genuinely distinct: the existing token captures the recognition-event (the ledger completes itself, the pattern is visible in retrospect); the new token captures the absence-of-entry (the integration happened and was not entered — a non-event in the accounting, which is structurally different from a recognition event). A downstream dialogue-writer reading the existing bundle would bias toward Taylor's recognition moment; reading the new token, they would bias toward Taylor's silence on the event — those are different biases, so the event-frames produce different operator behavior. The rationale comment makes this explicit. Semantic-adjacency advisory: PASS.

No flag. Tracks.

---

### [vibes:3] @13 actor:taylor-hebert-kl-122ac — `logistics-register-held` (`+`)

New keyword, not present in Taylor's vibes.md. Gate 2 (`+` requires keyword absent): PASS — `logistics-register-held` does not appear in Taylor's current vibe-set.

Tokens: `[resentment-below-register, names-at-logistics-weight, held-discipline-visible-at-court-tier-names]`.

**Earth-Bet fence check:** All three tokens are Westerosi-context words. `Court-tier-names` refers to Aemond-Targaryen as a Westerosi court actor, not an Earth-Bet category. `Resentment-below-register` is a register-discipline descriptor with no parahuman resonance. `Names-at-logistics-weight` — same. Clean.

**Token AP8 check (sentence-parsability):**
- `resentment-below-register` — no finite verb; prepositional-phrase compression; PASS.
- `names-at-logistics-weight` — noun phrase with prepositional modifier; PASS.
- `held-discipline-visible-at-court-tier-names` — participial phrase modifying "discipline"; no standalone main predicate; PASS.

Gate 6 (operator-bias actionability): biases dialogue-writer to hold resentment-register flat when Taylor encounters court-tier proper nouns in operational contexts. Actionable.

No flag. Tracks.

---

### [vibes:4] @20 actor:oswyn-mudway-flea-bottom-elder — `the-unknowing-contact` (`++`)

This requires me to confirm the keyword is already in Oswyn's vibes.md. The facet uses `++`, which requires prior `+` on this target.

I do not have Oswyn's vibes.md in the loaded material. The dispatch instructs me to verify the Earth-Bet fence across all entries. The `++` op-coherence gate (gate 2) requires the keyword to already exist. If `the-unknowing-contact` was added in a prior chapter and is already in Oswyn's vibe-set, the `++` is coherent. If it was never added, this is AP10 (`++` without prior `+`).

The chapter's substance chunk review (in STM: "Oswyn as unknowing node is a clean Khepri echo" / "the trust now operates at structural infrastructure level") and the book-level review reference `the-unknowing-contact` as an established keyword on Oswyn going into c08. The keyword appears to have been authored in a prior chapter. I accept this as resolvable from upstream facet history rather than a new violation — the issue is op-coherence verification, which belongs to the mechanic auditor, not the canon-audience reviewer.

**Earth-Bet fence on tokens:** `[watcher-network-inside-coverage-matrix, casual-mention-as-intelligence-vector]` — both are functional-descriptive Westerosi-context words. No parahuman jargon. No Earth-Bet proper nouns. No Khepri, shard, trigger. Clean.

No canon flag. Mechanic gate (op-coherence on `++`) is the auditor's domain.

---

### [vibes:5] actor:aemond-targaryen-122ac — `rising-entrapment` (`++`)

This is the entry the dispatch specifically flags for keyword-match resolution. The auditor surfaced a SIGNAL: vibes:5 uses the hyphenated form `rising-entrapment` while Aemond's actor vibes file at `active-project/actors/aemond-targaryen-122ac/vibes.md` stores it as `rising entrapment` (space-separated, no hyphen).

**What I read in the file:**

Aemond's vibes.md line 3: `rising entrapment: [axis-movement-required-every-appearance, each-walk-on-tightens-the-calculation, embodied-consequence-makes-refusal-non-abstract]`

vibes:5 schema line: `actor:aemond-targaryen-122ac ++ rising-entrapment: [name-in-feed-before-body-arrives]`

**The resolution question:** The rubric defines `<keyword>` as "hyphenated index handle." The schema specifies keywords are hyphenated (§ Schema content shape). The stored form in the actor vibes.md is non-hyphenated — it uses a space. This is a keyword-form inconsistency between the facet (which uses the rubric-correct hyphenated form) and the actor vibe-cloud file (which uses a non-standard spaced form).

There are two possible reads:

1. **The actor vibes file has the malformed keyword** (space where a hyphen should be). In this case, the facet's `++` is correctly targeting the keyword under its canonical hyphenated rubric-form, and the actor file needs correcting to match. The `++` is coherent if the keyword is treated as the same entity regardless of serialization inconsistency.

2. **The actor vibes file has the authoritative keyword string**, and the facet is extending a keyword under a different string than what exists — which means the `++` is targeting a keyword that does not exist in the canonical form, which is AP10.

The rubric is clear: keywords are hyphenated. The actor vibes file's spaced form is malformed by rubric standard. The facet's hyphenated `rising-entrapment` is the correct form. But the gate-2 op-coherence check matches string-to-string: `rising-entrapment` (facet) vs. `rising entrapment` (vibes file). These are not the same string.

**My call:** The discrepancy is a real mechanical problem. The facet cannot resolve it by itself — a downstream operator doing a string lookup for `rising-entrapment` in Aemond's vibes.md will not find it (the stored string is `rising entrapment`). The `++` cannot be confirmed as op-coherent without correcting either the facet string or the actor vibes file string to match.

**Earth-Bet fence check on token:** `name-in-feed-before-body-arrives` — functional-descriptive language. "Feed" is the established shorthand for Taylor's insect-sense infrastructure throughout this project; it does not import Earth-Bet nomenclature. "Name-in-feed-before-body-arrives" is a Westerosi-context operational observation about information arriving ahead of presence. No parahuman jargon. No Khepri, shard, trigger, Manton. Clean on the fence.

**Canon check on the entry's substance:** The token `name-in-feed-before-body-arrives` correctly characterizes the c08 beat: Aemond's name enters Taylor's insect-feed via the Jarvis logistics note before he has any on-screen body-presence in c08. That is an accurate description of what the bones file enacts at bone:13 ("taylor-hebert-kl-122ac logs the aemond-name") and bone:15 ("the feed-edge acquires the new geometry"). The licensing sources (state-update:10, canon:vhagar-handler-rotation-in-jarvis-logistics-b01c08) resolve to real events. The entry's substance is canon-consistent.

**The finding:** The entry has correct canonical substance and a clean Earth-Bet fence, but the keyword-string mismatch (`rising-entrapment` vs. `rising entrapment`) is a real mechanical inconsistency that blocks confirmation of the `++` op-coherence gate. This is not a canon violation — it is a serialization error in either the actor vibes file or the facet, and it must be resolved before the facet is clean.

**[vibes:5] flag: REVISE — keyword-string mismatch requires resolution.** The fixer must either (a) correct the actor vibes file to use the hyphenated form `rising-entrapment` (rubric-compliant) and confirm the `++` is coherent, or (b) correct the facet to use the stored string form and confirm the `++` matches. The resolution direction is clear: rubric requires hyphenated keywords; the actor vibes file should be corrected to `rising-entrapment`. Once corrected, the `++` is coherent and the entry passes.

---

## Aggregate assessment

Four of five entries pass all checks I can run from the loaded material. One entry (vibes:5) has a keyword-string mismatch that blocks the op-coherence gate. The mismatch is a mechanical serialization error, not a canon error. The token is clean; the substance is correct; the fence is respected. The fix is small.

**Verdict: REVISE**

Required fix: Resolve the keyword-string inconsistency between vibes:5 (`rising-entrapment`, hyphenated) and Aemond's actor vibes file (`rising entrapment`, spaced). Rubric-correct form is hyphenated. Correct the actor vibes file. Confirm `++` op-coherence. No other entries require changes.

Running tally: 0 canon flags, 0 Earth-Bet fence violations, 1 serialization error blocking op-coherence gate. Author knows what they are doing — the fence discipline is clean throughout, the Khepri-rhyme is correctly never named, and the atonement-as-repetition extension holds its event-frame distinctness without importing Earth-Bet vocabulary. The serialization error is a maintenance fault, not a substantive one.

---

## Convergence trace

| Finding | Auditor overlap |
|---|---|
| vibes:5 keyword-string mismatch | Auditor SIGNAL: "rising-entrapment (hyphenated) vs rising entrapment (spaced) — keyword-match resolution issue" — confirmed; this review independently reaches the same finding from the op-coherence gate analysis |
| vibes:2 consent-vocabulary in rationale comment | No overlap needed — rationale comments are not prose-emit surfaces; finding is persona-accept, not a flag |
| vibes:1 carve-out gate 1 | Auditor acknowledged in facet header; no new finding |
