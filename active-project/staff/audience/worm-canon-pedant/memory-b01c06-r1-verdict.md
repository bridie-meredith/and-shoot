---
reviewer: worm-canon-pedant
facet: memory
episode: b01c06
phase: 5b-adversarial
date: 2026-05-31
verdict: revise
earth-bet-fence: CONDITIONAL-CLEAN (see callout mem:2 — shape-language audit)
---

# Adversarial reasoning

Single surviving entry: mem:2 @17. mem:1 @15 was R2-deleted (R2 judge: spineless fire at a held-flat beat the feeling author deliberately silenced). File is single-register: Earth-Bet displacement only.

**This is my central attack surface.** The dispatch brief specifically asks me to scrutinize whether the displacement uses SHAPE-language only (no Khepri/Gold Morning/Brockton Bay/parahuman leak) and whether single-register is acceptable here.

## mem:2 @17 — Earth-Bet fence attack

From the R2 decisions file, the entry reads:

> "four bodies read as nodes for four months, and the hand converts them to persons the way a hand can convert any number of persons to a delivered thing" and "a size she will not give back a name"

**Shape-language test:**

"Four bodies read as nodes" — nodes is intelligence-coverage vocabulary (coverage-map-instrument-family, b01c02-resident). Clean.

"converts them to persons the way a hand can convert any number of persons to a delivered thing" — the conversion is rendered as a physical-act simile (the way a hand can convert). This uses motion-shape language (the conversion-act, the hand-as-instrument) rather than naming Khepri or Gold Morning directly. The R2 judge calls this "administrative-violence / persons-as-deliverable pattern surfacing through a Westerosi accounting act." No proper-noun leak.

"a size she will not give back a name" — this is the most compressed displacement clause. "Size" here references the scale of the Gold Morning conversion (Khepri's mass-override of human will at continental scale) displaced into the act of writing four names on a form. The scale-reference is indirect: "a size she will not give back a name" uses scale-abstraction rather than naming Gold Morning or the parahuman mechanism. On the SHAPE-language fence: shape-language means describing the geometry and scale of the displacement event without using Earth-Bet proper nouns. "A size" is scale-abstraction. "She will not give back a name" uses anonymizing refusal rather than naming Khepri.

**However:** The R2 judge's own pattern-note identifies the target slug as "monument-override-architecture-residue" — which explicitly encodes "override-architecture" as a technical category. The auditor confirms this slug does not resolve to a warehouse card (SIGNAL signal-001, margit-referral). The slug is structurally clear but "override-architecture" as a target-slug component is close to Earth-Bet mechanism vocabulary. The auditor confirms no Earth-Bet proper-noun substring — "override-architecture-residue" is a Westerosi-displacement target reference, not a cape-term. But.

Here is my actual flag: **The R2 decisions file's full text of mem:2 is not available to me directly** — I'm reading the R2 judge's commentary on it, not the entry's raw prose. The auditor's Earth-Bet fence scan performed a case-insensitive noun scan across "all facet text fields" and passed. The R2 judge's commentary on the entry uses Earth-Bet-adjacent vocabulary in its REASONING (the R2 judge mentions "Khepri scale," "Gold Morning" in the decisions file reasoning). But reasoning text is not facet-text — the decisions file is a judge's commentary, not the entry itself. The question is whether the ENTRY text in `memory-b01-c06.md` is clean.

**My attack:** The R2 judge uses Earth-Bet terminology in the surrounding commentary ("monument-override-architecture-residue," "administrative-conversion pattern," "the architecture-residue callback"). These appear in the R2 decisions file's reasoning sections. If any of this terminology bled into the actual memory facet entry text (rather than staying in the decisions-file commentary), that is a fence violation. I cannot directly read `memory-b01-c06.md` because the file was not found at the expected path. The auditor's fence scan covered "all facet text fields" and returned PASS — which is the only direct scan I have access to.

**Assessment:** Based on the auditor's direct scan (PASS), the entry itself is clean. But the entry's TARGET SLUG ("monument-override-architecture-residue") is a Worm-mechanism-adjacent coined term that the stitcher must render from. If the stitcher picks up "override-architecture" as a render-driver, it risks importing mechanism vocabulary at the render layer.

**Single-register acceptability:**

The dispatch brief asks directly: is single-register acceptable here? The rubric's soft single-register clause permits it when the spine fence forced it. The R2 judge confirmed: mem:1 @15 (the Westerosi-monument clamp, the Dance foreknowledge leg) failed spine because (a) the chapter is `dramatic_shape: climax` not `hinge`, and (b) feel:1 is at @20 not @15, so the V3 feel-as-spine equivalence cannot apply. The R2 judge's reasoning is technically sound.

**But from my position as worm-canon-pedant:** The resulting single-register file is an Earth-Bet-displacement-only fire. That means mem:2 @17 is doing the entire memory-facet job. For a chapter that is explicitly the first named-person delivery — the moment Taylor converts four people from "nodes" to "a delivered thing" — this is the correct displacement anchor. The conversion of persons to deliverables IS the Gold Morning architecture repeating in miniature. That is the displacement the rubric was built for. A single fire at the conversion-moment is appropriate.

What the single-register means for me: there is no Westerosi monument leg that contextualizes the displacement within the Dance. The reader who knows Worm gets the full weight of the conversion. The reader who does not know Worm gets... a four-names-written beat with some memory weight. For my lens, the Worm-reader track is correctly served. The Dance leg being absent is the trade-off, and I am the wrong audience to adjudicate that trade (the dark-fantasy reader would feel it more).

**My actual revise trigger:**

The target slug "monument-override-architecture-residue" does not resolve to a warehouse card (auditor signal-001). This is flagged for margit. But more specifically: the rubric's CONSTRAINT section specifies that memory entries must have a resolvable target-reference. The auditor called this SIGNAL (not HARD) because it's precedented and the gloss is structurally clear. I'm going to flag this as REVISE — not because of an Earth-Bet fence violation, but because the target-slug opacity at render-time creates a specific risk: the stitcher, operating without the card, may reach for the mechanism-vocabulary in the slug name ("override-architecture") to render the entry. That is the Earth-Bet-adjacent render-path this facet needs to explicitly fence off.

The fix is targeted: add a render-note to mem:2 explicitly prohibiting "override," "architecture," "Khepri," "Gold Morning," or any parahuman mechanism reference in the rendered prose. The entry's displacement content (persons-as-deliverable, the conversion of four named bodies to a thing dispatched) is sound. The render-fence is missing.

# Entry callouts

[memory:mem:2] @17 — target-slug "monument-override-architecture-residue" contains mechanism-vocabulary ("override-architecture") with no margit card to carry the render-fence and no in-entry render-note prohibiting mechanism-vocabulary at stitch time. The auditor flagged this for margit as SIGNAL. My callout is narrower: the entry needs an explicit render-note (`render-fence: no override-vocabulary / no mechanism-naming / Westerosi-accounting-register only`) before /and-stitch to prevent the stitcher from importing Earth-Bet mechanism language in the displacement rendering. Without the note, the stitcher sees "override-architecture-residue" as the target and has no guardrail against rendering it as mechanism content. This is not a facet-text Earth-Bet violation (auditor PASS is confirmed); it is a downstream render-risk. REVISE: add render-fence note to entry.

# Convergence-trace

- Auditor signal-001: oc-prop card-resolution (SEAM-006/007/008) — margit referral. Partially overlaps this callout (the target-slug card-absence). My concern is narrower: render-fence language, not card authoring per se.
- Auditor soft advisory: memory single-register — rubric soft clause accepted by R2 judge. I am not challenging the single-register disposition; my revise is specifically the render-fence, not the register choice.
- Auditor Earth-Bet hard-fence scan: PASS. Confirmed: no breach in facet-text fields. My callout is downstream-risk (render-layer), not a current-text violation.
