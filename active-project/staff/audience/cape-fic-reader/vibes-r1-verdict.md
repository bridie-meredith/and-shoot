---
reviewer: cape-fic-reader
facet: vibes
cycle: 1
episode: b01c01
date: 2026-05-23
verdict: revise
---

# Verdict reasoning

fault-011 is repaired — vibes:9's licensed-by reads `proto:27` only, no dead feeling:3 citation; that HARD is closed. The vibes:7 / vibes:8 split on wren earns: `observation` and `silence` are distinct operator targets — a dialogue-writer pulling wren's vibe-set needs both the reporter-not-interpreter tag and the withheld-follow-up tag to generate different register on different beats. But vibes:2's shared license is doing the thing I hate most in this genre: the citation looks complete because it has multiple sources, and one of those sources (`state-update:2`, time-of-day null→morning) has zero informational bearing on why tallow-smoke confirms the city's register. The operational question — would `state-update:2` cause any downstream operator to behave differently toward the `king's-landing` vibe? — is no. The initialization pair is asymmetric: `state-update:1` (location arrival) plausibly scaffolds a king's-landing vibe; `state-update:2` (morning) does not. The shared license is padding that obscures the real anchor, which is `proto:6` alone.

# Entry-level callouts

- [vibes:2] @6 — `state-update:2` (time: null→morning) does not license a `king's-landing` keyword extension about tallow-smoke register; morning-as-initialization carries no information about why the smell confirms the city's quality; the operative license is `proto:6` plus optionally `state-update:1`; citing the full initialization pair makes vibes:2 look like it's riding vibes:1's coattails rather than standing on the @6 event.

# Convergence trace

fault-017 (shared-license flag on vibes:1 + vibes:2 sharing state-update:1 and state-update:2).
fault-009 (vibes:7 + vibes:8 split on wren — adversarial probe; split holds; no callout generated).
fault-010 / fault-011 (vibes:9 licensed-by repair — verified clean in current file; HARD is closed).
