---
reviewer: cape-fic-reader
facet: vibes
chapter: b01-c01
cycle: 1
verdict: revise
date: 2026-05-25
---

# Per-reviewer adversarial verdict — cape-fic-reader

## Verdict: REVISE

---

## Entry-level callouts

**[vibes:1] @off-anchor — `atonement-as-repetition` `++`**
No attack. The `++` op is valid: `atonement-as-repetition` is pre-loaded in taylor's vibes.md. New tokens `subsistence-range-held-by-effort-not-by-incapacity` and `three-weeks-in-the-drain-angle-as-the-argument-she-makes-to-herself` are string-non-overlapping with the existing bundle and both parse as noun-phrases not sentences. The off-anchor carve-out is procedurally documented. The downstream operator bias is legible (dialogue-writer gets "the subsistence work as argument she makes to herself" as a voice-register constraint). Passes.

**[vibes:2] @off-anchor — `insect-sense-as-residue-not-spectacle` `++`**
No attack. `insect-sense-as-residue-not-spectacle` is pre-loaded. New token `range-ceiling-felt-as-sharpening-not-as-pain` is string-non-overlapping. No AP8 issue. Licensed basis is the same as vibes:1 — off-anchor aggregate. Passes.

**[vibes:3] @12 — `actor:taylor-hebert-kl-122ac` `+` `the-first-crack`**
Passes formal gates. Keyword absent from taylor's pre-loaded vibes. Tokens all noun-phrase form. The fault-005 revised token (`deployment-preceding-permission`, revised from a finite-verb form) holds — present-participle structure passes sentence-parsability test. Licensed by proto:12 (the insects propagate) and proto:13 (the nearest dozen bodies yield) — two sources. Multi-source satisfied.

One concern I'm sitting with: `the-first-crack` on `actor:taylor-hebert-kl-122ac` is a new permanent keyword that will bias every downstream dialogue-writer for Taylor. That's correct and wanted — this is the chapter that plants the crack. No attack warranted. Passes.

**[vibes:4] @12 — `episode` `+` `threshold-crossed-unconsented`**
Attention here. The `episode` target receives a `+` (fresh add). The V1.1 Patch 1 requires the showrunner to verify the episode vibes state before firing `+`. No episode vibes file was consulted or cited in the authoring record. If the episode scope was pre-seeded at project activation (as Patch 1 requires checking), this `+` fires blind. The b01c01 facet has no documentation of a Patch-1 compliance check on the episode target.

However: reviewing available project files, no episode-scope vibe file was surfaced. The series activation documents don't show a b01c01-episode vibes pre-seeding event. The Patch 1 hazard is real but the evidence of a pre-existing `threshold-crossed-unconsented` keyword on the episode target is absent. In the absence of a pre-seeded conflicting keyword, the `+` is likely valid. This reads as an undocumented check rather than a proven AP5 violation. Soft concern, not blocking. Passes with note.

Token AP8: `the-chapter-that-opens-the-account` — noun-phrase with relative clause; no standalone sentence. Passes.

**[vibes:5] @21 — `actor:oswyn-mudway-flea-bottom-elder` `+` `the-categorizing-witness`**
No attack. Keyword absent from Oswyn's vibes. Tokens noun-phrase form. Fan-out for the witch-label event includes oswyn as a required target and this fires it. Passes.

**[vibes:6] @21 — `actor:taylor-hebert-kl-122ac` `+` `the-ward-category`**
No attack. `the-ward-category` is absent from Taylor's pre-loaded vibes. Tokens pass AP8. Licensed by proto:21 (Oswyn takes lane-mouth), proto:26 (Oswyn lifts the chin), and the world-build condition. Three sources. Passes.

**[vibes:7] @21 — `episode` `+` `witch-label-assembling`**
Same Patch-1 compliance concern as vibes:4 — no documented episode vibes pre-seeding check. Same reasoning applies: absent evidence of a pre-seeded keyword, the `+` is likely valid. Soft concern, not blocking.

Token scrutiny: `foreign-woman-who-knew-before-anyone-told-her`. Length (9 segments) triggers advisory review. Parse: "foreign woman [who knew before anyone told her]" — the relative clause modifies "foreign woman"; the overall token is a noun-phrase with an embedded relative clause; no standalone main predicate. Passes sentence-parsability test.

**[vibes:8] @12 — `loc:loc-flea-bottom` `+` `the-rescue-witnessed`**
No attack. Location target. Keyword not pre-existing on the location. Four licensing sources (proto:12, :13, :14, :21). Token AP8: all noun-phrases. Fan-out coherence: the crowd-pressure event affects the location — correct target. The location-as-witness is the right qualitative-consequence layer above whatever state-updates wrote about the crowd. Passes.

Cross-target check: the E2 rescue event affects taylor (@12/vibes:3), episode (@12/vibes:4), and location (@12/vibes:8). Does it also require Oswyn to fire? Oswyn is not on-stage at @12 — he arrives at @21. Fan-out restricted to on-stage entities at @12 is correct. No AP12 issue.

**[vibes:9] @27 — `actor:wren-stitch-maker-flea-bottom-ward` `+` `the-first-facing`**
No attack. Wren's vibes don't carry `the-first-facing`. Tokens: `perceptual-orientation-before-naming`, `noticed-before-having-a-word`, `reader-knowing-before-taylor-does`. All noun-phrase form. Three licensing sources (proto:27, proto:2, proto:25). The proto:2 (`the tallow smoke crosses the stitch-house lane`) and proto:25 (`the tallow smoke layers the lane-floor`) are both bare in the cite-index — they have no back-citation from vibes:9 in the cite-index as written (lic-out for vibes:9 shows proto:27, proto:2, proto:25 but the cite-index bare-list @25 shows no entry and @2 shows sensory:1 back=Y). This means the cite-index's `lic-out` field for vibes:9 is correct but the inverse citation from @2 and @25 is not reflected. This is a cite-index symmetry gap, not a vibes fault — the vibes facet is not responsible for the cite-index's back-pointer completeness. Passes on the vibes rubric; note for cite-index completeness.

**[vibes:10] @27 — `actor:taylor-hebert-kl-122ac` `++` `tragic-causal`**
`tragic-causal` is pre-loaded on Taylor with existing bundle `[second-answer-is-always-a-choice, auditable-mistakes-are-visible-to-her-in-others-and-opaque-in-herself, chain-is-already-set-recognition-comes-late]`. New token: `cost-bearer-in-the-frame-before-the-calculus-opens`. String-non-overlapping. AP8: noun-phrase with prepositional modifier; no finite verb; passes.

AP11 semantic-adjacency advisory: `cost-bearer-in-the-frame-before-the-calculus-opens` is semantically adjacent to `chain-is-already-set-recognition-comes-late` (both concern the structural-inevitability of Taylor's blindspot). But the event-frames are genuinely distinct: `chain-is-already-set` operates at the series-arc level; `cost-bearer-in-the-frame-before-the-calculus-opens` fires on the specific b01c01 on-screen event (Wren in Taylor's line of sight before Taylor has a cost-frame for Wren). Different operator behavior: `chain-is-already-set` biases dialogue toward existential-closing; `cost-bearer-in-the-frame-before-the-calculus-opens` biases dialogue toward Wren-specific perceptual-preceding-accounting framing. Distinct enough. Passes.

---

## Primary revise-trigger

**Volume shortfall against rubric floor.**

The vibes rubric V1 (§ Sparsity / volume) states: "Expected s01e01 yield: ~12-25 entries. Range tolerance: 12-30." This file delivers 10 entries. That is below the stated floor of 12.

The rubric calibration anchors (C1-C4) identify four expected single-entity fires for a single vibe-causing event. The rubric's Phase 0 corpus table (E1-E5) identifies 5 vibe-causing events yielding ~16-20 expected fires. The delivered file covers only 3 events (E_rescue/crowd-control, E_witch-label, E_wren-facing). Events E1 (census/officer-arrives) and the series-level setup events are absent — but those calibration anchors reference different chapter content (a 77-beat s01e01 episode with a census officer, letter, naming event, septon-absence, yard-witnesses). Those specific events don't exist in b01c01, which is a 27-bone chapter.

This creates an ambiguity I cannot resolve without clarification: the rubric was written for a 77-beat episode context, but the facet file is for a 27-bone chapter. If the rubric's "s01e01" maps to this chapter, 10 entries for 3 events is potentially a fan-out gap. If "s01e01" in the rubric refers to a larger episode scope that doesn't map 1:1 to b01c01, the 10-entry count may be chapter-appropriate.

What this reader sees: three vibe-causing events covered, each with reasonable fan-out. The rubric says the chapter should yield 12-30 entries if it's an s01e01-class chapter. At 10, the file is below the stated tolerance floor. The rubric does not say this is a hard gate — it's an expected range — but the gap is real and the rubric says the mechanic checks coherence.

This is the revise trigger: **the file is below the rubric's range tolerance floor of 12 entries.** Two additional entries are needed at minimum to reach the floor. Candidate fires this reader would expect: a `+` on `actor:taylor-hebert-kl-122ac` for the ongoing `capability` dimension that is now first cracked (the first deployment is also a first capability-vibe event, distinct from the moral crack); and a `+` or `++` on `loc:loc-flea-bottom` for an additional qualitative-consequence layer beyond `the-rescue-witnessed` (the location absorbs the witch-label-formation event as a permanent charge, not only the rescue).

---

## Convergence-trace

- Volume shortfall (this reviewer) — no overlapping auditor finding IDs located in available Phase 5 reports; the auditor report for vibes facet was not located on disk. The volume concern is audience-originated, not auditor-carried.
- Patch-1 episode-check undocumented (vibes:4, vibes:7) — minor; no convergence trace available.
