---
reviewer: sensory-disambiguation-pedant
facet: sensory
cycle: 2
episode: b01c02
date: 2026-05-21
verdict: accept
---

# Verdict reasoning

Cycle-1 finding: sensory:2 @22 was action-verb self-charged (`lights the lamp`). The fix relocated sensory:2 to @23.

I re-run the disambiguation gate on both entries as they now stand.

**sensory:1 @7** — proto-line: `the city-watch passes the hook`. No change since cycle-1. `passes` is bare locomotion; the column's audible texture is not in the language. Sound modality matches the perceptual axis (marching column, narrow alley). The flag does work the proto-line does not. No charged word or action-verb self-charge in `passes`. PASS — unchanged from cycle-1.

**sensory:2 @23** — proto-line: `taylor-hebert-kl-122ac opens the ledger`. The cycle-1 failure was `lights the lamp` @22 — the ignition verb that IS the light-onset. The fix moved the anchor to @23.

I apply the action-verb self-charge test to `opens the ledger`. The verb `opens` names a physical action — parting covers, separating pages. Does `opens` self-carry a light-onset? No. The light is already present from the lamp at @22 (loc-state:11 establishes the lamp newly lit). `Opens` does not name the perceptual event of light onset; it names the ledger-access gesture. The bare-word test on `ledger` confirms: `ledger` is a bare content-noun, not a charged light-noun like `shadow`, `blaze`, or `glare`.

The sensory entry's delta `light: unlit-lodging-interior -> lamp-lit-tight-radius` fires at @23 — the first bone worked under the stable lit state after the lamp-lighting at @22. The fire's purpose is to register the light condition of the new working environment. The proto-line `opens the ledger` does not surface the light inflection; the audience would receive only a mechanical ledger-access gesture without the flag. Q1 clears.

Q2 (magnitude): the shift from `unlit-lodging-interior` to `lamp-lit-tight-radius` is a large perceptual delta — room-dark to single-flame working-light. This is audience-experiential-scale. Q2 clears.

Action-verb self-charge test negative. `Opens` is not in the class of light/ignite/extinguish/catches. The charged-word audit of `opens the ledger` finds no self-carrying charged word. The flag is not doubling; it is adding.

PASS.

Both entries clear disambiguation. File passes my axis.

# Entry-level callouts

None. Both entries pass the disambiguation gate.

Cycle-1 finding (sensory:2 @22 action-verb self-charge) is resolved by relocation. The new anchor @23 (`opens the ledger`) is bare on the light modality. No new disambiguation seam opened by the relocation.

# Convergence trace

- sensory:2 relocation @22→@23: directly resolves the cycle-1 `[sensory:2] @22` action-verb-self-charge finding. The fix-log item 8b names the exact relocation. Cite-index updated; co-citations for sensory:2 now show @23 with co=[exposition:5, state:5].
- `opens the ledger` vs. `lights the lamp` disambiguation: the former is bare on light; the latter was charged (action-verb self-charge). The distinction holds under the rubric's action-verb self-charge note — only verbs whose semantic content IS the sensory event charge the line.
- No new disambiguation findings surfaced. The file is clean on my axis.
