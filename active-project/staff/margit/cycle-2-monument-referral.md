referral: mem:2 monument-card resolution
date: 2026-05-19
decision: USE-EXISTING monument-override-architecture-prohibition-122ac
existing-cards-surveyed:
  - monument-override-architecture-prohibition-122ac  (found: cards/conditions/monument-override-architecture-prohibition-122ac.card.md)
  - cond-override-architecture-residue-122ac           (not found — does not exist on disk; referral brief's "nearest existing card" interim was a phantom slug)
  - cond-no-parahuman-infrastructure                   (exists; adjacent world-law card; not a monument card)
created-card: none
mem:2-target-reference-updated-to: unchanged (already monument-override-architecture-prohibition-122ac)
notes: |
  Survey result: monument-override-architecture-prohibition-122ac already exists at
  cards/conditions/monument-override-architecture-prohibition-122ac.card.md.
  The card is quality: full, scope: library, world: planetos, origin: authored.
  It is indexed in cards/conditions/INDEX.md under by_world (planetos), by_quality (full),
  and by_type (monument-interior). The monument-interior type is already established in the
  index, confirming the monument-* subclass is precedented and supportable under the current
  condition card schema (schema authority: schemas/card.schema.md).

  The card's content precisely matches the referral request: Khepri-period scale-coordinating-
  override capability + the vow prohibiting its redeployment + the threshold-of-deployment
  dynamic. The Interaction Notes section explicitly instructs memory-flag authors to use
  this card as the target-reference for the triggering pattern described in mem:2 @23.

  Interim slug status: cond-override-architecture-residue-122ac (named in the referral brief
  as the fixer's interim) does not exist on disk. The fixer's inline comment in memory.md
  line 19 describes the pre-remediation state. The live target-reference field in mem:2 @23
  already reads "monument-override-architecture-prohibition-122ac" — the fixer set the
  correct slug in the same pass that authored the remediation comment. No edit is required.

  Schema notes: The condition class schema (schemas/card.schema.md) defines four body
  sections for condition cards: Description, Sensory Impact, Duration, Interaction Notes.
  The existing card uses these sections correctly and adds a "Monument Body" section for
  the monument-specific content. This extension is consistent with the behavior-class
  schema pattern (memory monuments section) and with the monument-interior type already
  indexed. No schema violation detected; no gap requiring documentation.

  Conclusion: referral resolves to USE-EXISTING. mem:2 target-reference is already correct.
  No card creation, no file edits, no index updates required.
