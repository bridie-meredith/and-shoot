audit:
  scope: chapter
  target: b01c09
  timestamp: 2026-05-31
  trigger: /and-substance chapter b01c09 Phase 5 (contract audit)
  findings:

    - id: fault-001
      type: pass
      what: "Check 1 — s01 chunk text vs relational_anchor_status +0.5 claim"
      why: |
        The coverage-without-ledger-entry mechanism is explicitly present and concretely staged.
        The chunk names the specific mechanism: ward expansion three weeks prior moved the boundary
        as a density-fill, inserting Wren's lanes into routine feed-coverage without Taylor
        arranging it. Taylor then runs the geometry with attention, reads the accumulated pattern
        (stitch-shop / water-sellers timing, lane geometry, one-block unobserved stretch), holds
        it in her internal map, and routes the deliverable-to-Jarvis without the pattern. The final
        two force tokens ("wren-anchor-structurally-present-without-ledger-entry" + the feed runs
        the new ground "the same way it runs the old") confirm the axis-move mechanism is present
        in the text. No rank claim without described cause — the cause is fully described.

    - id: fault-002
      type: pass
      what: "Check 1 — s02 chunk text vs political_register-prot +0.5 claim"
      why: |
        The courier-inference-deepens-resentment-color mechanism is present. The chunk text
        stages: courier at the Dragonpit lower gate at the wrong hour/ward/posture for an
        Oswyn errand; Taylor reads body-posture as delivery-complete; identifies the household
        the lower gate services as carrying Black-faction alignment from c05 feed-record; infers
        a Black-faction contact direction without a conclusion. The key force token
        "black-faction-contact-inferred-from-posture-and-location" names the mechanism exactly.
        The observation is logged in the internal feed-record and explicitly not routed to Jarvis.
        The resentment-color deepening is present ("The color accrues now — not a conclusion, a
        direction"), consistent with the cl-d05 continuation-draw claim. Mechanism substantiated.

    - id: fault-003
      type: pass
      what: "Check 1 — s03 chunk text vs empty axes_in_motion claim"
      why: |
        s03 is a genuine held-discipline accounting-close, not a smuggled axis-move.
        The chunk stages the double-omission as a structural fact: both omitted items (Wren's
        pattern, courier's observation) are named as present in the internal map, neither
        routes to the Jarvis packet. The seal goes down. Taylor does not revisit either entry.
        No new relational or political inference is generated — the scene is entirely the
        physical-enactment of the split substrate, with nothing that would constitute a
        fresh axis-advance. The stakes_axis (moral_legibility_to_self) is declared in
        axes_held, not axes_in_motion, and the rationale explicitly reads the scene as
        the gap-before-recognition, consistent with "not advancing the axis." No undeclared
        axis movement is smuggled through the closing prose.

    - id: fault-004
      type: pass
      what: "Check 2 — chapter roll-up"
      why: |
        The header ROLL-UP CHECK is arithmetically correct and confirmed by inspection of the
        scene contracts:
          s01: relational_anchor_status +0.5 | political_register-prot 0
          s02: relational_anchor_status 0    | political_register-prot +0.5
          s03: relational_anchor_status 0    | political_register-prot 0
          Total: relational_anchor_status +0.5 | political_register-prot +0.5
        Chapter contract (memory.md:5317-5327): relational_anchor_status +0.5,
        political_register-prot +0.5. EXACT MATCH. No tolerance call required.

    - id: fault-005
      type: pass
      what: "Check 3 — cost-ledger anchor decision: cl-d08 first tranche vs cl-d06 deferred second tranche"
      why: |
        The cl-d08 anchor for s01 relational_anchor_status +0.5 is correct.

        cl-d08 gain: "relational_anchor_status +2"
        cl-d08 cost: "journey-required: cl03b ([cost-bearer] structurally necessary to coverage
        map without appearing in the ledger)"

        The c09 s01 event matches the cl-d08 cost clause precisely. The mechanism is
        coverage-architecture extension: Wren is structurally present in the coverage map through
        the ward-expansion geometry without appearing in the deliverable ledger. The journey-required
        cost (cl03b: network build + witch-label exposure, anchored at c03-c04) is confirmed as
        already paid. c09 is described as the first moment the cl-d08 gain is enacted: first
        tranche +0.5 of +2 drawn, +1.5 remaining. This accounting is internally consistent and
        consistent with the series cost_ledger entry at memory.md:1382-1385.

        The cl-d06 deferred second tranche (+1.0, per pl-2026-05-30-001) is correctly NOT settled
        here. The draft's header reasoning correctly distinguishes the two: cl-d06's second tranche
        belongs to a "spoken/recognition-register Wren event" (cl-d06 cost = moral_framework -1,
        confirmed the appropriate register); c09's event is architectural-surveillance, not a
        spoken/recognition event, and c09 takes no moral_framework move. The decision to carry
        cl-d06's second tranche to c10/later is consistent with pl-2026-05-30-001's declared
        candidate window (b01c08-b01c10) and the draft's own cost reasoning. The draft correctly
        notes the c09 event register (architectural) is not the cl-d06 register (spoken/recognition).

        cl-d05 anchor for s02 political_register-prot +0.5: cl-d05 gain = "political_register-prot +3"
        cost = "opportunity-missed: resentment becomes the permanent register; neutral-observant
        foreclosed from d05 forward" (memory.md:1362-1365). The draft cites this as a continuation
        draw (opened at c05; not a new opening), consistent with the memory record of cl-d05
        opening at c05 and the antagonist_pressure entry for political_register-prot (memory.md:1446:
        "feeds political_register-prot monotonically between d05 and d13"). A continuation draw on
        an open ledger entry is a valid operation. No accounting error found.

    - id: fault-006
      type: pass
      what: "Check 4 — THEMATIC-AXIS-UNDECLARED (URI-CONTRACT-THEMATIC-AXIS, HARD)"
      why: |
        The chapter goal names two thesis-adjacent elements: Taylor surveilling Wren "without
        calling it that" and the double-omission (courier + Wren). The two thesis axes are:
        (a) relational_anchor_status — declared in axes_in_motion[] at s01, axes_held[] at s02
            and s03. PRESENT.
        (b) moral_legibility_to_self — declared in axes_held[] at all three scenes, each with
            rationale explicitly naming the discipline ("the gap between what she has done and
            what she would have to admit is the scene's substance contribution WITHOUT advancing
            the axis"). PRESENT.
        No thesis axis is silently absent. moral_legibility_to_self is in axes_held, which is
        the correct list for an axis that is active in the scene's thematic substance without
        moving in rank. URI-CONTRACT-THEMATIC-AXIS is satisfied.

    - id: fault-007
      type: pass
      what: "Check 5 — held-axis discipline: moral_framework holding at rank 0"
      why: |
        The contract's position — that moral_framework holds because Taylor's accounting does not
        open on either act — is supported by the chunk text. In s01, the withholding is described
        as enacted through substrate selection: "the internal-map/deliverable substrate split is
        not entered in Taylor's accounting as a withholding act." The ward-coverage notes receive
        "everything they have always received." In s02, the courier observation is "not entered
        in Taylor's ledger as a breach." In s03, "the accounting closes without an entry for
        either omission."

        The specific contract claim is that Taylor does not NAME the gap as a withholding act —
        this is distinguished from whether it IS a breach (the book's thesis). The chunk texts
        consistently stage the acts as operational (substrate-selection, feed-logging, packet-sealing)
        rather than as moral narration. The s01 rationale ("she does not name the gap as an omission")
        and s03 rationale ("the double-omission is structural fact, not a named breach") are
        substantiated by the chunk prose. No undeclared moral_framework move is present in any scene.

        The book's thesis is not that Taylor's framework is clean — it is that Taylor's accounting
        does not open, and the gap between what she does and what she accounts for IS the thesis.
        The chunk text supports "not named as breach" without smuggling an undeclared axis-move.

    - id: fault-008
      type: pass
      what: "Check 6 — schema conformance: direction/magnitude, stakes_axis, empty axes_in_motion"
      why: |
        All three scenes: direction UP on all in-motion entries; target_delta_magnitude 0.5 for
        both (within scene delta_per_signature_axis range 0-1.5 per chunk_targets.scene). No
        null-direction or zero-magnitude entries.

        stakes_axis conformance:
          s01 stakes_axis: relational_anchor_status — this axis IS in s01's axes_in_motion[].
          LEGAL.
          s02 stakes_axis: political_register-prot — this axis IS in s02's axes_in_motion[].
          LEGAL.
          s03 stakes_axis: moral_legibility_to_self — this axis IS in s03's axes_held[].
          Per the "union of axes_in_motion[] and axes_held[]" rule (scene-conflict.stakes_axis
          must be in the scene's axes union), this is LEGAL.

        s03 empty axes_in_motion: axes_in_motion is [] but axes_held is populated (5 entries)
        and chapter_class is standard (not frame-coda). Per the command body: a chunk with both
        axis lists empty is malformed; s03 has axes_held populated, so the empty axes_in_motion
        is schema-legal. Chapter_class standard with a scene holding but not moving is consistent
        with the book-level structure (accounting-close scenes that hold axes while the prior
        scenes' advances settle). No malformation.

        density_target ranges: s01 0.65-0.80, s02 0.65-0.80, s03 0.55-0.70. All within the
        chapter-level density_target 0.5-0.7 declared in the chapter contract. Note: s01 and s02
        upper bound (0.80) exceeds the chapter-level declared ceiling (0.7). This is a SOFT
        observation — see fault-009.

    - id: fault-009
      type: flag
      what: |
        s01 density_target 0.65-0.80 and s02 density_target 0.65-0.80 have an upper bound of
        0.80, which exceeds the chapter contract's stated density_target ceiling of 0.5-0.7
        (memory.md:5337). The series chunk_targets.scene range is 0.6-0.9 (memory.md:1464),
        so 0.80 is within series-level tolerance.
      why: |
        The chapter-level density_target (0.5-0.7) is the closer scope; scene ranges exceeding
        the chapter ceiling produce a potential SUBSTANCE-SUSPECT-cheap-gain pressure at
        /and-write Phase 6 if the bone-gate auditor reads chapter ceiling as the governing
        constraint rather than series ceiling. For s03 (0.55-0.70) there is no discrepancy.
        If the chapter density ceiling was intended as 0.5-0.9 (matching series), no fault
        exists. If the chapter ceiling of 0.7 was deliberate (consistent with prior chapter
        contracts that declare tighter ceilings for standard chapters), then s01/s02 scene-level
        density ceilings should be revised to 0.65-0.70 or the chapter-level ceiling should be
        revised upward. Does not block decomposition. No fixer dispatch needed; surfaces for
        screen-writer's attention at /and-write Phase 0 density check.

  summary:
    hard_count: 0
    soft_count: 0
    signal_count: 0
    flag_count: 1
    pass_count: 7

    cost_ledger_anchor_verdict: |
      CLEAN. The cl-d08 first-tranche anchor for s01 relational_anchor_status +0.5 is
      correctly matched to the c09 event (structural coverage-architecture gain without
      ledger entry = exact cl-d08 cost-clause match). The journey-required cost (cl03b)
      is confirmed paid at c03-c04. cl-d06's deferred second tranche (+1.0) is correctly
      not settled here — the c09 event register (architectural) does not match cl-d06's
      required register (spoken/recognition + moral_framework -1 cost). The cl-d05
      continuation draw for s02 political_register-prot +0.5 is correct (open since c05;
      monotonic feed between d05 and d13). No ledger accounting error.

    thematic_axis_verdict: |
      CLEAN. Both thesis axes — relational_anchor_status and moral_legibility_to_self —
      are declared in the correct axis lists across all three scenes. No thesis axis is
      silently absent. URI-CONTRACT-THEMATIC-AXIS satisfied.

    decomposition_verdict: CLEAR TO PERSIST
    notes: |
      The one flag (fault-009) is a density-ceiling discrepancy between s01/s02 scene-level
      upper bounds (0.80) and the chapter-level declared ceiling (0.7). It does not affect
      the structural integrity of the decomposition. The contract, roll-up, cost-ledger
      anchoring, and thematic-axis declarations are all clean. No HARD findings. No SOFT
      findings. The decomposition is CLEAR TO PERSIST.
