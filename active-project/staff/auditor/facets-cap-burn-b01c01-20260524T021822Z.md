---
report: facets-cap-burn
chapter: b01c01
cycle_burned: 3
of_cycles_cap: 3
date: 2026-05-24T02:18:22Z
trigger: cycle-3 ADD pre-validation failure per URI-FACETS-CYCLE-N-ADD A3 → DELETE-on-cycle-3 fallback per URI-FACETS-CAP-BURN-SEMANTICS A2
disposition: cap-burn DELETE executed; resolution path canonical; trade-offs accepted and logged
orchestrator_critic_verdict: NOT-SUCCESSFUL (per cap-burn semantics step 5; not a HARD-BLOCK against /and-stitch)
---

# Cap-burn Report — b01c01 cycle 3

## Summary

One facet entry deleted at cycle-3 cap-burn: `[coll-net-mender-flea-bottom:1] @8` in `theater/dialogue/coll-net-mender-flea-bottom.md`. This was the only Coll dialogue entry in b01c01. The cycle-3 fixer recommended DELETE after the state-updates-Coll ADD candidate failed pre-validation on three independent rubric axes per URI-FACETS-CYCLE-N-ADD A3; user disposition (cycle-3 phase-boundary decision) elected to execute the DELETE proactively rather than re-fire Phase 5b and accept an inevitable second-time failure with the same root cause.

The other two cycle-2-failing facets (interest-narrator, vibes) cleared cycle-3 cleanly under 3-of-3 ACCEPT (see `facets-audience-gate-r3.md`). The three pulp-enthusiast-arbiter-deferred facets (memory, sensory, dialogue-taylor) ship as-is with attribution trail (see `facets-audience-gate-r2.md` § Orchestrator-judgment arbitration).

## Deleted entries

### `[coll-net-mender-flea-bottom:1] @8`

**Original entry:**
```
1 @8 | offer work without asking questions | "There's mending if you can hold a needle."
```

**Anchor (proto-line @8):**
```
8 coll-net-mender-flea-bottom speaks to taylor-hebert-kl-122ac
```

**Callout chain (cycle-1 → cycle-3):**

- **Cycle 1 (dark-fantasy-reader REVISE):** facet-license cross-anchor — sidecar `Facet-licenses:` cited an entry whose anchor did not match the speech-act anchor; behavior-card paths searched returned absent; planetos-specificity dissent.
- **Cycle 2 (dark-fantasy-reader REVISE, ESCALATED to DEMAND):** facet-license cross-anchor unaddressed (cite-index walk for @8 returned only `[coll-net-mender-flea-bottom:1]` — bare of co-located lens facets; `exposition:2 @4` resolves at @4, not @8; rubric CONSTRAINT § citation-completeness URI-FACETS-CYCLE-1 promotion classifies cross-anchor non-resolving citation as HARD per entry). Behavior-card-absence callout DISMISSED by orchestrator on path-search error (card exists at `active-project/actors/coll-net-mender-flea-bottom/card.md` per /and-cast Phase 4 convention; Q1 §-section verifiability axis closes once correct path is read). Planetos-specificity deferred per user directive.
- **Cycle 3 (cap-burn DELETE):** cycle-3 fixer attempted Path (a) — state-updates-Coll ADD at @8 (`actor:coll-net-mender-flea-bottom.relationship-to-taylor: null -> offer-extended`) — pre-validation FAILED on three rubric axes:
  1. **Reality** — Anti-pattern #1 (registration-as-state): speech is a transient act, not a persistent field mutation. The per-actor Coll state-updates file already carries an explicit on-disk REFUSE at @8: "NOT a state-update — speech is not a tracked field. No field changes on Coll's state card. REFUSE." Locked rubric defense by the character's own authoring fork.
  2. **AP#9 density-on-flat** — Scene-A rhythm-shape is `flat-low`, no peak-bones (scene-map confirms). @8 is in the flat-low zone. State-updates rubric §Curve-shape: "flat-low zones are nearly silent." Adding a fire here without reality-axis justification is density-on-flat contamination.
  3. **Authority / Field-extension** — `relationship-to-taylor` is not a tracked field on Coll's actor state schema. Rubric §Field-extension protocol: extension is licit only when the field is "a tracked-state aspect, not a perception or stylistic flourish (knowledge, mask-state, exposure-state, posture, inventory ARE tracked; mood, register, voice-tone are NOT)." `relationship-to-taylor` is relational register — closer to forbidden mood/register class. Anti-pattern #6 (invented-field) risk HIGH.

  ADD REFUSED at cycle 3 per URI-FACETS-CYCLE-N-ADD A3 (cycle-3 ADDs that fail pre-validation are REFUSED; no remediation budget remains). Path (b) — rubric edit permitting upstream-same-scene cross-anchor facet-licensing — flagged as out-of-band follow-on for authoring-staff resolution.

**DELETE reason:** facet-license cross-anchor structurally unresolvable at @8 (no co-located lens facets); cycle-3 ADD pre-validation failed on all reasonable candidates; rubric edit deferred to follow-on; cap-burn DELETE is the canonical resolution path per URI-FACETS-CAP-BURN-SEMANTICS A2.

**Deletion marker (in `theater/dialogue/coll-net-mender-flea-bottom.md`):**
```
# DELETED coll-net-mender-flea-bottom:1 @8 - facet-license cross-anchor unresolvable in cite-index post-R2; ADD pre-validation FAILED on three rubric axes (state-updates-Coll Reality / AP#9 density-on-flat / Authority invented-field per rubric §Field-extension); rubric-edit Path (b) flagged as follow-on (cap-burn cycle 3, 2026-05-24).
```

**Cite-index cascade:** `build_cite_index.py` (extended this session to scan `theater/dialogue/` for deletion markers — small focused patch; see commit `b6df1e6`) auto-stripped `[coll-net-mender-flea-bottom:1]` from canonical proto-line @8. Proto-line @8 now reads `8 coll-net-mender-flea-bottom speaks to taylor-hebert-kl-122ac` with zero citations. Cite-index `_cite-index.md` rebuilt; @8 surfaces in the Bare protolines section. Stale-citation check: CLEAN.

**Sidecar (`active-project/staff/dialogue-writer/coll-net-mender-flea-bottom.drafts.md`):** matching deletion marker added at top of file; entry content preserved below for forensic / future rubric-edit revival.

## File-level shape gaps introduced by the DELETE

### Gap 1 — Bare speech bone @8 (URI-DIALOGUE-COVERAGE-GATE)

Phase 0 `speech_bones` inventory included @8 (`coll-net-mender-flea-bottom speaks to taylor-hebert-kl-122ac`). Post-DELETE, proto-line @8 carries zero `<character-slug>:<id>` citations. Per `.claude/commands/and-facets.md` § CONSTRAINT § dialogue-coverage: "Bare speech bone (a `<X> speaks to <Y>` proto-line with zero dialogue citations post-R2) → HARD per bone."

**ACCEPTED-AT-CAP-BURN.** Trade-off rationale:
- The cite-index walk for @8 is structurally bare of co-located lens facets — no upstream lens fire could co-anchor without an invalid ADD attempt.
- The chapter's substance for the Coll role is carried by Coll's on-screen action bones (@3 `coll lifts the eyes`; @4 `coll works the net`; @20 `coll folds the net`) and the `exposition:2 @4` first-mention gloss. These carry the cost-bearer / Flea Bottom fixture function without requiring the speech-act.
- The lost line ("There's mending if you can hold a needle.") was a single offer-utterance carrying §Voice "offer work without asking" affirmative-demonstration. Its loss removes one speech beat from the prose layer but preserves the structural role.
- See URI-FACETS-CAP-BURN-SEMANTICS A2: NOT-SUCCESSFUL orchestrator-critic verdict is recorded but is NOT a HARD-BLOCK against `/and-stitch`.

Auditor fault ID: **fault-030** in `facets-final-audit-cycle3.md`. Flagged ACCEPTED-AT-CAP-BURN.

### Gap 2 — Empty speaker file body / Phase 6 ≥1-entry gate

Phase 0 inventory's `speakers` list includes coll. The file `theater/dialogue/coll-net-mender-flea-bottom.md` exists on disk but its body is deletion-marker-only (zero `<id> @<anchor>` entries). Per Phase 6 6a persistence gate language (`active-project/theater/dialogue/<slug>.md` exists with ≥1 entry), the gate language is breached at the body-content level even though the file exists.

**ACCEPTED-AT-CAP-BURN.** Trade-off rationale:
- This is the same root cause as Gap 1 — the DELETE removed the only dialogue entry, leaving the speaker file as a structural marker without content.
- The auditor's interpretation note: "the file IS on disk; its body is deletion-marker-only" — used the Phase 6 ≥1-entry clause as the operative standard since the Phase 0 "missing file" language reads literally as a path-existence check (which passes).
- Coll's structural role is preserved as in Gap 1.

Auditor fault ID: **fault-031** in `facets-final-audit-cycle3.md`. Flagged ACCEPTED-AT-CAP-BURN.

## Follow-on flags

### Rubric-edit follow-on — Path (b)

The dialogue rubric's CONSTRAINT § citation-completeness language (URI-FACETS-CYCLE-1 promotion) classifies cross-anchor non-resolving citation as HARD per entry. This is the structural rule that drove the @8 cap-burn DELETE. The cycle-3 fixer flagged a rubric-edit option:

> Propose a rubric edit to permit upstream-same-scene cross-anchor citation when (i) the cited facet entry is in the same scene as the speech-act (per scene-map), (ii) the citation is upstream (earlier flat_id, not later), (iii) no co-located lens facet exists at the speech anchor.

Rationale: the `exposition:2 @4` cite-index walk failure in this case was a structural mismatch between the rubric's "facet-license must fire at the speech anchor" rule and the realistic graph state where some speech beats are at flat-low anchors that no lens facet would naturally fire on. Permitting upstream-same-scene cross-anchor citation would resolve the false-negative without weakening the citation-completeness gate's core function.

**This is a follow-on issue for authoring-staff / margit review**, not a cap-burn-cycle action. Per CLAUDE.md rule 11, a rubric REJECT-section edit auto-promotes to RUBRIC-FIDELITY at next audit. If the edit lands before the chapter is re-faceted, the @8 DELETE could in principle be reverted; without the edit, the DELETE stands as the canonical resolution.

### Cite-index builder follow-on (LANDED this session)

The cite-index builder's `collect_deletions` function did NOT scan `theater/dialogue/` for deletion markers prior to this session — a protocol-vs-implementation gap surfaced by this cap-burn. Patch landed at commit `b6df1e6`: `collect_deletions` now optionally accepts a `dialogue_dir` parameter and scans dialogue files for deletion markers (treating the character slug as citation prefix). No behavioral change for non-deletion runs. The patch closes the contract gap between A5 protocol and implementation.

### `# DELETED ENTRIES` section in cite-index — pending implementation

Per the cite-index builder's inline protocol comment (lines 143-146), the cite-index should surface a `# DELETED ENTRIES` section. The current `build_index` function does NOT generate this section. Documented gap; not blocking for this cap-burn (deletions surface in the proto-lines bare-protolines section structurally, and this cap-burn report carries the audit trail).

## Showrunner-memory updates

- `chapters[b01c01].status`: `audited-r1-mechanical` → `audited-r1` (per URI-FACETS-CAP-BURN-SEMANTICS A2 step 6: cap-burn DELETE treated as resolution path; status reflects audience-gate resolved-even-if-NOT-SUCCESSFULly).
- `chapters[b01c01].audience_gate_cap_burned`: `false` → `true`.
- `chapters[b01c01].cap_burn_deletions`: `[]` → `[coll-net-mender-flea-bottom:1 @8]`.
- `chapters[b01c01].audience_gate_path`: `staff/auditor/facets-audience-gate-r2.md` → `staff/auditor/facets-audience-gate-r3.md`.
- `chapters[b01c01].audience_gate_cycle`: `2` → `3`.
- `chapters[b01c01].audience_gate_complete`: `false` → `true`.
- `chapters[b01c01].audience_gate_facets_passed`: extends with `[interest-narrator, vibes]` (8 facets total cleared 3-of-3).
- `chapters[b01c01].audience_gate_facets_failed`: cleared (`dialogue-coll` removed; moved to `cap_burn_deletions`).
- `chapters[b01c01].audit_path`: `staff/auditor/facets-final-audit-cycle2.md` → `staff/auditor/facets-final-audit-cycle3.md`.
- `chapters[b01c01].orchestrator_critic_verdict`: `NOT-SUCCESSFUL` (per cap-burn semantics step 5; not a HARD-BLOCK against /and-stitch).
- `chapters[b01c01].audience_gate_cap_burn_path`: NEW — `staff/auditor/facets-cap-burn-b01c01-20260524T021822Z.md`.

## Orchestrator-critic verdict pointer

Per `.claude/commands/and-facets.md` § 6c orchestrator-critic verdict, the run-judge card at `staff/orchestrator-critic/card.md` should be evaluated against the 7 acceptance criteria. The cap-burn disposition triggers NOT-SUCCESSFUL via the `Audience-gate: CAP-BURNED` criterion + `HARD findings post-audit: > 0` (the two ACCEPTED-AT-CAP-BURN HARDs). The verdict is recorded; the chapter ships as-is with the deletions and the trade-off documented. Downstream chapters do not inherit cap-burn debt; each chapter's audience-gate runs fresh.

## Downstream impact

- `/and-stitch b01-c01` is NOT blocked. The cap-burn report attaches; the chapter ships at stitcher cold-read terminal gate per the canonical pipeline.
- The lost Coll line ("There's mending if you can hold a needle.") will not surface in the stitcher output. Coll's role as Flea Bottom fixture / cost-bearer carrier is preserved structurally via his action bones and the exposition:2 gloss.
- The b01c02 audience-gate runs fresh; the b01c01 cap-burn does not propagate.
