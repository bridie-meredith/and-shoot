# render-log — b01-c12

profile: schema-default (cut-aggressiveness strict; bones-cuttable anchor-cut-only; reshow-min-sources 2; reword-density-cap 2)
persona: neutral (no lens overrides; no Phase-7 aggressiveness overrides)
narrator: taylor-hebert-kl-122ac (cold-utilitarian, explicit moral ledger, affect suppressed-not-absent, thesis never spoken on-page)
voice: first-person past tense, contractions on
voice-exemplar: active-project/voice-exemplar.md — single-arm POV-prefilter PASS (Robinson contemplative 1st-person; cadence/sentence-shape/register/noticing-pattern transferred ONLY; no content/name/place/format import; surface-convention fence held). Used as airless-counterweight: variance + embodiment against the bones' clipped-apparatus default cadence.
phase-1-mode: scene-window (4 scene-level forks; per-anchor dispatch infeasible for a 42-bone interior-accounting chapter, batched to scene-window real forks)
inline-phases-applied: P2 redundancy-cull, P3 same-subject compression, P4 first-person-past voice transform, P5 local-flow variance, P6 buildup-preservation (speaker-paragraph N/A — silent chapter, zero dialogue-anchor bones)
output: draft/b01-c12.phase-6.draft.md (body only; no preamble [@0 bridge prepended at finalize]; no scene headers; scene boundaries = blank line)
word-count: 1109
dialogue: none (silent chapter — 0 dialogue-anchor bones; no per-character dialogue files)

# ============================================================================
# CRITICAL-DISCIPLINE LEDGER (chapter-level)
# ============================================================================
# - Voice-embodiment (PROP-0022): PERSON-FIRST applied at all VOICE-FIXABLE anchors
#   (@3 body in gate-tower shadow; @8 hand lifting stylus without reaching source-field;
#   @20/@21 hand stops before explanation field / stylus lifts; @30 full-circuit weight
#   felt settling in me; @40 palm flat on closed page; @42 breach filed at end-of-day).
#   The apparatus is the lens, not the subject; a person inhabits every accounting beat.
# - @38 shape-word (DEC-0076 / EARTH-BET FENCE): rendered SHAPE-LANGUAGE ONLY — "the
#   older word" / "the shape of the thing I did when the world was ending" / "the one
#   this rhymes with". ZERO proper noun (no Khepri, no Gold Morning, no cape names).
#   Registers THROUGH the accounting register ("the count was running and the record
#   reached past any name for the architecture and landed on the older word") — the
#   apparatus vocabulary does NOT bury the recognition; word surfaces for one count,
#   pushed back down (@39 advances / @40 closes without it). meta:1 rendered cipher-clean
#   ("the architecture was the older shape, built in a different city").
# - s04 causal seam @30→@31→@32 (DEC-0076): rendered legible WITHOUT expository bridge.
#   @30 muddy-way placement → @31 "That placement closed the fifth-ward circuit" → @32
#   "And with it closed the feed returned all five wards ... for the first time". The
#   "why now" reads as a CONSEQUENCE of this afternoon's placement, not an arbitrary pivot.
# - Vibes/state NEVER rendered (schema-forbidden). 19 vibes + 8 state entries consumed as
#   licensing-context only.
# ============================================================================

# fork-001 — scene-A @1-@10 (the-hook-ward + the-feed-station; morning circuit; low-heat establishment)
# rhythm-shape: low-heat establishment. Lens loads consumed: NI:1@3, NI:2@7, NI:3@10;
#   mem:1@3 (witch-label); loc-state:1@1, loc-state:2@3, loc-state:3@8; exposition:2@1
#   (em-dash-fold, first-mention place); vibes:1/2/3/4 (context only).
# variance-moves:
#   - @1-@2 fused (fusion-eligible-run): "came back to me off the overhang-joints first ...
#     and then off the lane-mouth" — terrain-constraint envelope as one ambient motion;
#     two return-targets kept legible.
#   - exposition:2 @1 em-dash-fold rendered verbatim modulo voice ("Taylor's"→"my" per
#     first-person POV resolution): "the lanes east of the water-gate — the old lower-city
#     stretch where close overhangs let a thing pass hand-to-hand, the one ground my
#     coverage cannot reach".
#   - @3 PERSON-FIRST (VOICE-FIXABLE): "I took the gate-tower shadow before I stepped into
#     it" — body placed inside lane geometry (loc-state:2) BEFORE the apparatus reads.
#     mem:1 folded: "the word these streets keep for women like me. I know the word. I let
#     the placement stand anyway." (mem:1 verbatim-modulo-voice; witch-label clamp).
#   - @4-@5 fused (fusion-eligible-run) BUT both landmark-walls kept legible per protected-
#     pattern: "closed the gate-tower boundary on the west. It closed the rendering-yard
#     wall on the east." — the gap is a bounded fact only with both edges named.
#   - @6/@7 two-substrate split PRESERVED (WREN-AS-BOUNDARY-NOT-AFFECTION): @6 "came back
#     to me off the stitch-house route ... returned on the feed" (FEED substrate); @7 "the
#     map marked the stitch-maker route ... written into the internal record as a line"
#     (MAP substrate). NI:2 folded ("runs the map's eastern edge and stops there,
#     unindexed"). Record-keeping register, NO affection/interior moral narration.
#   - @8 PERSON-FIRST + WITHHOLD-AS-BODILY-FACT (VOICE-FIXABLE): "I lifted the stylus. The
#     northern ward-cluster wanted the source-field and my hand did not bring it down; the
#     hand came up off the surface with the field still bare." — physical lift, NOT "she
#     chose not to name".
#   - @9 concrete: "I set the cluster — the first tranche north, the insects laid into the
#     upper lanes". @10 NI:3 folded: "The entry closed and the eastern lanes were not
#     inside it. The boundary was load-bearing now, and the thing that held it had never
#     been priced."
# refusals:
#   - REFUSED invent route/transit detail at @6/@7 beyond loc-state geography (stitch-house
#     route / rendering-yard passage are graph-resident; no added direction).
#   - REFUSED interior-moral-narration at @6/@7/@8 (protected-pattern: record-keeping only).
# bone-walk:
#   @1 -> rendered (fused @1-@2)    @2 -> rendered (fused @1-@2)
#   @3 -> rendered (person-first; mem:1 + NI:1 folded)
#   @4 -> rendered (fused @4-@5; west wall legible)
#   @5 -> rendered (fused @4-@5; east wall legible)
#   @6 -> rendered (feed substrate)    @7 -> rendered (map substrate; NI:2 folded)
#   @8 -> rendered (person-first bodily withhold)
#   @9 -> rendered (concrete placement)    @10 -> rendered (peak; NI:3 folded)
# drift-risk: LOW. Watch: do not let @6/@7 read as affection (held — pure record-keeping).

# fork-002 — scene-B @11-@16 (the-feed-station; morning; held-breath)
# Lens loads: sensory:1@12 (wax-crack spike), sensory:2@16 (bay-warmth up), NI:4@14;
#   loc-state:4@11; vibes:5/6 (context only).
# variance-moves:
#   - @11-@12 fused (fusion-eligible-run): "placed the packet ... and was already most of
#     the way back to the channel. I broke the wax seal" — delivery+open as one motion.
#     loc-state:4 folded (working face where the accounting runs). opposing-force arrives.
#   - sensory:1 @12 SPIKE leads (lens-decider rule 2): "the small sharp crack of it loud in
#     the channel-quiet of the room" — sense-shift is the moment.
#   - COLLISION-COMPLETE-BEFORE-STILLING honored: @13 gate-tower corridor named → @14
#     rendering-yard wall named (NI:4 folded: "the request reading the thing back to me with
#     a resolution it lacked the means to fill") → THEN @15 packet set / stilling. Stilling
#     does NOT split the two boundary-names. Apparatus-terrain-literacy EXACTNESS rendered.
#   - @16 BAY-WARMTH-BODY-PAIRED (VOICE-FIXABLE-adjacent): sensory:2 rendered as body at a
#     working surface, NOT ambient atmosphere — "the morning over the bay had gone warm
#     against me, the damp burned off the stone before the wind off the water came in to
#     clear it." @16 is the SOLE bay-warmth carrier (n07 trimmed upstream); clean close.
# refusals:
#   - REFUSED invent Otto/Jarvis dialogue or packet-content beyond covering-sheet boundary-
#     marks (bones bare; no dialogue file; silent chapter).
#   - REFUSED ambient-atmosphere padding at @16 (body-paired per protected-pattern).
# bone-walk:
#   @11 -> rendered (fused @11-@12)    @12 -> rendered (fused; sensory:1 spike leads)
#   @13 -> rendered (gate-tower named)    @14 -> rendered (rendering-yard named; NI:4 folded; collision complete)
#   @15 -> rendered (stilling-reaction, after both boundaries)
#   @16 -> rendered (bay-warmth body-paired; clean close)
# drift-risk: LOW. Watch: @16 must not linger into atmosphere (held — single body-paired line).

# fork-003 — scene-C @17-@28 (the-feed-station + anchor-column; midday; THE CLIMAX)
# Lens loads: NI:5@19, NI:6@21, NI:10@24, NI:7@27; feel:1@20 (hand-stops);
#   loc-state:5@17, loc-state:6@23; vibes:7/8/9/10/11 (context only).
# variance-moves:
#   - @17-@19: "I took the stylus up. The coverage-entry opened the gap-column, and I wrote
#     the boundary into it in the same flat hand as every deliverable before it" — NI:5
#     folded (the refusal as structural limit, reason not written). REFUSAL-STAYS-FLAT.
#   - @20-@21 rendered as TWO distinct physical acts (fusion-eligible BUT split required):
#     @20 feel:1 PERSON-FIRST (VOICE-FIXABLE): "My hand stopped short of the source-field
#     and did not come down." (feel:1 verbatim-modulo-voice). @21 PERSON-FIRST: "The stylus
#     lifted before the explanation field, the hand not reaching it" (NI:6 folded).
#     REFUSAL-WITHOUT-EXPLANATION-AS-PHYSICAL-WITHHOLD: the absent clause, NOT interior
#     enumeration of what she declines to write.
#   - @22 + @24 (with @23 between, monotonic): @22 "I closed the response entry over the
#     empty field" → NI:10 folded ("the route never written, the burned cloth-account never
#     written, and this; the same withhold, the third time, recognized and not stopped") →
#     @23 "Jarvis took the sealed packet, and the apparatus accepted the coverage limit ...
#     would route around it, which made the withholding a thing no one would ever see" →
#     @24 "The response entry closed the gap-column in flat register." Bone order preserved.
#   - @25-@27 (anchor-column): @25 "I opened the anchor-column." @26-@27 fused (fusion-
#     eligible mirror of @19/@22): "I wrote the entry into it, and I closed it, and the
#     weight settled where I had put it" — ANCHOR-SETTLEMENT-AS-RECORD-KEEPING: NI:7 folded
#     (the route made the eastern boundary; second tranche paid; NO name). "no name set
#     down to carry it" — she does not write a name (stays COLD).
#   - @28 LIFT-AND-MOVE: "I lifted the hand off the column." Clean close, NO catharsis, NO
#     camera-lingers.
# refusals:
#   - REFUSED interior enumeration of withheld content at @20/@21/@24 (the withholding is
#     the physical sequence + the absent clause; NI:10 carries the recognition that it IS a
#     withhold, not WHAT is withheld).
#   - REFUSED name/sentiment at @26/@27 (record-keeping; relational +1.0 enacted as ledger-act).
#   - REFUSED contempt-color at @19/@24 (political_register-prot held; flat register).
# bone-walk:
#   @17 -> rendered (stylus up)    @18 -> rendered (gap-column opens)
#   @19 -> rendered (peak; refusal written flat; NI:5 folded)
#   @20 -> rendered (person-first; feel:1 hand-stops)    @21 -> rendered (person-first; stylus lifts; NI:6)
#   @22 -> rendered (peak; response sealed; NI:10 folded)
#   @23 -> rendered (jarvis takes; apparatus accepts)    @24 -> rendered (gap-column closes flat)
#   @25 -> rendered (anchor-column opens)
#   @26 -> rendered (fused @26-@27; settlement; NI:7)    @27 -> rendered (fused @26-@27; peak; no name)
#   @28 -> rendered (lift-and-move clean close)
# drift-risk: LOW-MED. Watch: @26/@27 must not slip into sentiment (held — ledger-act, no
#   name). @20/@21 kept as two acts (held).

# fork-004 — scene-D @29-@42 (the-feed-station + the-muddy-way; afternoon; INTERIOR CLIMAX then cold-close)
# Lens loads: mem:3@38 (shape-word; Earth-Bet fence), meta:1@38 (cipher-clean), NI:8@38,
#   NI:9@42; sensory:3@30 (full-circuit-weight up), sensory:4@40 (palm-flat drop),
#   sensory:5@42 (end-of-day light down); feel:2@40 (breath-flat); loc-state:7@29,
#   loc-state:8@42; vibes:12-19 (context only).
# variance-moves:
#   - @29-@30 fused (fusion-eligible-run; capability gain lands on @30): "I extended it into
#     the Muddy Way's upper margin ... and the insects filled the upper margin". @30 sensory:3
#     PERSON-FIRST (VOICE-FIXABLE): "the partial weight that had sat under the count all day
#     went to a full one, the circuit settled and whole" — the count's weight felt settling.
#   - s04 CAUSAL SEAM @30→@31→@32 (DEC-0076; rendered legible, NO expository bridge):
#     @31 "That placement closed the fifth-ward circuit." → @32 "And with it closed the feed
#     returned all five wards ... the count running the full circuit for the first time".
#     The full-deployment threshold reads as CONSEQUENCE of this afternoon's placement.
#   - @33 "The count ran the full-circuit return." @34 ACCUMULATION-AS-REFERENT-WEIGHT (do
#     NOT trim): "the place I had come to cover returned now at the full scale, every
#     approach into it mapped and held" — Flea Bottom approaches at prior-architecture-scale.
#   - @35-@37 accounting accumulation kept (referent-weight build before @38): aggregate-
#     shape entry / @36 RATIONALIZATION-ACTIVE harm-prevention column RUNNING ("the gain
#     weighed against the cost the way the gain is always weighed") / @37 reaches breach column.
#   - @38 SHAPEWORD-SURFACE-AND-SUPPRESS (THE most dangerous register-point; EARTH-BET FENCE
#     CLEAN; registers THROUGH the accounting): "the count was running and the record reached
#     past any name for the architecture and landed on the older word — not a description of
#     the thing in front of me but the shape of the thing I did when the world was ending, the
#     one this rhymes with." (mem:3 + NI:8 verbatim-modulo-voice; SHAPE-LANGUAGE ONLY, no
#     proper noun). meta:1 cipher-clean: "The architecture was the older shape, built in a
#     different city." "It was there for one count." — surfaces for one count.
#   - @39 suppression act: "I did not let it settle. The accounting advanced the count past
#     it" (WILLFUL routing-around, NOT dissociation/deliberation).
#   - @40 closes architecture entry WITHOUT it (suppression complete); sensory:4 + feel:2
#     PERSON-FIRST (VOICE-FIXABLE): "my palm flat on the closed page where the stylus had
#     been, my breath staying flat and not lifting." (feel:2 verbatim-modulo-voice).
#   - @41 "The ledger entry took the full-circuit count."
#   - @42 BREACH-AS-COLD-LEDGER-FACT (VOICE-FIXABLE; person-first filing): "The breach column
#     took the threshold entry — the cost filed in the same flat column register as every
#     entry above it" (NI:9 folded). sensory:5 end-of-day light: "the end-of-day light gone
#     off the surface to dimming". Closes COLD: "the word not filed with it." NO catharsis,
#     NO recognition arriving.
# refusals:
#   - REFUSED any Earth-Bet proper noun at @38 (fence CLEAN — shape-language only).
#   - REFUSED expository causal-bridge at @30→@31→@32 (DEC-0076 — rendered as felt
#     consequence via "That placement ... And with it closed").
#   - REFUSED over-compression of @32-@34 accumulation (referent-weight must build).
#   - REFUSED catharsis / recognition-arriving at @42 (cold ledger fact; the moral_framework
#     -1.0 files without absolution).
#   - REFUSED dissociation/moral-deliberation framing at @38/@39 (willful behavioral routing-
#     around; worm-canon consistent).
# bone-walk:
#   @29 -> rendered (fused @29-@30)    @30 -> rendered (person-first; sensory:3; capability gain)
#   @31 -> rendered (causal bridge)    @32 -> rendered (threshold as consequence)
#   @33 -> rendered (full-circuit return)    @34 -> rendered (Flea Bottom approaches; accumulation)
#   @35 -> rendered (aggregate-shape entry)    @36 -> rendered (harm-prevention column running)
#   @37 -> rendered (breach column reached)
#   @38 -> rendered (PEAK; shape-word surfaces; mem:3+meta:1+NI:8; FENCE-CLEAN, through accounting)
#   @39 -> rendered (advances count; suppression)
#   @40 -> rendered (person-first; sensory:4 palm-flat + feel:2 breath; architecture entry closed without word)
#   @41 -> rendered (full-circuit count taken)
#   @42 -> rendered (PEAK; cold ledger fact; sensory:5 end-of-day light; NI:9; word not filed)
# drift-risk: MED at @38 (the airless lever / fence-point) — held: shape-language only,
#   surfaces through accounting, no proper noun, one-count presence then suppressed. LOW
#   elsewhere.

# ============================================================================
# COVERAGE SUMMARY
# 42/42 bones rendered. 0 CUT-BONE. 0 RENDERED-ILLEGIBLE. 0 EMBODIMENT-BLOCKED.
# 0 silent drops. Monotonic bone order preserved (no cross-bone temporal reorder).
# No cross-scene facet migration. Vibes/state never rendered.
# Fusions logged: @1-@2, @4-@5, @11-@12, @26-@27, @29-@30 (all fusion-eligible per scene-map).
# All VOICE-FIXABLE anchors person-first: @3, @8, @20, @21, @30, @40, @42. Confirmed.
# @38 Earth-Bet fence CLEAN (no proper noun); registers THROUGH accounting register. Confirmed.
# s04 causal seam @30→@31→@32 legible without expository bridge. Confirmed.
# ============================================================================
</content>
