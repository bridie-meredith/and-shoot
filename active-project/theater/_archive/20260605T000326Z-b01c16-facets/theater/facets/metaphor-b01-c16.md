facet: metaphor
episode: b01-c16
author: editor
---
# refused: 0 fires. FALLING aftermath chapter; single rank-mover (moral_legibility +0.5). The one
#   HINGE-magnitude beat — the walk-away (@19 stop / @21 turn) as foreclosure-not-suppression — is the
#   exact beat the scene-map flags NO-THESIS-RESTATEMENT (DEC-0090 item 4, HARD at /and-stitch Phase 9):
#   the foreclosure-vs-suppression distinction MUST land in behavior, never in narrator-voice. A figure
#   here ("she turned the way a ledger closes on a line it will not reopen") restates the distinction the
#   behavior is built to carry — AP2 (figure already in the proto-line/peak-bone enactment) + the over-
#   explanation DEC-0090 forbids. The turn IS the figure; figurative re-render is anti-form at the peak.
#
# --- REFUSE LOG ---
# scene-A @1-@8: all refused. The-sept-corner geometry (eaves/reed-mat/crates) + populated clearing
#   (old man/woman/child) are grounding/world-weight — no memory or feeling anchor; functional-register
#   fail (no callback, no dark-humor). @7 (Halvard lifts the head): scene-break peak rendered direct;
#   metaphor at peak anti-form.
# scene-B @9-@18: refused. The candidate the brief names — the rendering-yard fence as "the edge of
#   the accounting" (@13 press / @16 grip / @25 reach) — is REFUSED. The fence-grip is the protected
#   POSITION-ARITHMETIC-AS-FENCE-GRIP mechanism (scene-map, all 3 audience personas named it the chapter's
#   highest-load beat): the stitcher MUST hold the bare physical specificity (press/grip/release) and NOT
#   dilute it. A simile that names the fence as "the edge of her accounting" abstracts the exact concrete
#   correlate the scene is built to keep physical — AP2 + dilution of the protected pattern. Memory:1 @10
#   ("the account they post to does not change") already carries the ledger figure in the lens layer; a
#   fence-as-ledger metaphor doubles it (AP4). @17 (the explicit ask): peak bone, rendered direct.
# scene-C @19-@27: refused (see header). @21 turn / @19 stop = the foreclosure peak; behavior carries it
#   per the HARD Phase-9 watch. feeling:1 @19 already holds the somatic ("the way a thing stands when it
#   has stopped weighing") — a metaphor at @19 doubles the feeling-flag (AP3). @27 (circuit resumes):
#   CIRCUIT-RESUMES-UNCHANGED — render flat; figurative reach on the flat-resume inverts the bleakness.
