facet: vibes-updates
episode: b01-c10
author: showrunner (R1 author exception — cross-cutting vibe-cloud visibility)
mode: R1-blind
generated: 2026-06-02
cull-count: 0
---

# rubric-carve-out — chapter-internal cast member and locations without library cards (corwick; loc:the-feed-station)
#
# rubric-vibes.md (design/shoot-v2/rubric-vibes.md) § Required gates, gate 1 (Target validity)
#
# Carve-out scope: vibes:15 targeting actor:corwick; vibes:16 targeting loc:the-feed-station.
#
# Carve-out rule: gate 1 requires slug to exist in card library. corwick is a named cast member
#   in the b01c10 bones header (cast: jarvis-coin-kl-courier, taylor-hebert-kl-122ac, corwick)
#   with no formal card at cards/personas/ or active-project/actors/. loc:the-feed-station appears
#   across scene-A and scene-D (scene-map; location-state facets b01c08–b01c10) as the recurring
#   accounting workspace and chapter-close anchor, with no formal card at cards/locations/ or
#   active-project/warehouse/. The c08 vibes facet established this pattern for chapter-internal
#   staging locations (vibes-b01-c08.md, carve-out preamble, vibes:1 / loc:the-hook-ward); the
#   same logic applies here.
#
# Coverage justification (corwick): the apparatus-ran event (scene-C @15-@18) directly affects
#   Corwick as the detained party — the fan-out coherence gate (rubric gate 7) requires firing
#   across affected entities; targeting episode-scope only would violate AP9. Corwick's operational
#   consequence (body-mapped across months, delivered through the channel, detained within two
#   circuit-passes, face persisting in the feed-record) is entity-bound and cannot be held by
#   episode scope without loss of operator-bias specificity. Carve-out scoped to vibes:15 only.
#
# Coverage justification (the-feed-station): the feed-station is the recurring accounting
#   workspace across b01c07–b01c10 and into future chapters; vibes:16 establishes the first
#   keyword on a repeatedly-staged entity whose atmosphere is load-bearing for studio palette.
#   The entity-bound vibe (accounting-workspace-weight) biases studio toward end-of-day ledger
#   register specifically at this location, not at all locations. AP9 requires entity over scope;
#   the entity-bound keyword is the correct form here. Carve-out scoped to vibes:16 only.
#
# Per-entry annotations:
#   - vibes:15 (no anchor): chapter-internal-cast carve-out (gate 1); no actor card at
#     cards/personas/ or active-project/actors/; bones-header named cast member; c08 precedent
#     (loc:the-hook-ward) applies as structural parallel; auditor may promote to card-creation task.
#   - vibes:16 (no anchor): chapter-internal-location carve-out (gate 1); same carve-out as
#     c08 vibes:1 (loc:the-hook-ward); no formal location card; recurring chapter-close anchor;
#     auditor may promote to card-creation task.

# ── scene-A: the formalization arrives (@1-@5) ───────────────────────────────────

1 @2 actor:taylor-hebert-kl-122ac ++ atonement-as-repetition: [formal-naming-as-fait-accompli, function-declared-before-consent-re-sought, informal-deniability-foreclosed-in-the-text] | licensed-by: state-update:1, proto:2
  # The packet-text at @2 (scene-A peak; position-prot-rise +0.5; cl-d07a first tranche) names
  # Taylor as an ongoing function in Otto's architecture — not a question but a declaration.
  # This is a new event-frame for atonement-as-repetition: prior tokens cover
  # came-to-refuse-control, builds-surveillance-architecture, specific-shape-of-what-she-can-do-
  # meets-what-the-situation-required (the-recognition-is-the-ledger-completing-not-a-revelation)
  # from the world-build and watcher-network-absorbed-without-asking (c08) and
  # double-omission-sealed-in-wax-not-named (c09). The formal-naming is structurally distinct:
  # the arrangement is named aloud in writing for the first time; the form fits in a new register
  # (the declaration, not the accumulation). No string overlap with existing bundle.

2 @2 actor:taylor-hebert-kl-122ac + the-formal-instrument: [declared-function-in-otto-architecture, instrument-register-not-contractor, position-of-no-exit-named-before-confirmed] | licensed-by: state-update:1, proto:2
  # New keyword. The qualitative consequence of the naming: Taylor moves from informal-conduit
  # to declared-function in the packet-text. The existing vibe-set has no keyword for this
  # operational-standing shift. Biases: dialogue-writer toward instrument-register (not hired-
  # help or contractor; she is named as a function); NI toward position-non-exit read on
  # every subsequent deployment; behavior-pack toward the no-exit posture Taylor now inhabits.
  # The informal-deniability foreclosed (token in vibes:1) is the opportunity-cost side;
  # the-formal-instrument is the qualitative-consequence side — what she now IS in the
  # architecture, not only what she has lost.

3 @4 actor:taylor-hebert-kl-122ac ++ rising entrapment: [fold-as-body-enactment-of-acceptance, acceptance-without-interrogation, the-body-receives-before-the-calculus-closes] | licensed-by: state-update:2, proto:4
  # @4 (taylor folds the packet; social_tether-prot-rise +0.5; cl03b). The fold is a body
  # gesture of receiving-and-accepting: Taylor folds on the content without interrogating it.
  # New event-frame: prior rising-entrapment tokens cover each-trade-lowers-next-refusal-
  # threshold, network-is-the-cage, second-answer-forecloses-the-path-back, arrangement-
  # outlasts-the-architect (world-build) and daily-pattern-of-named-individual-mapped-and-
  # withheld, coverage-architecture-now-person-specific, vulnerable-window-inside-the-grid (c09).
  # The body-acceptance gesture is distinct: the entrapment now operates at the somatic level —
  # the body receives the arrangement before the calculus closes. No string overlap.

# ── scene-B: the courier surrender (@6-@12) ──────────────────────────────────────

4 @10 actor:taylor-hebert-kl-122ac ++ atonement-as-repetition: [body-map-translated-across-substrate-boundary, months-of-observation-converted-to-channel-register, named-person-commissioned-through-the-form] | licensed-by: state-update:3, feel:1, proto:10
  # @10 (taylor translates the body-map; moral_framework -0.5 first tranche; cl03a cost-side).
  # The translation is the act of rendering internal substrate into channel form — the most
  # specific instance of the atonement pattern yet: prior accumulations (watcher-network,
  # double-omission) were geographic or archival; this is a named person's body-map, held
  # internally for months, converted into a channel-transmissible record in one act. New
  # event-frame from the prior bundle. feel:1 licenses the somatic residue. No string overlap.

5 @10 actor:taylor-hebert-kl-122ac + cost-made-visible-to-the-ledger: [first-named-commission-priced, body-map-as-ledger-entry, moral-framework-cost-entered-not-deferred] | licensed-by: state-update:3, feel:1, proto:10, proto:11
  # New keyword. The qualitative consequence of the first formal commission: the cost is now
  # a ledger entry, not a withheld item or a rationalized exception. Distinct from
  # the-formal-instrument (which captures standing in the architecture); this captures the
  # first moment a named person's body appears in the cost column. Biases: feeling fork toward
  # cost-bearing somatic register (the body marks the moment the ledger opens on a name);
  # dialogue-writer toward closed-entry voice when Taylor processes subsequent commissions;
  # NI toward recognition-not-yet-recognized register on @21 and @27.

6 @11 actor:jarvis-coin-kl-courier ++ rising entrapment: [channel-receives-first-named-commission, structural-exposure-vector-no-longer-latent, the-delivery-that-completes-the-chain-from-taylor-to-otto] | licensed-by: state-update:4, proto:11
  # @11 (taylor routes the body-map; social_tether-antag +0.5; cl-antag-d10 opening). Jarvis
  # is the channel through which the body-map travels. Pre-seeded token
  # structural-exposure-vector-latent-until-d10 named the latency; d10 is now on-page and the
  # latency resolves at this chapter. New event-frame: the courier handles the first named-person
  # commission; his structural exposure is no longer latent. The token the-courier-detained-at-d10-
  # whose-route-map-reached-Otto (pre-seeded) captures the future consequence; the new tokens
  # capture the present act. No string overlap with existing bundle.

# ── scene-C: the detention lands in the feed (@13-@19) ──────────────────────────

7 @15 episode + the-apparatus-ran: [green-channel-operational-in-two-circuit-passes, absence-as-confirmation, apparatus-moved-through-the-network-without-consent-or-design] | licensed-by: proto:15, proto:17, proto:18, state-update:5
  # New episode-scope keyword. The chapter's central operational consequence: the Green
  # succession channel ran through Taylor's network within two circuit-passes of her delivery.
  # Corwick absent from geometry (@15) + Gold Cloak patrol posted (@17) + insect-feed
  # apparatus-confirmation (@18) together constitute the apparatus-ran event. Episode-scope
  # because the operational consequence is ambient for the whole chapter's rendering context —
  # all operators working in b01c10 read this as the frame within which every scene-D bone
  # occurs. social_tether-antag +1.0 (state-update:5) confirms leverage structural.

8 @15 actor:taylor-hebert-kl-122ac + the-apparatus-ran: [feed-confirmed-downstream-consequence-of-delivery, absence-reads-as-action, operational-cost-not-yet-named] | licensed-by: proto:15, proto:17, proto:18, state-update:5
  # New keyword on Taylor. The feed confirms the apparatus ran: the road losing Corwick is
  # the first perceptual evidence that her delivery produced a downstream consequence. Distinct
  # from cost-made-visible-to-the-ledger (which is the commission moment at @10); this is the
  # consequence-landing (the confirmation that the body-map reached the apparatus and the
  # apparatus acted). Biases: NI toward operational-consequence read when Taylor sweeps the
  # errand-corridor; feeling fork toward cold-registration somatic tells as the feed returns
  # absence; dialogue-writer toward consequence-not-named register in scene-D.

9 @18 actor:taylor-hebert-kl-122ac ++ rising entrapment: [feed-confirms-apparatus-as-actor, the-cage-is-demonstrably-operational, network-executed-without-her-consent-or-design] | licensed-by: state-update:5, proto:18
  # @18 (insect-feed sweeps the errand-corridor; social_tether-antag +1.0; cl-antag-d10).
  # The feed sweep confirms the apparatus ran — the cage is now demonstrably operational, not
  # merely structural. New event-frame: prior rising-entrapment tokens cover the architecture's
  # form (network-is-the-cage, each-trade-lowers-next-refusal-threshold) and person-specific
  # entrapment of others (c09: daily-pattern-of-named-individual-mapped-and-withheld). This
  # token captures the cage's demonstrated operational agency: the network executed a consequence
  # without Taylor's direct involvement in the final step. No string overlap with existing bundle.

# ── scene-D: the ledger's first closed entry with a name (@20-@27) ──────────────

10 @21 actor:taylor-hebert-kl-122ac ++ atonement-as-repetition: [named-person-closed-entry-in-the-ledger, systematic-override-rationalized-and-filed, the-ledger-entry-is-the-act] | licensed-by: state-update:6, proto:21
  # @21 (taylor writes corwick; moral_framework -0.5 second tranche; cl03a second tranche;
  # systematic-override-rationalized threshold crossed). The inscription act: the body-map
  # (months of observation, translated at @10) becomes a closed entry with a name in the ledger.
  # New event-frame: no prior atonement-as-repetition token covers the formal-inscription act
  # (naming-a-person-in-the-ledger as the form of the atonement's completion). The ledger-entry-
  # is-the-thing token in the base contempt-without-refusal cloud is about contempt's register,
  # not the inscription act itself — no string overlap. No string overlap with existing bundle.

11 @27 actor:taylor-hebert-kl-122ac ++ cold-utilitarian interiority: [face-persisting-as-feed-datum-after-accounting-close, ledger-does-not-ask-for-the-face-back, feed-record-outlasts-the-accounting] | licensed-by: state-update:6, feel:2, proto:27
  # @27 (corwick squares the feed-record; moral_legibility_to_self +0.5; terminal; W2 bone 3).
  # The face that persists in the feed-record after the ledger closes. New event-frame: existing
  # cold-utilitarian-interiority tokens cover ledger-runs-without-being-switched-on,
  # accounting-is-what-she-does-to-herself, calculation-precedes-feeling-not-replaces-it,
  # affect-appears-as-brief-body-signal-then-suppressed. The face-persisting-as-feed-datum is
  # a new form of the cold-utilitarian register: the calculation is complete (ledger closed @25)
  # but the feed-record does not release. The cold register now includes the shape of what it
  # cannot close out. feel:2 provides the somatic licensing. No string overlap with existing bundle.

12 @27 actor:taylor-hebert-kl-122ac + the-closed-entry-that-stays: [face-as-feed-datum-not-ledger-notation, record-persists-past-accounting-close, ledger-closed-feed-record-open] | licensed-by: state-update:6, feel:2, proto:27
  # New keyword. The qualitative consequence of the terminal image: the face in the feed-record
  # is distinct from the closed ledger entry. The ledger is done; the feed-record is not.
  # Distinct from cost-made-visible-to-the-ledger (which is the commission moment @10/@11);
  # this is the residue after the close — the register mismatch between what the accounting
  # can conclude and what the feed returns. Biases: NI toward feed-datum register in future
  # chapters when Taylor's insect-feed carries a person she has priced (the face stays; the
  # ledger entry is separate); feeling fork toward suppressed-recognition somatic tells where
  # the accounting is closed but the feed is not; dialogue-writer toward silence-after-close register.

# ── episode scope ─────────────────────────────────────────────────────────────────

13 episode + accounting-close-with-name: [first-named-commission-formalized-and-closed, cold-formalization-to-irreversible-surrender-to-operational-consequence-to-accounting-close, the-arc-runs-in-one-chapter] | licensed-by: proto:2, proto:21, proto:25, proto:27
  # New episode-scope keyword. The chapter's tonal spine as ambient register: the naming
  # (scene-A), the body-map surrender (scene-B), the apparatus-ran confirmation (scene-C),
  # and the ledger close with face-persistence (scene-D) form a single closed arc — the first
  # chapter in which a named person goes from body-map to ledger entry to feed-datum residue
  # within one accounting session. Biases all operators toward cold-formalization register:
  # no catharsis, no open question, no deflation — the arc closes and the face stays.

# ── cast member without formal card ───────────────────────────────────────────────

14 actor:corwick + detained-through-the-channel: [body-mapped-across-months-delivered-through-taylor-channel, detained-within-two-circuit-passes-of-delivery, feed-datum-persisting-in-the-record-after-detention, apparatus-confirmed-through-this-body] | licensed-by: proto:9, proto:15, proto:17, proto:18, proto:27
  # Off-anchor: the detention spans scene-C (@15-@18) and persists as feed-datum residue in
  # scene-D (@27). No single bone is the full licensing event. Carve-out (gate 1): no formal
  # actor card; bones-header named cast member; same carve-out logic as c08 loc:the-hook-ward.
  # New keyword for corwick (empty vibe-set). The fan-out coherence gate requires the apparatus-
  # ran event to fire on affected entities: Corwick is the detained party. Biases: future
  # impersonator / reference operators toward apparatus-victim register if Corwick is referenced
  # in b01c11+; NI toward the feed-datum register when the posture-class / gait-signature appear
  # in the feed-record; studio toward the errand-corridor and lower-gate-road as the geometry
  # of this detention. The keyword is also load-bearing for the relational_anchor_status axis
  # (Corwick as the first priced named person in the ledger — distinct from the cost-bearer
  # [Wren], who is never priced; Corwick IS priced and this is the qualitative register of that).

# ── location without formal card ─────────────────────────────────────────────────

15 loc:the-feed-station + accounting-workspace-weight: [end-of-day-ledger-surface, wax-and-notation-and-silence, the-place-where-the-accounting-runs-and-does-not-run-clean] | licensed-by: proto:22, proto:25, proto:26, proto:27
  # Off-anchor: the feed-station's keyword is the accumulation of scene-A and scene-D — the
  # accounting happens here across the chapter. No single bone is the full licensing event.
  # Carve-out (gate 1): no formal location card; recurring chapter-close anchor across
  # b01c07–b01c10; same carve-out logic as c08 loc:the-hook-ward. New keyword (first vibe
  # for this location). Biases: studio toward end-of-day-weight palette when the feed-station
  # is the set (the stone surface, the dry wax, the ledger, the silence after close); sensory
  # fork toward tactile-anchor and thermal-register when the accounting is in progress or
  # concluded; metaphor licensing toward the accounting-surface as a legitimate environmental
  # anchor for cost-register figures.
