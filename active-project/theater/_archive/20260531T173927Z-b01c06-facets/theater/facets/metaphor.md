facet: metaphor
episode: b01c06
author: editor
---

# rubric-carve-out — zero-fire episode; full refuse-log below
#
# design/shoot-v2/rubric-metaphor.md § Per-scene cap (hard) and sparsity
#
# Carve-out scope: N/A — no entries; refuse-log documents per-candidate editorial judgment
# Carve-out rule: zero-fires is explicitly permitted ("zero-fires-per-episode acceptable"); no
#   carve-out from rubric is required; this preamble is a refuse-log only.
# Coverage justification: every peak and peak-shadow bone in b01c06 is blocked by one or more
#   of AP7 (peak-zone fire), protected-pattern conflict (de-abstraction integrity, omission-as-
#   physical-act, no-interior-narration), AP13 (figurative-mode / beat-position mismatch), or
#   absence of a confirmable memory/feeling anchor (R1 BLIND — anchor must be named by description;
#   no anchor survives the anti-pattern analysis to the point where a provisional entry is
#   warranted). Refuse-log follows for auditor traceability.

# --- REFUSE LOG ---
#
# @4 (Wren speaks to Taylor — peak-bone, scene-A, relational_anchor_status +1.0)
#   Refused: AP6 voice-register conflict. The scene-map and substance contract enforce Wren's
#   delivery as Flea Bottom plain-speech ("she says it plainly, the way a Flea Bottom child says
#   something she has already checked and found true"). A metaphor overlaid on this beat aestheticizes
#   a register that the chapter's protected patterns require to stay practical. Additionally, @4 is
#   the scene-A climax bone — AP7-equivalent under the peak-bones framework applies, and no dark-humor
#   deflation is structurally appropriate at a relational-gain peak (deflation would contradict the
#   beat's function as first-tranche of relational_anchor_status).
#
# @8 (Taylor blanks the contact-source field — peak-bone, scene-A, omission ENACTED)
#   Refused: protected-pattern hard conflict. The scene-map explicitly marks @7-@8 as
#   "omission-as-physical-act — must NOT become interior moral narration." A metaphor at @8
#   ("the blank is X") is exactly what this protected pattern prohibits: naming the comparison
#   instead of letting the physical act carry it. De-abstraction integrity applies.
#
# @15 (Taylor lowers the Jarvis-channel form — peak-shadow, scene-B)
#   Refused: AP7-equivalent. Scene-B has no peak-bones by design (held scene — flat-tense). @15
#   is the loaded-pause bone; the scene-map protected pattern for @15 is "loaded-pause integrity —
#   the not-sending is the tension, must not read as idle procedure." A metaphor here risks
#   converting the procedural tension into a named figure, which would aestheticize the ambiguity
#   the held scene depends on. Additionally no confirmable feeling/memory anchor at this bone
#   (R1 BLIND: the held scene has no axis-move and no AP-licensed interior registration).
#
# @20 (Taylor stills the hand — peak-shadow, scene-C, verdict-pause)
#   Refused on two grounds:
#   (1) Beat-position mismatch (AP13-equivalent): @20 is the anticipatory rise into the seal
#   (@23), not the trailing edge of a peak. A callback-register metaphor ("the hand holds the
#   shape it held the last time...") is mis-registered at an ascending beat — it would import
#   retrospective recognition into a beat whose function is suspended action before the move fires.
#   (2) Protected-pattern conflict: the scene-map marks @20 as "render as physical stillness/
#   suspension of the hand — NOT interior verdict-narration." A simile comparing this stillness
#   to a prior irrevocable threshold act is a form of interior narration even without named
#   Earth-Bet nouns.
#
# @23 (Taylor seals the Jarvis-channel form — peak-bone, scene-C, moral_framework -1.0 THE SEND)
#   Refused: AP7 default-refuse. @23 is the moral peak of the chapter and of the bone-gate CLIMAX
#   scene. The dark-humor-deflation exception exists but does not apply: the chapter's declared
#   register is "tragic/ironic-causal (the ledger is the catastrophe; the accuracy is the horror)"
#   and the task framing confirms "metaphor earns its place only at the moral peak if at all" —
#   meaning the bar at the peak is maximal, not lowered. A deflating metaphor at THE SEND would
#   soften the moral weight of the irrevocable breach. The scene-map explicitly protects this bone:
#   "de-abstraction integrity — do NOT re-abstract at stitch." A metaphor here is re-abstraction.
#
# @25 (Taylor opens the ward-coverage notes — peak-bone, scene-C, moral_legibility_to_self +1.0)
#   Refused: de-abstraction integrity. The scene-map protected pattern for @25-@26 is "omission-
#   contrast — enacted as two physical acts, the axis-move fires on the opening, NOT on Taylor
#   naming the comparison." A metaphor at @25 ("the blank field is X") is precisely the naming of
#   the comparison that the protected pattern prohibits. The contrast must be enacted, not figured.
#   Additionally, any metaphor that frames the blank field figuratively re-introduces the abstraction
#   that the b01c06 depth pass (post-AIRLESS cold-read) was designed to cure.
#
# --- PER-FILE CULL: zero entries authored; zero entries culled. ---
# --- SPARSITY CHECK: 0/26 = 0%. Within 0-3% target. PASS. ---
# --- PER-SCENE CAP CHECK: 0 fires in all three scenes. PASS. ---
