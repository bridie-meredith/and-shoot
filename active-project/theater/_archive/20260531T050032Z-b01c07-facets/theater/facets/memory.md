facet: memory
episode: b01c07
author: taylor-hebert-kl-122ac (dialogue-writer interiority fork, R1 fresh)
narrator: taylor-hebert-kl-122ac
r2-judge: taylor-hebert-kl-122ac (R2 GRAPH-AWARE / FACET-JUDGE OVERRIDE MODE)
r2-rubric: design/shoot-v2/rubric-memory-flags.md (V3 LOCKED 2026-05-21)
r2-counts: KEEP 1 / DELETE 1 / ADD 0 / REWRITE 0
r2-cite-index-hash: 4b1789810b83040f443958cffc443ec91345befc
r2-judge-date: 2026-05-31
---
# R2 GRAPH-AWARE MUTATION (memory judge, POV taylor-hebert-kl-122ac)
# ============================================================================
# mem:1 @6 — DELETED at R2. Spineless fire. The R1 carve-out (below, retained for
#   audit trail) deferred the @6 spine to the NI/feel author as a REQUIRED co-citation,
#   on the premise it would be supplied this round. At R2-judge time the LOCKED graph
#   shows NI is SILENT at @6 — interest-narrator.md fires narrator:1-7 on @13/@14/@15/
#   @19/@20/@22/@23 ONLY; the NI carve-out header documents Scene-01 (@1-@8) firing ZERO
#   BY DESIGN (all seven NI fires cluster on the rising scenes 13-23). The NI refusal at
#   @6 is deliberate and rubric-correct (scene-A flat-low, all-held; NI reads forward on
#   rising zones — exactly the inverse of memory-flags). There is NO missed NI fire to
#   repair. The V3 feel-as-spine carve-out does NOT rescue: conditions (1) hinge ✓ and
#   (2) stakes_axis social_tether ∈ axes_held ✓ hold, but condition (3) — a feel-flag
#   fire on the SAME @6 anchor — FAILS: feeling.md fires ONLY @18 (one fire, taylor:2).
#   No spine, standard or carve-out, exists at @6. Per § Licensing-discipline REJECT /
#   anti-pattern #7 (spineless fire) → DELETE. Doubled-register impact assessed below
#   (per-season coverage; single-register episode acceptable; spineless fire is not
#   retained to satisfy a per-season file-level preference).
# ============================================================================
#
# --- R1 carve-out header (RETAINED FOR AUDIT TRAIL — its @6 spine-deferral did not resolve) ---
# rubric-carve-out — spine co-citation deferred to NI/feel author (R1 blind-fork)
#
# rubric-memory-flags.md (design/shoot-v2/rubric-memory-flags.md) § Licensing-discipline / narrator-interest co-citation
#
# Carve-out scope: both fires (mem:1 @6, mem:2 @19).
# Carve-out rule: this is the Phase 1 R1 blind fork — the fork authored against rubric + POV stack +
#   base proto-lines + substance + monument cards only, fenced from NI working memory. Per-fire spine
#   is named below as a REQUIRED co-citation the NI (or feel, under V3) author must satisfy; if the NI
#   author does not fire on the named anchor, this is a flag-back to NI for missed-fire repair, NOT a
#   memory-flag defect.
#   [R2 RESOLUTION: at @6 the NI refusal is DELIBERATE-AND-CORRECT (scene-A flat-low, all-held;
#    NI fires forward on rising zones per its own rubric), so there is no missed-fire to flag back.
#    A spineless fire with no repairable missed-fire is a memory-flag DELETE, not a NI kickback.
#    The @19 deferral DID resolve — NI fires narrator:4 @19.]
# Coverage justification: chapter is dramatic_shape: hinge (showrunner memory). Both anchors sit at
#   beats the substance hand-off names as load-bearing NI anchors (s01n06 WATCH-5; s03n01+n02 named-death
#   ledger image). Standard NI co-citation expected at both; V3 feel-as-spine carve-out is the fallback
#   at @6 only (scene-A stakes-axis social_tether HELD per all_held:true).
#   [R2: the @6 fallback FAILED — feel fires only @18, not @6, so carve-out condition (3) is unmet.]
#
# Doubled-register construction (file-level — both registers present):
# - mem:1 @6  Earth-Bet displacement, shape-only: the override-architecture residue (the wide tracking-net
#             she once ran, sealed; surfaces as the passive feed that stands where it stands).
#             [R2: DELETED — spineless. File now carries the Westerosi register only this episode.]
# - mem:2 @19 the doubled-register exemplar — one bone, one scene, one fire (s01e01:73 precedent):
#             clause-1 = Westerosi-monument clamp (the founding death the slower method produced — the
#             fever season two years back, the maester-call not routed, "she's why I'm in Flea Bottom at
#             all"); clause-2 = Earth-Bet displacement, shape-only (the dying-ward / failed-recognition-
#             by-protector shape, the smaller door, the same arithmetic in another tongue).
#             [R2 NOTE: mem:2's clause-2 carries an Earth-Bet displacement SHAPE inside a single
#              spined fire — so the deleted-mem:1 register survives AS THE SUBORDINATE CLAUSE of the
#              surviving doubled-register exemplar. The file is single-FIRE-register (one Westerosi-
#              primary fire) but the Earth-Bet shape is not wholly absent from the file; see gate
#              status in the decision shard. Per-season doubled-register coverage is the binding test.]
# Frequency: [R1: 2 fires / 25 bones = 8%.] R2: 1 fire / 25 bones = 4.0% (in the 1-5% scene-cap-pulled
#   floor zone; below the 5-12% curve-shape band — flagged as a per-season under-fire SIGNAL, NOT a
#   per-entry defect: the surviving fire is correct and the deleted fire was spineless. Sparsity is
#   load-bearing; under-firing a spineless beat is the rubric-preferred move).
#   Zero fires on peak-bones (@14 @15 @18 @22) — @19 is peak-shadow resonance-tail of the @18 naming-peak.
#
# Per-entry annotations:
# - mem:1 @6:  [DELETED @R2 — see mutation header.] REQUIRED spine — NI @6 (WATCH-5 insect-feed-placed-
#     Halvard; named LOAD-BEARING in the bones FACET HAND-OFF) → standard NI co-citation expected. V3
#     feel-as-spine fallback eligible (hinge; scene-A social_tether HELD) if NI is silent and feel @6
#     fires. [R2: NI silent @6 AND feel silent @6 → no spine → DELETE.]
# - mem:2 @19: REQUIRED spine — NI @19 (named-death-as-ledger-entry image / WATCH-1 weight; named
#     LOAD-BEARING in the bones FACET HAND-OFF) → standard NI co-citation expected.
#     [R2: NI fires narrator:4 @19 ("the name goes down the way an entry goes into a ledger — Wenna
#      Cobb, six, Pig-Tallow Lane ...") — standard NI co-citation SATISFIED. KEEP.]
#
# margit referral (SIGNAL, not HARD — § monument-card-resolution test): mem:2 @19 primary target uses a
#   structurally-clear free-text gloss; no monument card yet exists for the founding-death /
#   slower-method-body-count Westerosi monument. Refer for card creation with a mechanism-descriptive
#   slug (e.g. monument-founding-death-the-method-produced). The earth-bet displacement gloss is the
#   secondary cross-register note, not a separate fire.
#   [R2: margit referral CARRIED FORWARD — KEEP mem:2 with the free-text gloss; the referral is the
#    resolution path (SIGNAL, gloss structurally clear, card queued). NOT a HARD blocker.]

2 @19 the name goes down level: a child, a lane, a fever two seasons back her coverage was not yet here to route against — the founding entry, the one the slower way left unpaid and she did not, which is the whole reason the count is hers to keep; and under it the older shape, a smaller door that stayed shut on someone she could not reach in time, the same arithmetic in another tongue. -> (westeros: founding-death-the-slower-method-produced) [+ earth-bet: failed-recognition-by-protector / dying-ward shape]
