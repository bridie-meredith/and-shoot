facet: metaphor
episode: b01c10
author: editor
r2-pass: R2 graph-aware metaphor judge (2026-06-02; cite_index_hash f0fe62ec02a561fa69d8587b2d2be0f1c0e0d841; LOCKED R1 graph; self-scoped deletes only; add-cap 3; REFUSE-BY-DEFAULT). Verdict: 0 entries confirmed. K/D/A = 0/0/0. R1 graph reveals 4 anchors (feel:1 @10, feel:2 @27, mem:1 @16, mem:2 @24). All 4 examined for licensed ADD. All 4 REFUSED — see r2-decision-shard.md. 0/27 = 0% remains in-band (sparsity 0–3%).
---

# R2 confirms 0 entries. All candidates refused.
#
# Anchor-level ADD analysis (R2 graph-aware pass):
#
# feel:1 @10 (scene-B peak — taylor translates the body-map)
#   REFUSE. feel:1 is 1 layer from {memory, feeling, tens}. No memory anchor at @10.
#   Tens = peak-zone (AP7 default-refuse; scene-map marks @10 as a central-event peak-bone).
#   Even setting AP7 aside: the scene-map protected-pattern SURRENDER-AS-IRREVERSIBLE-ACT
#   explicitly forbids figurative overlay at @10/@11 — "render as physical irreversible acts,
#   NOT data-record transaction; apparatus-muffle." Dark-humor deflation at the surrender peak
#   would impose ironic cognitive register at the beat the chapter contract requires direct.
#   NI @10 is present and carries figurative pressure ("the months of it come up out of the
#   feed-record in her hand ... the conversion priced and paid in the one motion that cannot
#   be unmade") — any metaphor would AP3-double the NI content. ≥2-layer test: 1 qualifying
#   layer (feel:1). FAIL.
#
# feel:2 @27 (scene-D terminal — corwick squares the feed-record)
#   REFUSE. feel:2 is 1 layer from {memory, feeling, tens}. No memory anchor at @27.
#   Tens = terminal thesis-image (falling-to-thesis-image, end-chapter weight; not quiet-zone).
#   Protected-pattern hard-forbids any figurative mode that functions as interior-recognition
#   vehicle: "render the face as a physical feed-datum [posture-class/gait-signature persisting]
#   NOT as interior emotional realization." Dark humor or memory-callback metaphor at @27 would
#   convert the suppressed-recognition into named-recognition — the exact inversion the
#   chapter's suppressed-recognition architecture (moral_legibility_to_self +0.5, crack deepens
#   does not open) is built to prevent. NI @27 already holds the full weight ("the posture
#   squares again in the feed-record after the entry is closed ... and the ledger does not ask
#   her to stay"). ≥2-layer test: 1 qualifying layer (feel:2). FAIL.
#
# mem:1 @16 (scene-C peak-shadow — the stone-post marks the side-exit)
#   REFUSE. mem:1 is 1 layer from {memory, feeling, tens}. No feeling anchor at @16.
#   Tens at @16: pre-peak in a rising-to-PEAK scene (@17/@18 are the peak-bones) — approach
#   zone, not trailing-edge. G5 position-gate: an add at approach-zone must defend against
#   deceleration; a metaphor here arrests the rising momentum before the detention peak lands.
#   Tens approach-zone does not support (AP7-adjacent for pre-peak; rubric allows tens=2
#   only at trailing-edge-POST-peak). AP4 risk: the memory-flag text ("the side-exit keeps
#   the shape of how a machinery this old empties a corridor of one body and leaves the stone
#   unmarked") is already figurative in register and carries the apparatus-shape comparison —
#   an explicit metaphor entry would double the figure mem:1 already deploys. NI @16 present
#   (added by R2-NI as spine provision) but NI is not a qualifying layer for the
#   ≥2-from-{memory, feeling, tens} test. ≥2-layer test: 1 qualifying layer (mem:1). FAIL.
#
# mem:2 @24 (scene-D peak-shadow — corwick crosses the errand-corridor)
#   REFUSE (AP4). mem:2 is 1 layer from {memory, feeling, tens}. No feeling anchor at @24.
#   Tens: falling-to-thesis-image scene-D, peak-shadow position. Possible tens=2 in falling
#   zone — granting tens=2 as a support layer gives 2 qualifying layers (mem + tens=2),
#   technically meeting the ≥2 threshold. However Q1 blocks: the memory-flag text for @24
#   ("the crossing is one entry in the months of him she kept without his leave, the record
#   closing the same way the old architecture closed around bodies it never asked") already
#   deploys the comparison — "closing the same way the old architecture closed" IS the
#   figurative move. An explicit metaphor entry at @24 would AP4-double the figure mem:2
#   already carries. NI @24 (added by R2-NI) carries the same accumulated-feed register.
#   Q1 (does the metaphor add what mem:2 + NI @24 don't already carry?) fails — the
#   comparator that would license a metaphor here is already embedded in the memory-flag text.
#   Additionally, the scene-map W2 structure (@23/@24/@27) protected-pattern forbids
#   interior-collapse: a metaphor at @24 risks bleeding figurative weight into the @27
#   terminal bone, which must render as somatic feed-datum only. REFUSE (AP4 + Q1).
#
# Root cause (confirmed at R2): silent, cold-apparatus chapter with suppressed-recognition
# architecture throughout. The 4 anchors visible in the locked R1 graph either fail the
# ≥2-layer test from {memory, feeling, tens}, carry hard protected-pattern violations, or
# fail Q1 on AP4 grounds (the figure already exists inside the memory-flag text). Zero fires
# is the correct outcome. 0/27 = 0% — within sparsity (0–3%). Fully acceptable per rubric
# §Per-scene cap and sparsity.
