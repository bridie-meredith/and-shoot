facet: metaphor
episode: b01c20
author: editor
---

# 0 entries — refuse-by-default; licensing pool empty; cold-accounting register consistent with c18/c19 zero-fire pattern.
#
# Full gate-record below. 0 fires / 30 bones = 0.0% (within 0-3% sparsity target).
#
# ── licensing pool ──
#
# memory-b01-c20.md: does not exist — no memory facet authored for this chapter.
# feeling-b01-c20.md: does not exist — no feeling facet authored for this chapter.
#
# Rubric anchor requirement: every metaphor entry MUST cite at least one of {memory:<id>, feeling:<id>}.
# With both upstream facets absent, the licensing pool is empty. All candidates are AP1 (unlicensed novel
# figuration) by definition. No metaphor can be licensed.
#
# ── candidate assessments (peak-bones only) ──
#
# Scene-A peaks: @1, @3, @4 (the-rendering-works-room; servant-passages empty / holdfast routes activate /
#   taylor lifts stylus)
#   No memory or feeling anchor at any of these bones. AP1 absolute. REFUSE.
#
# Scene-B peaks: @7, @8 (ward-junctions; men enter / patron channel shifts sequence)
#   No memory or feeling anchor. AP1 absolute. REFUSE.
#   Additionally: @8 (patron channel shifts sequence) is a peak-zone equivalent for the succession-
#   announcement register — AP7 default-refuse also applies. The succession apparatus executing
#   without Taylor's signal must render flat; figurative reach is anti-form here.
#
# Scene-C peaks: @12, @13, @14, @15, @17 (burn reaches outer wards / fire traces catalogue /
#   decommission message arrives / apparatus absorbs coverage / taylor marks social_tether entry)
#   No memory or feeling anchor. AP1 absolute. REFUSE.
#   @14 (decommission message arrives) carries the EXPULSION-AMBIGUITY-PRESERVED protected-pattern
#   (scene-map HARD watch): no resolution bone follows; ambiguity is deliberate thematic design.
#   A metaphor at @14 would, by definition, introduce a figurative layer that collapses the ambiguity
#   the scene-map prohibits resolving. Even if a memory anchor existed, REFUSE.
#   @17 (taylor marks social_tether entry): the ledger-mark IS the act of contempt-complete; a metaphor
#   here is the text pleased with itself at the precise beat the chapter's cold-accounting register
#   prohibits figurative self-satisfaction. Even if a memory anchor existed, REFUSE.
#
# Scene-D peaks: @19, @20, @22 (smoke fills lanes / heat disperses insects / signal drops from lanes)
#   No memory or feeling anchor. AP1 absolute. REFUSE.
#   These three bones form the PHYSICS-CASCADE protected-pattern (@19-@22, DO-NOT-FUSE): each bone is
#   one step in the physical mechanism; the recognition arrives as physics, not as interior register.
#   Per the dispatch prompt, the s04 recognition spine is rendered as PHYSICS and must not be duplicated
#   as metaphor (the smoke/heat/insects ARE the figure — no additional comparison layer licensed).
#   The RECOGNITION-HELD-BLANK protected-pattern (@22-@24) further prohibits figurative rendering at
#   the terminal recognition sequence: the three-beat shape IS the recognition arriving without discharge;
#   any metaphor introduces the interior discharge the protected-pattern withholds.
#   Even if a memory anchor existed, REFUSE on all three.
#
# Scene-E peaks: @25, @27, @30 (taylor closes feed / architecture releases wards / taylor exits south gate)
#   No memory or feeling anchor. AP1 absolute. REFUSE.
#   @30 (taylor exits south gate) is the terminal departure bone carrying both LOCK confirmations
#   (position-prot-collapse rank 1 + political_register-world rank 9); the scene-map requires the
#   two LOCK confirmations to land through the single concrete act of exiting the gate, not through
#   a figurative layer. Even if an anchor existed, a metaphor at @30 is the chapter ending pleased
#   with itself — the cold-accounting register prohibits figurative closure at the terminal beat.
#
# ── chapter-level sparsity verdict ──
#
# 0 fires / 30 bones = 0.0%. The absent licensing pool makes refusal structurally complete, not merely
# a taste call. The chapter's cold-utilitarian register (solitary/interior; zero dialogue; physics-rendered
# recognition; ledger-accurate contempt-complete) is the same register as c18 and c19, both of which
# produced 0 entries. Series-terminal chapter; REFUSE-BY-DEFAULT is the correct and expected output.
# Earth-Bet fence CLEAN (no metaphor authored; nothing to fence).
