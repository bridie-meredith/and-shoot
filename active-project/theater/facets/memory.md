facet: memory
episode: b01-c06
author: dialogue-writer-fork (taylor-hebert-kl-122ac, interiority output mode)
generated: 2026-05-30 (R1 /and-facets b01-c06 Phase 1)
r2-judged: 2026-05-30 (R2 GRAPH-AWARE judge, taylor-hebert-kl-122ac POV; KEEP mem:1, KEEP mem:2, 0 ADD, 0 DELETE)
r2-spine-status: RESOLVED in locked R1 NI file — NI fires @4/@8/@13/@22/@24 (narrator:1-5) AND @12 (narrator:6) AND @19 (narrator:7). The NI judge's co-citation ADDS at @12 and @19 ARE PRESENT in interest-narrator.md (entries 6 @12, 7 @19) at R2-judge time. mem:1 spine = narrator:6 @12 (re-read / names-vs-node registration); mem:2 spine = narrator:7 @19 (protect-target image-beat). V3 feel-as-spine carve-out N/A (chapter dramatic_shape: climax; feeling also silent at @12/@19) — but moot, NI co-citation is satisfied. CITE-INDEX IS STALE (predates NI @12/@19 adds; lists narrator:1-5 only): Phase 5 MUST rebuild the cite-index to reflect narrator:6/@12 + narrator:7/@19 before the cross-facet contract check passes. Spine-dependency seam DOWNGRADED HARD→SOFT (spine present in facet file; only the index needs refresh).
total-entries: 2
fires-per-bone: 2/25 = 8.0% (within 5-12% band)
doubled-register: YES (Earth-Bet displacement at @12; Westerosi-monument-clamp at @19)
monument-families: override-architecture-residue exercised across two distinct faces (body-record-cognition Earth-Bet displacement @12; protective-arrangement-at-distance Westerosi-monument-clamp @19) — single operating residue, doubled cue (rubric-admitted when the doubled register comes from one structural source)
per-scene-distribution: scene-A 0 fires; scene-B 1 fire (@12); scene-C 1 fire (@19)
chapter-dramatic-shape: climax (V3 feel-as-spine carve-out DOES NOT APPLY — requires dramatic_shape: hinge; standard narrator-interest co-citation required on every fire)
---

# rubric-carve-out — peak-bone silence; resonance lands off-peak
#
# rubric-memory-flags.md (design/shoot-v2/rubric-memory-flags.md) § Licensing-discipline / Quiet-beat anchor
#
# Carve-out scope (scene-A): @4 (first spoken exchange; relational_anchor_status +1.0; in scene-A peak-bones) and @8 (blanks the contact-source field — the omission; in scene-A peak-bones).
# Carve-out rule: memory does NOT fire in scene-A at all. @4 and @8 are peak-bones (default-forbidden); the relational substance is carried by the relational_anchor_status axis-move + narrator-interest + feeling, not by the licensing layer. The omission's resonance is NOT routed to @9 because firing it there would produce a second Earth-Bet residue fire in a scene adjacent to scene-B's @12 (anti-pattern #11 persistent-monument-firing / family-thinness). Sparsity is load-bearing; scene-A is correctly silent. Per rubric: "Far better to under-fire and miss a monument than over-fire and dissolve the licensing layer."
#
# Carve-out scope (scene-C): @22 (seals the form — the send; moral_framework -1.0; peak-bones) and @24 (opens ward-coverage notes — the contrast recognition; moral_legibility_to_self +1.0; peak-bones).
# Carve-out rule: memory does NOT fire on @22 or @24. The send and the contrast-recognition are the action-in-itself; the interior is fully committed at the peak. The Westerosi-monument-clamp fires at @19 (Sera rendered in the coverage record — the protective destination), a rising-zone backward-reach: the architecture's PURPOSE lights here, reaching back to what c03-c05 built, not forward to the send. Per rubric quiet-beat test: rising-zone-not-peak is "contested — require explicit argument that the monument is reaching backward, not forward"; the backward-reach argument holds (the arrangement-architecture is prior story content, not the bone's forward action).
#
# Per-entry annotations:
# - mem:1 @12: Earth-Bet displacement; scene-B flat-mid, no peak-bones — quiet-beat licit by default. The re-read is the names-vs-patterns category-crossing where the override-architecture-residue lights as body-record cognition.
# - mem:2 @19: Westerosi-monument-clamp; scene-C rising-zone backward-reach (NOT peak-bones, NOT peak-shadow-bones) — contested-but-defensible under the backward-reach argument; parallels c05 memory:1 @19 construction.

1 @12 the read is already done and what the ask wants is the bodies pulled out of the pattern and set down as names, the way bodies were set into a record once and the record became the route they were moved along -> cond-override-architecture-residue-122ac
2 @19 the record's far end holds a body kept where it is by an arm that is not hers at the length of an arm that is not hers, the same weight the arrangement was built to carry -> cond-override-architecture-residue-122ac

# ---
# Per-entry rubric audit (author's defensibility trail):
#
# mem:1 @12 — Earth-Bet displacement on body-record-cognition / override-architecture-residue
#   Monument family (one clause): override-architecture-residue body-record — the prior mass-accounting pattern where bodies entered a record that became the route they were moved along; the cognitive shape Taylor came to refuse to enact
#   Cue mode: Earth-Bet displacement, fused with intra-/cross-chapter callback (the movement-pattern deliveries since d04 — "names are different"; the ask converts pattern-nodes to named persons, the category-crossing the chapter turns on)
#   Displacement-discipline: description names the SHAPE (bodies pulled from the pattern and set down as names; the way bodies were set into a record once and the record became the route they were moved along) without naming Khepri, Gold Morning, or the prior architecture; the recursion "the way bodies were set into a record once and the record became the route" is the cue
#   Spine (cross-facet): narrator-interest @12 expected to co-fire on the re-read / names-vs-patterns registration (the re-read carries the ask's specificity per scene-map protected-pattern @11-@12; NI's natural territory — the interior IS reaching/registering the category-crossing); under chapter dramatic_shape: climax the V3 feel-as-spine carve-out does NOT apply — standard NI co-citation required; flagged forward as spine-dependency seam (R1 blind to NI file)
#   Quiet-beat anchor: scene-B flat-mid, peak-bones: none; @12 is the re-read in a no-peak scene whose whole function is the loaded pause — default-licit quiet-beat
#   Audience-meaningfulness: the category-crossing is the chapter's engine; the reader catches that this is a different KIND of delivery (names, persons) than the movement-patterns delivered since d04; the override-residue surfaces as Taylor's own cognition reading the pattern as a set of bodies-to-be-routed; meaningful to a reader with zero source fluency (the bodies-become-a-route shape carries on its own)
#   Functional-register: moment of realization (the reader catches the body-record-cognition rhyme even as Taylor files it as operational recall) + painting characterization (the residue operating AS Taylor's cognition — she reads the pattern the way the prior architecture read bodies) + social commentary (converting persons to a deliverable is the structural move the whole arrangement is built on); 3/4 hits — structurally overdetermined
#   Multi-justification: monument-trigger + callback-fused + spine-co-fire-expected + audience-meaningful + functional-register-3-of-4 + scene-eligible (first/only scene-B fire) + quiet-beat-default-licit; ≥6 converging justifications
#   Hard fence: no Earth-Bet proper noun; "the bodies" / "a record" / "the route they were moved along" is the licit displacement-cue construction; the recursion does not name what it echoes
#   Target-reference: cond-override-architecture-residue-122ac — resolves at active-project/warehouse/cond-override-architecture-residue-122ac.md (margit-resolvable); the body-record-cognition face of the residue; margit-split referral carried in seams
#
# mem:2 @19 — Westerosi-monument-clamp on protective-arrangement-at-distance / override-architecture-residue
#   Monument family (one clause): protective-arrangement-at-distance — the licensed-exception architecture Taylor opened c03-c04 (the Jarvis-routing as the mechanism that holds someone else's exposure at someone else's arm-length); reading-the-architecture's-destination-without-naming-it
#   Cue mode: Westerosi-monument clamp (proximity to the architecture in the act of marking the Red-Keep coverage record; the destination IS the architecture; Taylor knows the architecture by foreknowledge of what the arrangement is for, not by report), fused with callback (the arrangement / Otto-trade-for-Sera's-protection, s03 accounting; c03-c05 architecture)
#   Displacement-discipline: description names the SHAPE (a body at the record's far end that does not know how it is held, kept where it is by an arm that is not hers at the length of an arm that is not hers) without naming Sera, Otto, Alicent, Jarvis, or the Red Keep; the interior knows the destination; the description does not articulate it
#   Spine (cross-facet): narrator-interest @19 expected to co-fire on the Sera-rendering / protect-target registration (the protect-target rendered in the coverage record is registration content; @19 is the second accounting arm's image-beat per the chunk); under chapter dramatic_shape: climax the V3 carve-out does NOT apply — standard NI co-citation required; flagged forward as spine-dependency seam (R1 blind to NI file)
#   Quiet-beat anchor: scene-C rising-zone; @19 is NOT in peak-bones (@22, @24) and NOT in peak-shadow-bones (@20, @21, @23, @25) — a plain rising-zone bone, contested per the quiet-beat test; backward-reach argument: the architecture's PURPOSE (the protective destination) lights here reaching back to what c03-c05 built, not forward to the send at @22; the monument is reaching backward and outward, which is precisely the memory-flag direction
#   Audience-meaningfulness: the protective-arrangement is on-page (the Otto-trade-for-Sera is the chapter's stated stakes; the second accounting arm is names-vs-Sera's-exposure); the reader feels the trade — the four names go SO THAT the body at the record's far end stays held; meaningful without source fluency
#   Functional-register: painting characterization (Taylor's licensed-exception architecture surfaces as cognitive content — the protect-target is the shape of what the arrangement is for) + social commentary (the protection-mechanism IS the surveillance-mechanism; the same record that names the ward elders holds the protected body); 2/4 hits
#   Multi-justification: monument-trigger + callback-fused + spine-co-fire-expected + audience-meaningful + functional-register-2-of-4 + scene-eligible (first/only scene-C fire) + backward-reach-argument-named; ≥6 converging justifications
#   Hard fence: no Earth-Bet proper noun; no Sera/Otto/Alicent/Jarvis/Red-Keep named in description; "the record's far end" / "a body that does not know how it is held" / "an arm that is not hers" is the licit displacement-cue
#   Target-reference: cond-override-architecture-residue-122ac — resolves at active-project/warehouse/cond-override-architecture-residue-122ac.md (margit-resolvable); the protective-arrangement-at-distance face of the residue; margit-split referral carried in seams
#
# ---
# Cross-axis tests (file-level):
#
# Monument-name test: PASS
#   - mem:1: "override-architecture-residue body-record" (4 words) — Earth-Bet displacement
#   - mem:2: "protective-arrangement-at-distance" (5 words / 1 hyphenated token) — Westerosi-monument-clamp
#
# Displacement-cue test: PASS
#   - mem:1: "the bodies pulled out of the pattern and set down as names" / "the way bodies were set into a record once and the record became the route they were moved along" — names the SHAPE; no monument named, no Khepri proper noun
#   - mem:2: "a body that does not know how it is held" / "an arm that is not hers at the length of an arm that is not hers" — names the SHAPE; no monument named, no Sera/Otto proper noun
#
# Licensing test: PASS
#   - Without mem:1, stitcher has no metaphor-facet license at @12 — the body-record-cognition / names-vs-patterns rhyme cannot land structurally; the movement-pattern-delivery callback loses its memory-anchor
#   - Without mem:2, stitcher has no metaphor-facet license at @19 — the protective-arrangement irony (the four names go so the held body stays held) cannot land structurally
#
# Quiet-beat test: PASS
#   - mem:1 @12: scene-B flat-mid, peak-bones none; default-licit quiet-beat
#   - mem:2 @19: scene-C rising-zone, NOT peak-bones, NOT peak-shadow-bones; contested → backward-reach argument named (the arrangement-architecture is prior story content the monument reaches back to, not the bone's forward send-action)
#
# Narrator-interest spine test: EXPECTED PASS (R1 parallel; NI file not loaded per discipline)
#   - mem:1 @12: NI expected to co-fire on the re-read / names-vs-patterns registration (scene-map protected-pattern @11-@12: "the re-read carries the weight of the ask's specificity")
#   - mem:2 @19: NI expected to co-fire on the Sera-rendering / protect-target image-beat (the second accounting arm's image per the chunk)
#   V3 feel-as-spine carve-out: NOT APPLICABLE (chapter dramatic_shape: climax, not hinge) — standard NI co-citation mandatory; spine-dependency flagged as seam for Phase 5 mechanic-auditor + Phase 5b
#
# Doubled-register test (file-level): PASS
#   - mem:1: Earth-Bet displacement (override-architecture-residue body-record)
#   - mem:2: Westerosi-monument-clamp (protective-arrangement-at-distance)
#   Both clear audience-meaningfulness (the @19 Westerosi-clamp routes through the on-page protective-arrangement, NOT through insider-only Dance-foreknowledge — the Red-Keep/Dance-succession foreknowledge at @19 is interior-only and would FAIL the audience-meaningfulness gate; the protective-arrangement reading is the meaningful one and is the register selected)
#
# Hard-fence test: PASS
#   - No Earth-Bet proper nouns in either description (no Khepri, no Gold Morning, no Endbringer, no Skitter, no Annette, no S9, no Brockton, no PRT, no Wards)
#   - No Dance-specific proper nouns in either description (no Aegon, no Rhaenyra, no Aemond, no Vhagar, no Dance, no Doom, no Hour, no Harrenhal)
#   - No Westerosi character/place named (no Sera, no Otto, no Alicent, no Jarvis, no Red Keep)
#   - target-reference slug cond-override-architecture-residue-122ac uses mechanism-descriptive form; URI-032 compliant
#
# Monument-card resolution test: PASS (cond-override-architecture-residue-122ac exists at active-project/warehouse/cond-override-architecture-residue-122ac.md; resolves via margit)
#   - Margit referral candidate (SOFT, queued in seams): split into monument-* class cards for cleaner metaphor-facet downstream routing — monument-protective-arrangement-at-distance (covering mem:2's register) + monument-body-record-cognition (covering mem:1's register). Current cond-* slug is licit per existing project state-file references and warehouse presence; the monument-class split is a downstream-optimization, not a blocker. (Carries forward the identical c05 SOFT seam.)
#
# Curve-shape file-level test: PASS
#   - Sparsity: 2/25 = 8.0% (within 5-12% band; comfortable mid-band)
#   - Inverted pressure-signal alignment: 2 fires in flat-mid/rising-backward-reach zones; 0 fires in peak-bones; ratio = ∞ (ideal)
#   - Doubled-register: YES (both registers fire; both clear audience-meaningfulness)
#   - Monument-family diversity: single operating residue (override-architecture) exercised across two distinct faces — body-record-cognition (cost side, @12) and protective-arrangement-at-distance (gain side, @19); the doubled register comes from one structural source, which the rubric admits as single-monument-family-with-doubled-cue. For a 25-beat chapter this is within range (the ≥3-family target is the >50-beat-corpus expectation); margit-split referral flagged for cleaner downstream routing
#   - Quiet-beat distribution: one fire mid-chapter (scene-B @12), one chapter-close-adjacent (scene-C @19); no front- or back-loading
#
# ---
# Per-file cull (delete-only): NONE
# Authoring produced 2 entries; both survive all three axes and all anti-patterns; no rewrites attempted; no entries deleted.
#
# ---
# R2 GRAPH-AWARE JUDGE PASS (2026-05-30) — now sighted on the full locked R1 graph:
#
# mem:1 @12 — KEEP. All three axes re-confirmed against the cite-index and the locked NI/feel/sensory
#   files: monument-trigger (override-architecture-residue body-record) lit; displacement-cue holds the
#   SHAPE without any Earth-Bet proper noun; quiet-beat (scene-B flat-mid, peak-bones none) default-licit;
#   audience-meaningful (bodies-become-a-route carries at zero source fluency); functional-register 3/4
#   (realization + characterization + social-commentary); per-scene cap clear (sole scene-B fire); target-
#   reference resolves at warehouse. SPINE: RESOLVED — the locked R1 NI file (interest-narrator.md) FIRES
#   at @12 (narrator:6, "a name is a different unit than a node ... she reopens to confirm the conversion is
#   what is asked, not less"). This is exactly the re-read / names-vs-node registration the R1 defense predicted
#   NI would naturally carry (scene-map protected-pattern @11-@12). Standard narrator-interest co-citation
#   SATISFIED; the missing-spine gate clears. V3 feel-as-spine carve-out N/A (climax, not hinge — and feeling
#   is silent at @12) but moot, NI carries the spine. NOTE: the cite-index is stale (generated before the NI
#   @12 add; lists narrator:1-5 only). The spine is present in the FACET FILE; Phase 5 must rebuild the index.
#
# mem:2 @19 — KEEP. Monument-trigger (protective-arrangement-at-distance, the c03-c05 architecture's
#   destination) lit; displacement-cue holds the SHAPE (a body that does not know how it is held; an arm
#   that is not hers) without Sera/Otto/Jarvis/Red-Keep; quiet-beat: @19 is scene-C rising-zone, NOT in
#   peak-bones (@22/@24) — contested, carried by the backward-reach argument (the arrangement-architecture
#   is prior story content @19 reaches back to, not the @22 send's forward action); the backward-and-outward
#   reach is exactly the memory-flag direction, argument holds; audience-meaningful (the four-names-so-the-
#   held-body-stays-held trade is on-page stated stakes); functional-register 2/4 (characterization + social-
#   commentary); per-scene cap clear (sole scene-C fire). SPINE: RESOLVED — the locked R1 NI file
#   (interest-narrator.md) FIRES at @19 (narrator:7, "the record's far destination is a body she has watched
#   through the feed and never met ... the four names travel so that body stays exactly where an arm that is
#   not hers has set it"). This is the protect-target image-beat / second-accounting-arm registration the R1
#   defense predicted. The NI:7 construction directly mirrors mem:2's displacement-cue ("an arm that is not
#   hers") — spine and monument-content are consistent, no contradiction. Standard NI co-citation SATISFIED;
#   missing-spine gate clears. For a climax-shape chapter this is the NI-spine the KEEP criteria require. V3
#   carve-out N/A (climax; feeling silent at @19) but moot. Cite-index stale (lists narrator:1-5 only) — index
#   rebuild needed at Phase 5; spine present in the facet file.
#
# Doubled-register re-confirmed: mem:1 Earth-Bet displacement + mem:2 Westerosi-monument-clamp. PASS.
# Both registers clear audience-meaningfulness (mem:2 routes through the on-page protective-arrangement,
# NOT through insider-only Dance/Red-Keep foreknowledge — the meaningful register is the one selected).
#
# ADD scan (cap 5) — REFUSE ALL, 0 adds. 2/25 = 8.0% is already comfortable mid-band; refuse-by-default.
#   No rhythm-shape transition or peak-bone genuinely WANTS a memory fire it lacks:
#   - Peaks @4/@8/@22/@24 are peak-bones (default-forbidden); their substance is carried by the relational/
#     moral axis-moves + NI + feeling, not the licensing layer (scene-A SIGNAL silence is deliberate; the
#     Wren-omission stays surface-carried per the R1 defense, re-confirmed CORRECT).
#   - The s01->s02 transition (@10) and the accounting middle (@16-@18) are grounding-ledger-OPEN (grd-001
#     @10, grd-002 @16, grd-003 @17-@18) — sensory grounding territory authored at Phase 4.6, NOT memory;
#     and the @17/@18 moral-balance is NI-weave territory (Phase 2.5 WEAVE-FIXABLE → NI judge). A memory fire
#     in the middle would double the Earth-Bet residue adjacent to mem:1 (anti-pattern #11 persistent-
#     monument-firing) and dissolve the licensing gradient. Default is silence; silence held.
#
# Per-file R2 deletes: NONE. Per-file R2 adds: NONE. f-r2-counts: KEEP 2 / DELETE 0 / ADD 0 / REWRITE 0.
#
# ---
# Seams flagged forward (for cross-facet consistency + audit + Phase 5b):
#
# 1. SOFT (DOWNGRADED from HARD) — narrator-interest spine RESOLVED in facet file; cite-index rebuild needed
#    [R1 text retained] Both fires REQUIRE standard NI co-citation on the same @-id (chapter dramatic_shape: climax → feel-as-spine carve-out does NOT apply). R1 was blind to the NI file; mem:1 @12 and mem:2 @19 are anchored at beats where NI co-fire is near-certain (re-read registration @12; protect-target image-beat @19), but the spine was ASSERTED, not verified, at R1.
#    [R2 RESOLUTION 2026-05-30 — SPINE CONFIRMED PRESENT, no longer a HARD dependency] At R2-judge time the locked R1 NI file (interest-narrator.md) contains SEVEN entries: narrator:1-5 at @4/@8/@13/@22/@24 AND narrator:6 @12 ("a name is a different unit than a node ... she reopens to confirm the conversion is what is asked") AND narrator:7 @19 ("the record's far destination is a body she has watched through the feed and never met ... the four names travel so that body stays exactly where an arm that is not hers has set it"). The orchestrator-coordinated NI co-citation ADDS at @12 and @19 ARE ALREADY LANDED in the facet file. mem:1 spine = narrator:6 @12; mem:2 spine = narrator:7 @19. BOTH missing-spine gates clear. The standard NI co-citation is satisfied; no feel-as-spine fallback needed (and would be N/A on a climax chapter anyway). The ONLY residual is provenance bookkeeping: the cite-index (_cite-index.md) is STALE — it was generated before the NI @12/@19 adds and lists only narrator:1-5 (mem:1/mem:2 appear in its "Lonely entries" list because the index does not yet see their NI co-citations). The dependency is therefore NOT on whether NI fires (it does) but on whether the index reflects it.
#    Routing: Phase 5 mechanic-auditor MUST rebuild the cite-index (build_cite_index) so narrator:6 @12 + narrator:7 @19 are indexed and the mem:1/mem:2 co-citation links resolve; THEN run the cross-facet contract check (it will pass — the spine entries exist in the source facet file). No NI-author missed-fire repair is needed (NI is not silent). No mem-author DELETE is needed. If, contrary to the file state read here, Phase 5 finds narrator:6/@12 or narrator:7/@19 absent from the final locked NI file, THEN the original HARD missing-spine gate re-arms for the affected fire (climax → no fallback; route to NI missed-fire repair or mem DELETE) — but as read at R2, both spines are present.
#
# 2. SOFT — monument-class card split for mem:1 + mem:2 target-references
#    Current target-reference cond-override-architecture-residue-122ac covers both fires per the unified-residue defense (one architecture, two faces). Clean metaphor-facet downstream routing would benefit from split into monument-body-record-cognition (mem:1's register) + monument-protective-arrangement-at-distance (mem:2's register). Margit class-review candidate. Identical to the c05 SOFT seam — re-surfaced, not new.
#    Routing: margit referral; not blocking R1 authoring.
#
# 3. SOFT — Sera-architecture facet anchor (continuity from c05 dark-fantasy-reader required deliveries)
#    mem:2 @19 anchors the protective-architecture context at the bone where the coverage record's destination IS the protected body. Concurrent narrator-interest + exposition facet authors should independently deliver their required layers (NI: the protect-target as registration; exposition: reader-facing arrangement-state surface). This R1 emission claims the delivery on the memory side.
#    Routing: Phase 5b audience verdict; this entry contributes one of the facet-layer deliveries.
#
# 4. SIGNAL — scene-A licensing-layer silence (deliberate)
#    The chapter's relational substance (first spoken exchange @4; the omission @8) is peak-bones and is carried by the relational_anchor_status axis-move + NI + feeling, NOT by the memory licensing layer. mem deliberately does NOT fire in scene-A (no @9 resonance-tail fire) to preserve sparsity and avoid a second adjacent Earth-Bet residue fire (anti-pattern #11). If a downstream reviewer reads the Wren-omission as a missed memory fire, the defense is: the omission is the un-priced move foregrounded by the GOAL; pricing it via the licensing layer would risk over-rendering it. The omission stays surface-carried.
#    Routing: Phase 5 mechanic-auditor refusal-CORRECT confirmation (scene-A NONE defensible under sparsity).
