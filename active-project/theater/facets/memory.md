facet: memory
episode: b01-c05
author: taylor-hebert-kl-122ac-impersonator (R1 fresh fork)
r2-judge: taylor-hebert-kl-122ac-impersonator (R2 facet-judge mode, locked-graph review)
cite-index-hash: 09f0e4c9df744eb5
---

# rubric-carve-out — single-register displacement + @18-@27 paired-fire as escalation
#
# design/shoot-v2/rubric-memory.md § Register requirements + both-meaningful doubled-register gate
#
# Carve-out scope: both entries (mem:4 @18, mem:3 @27)
#
# Carve-out rule (1) — single-register displacement:
#   Both entries share the same register (displacement — cost-borne by the unconsenting). The
#   both-meaningful gate normally requires doubled-register (one present, one Earth-Bet). At b01c05
#   the Earth-Bet register is gated by cond-override-architecture-residue-122ac, which hard-silences
#   Khepri-architecture callbacks until c12. The chapter contract enforces single-register:
#   operational-filing is the only licensed surface; no Earth-Bet residue surfaces in narration.
#   Therefore both fires must be displacement-register; doubled-register cannot be enacted without
#   violating the condition. The carve-out is condition-driven, not authorial preference.
#
# Carve-out rule (2) — @18-@27 paired-fire as escalation:
#   The two fires are NOT redundant re-statements of the same action. They are escalation steps:
#   mem:4 @18 = cost-borne entry (face added to body-map at the moment of categorization —
#     the cost accrues without consent; the filing is the action and the entry is the monument).
#   mem:3 @27 = file-closing residue (the evening review closes the record around the courier's
#     face — the active filing-and-closure is distinct from the initial cost-borne entry; the
#     closure is a second action with its own weight, not a re-statement of mem:4).
#   The board HAS shifted between @18 and @27: at @18 Taylor adds the face; at @27 she runs the
#   evening review and the file closes around it. The courier passes from open-entry to closed-record.
#   These are separate archive-operations, not symmetric re-firings.
#   The chapter contract (chapter-goal: plant the courier figure whose face will matter at d10)
#   specifically requires both the initial planting (@18, cost-borne) and the evening consolidation
#   (@27, file-closed). A single memory fire would either plant without consolidation or consolidate
#   without establishing the plant-cost.
#
# Per-entry annotations:
# - mem:4 @18: carve-out-clause = condition-gate-single-register + escalation-step-1-cost-borne;
#   rubric-clause-carved-from = both-meaningful doubled-register;
#   defense = cond-override silences Earth-Bet layer; @18 is the courier-face-added-to-map moment.
# - mem:3 @27: carve-out-clause = condition-gate-single-register + escalation-step-2-file-closing;
#   rubric-clause-carved-from = both-meaningful doubled-register + redundancy-prohibition;
#   defense = cond-override silences Earth-Bet layer; @27 is the file-closed-around-courier-face moment;
#   distinct from mem:4 (open-entry vs closed-record; action-of-addition vs action-of-closure).

4 @18 a face is set into the body-map without the bearer's hand on the entry and the entry holds -> monument-cost-borne-by-the-unconsenting
3 @27 a face goes into the file under a name she has not asked for and the file closes around it -> cond-override-architecture-residue-122ac
