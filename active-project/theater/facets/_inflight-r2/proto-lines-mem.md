# R2 in-flight citation cascade — memory facet — b01-c07
# Bodies byte-identical to the locked memory.md live entries. Citation-only file:
# carries the surviving mem fire(s) keyed to proto-line anchor for the Phase 4
# build_cite_index rebuild + the /and-stitch Phase 0/1 proto-line merge.
#
# cite_index_hash: 4b1789810b83040f443958cffc443ec91345befc
# r2-counts: KEEP 1 / DELETE 1 / ADD 0 / REWRITE 0
#
# DELETED at R2 (recorded; NOT cascaded — no live citation):
#   mem:1 @6  — spineless fire (NI silent @6 by design; feel silent @6; V3 carve-out
#               condition (3) fails). DELETE. No proto-line citation carried forward.
#
# Surviving fire (body byte-identical to memory.md entry):

2 @19 the name goes down level: a child, a lane, a fever two seasons back her coverage was not yet here to route against — the founding entry, the one the slower way left unpaid and she did not, which is the whole reason the count is hers to keep; and under it the older shape, a smaller door that stayed shut on someone she could not reach in time, the same arithmetic in another tongue. -> (westeros: founding-death-the-slower-method-produced) [+ earth-bet: failed-recognition-by-protector / dying-ward shape]
