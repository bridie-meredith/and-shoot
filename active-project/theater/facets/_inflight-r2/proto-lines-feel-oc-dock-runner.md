# R2 in-flight annotation — feeling / oc-dock-runner / s01e02
# Body-integrity HARD RULE: proto-line bodies below are reproduced verbatim from
# `active-project/theater/proto-lines/s01e02.md`. Annotations are added as comment lines only.
# No proto-line body may be edited, reordered, or paraphrased. Verify against source on review.

episode: s01e02
narrator: taylor-hebert-flea-bottom
character-of-record: oc-dock-runner

# ---- Anchor inventory for oc-dock-runner in s01e02 ----
# Single anchor only. Established by scan of the proto-lines body for oc-dock-runner slug.

# @37 — the wasps relay oc-dock-runner
#   Position in stream: middle of an interior-relay band (@34, @35, @37, @42, @44) — POV
#     swarm-perception sweeping the surrounding city. No scene boundary opens or closes on this
#     beat; loc-state does not advance; sensory does not fire here.
#   On-page body present for the runner: none. The proto-line beat is the swarm acting on the
#     runner as relay-target, not the runner acting. There is no posture / breath / gaze /
#     micro-action of the runner's that the rubric §Form would license a clause for.
#   Card §Voice cues active: "comfortable with silence", "irony as default register", "does not
#     pretend to trust more than is there". Card hard fences: trust revokes on a burned
#     situation; runner does not commit to causes; runner does not know what the girl is.
#   Vibes-cloud activated keys: route-knowledge-as-power, irony-as-default, gap-as-load-bearing,
#     non-commitment-as-discipline.
#
#   G1 (cold-read of any revision): N/A — no R1 entry to revise.
#   G2 (motive for add): fails. The only "wanting" the graph offers is niche-driven (the runner
#     has no facet fires at all in s01e02; the niche is total). At-rest, the proto-line does
#     not call for a somatic tell — there is no body on page. T4 (niche-driven add) would fire
#     immediately on any attempt to add here. Refuse the add.
#   G3 (at-rest without adjacent context): fails. Any candidate fire would lean on the POV
#     swarm-sweep context to invent a body for the runner; the entry would not stand if the
#     adjacent relay beats (@34, @35, @42, @44) were masked.
#   G4 (pattern-scan): n/a — no entries in this character-slice.
#   G5 (position-gate): @37 sits inside an interior-administrative relay-cluster, not at an
#     escalation peak, trailing edge, or threshold. A non-POV somatic fire here would not open
#     or close anything; it would decorate a swarm-sweep beat. Position pressure refuses the
#     add.
#
#   §Form re-test: cannot be performed — there is no candidate clause that survives the body-
#     presence prerequisite. Default-to-silence per rubric.
#
#   Verdict: NO-ADD CLEARED. Cap-refusal logged.

# ---- End of oc-dock-runner anchors ----
# Slice remains zero-entries. The slice header preserves the R1 reasoning verbatim and appends
# the R2 re-test note. See decision shard for paragraph-level reviewer voice.
