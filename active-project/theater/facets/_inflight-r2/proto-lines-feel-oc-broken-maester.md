# annotated proto-lines — feeling-oc-broken-maester R2 pass
# episode: s01e02
# generated: 2026-05-11
# Body-integrity HARD RULE: proto-line bodies are reproduced from the canonical s01e02 proto-lines
# file unchanged. No [feel:<n>] markers exist for the maester (R1 slice empty; R2 adds: 0).
# Annotation only — author commentary appears below the maester's on-stage windows in indented
# comment lines. Bare proto-lines outside the maester's on-stage windows are elided.

## Scene J/K window — upper room visitor exchange (132–143)

132 the visitor enters the side alley [loc-state:7]
133 the visitor enters the stairwell
136 the visitor enters the upper room [loc-state:8]
137 the visitor speaks to the maester
    # R2 add candidate (refused): the speech-act with the visitor is the most plausible audience-
    # eligible window for a maester somatic-tell — visitor present, visitor receiving, interior
    # could in principle be shown through gaze/posture/breath. Card §Voice forecloses it: "Quiet
    # register. Reads aloud key phrases. Wry precision. Does not perform deference. Does not
    # perform hope." The card's structural register is the anti-performance baseline; the only
    # somatic shape consistent with the card at this beat is the absence of any tell, and the
    # absence is the proto-line content already (the beat is named as the visitor's speech-act,
    # not the maester's reception). Q1 fails on the at-rest read: there is no disambiguation
    # target — the maester's interior at this beat is "Citadel structure receiving an utterance",
    # which is precisely what the proto-line's framing carries. Cap-refusal logged. No-add.
138 the maester speaks to the visitor
    # R2 add candidate (refused): the maester's outbound speech is the second-most plausible
    # window — wry-precision register could in principle be shown through a body-tell that
    # accompanies the articulation (a hand on the record, a glance at the page, a held breath
    # before the phrase lands). The same card-discipline forecloses: any such tell would tilt
    # the maester toward visible-characterization at the very beat his function is to be heard-
    # but-not-collected. The vibe-cloud names "conscience-without-leverage" and "right-too-late"
    # — these are anti-tells; the maester's structural function is for the saying to be exact
    # and unaccompanied by performance. Q2 fails: the interior at this beat is "the saying
    # itself is the act", which the proto-line and the downstream beetles-relay-phrase at @149
    # already carry without a feeling-fire. No-add.
139 the beetles relay the register [narrator:25]
140 taylor-hebert-flea-bottom holds the feet [narrator:26]
141 the visitor exits the upper room
142 the visitor exits the stairwell
143 the visitor exits the side-alley door

## Scene K — rhythm/phrase relay window (148–150)

148 the beetles relay the rhythm
    # R2 add candidate (refused): the rhythm-relay window is the post-visitor reading-aloud
    # episode where the maester articulates the phrase that lands in his record. The body-fact
    # underneath the rhythm IS the proto-line — the beetles register an articulation cadence,
    # which is the somatic-tell itself, captured through Taylor's swarm-sense. Q1 fails by
    # the rubric's Q1-interior gate: the audience CAN read what is happening (the maester is
    # reading aloud, working through a phrase, settling on it) directly from the proto-line
    # bodies @148/@149/@150 plus state-update:5 (@149 record_anomaly_logged: true ->
    # phrase-isolated) plus vibes:16 (insect-as-anomaly-named). A feeling-fire would duplicate
    # the rhythm-as-body-show that the proto-line is already doing.
149 the beetles relay the phrase [narrator:27] [state:1]
    # R2 add candidate (refused): @149 is the tens=2 escalation where the phrase commits.
    # The state-update fire on the maester's side is the canonical record-change; vibes:16
    # licenses the insect-as-anomaly-named register; narrator:27 carries the POV side. The
    # maester's body at this beat is the page-side body of a man finishing a sentence in his
    # record — there is no audience present to make the tell legible, and the swarm-sense IS
    # the audience but it reads via rhythm/phrase content, not via posture. Q1 fails (proto-
    # line + state-update + vibes already carry the interior); Q2 fails (a body-tell here
    # would be inventing a posture-show the proto-line does not stage). No-add.
150 the beetles relay the rhythm
    # R2 add candidate (refused): the trailing rhythm-relay closes the articulation window.
    # Same reasoning as @148 — the rhythm is the body-fact; feeling-fire duplicates. No-add.

## Scene K — crossing window (156–159)

156 oc-broken-maester exits the apothecary [loc-state:9]
157 oc-broken-maester enters the side alley
158 oc-broken-maester enters the upper room [loc-state:10]
159 the beetles relay oc-broken-maester [mem:7] [narrator:29] [vibes:15] [vibes:16] [vibes:17]
    # R2 add candidate (refused): the crossing window is pure spatial relocation; no observer
    # except the swarm. The beetles register the maester as a tracked body — the relay IS the
    # somatic-show, and it is captured at the POV-side facet stack (mem:7, narrator:29,
    # vibes:15-17). A maester-side feeling-fire would have to invent posture beyond the
    # proto-line ("walks heavily", "pauses at the landing", etc.) — manufactured body-show
    # outside what the proto-lines render. Q1 fails (the relay itself is the disambiguation
    # the audience needs); Q2 fails (a body-tell here would steal the vibe-fire's structural
    # work of naming the brake-not-ally function). No-add.

## Summary

R1 slice was empty (0 fires). R2 graph-aware re-pass tested four candidate windows: @137, @138,
@148-150, and @156-159. All four candidates refused at G1 cold-read and G2 honest-motive. The
card's §Voice forecloses on-stage somatic performance ("Does not perform deference. Does not
perform hope."); the structural function (ambient-signal-becomes-named; brake-not-ally) requires
the maester to be heard-without-being-collected, which the proto-line stream and the
vibes/state-update/memory citations already render. Default-to-silence per rubric. No-add
cleared. No body-integrity violations: proto-line bodies untouched; no markers added.
