# Proto-line citation cascade — exposition facet (R2)

facet: exposition
episode: b01-c08
date: 2026-05-31
author: exposition-author R2 (judge-mode)
cite_index_hash_at_emit: 3a66ceaac2a9e03ed360f0a07077045c8694802a6ccca7773b16c96a6e8bbfac

## Cascade summary

R2 verdict: KEEP 2 / DELETE 0 / ADD 0 / REWORD 0.

No protoline citation tokens change. The R1 protoline-citation set for exposition
is preserved in full; this file records the cascade no-op so the Phase 4
cite-index rebuild can confirm the exposition facet has not introduced
deletions or additions that would require strip-or-attach work on
`proto-lines/b01-c08.md`.

## Citation map (R1 → R2; unchanged)

- exposition:1 → @0 (synthetic preamble anchor; no in-line protoline token; emit-disposition italic-preamble before body)
- exposition:2 → @13 (`taylor-hebert-kl-122ac logs the aemond-name`); protoline token `[exposition:2]` already present in `proto-lines/b01-c08.md` @13; co-located with `[feel:2] [narrator:3] [state:3] [state:4] [vibes:3]`; pile-up = 6 facets at @13 per `_cite-index.md`.

## Adds — none

ADD count = 0. The context-ledger (`active-project/staff/showrunner/context-ledger-b01-c08.md`)
opens zero CONTEXT-REQUIRED entries; no ledger-licensed adds apply. The
audience-pure R1 fired the two entries the union of personas requires
(prior-episode-bridge + Aemond first-mention); the locked lens facets confirm
no further orientation gap (the watcher-network is carried by loc-state:2/3 +
NI:1@6; the feed-station is carried by loc-state:4 + Jarvis-channel register;
Wenna Cobb is body-anchor-continuity from c07 dialogue; Corwick is
deliberately reserved for dialogue @20 + body-map @21 per muffle-guard).

## Deletes — none

DELETE count = 0. Both R1 entries survive R2 against the locked graph:

- exposition:1 @0 — the prior-episode-bridge is the c07→c08 board-refresh
  surface; no lens facet covers @0 (a synthetic preamble anchor is outside the
  protoline lens-fire regime); Phase 2.5 named it INVIOLABLE.
- exposition:2 @13 — the Aemond first-mention is the chapter's
  Aemond-foreshadow obligation-discharge anchor (chapters[b01c08].chunk +
  scene-map peak-bone @13). No locked lens facet covers the
  first-mention-character + folded Vhagar-embedded-noun + handler-rotation-
  significance triple-gap; NI:3@13 supplies held-register witness AT the
  bone but does NOT discharge the orientation gap (Aemond's age, Vhagar's
  identity, the Otto-direction, the handler-rotation-mechanism); the
  exposition-gloss remains the sole carrier of those four facts.

## Rewords — none

REWORD count = 0. The R1 cull-pass already trimmed exposition:2 from 58→40
words; the @13 gloss as it stands holds the four load-bearing facts in the
minimum word-count the post-bone-clause render-as supports. Further trim
would drop one of {age+Vhagar-rider-since-Driftmark / Otto-directed / where-
consequence-needs-a-body / apparatus-moving-the-largest-dragon-in-the-sky}
and regress the foreshadow-landing per Phase 2.5 audit.

## Hard-fence + AP-SCAN re-pass against locked graph

- Earth-Bet noun fence: clean across both entries + sources field
  (substring scan of `proto-lines-exposition.md` + `exposition-b01-c08.md`
  for `shard / parahuman / Skitter / Khepri / trigger / swarm-radius /
  Brockton / PRT / cape` — zero matches).
- AP-SCAN (anti-jargon / hollow-prose / asinine-pattern / new-plot-content /
  author-meta / voice-fault): clean against both entries; no new-plot-content
  introduced; author-meta absent; first-person Taylor at @0; post-bone-clause
  third-person about Aemond at @13 (pov-frame compatible per cond-taylor-pov-
  behavior).
- Re-gloss check: `glossed-terms.md` walked; zero terms re-glossed (Aemond +
  Vhagar are new to the project; Driftmark + Otto + the Hook + the
  arrangement + the count + the coverage + the feed + the Jarvis line + the
  courier + the septon are all register-resident and unchanged).
- License-completeness: both entries carry ≥1 persona-card slug + a specific
  gap-claim in their `licensed-by:` field (cape-fic-reader + dark-fantasy-
  reader + worm-canon-pedant named per entry, each with the per-persona
  gap-claim spelled out).
- Source-traceability: every `sources:` token resolves (cards + chunks +
  conds + handoff_in/out + glossed-terms register entries all named at
  fully-qualified paths or known slug-form).
- Provisional-anchor resolution: exposition:1's anchor is @0 (synthetic
  preamble, flat-id-independent); exposition:2's anchor is @13 (stable
  flat-id `taylor-hebert-kl-122ac logs the aemond-name` per
  `proto-lines/b01-c08.md` @13). No provisional anchors remain.

## Fence-window re-verification

Speech-bone set (re-walked): `[<character-slug>:<id>]` token scan of
`proto-lines/b01-c08.md` returns exactly one citation — `[oswyn-mudway-flea-
bottom-elder:1]` @20. SPEECH-BONE SET = {@20}; ±2 fence-window = {@18..@22}.

- exposition:1 @0 — EXEMPT (preamble; outside the protoline-window regime).
- exposition:2 @13 — anchor 7 bones from nearest speech bone @20; OUTSIDE
  fence-window {@18..@22}; cheapest render-as (em-dash-fold / inline-
  appositive) PERMITTED; R1 voluntarily escalated to post-bone-clause for
  s02 held-political-register protected-pattern. No fence violation;
  escalation honored.

Both entries clear the fence; no entry pre-exposes Corwick (no ADD anywhere
near @20/@21).

## R2 self-check against context-follow report INVIOLABLES list

The context-follow report (Phase 2.5, 20260531T180000Z) names twelve R2 frequency-
band-trim INVIOLABLES across the chapter's lenses; the exposition-specific
INVIOLABLE is **exposition:2@13's four load-bearing facts**. R2 confirms:

- "twelve years old" — held (line of the gloss).
- "Vhagar's rider since Driftmark" — held (line of the gloss; the
  120-AC-claiming canon weight rides on the Driftmark token + the
  rider-since framing).
- "the prince Otto's apparatus uses where consequence needs a body" —
  held (Otto-directed scope-down + where-consequence-needs-a-body
  function-description, both in one clause).
- "a name in a handler-rotation is the apparatus moving the largest
  dragon in the sky" — held (the c08 handler-rotation-significance
  mechanism, rendered post-bone-clause as reader-side orientation
  structurally distinct from Taylor's @13 flat-logging register).

All four facts preserved. The foreshadow-landing contract (cape-fic
pattern-recognition + dark-fantasy consequence-bearer + worm-canon
122-AC-scoped-Aemond) holds across the persona-trio.

## Phase 4 cite-index rebuild guidance

The exposition facet's R2 emit set is identical to R1: 2 entries, both
preserved, no IDs renumbered, no protoline citations added or removed. The
Phase 4 cite-index rebuild should produce the same `### exposition (2
entries)` block as the current `_cite-index.md` shows
(exposition:1 @0 back=N lic-out=[…]; exposition:2 @13 back=Y co=[…]).
