facet: metaphor
episode: s01e01
author: editor
r2-judge: metaphor-r2
---

2 @73 metaphor: the record book is a door already shut | licensed-by: memory:3 +tens:1

---

## Refusals (author-time hard cull)

@6 REFUSED — AP4/AP12: non-POV (mira); no callback potential; functional-register fail (no memory anchor, feeling only on non-POV). C4 calibration anchor.
@33 REFUSED — AP4: memory:1 already figurative; metaphor would restate memory's existing figure. AP13: tens=2 trailing-edge; pressure-zone, figure would need to render aftermath recoil not pressure — no clear callback beat supports that move without doubling memory:1's figure.
@39 REFUSED — AP7: tens=3 peak, default-refuse. AP5: cape-fence leak risk (feeling register at @39 touches worm-canon register without licensed Earth-Bet callback). C1 calibration anchor.
@57 REFUSED — AP12: non-POV (edric); AP2: proto-line already personifies (proto-line verb-form carries the figurative weight); tens=2 with AP12 tightening. Functional-register fail (non-POV feeling without callback).

## R2 deletions

@52 meta:1 DELETED — anchor mismatch (memory:2 is Conquest-charter-language monument; "the yard becomes a floor plan" does not mirror that monument). No memory or feeling fire co-located at @52; the nominated anchor (memory:2 @77) fires 25 beats forward and its monument does not structurally correspond to the spatial-mapping figure. AP1: unlicensed novel figuration — the figure reaches for Taylor's swarm-mapping/operational-geometry register, which has no monument entry in memory.md. Cross-facet delete-only rule applied.

## File-shape audit (post-R2)

- Sparsity: 1/155 = 0.6% — within 0-3% band
- Per-scene cap: @73 scene D (@68-@77) — one entry; cap satisfied; all other scenes blank (zero fires acceptable per rubric)
- Schema content-shape: entry uses required <id> @<pid> <kind>: <text> | licensed-by: <anchor> [+<support> ...] form
- Anchor verification: memory:3 (@92, locked R1 file) — resolves. Provisional feeling anchor feeling:(taylor @73) dropped: no Taylor feeling-flag fires at @73 in locked R1 feeling file; unresolvable; stripped at R2
- Multi-justification: memory:3 (anchor, 1 layer) + tens:1 (support, 1 layer) = 2 layers from {memory, feeling, tens} — clears ≥2 threshold
- Functional-register: callback — "a door already shut" mirrors the threshold-closure pattern in memory:3's monument scope (locker-tutor / helpless-protector; the monument-locker-referral within memory:3 grounds the door-as-irreversible-threshold figure)
- Voice-register: spare, non-ornate; fits Taylor's prose register; AP6 clean
