facet: metaphor
episode: s01e03
author: editor (R1)
---

# Zero fires — R1 hard-refuse

No memory or feeling facet files exist upstream for s01e03 at this R1 authoring pass.
Every metaphor entry requires a mandatory anchor from {memory:<id>, feeling:<id>} per the
rubric licensing gate. Without locked upstream anchor IDs, any entry constructed here would
be AP1 (unlicensed novel figuration). The rubric is explicit: anchor absence is a category
refusal, not a per-beat judgment call.

AP7 (tens=3 default-refuse) would additionally block the seven peak beats
(@11, @42, @67, @68, @90, @139, @162) independent of the anchor absence.

The interest-narrator facet (NI) exists and carries figurative compression at several
beats — AP3 (figurative-already-in-NI) would apply at NI-covered beats even if anchors
existed.

Structural note for R2: if memory and feeling facets are authored in later R1 passes and
produce eligible anchors, the following beat union is the candidate set for R2
reconsideration (tens-eligible quiet zones with NI coverage that might license callback
or dark-humor figures):

  @14 (tens=1, NI @6 — log opens same minute other book closed)
  @15 (tens=1, NI @7 — two entries warm simultaneously)
  @162 (tens=3 PEAK — wall-facing; AP7 default-refuse regardless)
  @165 (tens=1 — log closed on two entries side-by-side; no NI coverage)

No entries authored. Per-file cull: nothing to cull. Zero fires is the correct outcome.
