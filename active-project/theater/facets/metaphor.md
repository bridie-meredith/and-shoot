facet: metaphor
episode: b01c05
author: editor
r1-date: 2026-05-28
---

# rubric-carve-out — none
#
# No carve-outs in effect. All candidates refused under standard rubric discipline.
# Zero-fires is valid per refuse-by-default doctrine (rubric-metaphor.md: "zero-fires-per-episode acceptable").

# REFUSAL LOG — b01c05 R1
#
# Candidate inventory evaluated against rubric-metaphor.md (V1, LOCKED 2026-05-07)
# and scene-map-b01-c05.md peak-bones / axis-magnitude data.
# Upstream anchor files (interest-narrator.md, memory.md, feeling.md) not yet locked at R1;
# provisional anchor reasoning used per task authorization.
#
# ── @25 (scene-C peak-bone, +1.5 political_register-prot, the sole hinge-magnitude bone) ──
#   AP7: peak-zone fire, default refuse.
#   Exception tested: dark-humor deflation at peak — requires an explicit comparator
#   (Reading A: simile / predicative metaphor / allegory with structural operator).
#   The recognition-cessation at @25 ("taylor stops the rushwick-pass") is enacted through
#   the body-act; the beat IS the rupture. Any explicit comparison on top doubles the carry
#   without adding meaning (AP2 risk). No clean deflationary figure available that clears
#   the comparator-required fence without importing content the proto-line does not authorize.
#   REFUSE — AP7 (peak-zone); AP2 secondary.
#
# ── @13 (scene-B peak-bone, "the side-alley returns the sound") ──
#   AP7: scene-B peak-bone, default refuse.
#   Reading A test also run: "the feed has no field for the sound" is figurative compression
#   without an explicit comparator — no identity claim, no simile operator, no structural
#   correspondence operator. This is Reading B (environmental-agency idiom / figurative
#   compression already in proto-line register). Out of scope for the metaphor facet.
#   REFUSE — AP7 (peak-zone); Reading A scope miss secondary.
#
# ── @22 (scene-C shadow-peak, "the Hook-feed resolves") ──
#   Not an AP7 peak-bone (shadow-peak only). Licensing layers assessed.
#   AP2: the scene-C chunk already carries "flat, like a document she has already processed"
#   and "the information completing itself as information" — both explicit comparisons already
#   in the chunk prose at this beat. The metaphor facet doubling either figure is AP2
#   (figurative-already-in-proto-line / chunk content).
#   Q1 fails: a metaphor at @22 adds nothing the chunk prose does not already carry.
#   REFUSE — AP2 (document/flat-read comparison already in chunk prose).
#
# ── @29 and @31 (scene-C shadow-peaks, foreclosure quartet) ──
#   Not AP7 peak-bones. Per-scene cap allows ≤1 across scene-C (cap not yet consumed).
#   Allegory candidate assessed: flat-document-read-as-door-not-opening structural figure.
#   AP2: the chunk prose already performs the foreclosure through negation-repetition
#   ("The neutral read is not available to her for this content anymore"; "She runs it again.
#   The same."). The rhetorical figure IS the repetition; an explicit allegory on top is
#   ornamental doubling with no additive meaning.
#   AP5 boundary risk: door-figure maps to Earth-Bet structural register (locker / exclusion
#   / isolation) without a locked memory-flag anchor to license the doubled-register mechanism.
#   Per rubric: "AP5 is a label-fence, not a figure-fence — Earth-Bet monument resonance
#   carried through structural figure is the intended doubled-register mechanism" — but that
#   mechanism requires the memory-flag anchor to be present and locked. At R1, memory.md is
#   not yet authored; the provisional anchor does not resolve the AP5 risk.
#   Q1 fails: the prose already enacts the foreclosure; the allegory adds ornamentation only.
#   REFUSE — AP2 primary; AP5 boundary risk secondary; Q1 negative.
#
# ── @1–@7 (scene-A, 0 axis-magnitude) ──
#   Held-scene bones, 0 axis-magnitude. AP7 extended: task framing specifies
#   "default-refuse for bones outside hinge-magnitude band." Only @25 is in-band.
#   No memory or feeling anchors expected in scene-A held content.
#   REFUSE — outside hinge-magnitude band; no licensing layers.
#
# ── @8–@12, @14–@19 (scene-B non-peak, 0 axis-magnitude) ──
#   0 axis-magnitude, held-scene. No functional-register pathway to dark-humor or
#   memory-callback that clears Q1. Factual routing beats; any comparison would be
#   AP9 (painting-characterization without callback) or AP1 (unlicensed novel figuration).
#   REFUSE — outside hinge-magnitude band; functional-register fail; Q1 negative.
#
# FINAL COUNT: 0 entries. Zero-fires. Valid per refuse-by-default discipline.
